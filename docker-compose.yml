version: '3.8'

services:
  wayfinder:
    build:
      context: .
      dockerfile: Dockerfile
    # Host networking is required to receive the CAT062/UDP-multicast feed
    # (239.255.0.62:8600 by default) — multicast does not traverse Docker's
    # default bridge network. See DOCKER.md for details and the macOS/Windows
    # caveat.
    network_mode: host
    environment:
      # Firefly's CAT062/UDP-multicast feed (ADR 0006 / ICD-CAT062.md).
      FIREFLY_CAT062_GROUP: ${FIREFLY_CAT062_GROUP:-239.255.0.62}
      FIREFLY_CAT062_PORT: ${FIREFLY_CAT062_PORT:-8600}
      # Map center/zoom for the ASD frontend (default: Frankfurt, matching
      # Firefly's demo scenario).
      WAYFINDER_MAP_CENTER_LAT: ${WAYFINDER_MAP_CENTER_LAT:-50.0379}
      WAYFINDER_MAP_CENTER_LON: ${WAYFINDER_MAP_CENTER_LON:-8.5622}
      WAYFINDER_MAP_ZOOM: ${WAYFINDER_MAP_ZOOM:-8}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    restart: unless-stopped
