# Anforderungs-Register

Hier werden alle Anforderungen (funktional und nicht-funktional) dokumentiert und rückverfolgt zu:
- Design-Entscheidungen (ADRs)
- Code-Implementierung
- Test-Verifikation

| ID | Anforderung | Quelle | Design | Implementierung | Tests |
|----|-------------|--------|--------|-----------------|-------|
| FR-OPS-001 | Liveness-Probe (`/healthz`) signalisiert, dass der Prozess läuft | CLAUDE.md Abschnitt 8 (Cloud-nativ) | ADR0001 | `internal/server/server.go` | `internal/server/server_test.go` |
| FR-OPS-002 | Readiness-Probe (`/readyz`) signalisiert Betriebsbereitschaft (später: Firefly-Verbindung) | CLAUDE.md Abschnitt 8 (Cloud-nativ) | ADR0001 | `internal/server/server.go` | `internal/server/server_test.go` |
| FR-CFG-001 | Konfiguration (Multicast-Gruppe/-Port, Probe-/Map-/Sicherheits-Einstellungen, Log-Level via `WAYFINDER_LOG_LEVEL`, sowie Multi-Tenancy via `WAYFINDER_DB_URL`/`WAYFINDER_AUTH_MODE`/`WAYFINDER_OIDC_*`/`WAYFINDER_SESSION_*`) über Env-Vars mit dokumentierten Defaults (INSTALLATION §7, TECHNICAL §6) | CLAUDE.md Abschnitt 8 (12-Factor) | ADR0001, ADR 0006 | `cmd/wayfinder/main.go` (`Config`, `loadConfig`, `authConfig`) | `cmd/wayfinder/main_test.go` (`TestLoadConfig*`), `cmd/wayfinder/tenancy_test.go` (`TestLoadConfigTenancyEnvVars`) |
| FR-ADMIN-001 | **Tenant-skopiertes Admin-API (Wayfinder 2.0, WF2-31):** REST unter `/api/admin/*`, hinter `RequireRole(admin)`; die Mandanten-ID kommt **immer aus der Identity** (Middleware), nie aus Pfad/Body → ein Admin kann nur die **eigene** Mandanten-Config berühren (Isolation per Konstruktion, NFR-SEC-003). Endpunkte: `GET/PUT /api/admin/view` (Tenant-Default-Sicht: Zentrum/Zoom/AOI/FL/Layer, **server-validiert**: Lat/Lon/Zoom-Bereiche, AOI wohlgeformt, `fl_min ≤ fl_max`), `GET /api/admin/subscriptions` (eigene abonnierte Feeds), `GET /api/admin/feeds` (Katalog, read-only), `GET /api/admin/whoami` (Rollen-Probe der SPA, in WF2-32 von `/admin` hierher verschoben). **WF2-31b — admin-Provisioning (cross-tenant):** `GET /api/admin/tenants`, `GET/POST /api/admin/tenants/{tenantID}/subscriptions`, `DELETE /api/admin/tenants/{tenantID}/subscriptions/{feedID}` — Ziel-`tenant_id` aus dem **Pfad**, Doppel-Gate (`RequireRole` + in-handler `requireAdmin`, sonst `403`); `admin` ist die **einzige** cross-tenant-schreibende Rolle (Billing-Grenze). | Konzept §6, ROADMAP §1 (WF2-31) | ADR 0005/0006 | `pkg/adminapi/adminapi.go` (`Handler`, `validateView`, `requireAdmin`, `TenantStore`, DTOs), `cmd/wayfinder/main.go` (`/api/admin/`-Wiring hinter `tenantMW`+`RequireRole`) | `pkg/adminapi/adminapi_test.go` (DB-frei: Tenant-Scoping aus Identity, Validierung, **Cross-Tenant-Negativtest `TestCrossTenantRoutesForbidUser` = user → 403**, Grant/Revoke-Targeting, 401/404/405) + `adminapi_integration_test.go::TestIntegrationAdminAPI` (real-PG: View-Round-Trip + admin grant→list→revoke) + `TestWhoamiReportsIdentity`/`TestWhoamiUnauthorizedWithoutIdentity` |
| FR-ADMIN-002 | **Admin-UI (Wayfinder 2.0, WF2-32):** Bedienbare Admin-Oberfläche unter der Browser-Route `/admin` (Vue-3-SPA, `vue-router` **History-Mode**), die das WF2-31/31b-API konsumiert. **Eigenständige View, kein Overlay:** Navigieren zu `/admin` unmountet die ASD-Karte vollständig (MapLibre + WebSocket + Timer frei; `MapCanvas.onUnmounted → mapEngine.destroy()`). **View-Editor** (Zentrum/Zoom/AOI/FL-Band/Layer) mit **Client-Validierungs-Parität** zur Server-`validateView` (`src/admin/validateView.js`) **vor** dem PUT; **Abos/Feeds** read-only; **admin-Provisioning** (Mandant wählen → grant/revoke) hinter `isAdmin`-Gate. Rollen-Gating der UI ist **kosmetisch** — der Server erzwingt jede Grenze unabhängig (`requireAdmin → 403`, Tenant-ID aus Identity); Client-Validierung ist UX, **nie** die Sicherheitsgrenze (Defense-in-Depth). **SPA-History-Fallback** im Backend (`internal/webui/webui.go`): unbekannte Pfade → `index.html` (Deep-Links überleben Reload), API-Surface über speziellere Mux-Pattern **nie** beschattet. **AP3 (ADR 0009) — mandantenzentrierter Neuschnitt:** die alten Top-Level-Tabs sind durch ein **Master/Detail** ersetzt — eine **Mandanten-Übersicht** (`GET /api/admin/overview`: Status, aktive Features, Feeds, Zugangs-Anzahl je Mandant in einem Aggregat-Call) und pro Mandant eine **zentrale Detail-Konfiguration** (Status, Sicht, Features, Feeds, Zugänge). Die Detailseite bearbeitet die Sicht **jedes** Mandanten über neue cross-tenant Routen `GET/PUT /api/admin/tenants/{id}/view` (gleiche Server-`validateView`). **Center+Radius (NM) ↔ AOI-Bbox** wird **clientseitig** umgerechnet (`src/admin/geo.js`: `radiusNmToBbox`/`bboxToRadius`) — das Backend bleibt AOI-basiert (WF2-21.2 unberührt). `AdminProvisioning`/`AdminUsers` nehmen einen optionalen `tenantId`-Prop und werden eingebettet. | Konzept §6, ROADMAP §1 (WF2-32), ADR 0009 (AP3), Projektverantwortlicher (History-Mode + voller Komponenten-Austausch + mandantenzentriert) | ADR 0001 (Vue/Vuetify), ADR 0009 | `frontend/src/router/index.js`, `frontend/src/App.vue` + `views/AsdView.vue` + `views/AdminView.vue` (Master/Detail), `frontend/src/stores/admin.js` (`loadOverview`/`loadTenantView`/`saveTenantView`/`loadTenantEntitlements`/`setTenantEntitlement`), `frontend/src/admin/geo.js` (AP3), `frontend/src/admin/validateView.js`, `frontend/src/components/admin/{AdminTenants,AdminTenantDetail,AdminProvisioning,AdminUsers}.vue`, `internal/webui/webui.go` (SPA-Fallback), `pkg/adminapi/adminapi.go` (`whoami`, `getOverview`, `getTenantView`/`putTenantView`) | `frontend/src/admin/__tests__/validateView.test.js` (Validierungs-Parität), `frontend/src/admin/__tests__/geo.test.js` (AP3 Center/Radius↔Bbox, Round-Trip, Pol-Clamp), `frontend/src/stores/__tests__/admin.test.js` (whoami/Rollen, `saveView`-DTO, grant/revoke, 403-Pfad; **AP3** overview/tenant-view/entitlement-Actions; gemocktes `fetch`), `pkg/adminapi/adminapi_test.go` (**AP3** overview-Aggregat, tenant-view get/put/404/invalid, cross-tenant 403), `pkg/adminapi/adminapi_integration_test.go` (**real-PG** AP3 overview + tenant-view Round-Trip), `internal/webui/webui_test.go` (SPA-Fallback `/admin`→Shell, echte Assets nicht beschattet, Root→`index.html`) |
| FR-ADMIN-003 | **Live-Apply von Sicht-/Abo-Änderungen (Wayfinder 2.0, WF2-33):** Ändert ein Admin die View (`PUT /api/admin/view`) oder die Feed-Grants (`POST`/`DELETE …/subscriptions`), werden die **aktiven** `/ws`-Streams des betroffenen Mandanten **live re-skopiert** — ohne Reconnect. **Thread-Safety per Konstruktion:** Der Broadcaster ist ein Single-Goroutine-Actor; der Scope-Tausch ist ein Kommando durch denselben `Run`-Loop (`rescopeChan`/`ApplyScopes`), in dem auch die Track-Auswertung läuft → **kein Lock am heißen Pfad, keine Race**, Run-Loop nie blockiert (gepuffert, ctx-begrenzt). **Zwei-Phasen:** Snapshot über immutable Client-Identität (`ClientsForTenant`, nie der mutable Scope) → Auflösung **off-Run** pro distinct User (`resolveScope`, identisch zum Connect-Pfad → respektiert User-Overrides) → Apply in `Run` (Disconnect-Race via `clients.Load`-Guard übersprungen). **Shrink:** kein Delete-Signal — der Server sendet außenliegende Tracks schlicht nicht mehr, das Frontend coastet sie über den Client-Timeout aus (keep it simple). Bei Validierungsfehler (`400`) wird **nicht** re-skopiert. Ergänzt die Durchsetzung von **NFR-SEC-003** (Isolations-Rand jetzt auch live). | ROADMAP §1 (WF2-33), Projektverantwortlicher (Thread-Safety + Shrink-Leitplanken) | ADR 0005 | `pkg/broadcast/broadcast.go` (`Scope.UserID`, immutable Client-Identity, `ClientsForTenant`/`ApplyScopes`/`applyScopes`/`rescopeChan` + Run-Case), `cmd/wayfinder/main.go` (`resolveScope`, `rescopeTenant`, Verdrahtung), `pkg/adminapi/adminapi.go` (`RescopeFunc` + `triggerRescope` auf `putView`/`grant`/`revoke`) | `pkg/broadcast/rescope_test.go` (`TestApplyScopesShrinkAOILive`, `…GrantAndRevokeFeedLive`, `…OnlyTargetClients`, `…SkipsUnknownClient`, `TestClientsForTenantSnapshot`, **`TestRescopeRaceUnderLoad` unter `-race`**), `cmd/wayfinder/rescope_test.go` (`TestResolveScopeBuildsScope`, `TestRescopeTenantAppliesLive` Ende-zu-Ende), `pkg/adminapi/adminapi_test.go` (`TestPutViewTriggersRescope`, `TestPutViewInvalidDoesNotRescope`, `TestGrantTriggersRescope`, `TestRevokeTriggersRescope`) |
| FR-ADMIN-004 | **Zugangs-Verwaltung (Wayfinder 2.0, AP6, ADR 0009):** Der `admin` verwaltet pro Mandant einen oder mehrere **Zugänge** (Login-Konten, Rolle `user`) und kann ganze Mandanten pausieren — ohne DB-Konsole. **Schema** (`00005_user_status.sql`): `users.status` (`active`/`paused`, CHECK, Default `active`, nicht-breaking); Mandanten-Pause nutzt das vorhandene `tenants.status`. **Store:** getypter `Status` (`StatusActive`/`StatusPaused`, `Valid()`-Guard, geteilt von `User`+`Tenant`); `UserRepo.SetStatus`/`Delete` (Credentials/View-Overrides via `ON DELETE CASCADE`), `TenantRepo.SetStatus`. **Login-Enforcement (fail-closed):** ein **pausierter Zugang** — oder ein Zugang unter einem **pausierten Mandanten** — kann sich **nicht** anmelden, auch mit korrektem Passwort; gleiches generisches 401 (keine paused/active-Enumeration, Timing-uniform); ein Tenant-Lookup-Fehler gilt als suspendiert. **API** (cross-tenant, hinter `requireAdmin`): `GET`/`POST /api/admin/tenants/{id}/users`, `PATCH`/`DELETE …/users/{uid}`, `PUT …/users/{uid}/password`, `PATCH /api/admin/tenants/{id}` (Mandant-Pause). Neue Konten sind **immer** Rolle `user`; Passwort min. 8 Zeichen; doppelter Subject → 409; User-ID aus fremdem Mandanten → 404 (Ressourcen-Hierarchie ehrlich). **Sofort-Wirkung** auf laufende Sessions ist **AP7** (Session-Registry); AP6 gated nur neue Logins. **Frontend:** `AdminUsers.vue`-Tab (admin-only) mit Mandanten-Wähler, Zugangs-Liste (Status-Chip), Anlegen/Pausieren/Passwort/Löschen-Dialogen + Mandant-Pause; Gating kosmetisch (Server erzwingt). | ADR 0009 (AP6), Issue #63 | **ADR 0009** | `pkg/store/migrations/00005_user_status.sql`, `pkg/store/models.go` (`Status`, `User.Status`/`Tenant.Status`), `pkg/store/users.go` (`SetStatus`/`Delete`), `pkg/store/tenants.go` (`SetStatus`), `pkg/tenant/login.go` (`TenantLookup`, Status-Cascade), `pkg/adminapi/adminapi_users.go` (`UserStore`/`CredentialStore`, CRUD-Handler, `setTenantStatus`), `frontend/src/stores/admin.js` (`loadTenantUsers`/`createUser`/`setUserStatus`/`deleteUser`/`setUserPassword`/`setTenantStatus`), `frontend/src/components/admin/AdminUsers.vue`, `frontend/src/views/AdminView.vue` (Tab) | `pkg/store/store_integration_test.go::TestIntegrationUserStatusLifecycle` (**real-PG**: Default-active, Pause/Reaktivieren, Tenant-Pause, Delete kaskadiert Credential, `ErrNotFound`), `pkg/store/repo_test.go::TestStatusValid`, `pkg/tenant/login_test.go::TestLoginEnforcesStatus` (paused-Account/paused-Tenant/Lookup-Fehler→401, nil-Tenants skip), `pkg/adminapi/adminapi_users_test.go` (CRUD, 400/404/409, **`TestAccessRoutesForbidNonAdmin` = user→403 + keine Mutation**, `TestUserCrossTenantMismatch`), `frontend/src/stores/__tests__/admin.test.js` (AP6-Actions, 409-Pfad) |
| FR-ADMIN-005 | **Zero-Touch-Onboarding: Auto-Admin + Konto-Selbstverwaltung (ONB-1, ADR 0011):** Eine frische Instanz ist **ohne Terminal-Schritt** benutzbar. **Boot-Auto-Seed:** In `builtin`-Modus mit DB legt Wayfinder beim Start einen tenant-losen Standard-Admin (`admin`/`admin`) an — **keinen Mandanten** (ADR 0011 Nachtrag: der frühere Komfort-Mandant `default` entfällt; frische Instanz startet mit null Mandanten, Anlegen per UI/ONB-4) — — **nur** wenn kein aktiver Admin existiert (`CountActiveAdmins == 0`), idempotent, `runBootstrap` wiederverwendet. Der Seed-Admin trägt `must_change_password=true`. **Schema** (`00006_user_must_change_password.sql`): additive Spalte `users.must_change_password` (Default `false`, nicht-breaking). **Pflichtwechsel-Gate (fail-closed):** Trägt das eingeloggte Konto das Flag, weist das Admin-API **jede** Route außer `GET /api/admin/whoami`, `GET /api/admin/me`, `PUT /api/admin/me/password` mit **403 `password_change_required`** ab; das Flag wird vom `tenant.Identity` getragen (kein DB-Lookup), das SPA zeigt die Pflichtwechsel-Maske. **Selbstverwaltung** (`/api/admin/me/*`, rollen-unabhängig): `GET` (eigenes Konto), `PUT …/password` (aktuelles Passwort prüfen → 401 bei falsch; setzt Flag zurück), `DELETE` (eigenes Konto, **„letzter aktiver Admin"-Guard** → 409). **Session-Key-Komfort:** builtin ohne `WAYFINDER_SESSION_KEY` → flüchtiger Zufalls-Schlüssel + Warnung (fester Schlüssel für Produktion). **Deployment:** `docker-compose.onboarding.yml` (Postgres + Wayfinder builtin) → `docker compose up` genügt. | ADR 0011 (ONB-0/1), Betreiber-Wunsch „nur Container starten" | **ADR 0011** | `pkg/store/migrations/00006_user_must_change_password.sql`, `pkg/store/models.go` (`User.MustChangePassword`), `pkg/store/users.go` (`SetMustChangePassword`, `CountActiveAdmins`, `userColumns`/`scanUser`), `pkg/tenant/tenant.go` (`Identity.MustChangePassword`, Middleware), `cmd/wayfinder/seed.go` (`autoSeedDefaultAdmin`), `cmd/wayfinder/main.go` (Seed-Aufruf in `setupTenancy`, Session-Key-Auto-Gen), `pkg/adminapi/adminapi.go` (`ServeHTTP`-Gate, `passwordChangeAllowlist`, whoami-Flag, `/me`-Routen), `pkg/adminapi/adminapi_me.go` (`getMe`/`putMePassword`/`deleteMe`), `pkg/adminapi/adminapi_users.go` (`UserStore`/`CredentialStore` erweitert), `frontend/src/stores/admin.js` (`mustChangePassword`, `changeOwnPassword`), `frontend/src/views/AdminView.vue` (Pflichtwechsel-Maske) | `pkg/store/users_onboarding_integration_test.go::TestIntegrationMustChangePasswordAndAdminCount` (**real-PG**: Default-false, Toggle, `CountActiveAdmins` zählt nur aktive Admins), `cmd/wayfinder/seed_integration_test.go::TestIntegrationAutoSeed` (**real-PG**: Erst-Seed setzt Flag+Default-PW, Re-Seed no-op, rotiertes PW/Flag nicht überschrieben), `pkg/adminapi/adminapi_me_test.go` (Gate blockt/erlaubt, `getMe`-Flag, `putMePassword` Erfolg/401/zu-kurz, `deleteMe` Last-Admin-409/Erfolg), `frontend/src/stores/__tests__/admin.test.js` (ONB-1: `mustChangePassword`, `changeOwnPassword` PUT+Reload+401) |
| FR-ADMIN-006 | **Strikte Admin/Nutzer-Trennung + Plattform-Admin-Verwaltung (ONB-3, ADR 0011):** Plattform-Admins und Mandanten-Nutzer sind sauber getrennt. Ein **Admin ist global** (kein Mandant), ein **Nutzer gehört genau einem Mandanten**. **Schema** (`00007_admin_tenant_nullable.sql`): `users.tenant_id` nullable, bestehende Admins entkoppelt, **CHECK-Constraint** `admin XOR tenant` (admin ⇒ `tenant_id IS NULL`, user ⇒ `NOT NULL`). **Store:** getrennte Konstruktoren `UserRepo.Create` (Nutzer) / `CreateAdmin` (Admin, tenant-los), `ListAdmins`; `scanUser` liest NULL → `TenantID 0`. **Bootstrap/Seed:** `runBootstrap` verzweigt nach Rolle (Admin ohne `-tenant`); Auto-Seed legt einen tenant-losen Admin an (**kein** Komfort-Mandant mehr, ADR 0011 Nachtrag). **Login:** Mandanten-Pause-Kaskade wird für tenant-lose Admins übersprungen (keine Selbst-Aussperrung). **API (global, hinter `requireAdmin`):** `GET/POST /api/admin/admins`, `PATCH/DELETE /api/admin/admins/{id}`, `PUT …/{id}/password`. Die per-Mandant-Route `POST /api/admin/tenants/{id}/users` lehnt `role:"admin"` mit **400** ab. **„Letzter aktiver Admin"-Guard** (`wouldOrphanAdmins` → `CountActiveAdmins`): Pausieren/Löschen des letzten aktiven Admins → **409**. **Frontend:** dedizierte „Plattform-Administratoren"-Verwaltung (`AdminPlatformAdmins.vue`) mit eigener Header-Navigation; Store-Actions `loadAdmins`/`createAdmin`/`setAdminStatus`/`deleteAdmin`/`setAdminPassword` (409-Guard in verständliche Meldung übersetzt). | ADR 0011 (ONB-3), Betreiber-Wunsch „saubere Welten-Trennung" | **ADR 0011** | `pkg/store/migrations/00007_admin_tenant_nullable.sql`, `pkg/store/models.go` (`User.TenantID` 0=tenant-los), `pkg/store/users.go` (`Create`/`CreateAdmin`/`ListAdmins`/`scanUser`), `pkg/tenant/login.go` (Cascade-Skip für `TenantID==0`), `pkg/adminapi/adminapi_admins.go` (CRUD + `wouldOrphanAdmins`/`adminByID`), `pkg/adminapi/adminapi_users.go` (`UserStore` erweitert, `createUser` lehnt admin-Rolle ab), `pkg/adminapi/adminapi.go` (`/admins`-Routen), `cmd/wayfinder/bootstrap.go` (`provisionAccount`, rollen-abhängig), `cmd/wayfinder/seed.go` (tenant-loser Admin, kein Mandanten-Seed), `frontend/src/stores/admin.js` (Admin-Actions), `frontend/src/components/admin/AdminPlatformAdmins.vue`, `frontend/src/views/AdminView.vue` (Header-Navigation) | `pkg/store/store_integration_test.go::TestIntegrationAdminTenantSeparation` (**real-PG**: tenant-loser Admin, `ListAdmins`, CHECK lehnt beide Halb-Zustände ab), `cmd/wayfinder/bootstrap_integration_test.go::TestIntegrationBootstrap` (**real-PG**: Admin-Welt tenant-los + keine Tenant-Erzeugung, Nutzer-Welt mit Mandant, Welten-Konflikt), `cmd/wayfinder/seed_integration_test.go::TestIntegrationAutoSeed`, `pkg/adminapi/adminapi_admins_test.go` (CRUD, Last-Admin-Guard 409, Nicht-Admin-ID → 404, requireAdmin → 403), `pkg/adminapi/adminapi_users_test.go::TestCreateUserRejectsAdminRole`, `pkg/tenant/login_test.go` (tenant-loser Admin nicht ausgesperrt), `frontend/src/stores/__tests__/admin.test.js` (ONB-3-Block) |
| FR-ADMIN-007 | **Mandanten-Lebenszyklus über die Oberfläche (ONB-4, ADR 0011):** Der `admin` legt Mandanten **anlegen/löschen** ohne Terminal — der letzte rein-CLI-Provisionierungsschritt entfällt. **Store:** `TenantRepo.Delete` (einzeiliges `DELETE`, atomar via `ON DELETE CASCADE` auf Zugänge/Credentials/Abos/Entitlements/View-Konfig); `ErrNotFound` bei fehlender Zeile. **API (hinter `requireAdmin`):** `POST /api/admin/tenants` `{slug, name?}` → 201 (Slug DNS-label-artig validiert, `name` Default = `slug`, doppelter Slug → 409 via `GetBySlug`-Pre-Check); `DELETE /api/admin/tenants/{id}` → 204. **Guard B (Betreiber-Entscheidung):** Ein Mandant mit noch vorhandenen **Zugängen** wird **fail-closed mit 409** abgelehnt — der kaskadierende Löschvorgang ist ein bewusster Zweischritt (erst Konten entfernen), kein versehentlicher Ein-Klick-Verlust von Lotsen-Logins. Der frühere ADR-Guard „Mandant mit aktiven Admins" ist nach ONB-3 gegenstandslos (Admins sind tenantlos). **Frontend:** „Mandant anlegen"-Dialog in der Übersicht, „Mandant löschen" (mit Bestätigung) auf der Detailseite; Store-Actions `createTenant`/`deleteTenant` (409-Guard verständlich übersetzt). | ADR 0011 (ONB-4), Betreiber-Wunsch „nur Container starten" + Guard B | **ADR 0011** | `pkg/store/tenants.go` (`Delete`), `pkg/adminapi/adminapi_tenants.go` (`createTenant`/`deleteTenant`, Slug-Validierung, Guard B), `pkg/adminapi/adminapi.go` (`TenantStore` um `Create`/`Delete`/`GetBySlug` erweitert, Routen), `frontend/src/stores/admin.js` (`createTenant`/`deleteTenant`), `frontend/src/components/admin/AdminTenants.vue` (Anlegen), `frontend/src/components/admin/AdminTenantDetail.vue` (Löschen) | `pkg/store/store_integration_test.go::TestIntegrationTenantDeleteCascades` (**real-PG**: Cascade auf User/Credential/Abo, Feed überlebt, Re-Delete → ErrNotFound), `pkg/adminapi/adminapi_users_test.go` (`TestCreateTenant`/`…DefaultsNameToSlug`/`…Validation`/`…DuplicateSlug`, `TestDeleteTenantEmptySucceeds`/`…WithAccountsRefused`/`…UnknownIs404`, `TestTenantLifecycleRoutesForbidNonAdmin`), `frontend/src/stores/__tests__/admin.test.js` (ONB-4-Block) |
| FR-ADMIN-008 | **Feed-Lebenszyklus + Live-Receiver-Join/-Leave (ONB-5, ADR 0011):** Der `admin` legt Daten-Feeds **anlegen/löschen** ohne Terminal und ohne Neustart — der Server tritt der Multicast-Gruppe eines neuen Feeds **sofort** bei und verlässt die Gruppe eines gelöschten Feeds **sofort**. **Feed-Manager (`pkg/feedmanager`):** ein Supervisor hält je Feed-ID eine laufende Receiver-Goroutine (mutex-geschützte Map `feedID→cancel`), `Start`/`Stop`/`StopAll`/`Running`; die `Factory` ist injiziert → UDP-frei unit-testbar. **Prompte Beendigung:** `receiver.Run` setzt bei ctx-Cancel eine vergangene Read-Deadline (Watchdog), sodass ein blockiertes `ReadFromUDP` **sofort** zurückkehrt und die IGMP-Mitgliedschaft auch bei totem Feed prompt freigegeben wird. **Store:** `FeedRepo.Delete` (atomar, `ON DELETE CASCADE` auf Abos), `GetByName` (Dup-Pre-Check); Migration `00008_feeds_name_unique.sql` (UNIQUE `name`). **API (hinter `requireAdmin`):** `POST /api/admin/feeds` `{name, multicast_group, port, region?, sensor_mix?}` → 201 (Multicast-IPv4 + Port 1..65535 validiert, `sensor_mix` gegen Vokabular, doppelter Name → 409); **Create ist atomar über Katalog + Live-Join** — scheitert der Beitritt, wird die Katalogzeile zurückgerollt. `DELETE /api/admin/feeds/{id}` → 204 (Receiver verlässt Gruppe, dann Zeile; **Guard C**: kein Blockieren bei Abos — Grants kaskadieren). **Decode-Error-Metrik** ist prozessweit-monoton über Feed-Churn (Hook `OnDecodeError`, nicht Summe über die lebende Receiver-Menge). **Health-Registry:** `Forget(feedID)` beim Löschen, damit kein Phantom-Feed (ewig stale) im Dashboard bleibt; der Staleness-Monitor iteriert die **lebende** Feed-Menge (`feedManager.Running`). **Entkopplung:** `adminapi.FeedLifecycle` ist eine Primitiv-Param-Schnittstelle (kein Import der Transport-Schicht); main.go verdrahtet einen Adapter über Feed-Manager + Health-Registry. **Resilienz:** ein einzelner Receiver-Fehler beendet nicht mehr den ganzen Server (Manager vergisst den Feed, loggt). **Frontend:** „Feeds"-Eintrag in der Admin-Header-Navigation öffnet `AdminFeeds.vue` (Katalog-Tabelle mit `multicast_group:port`, Sensor-Mix, Gesundheits-Chip; Anlegen-Dialog + Lösch-Bestätigung); Store-Actions `createFeed`/`deleteFeed` (409-Duplikatname verständlich übersetzt). | ADR 0011 (ONB-5), Betreiber-Wunsch „nur Container starten" | **ADR 0011** | `pkg/feedmanager/feedmanager.go` (`Manager`/`Start`/`Stop`/`StopAll`/`Running`/`Factory`), `pkg/receiver/receiver.go` (Cancel-Watchdog in `Run`, `OnDecodeError`), `pkg/store/feeds.go` (`Delete`/`GetByName`), `pkg/store/migrations/00008_feeds_name_unique.sql`, `pkg/health/registry.go` (`Forget`), `pkg/adminapi/adminapi_feeds.go` (`createFeed`/`deleteFeed`/`validateMulticast`), `pkg/adminapi/adminapi.go` (`FeedStore`+`Create`/`Delete`/`GetByName`, `FeedLifecycle`, Routen, `feedDTO`+Multicast/Port), `cmd/wayfinder/feeds.go` (`newReceiverFactory`, `feedLifecycle`-Adapter), `cmd/wayfinder/main.go` (Feed-Manager-Wiring, churn-stabiler Decode-Counter, Monitor über lebende Menge), `frontend/src/stores/admin.js` (`createFeed`/`deleteFeed`), `frontend/src/components/admin/AdminFeeds.vue`, `frontend/src/views/AdminView.vue` (Feeds-Navigation) | `pkg/feedmanager/feedmanager_test.go` (UDP-freie Fake-Receiver: Start/Stop/Idempotenz/Listen-Fehler/Self-Error-Forget/StopAll/Base-Ctx, **`-race`**), `pkg/receiver/receiver_test.go` (Cancel-Pfad), `pkg/store/store_integration_test.go::TestIntegrationFeedDeleteCascades`/`TestIntegrationFeedNameUnique` (**real-PG**), `pkg/adminapi/adminapi_feeds_test.go` (`TestCreateFeed`/`…Validation`/`…DuplicateName`/`…InvalidSensorMix`/`…RollsBackOnJoinFailure`, `TestDeleteFeedSucceeds`/`…UnknownIs404`, `TestFeedLifecycleRoutesForbidNonAdmin`, `…WithoutLifecycle`), `cmd/wayfinder/feeds_test.go::TestNewReceiverFactory`, `frontend/src/stores/__tests__/admin.test.js` (ONB-5-Block) |
| FR-ADMIN-009 | **OpenAIP pro Mandant (ONB-6, ADR 0011 F3):** Jeder Mandant nutzt einen **eigenen optionalen OpenAIP-API-Schlüssel** und ruft Luftraum-/Navaid-/Wegpunkt-Daten gegen **seine eigene Area of Interest** (aus der Mandanten-View-Konfig: AOI-Box, sonst Box um das View-Zentrum) ab — statt eines prozess-globalen Schlüssels und einer global gecachten Region (Mandanten-Isolations-Leck). Mandanten **ohne** eigenen Schlüssel fallen auf den globalen `WAYFINDER_OPENAIP_API_KEY` zurück (abwärtskompatibel). **Schema:** Migration `00009_tenant_openaip_key.sql` (`tenants.openaip_api_key TEXT` nullable). **Store:** `TenantRepo.GetOpenAIPKey`/`SetOpenAIPKey` (Schlüssel isoliert gelesen, **nie** Teil der allgemeinen `Tenant`-Zeile/DTO). **Registry (`pkg/aeronautical`):** ein Supervisor hält je `tenantID` einen laufenden `Service` (eigener Client/Schlüssel + eigene BBox), `Start` (idempotent auf unveränderte Schlüssel+BBox)/`Stop`/`StopAll`/`Serve` (Fallback auf den globalen Cache, wenn kein eigener läuft); injizierte `ClientFactory` → transport-entkoppelt. **Endpunkte mandanten-aufgelöst:** `/api/airspace`, `/api/navaids`, `/api/waypoints` liegen im Multi-Mandanten-Betrieb **hinter der Tenant-Middleware** und liefern den Cache **des Mandanten des Requests** (kein Identity → leere Collection, graceful). **Feature-Gate server-seitig (E2E-Fix):** Jeder Endpunkt prüft das **Entitlement** des Mandanten (`airspaces` → `/api/airspace`, `vor_ndb` → `/api/navaids`, `waypoints` → `/api/waypoints`; `aeronautical.FeatureGate`, injiziert aus `cmd/wayfinder` via `featSvc.HasFeature`) und liefert bei **fehlendem** Feature eine **leere** Collection — die Overlay-Ebene erscheint nicht, unabhängig vom (kosmetischen) Frontend-Toggle. So wirkt der Feature-Schalter tatsächlich server-seitig (zuvor lieferte der Endpunkt Daten trotz ausgeschaltetem Feature); ein Live-Toggle-Aus leert das Overlay beim nächsten Refresh, weil der Server dann leer antwortet. **API (hinter `requireAdmin`):** `GET /api/admin/tenants/{id}/openaip` → `{configured: bool}` (**nie der Schlüssel selbst**); `PUT …/openaip` `{api_key: string|null}` → 204 (leer/Whitespace/`null` = löschen → globaler Fallback), **Live-Apply** (re)startet die Mandanten-Refresh sofort. **Live-AOI:** eine View-Änderung (`putView`/`putTenantView`) löst zusätzlich `triggerAeroApply` aus → Re-Fetch gegen die neue AOI; **Mandant-Löschen** (ONB-4) ruft `triggerAeroStop`. **Metriken:** `wayfinder_openaip_fetch_*` summieren über die globale + alle Mandanten-Instanzen (monoton über Mandanten-Churn). **Alle ADR-0004-Schutz-Eigenschaften bleiben** (Schlüssel server-seitig, best-effort/Last-Good-Cache, nicht-blockierender Start, Größengrenzen, „kein Schlüssel ⇒ Feature still aus" — jetzt pro Mandant). **Frontend:** Abschnitt „OpenAIP-Konfiguration" in `AdminTenantDetail.vue` (Status gesetzt/nicht gesetzt, Schlüssel nie angezeigt; Setzen/Löschen); Store-Action `setTenantOpenAIPKey`. | ADR 0011 (ONB-6 / F3), ADR 0004 (OpenAIP-Datenquelle), Mandanten-Isolation | **ADR 0011 / ADR 0004** | `pkg/store/migrations/00009_tenant_openaip_key.sql`, `pkg/store/tenants.go` (`GetOpenAIPKey`/`SetOpenAIPKey`), `pkg/aeronautical/registry.go` (`Registry`/`Start`/`Stop`/`Serve`/`Register`+`FeatureGate`/`ClientFactory`/`FetchSuccessCount`), `cmd/wayfinder/aero.go` (`aeroFeatureKey` Kind→Feature-Mapping), `cmd/wayfinder/main.go` (Gate-Closure via `featSvc.HasFeature`), `pkg/aeronautical/service.go` (`Serve`), `pkg/adminapi/adminapi_openaip.go` (`getTenantOpenAIP`/`setTenantOpenAIP`), `pkg/adminapi/adminapi.go` (`TenantStore`+`Get`/`SetOpenAIPKey`, `TenantAeroLifecycle`, Routen, View-Apply), `pkg/adminapi/adminapi_tenants.go` (`deleteTenant` → `triggerAeroStop`), `cmd/wayfinder/aero.go` (`newAeroClientFactory`, `tenantAeroLifecycle`-Adapter, `aeroBBoxFromView`), `cmd/wayfinder/main.go` (Registry-Wiring, Boot-Apply, tenant-aware Endpunkt-Registrierung, summierte Metrik), `frontend/src/stores/admin.js` (`setTenantOpenAIPKey`), `frontend/src/components/admin/AdminTenantDetail.vue` (OpenAIP-Abschnitt) | `pkg/store/store_integration_test.go::TestIntegrationTenantOpenAIPKey` (**real-PG**: Get/Set/Clear-Roundtrip, nil-Default, ErrNotFound), `pkg/aeronautical/registry_test.go` (Per-Tenant-Cache, Empty-Key-Fallback, Idempotenz, Stop-Fallback, Count-Aggregation, Handler-Auflösung, **`TestRegistryHandlerFeatureGateDeniesServesEmpty`** = fehlendes Feature → leere Collection trotz Cache/`-race`), `pkg/adminapi/adminapi_openaip_test.go` (`TestGetTenantOpenAIPReportsConfigured`/`…NeverLeaksKey`, `TestSetTenantOpenAIPSetsKeyAndApplies`/`…NullClearsKey`/`…UnknownTenantIs404`/`…InvalidBodyIs400`, `TestOpenAIPRoutesForbidNonAdmin`, `TestDeleteTenantStopsAero`), `frontend/src/stores/__tests__/admin.test.js` (ONB-6-Block) |
| FR-AERO-001 | **OpenAIP persistenter Cache + fetch-once (AERO-1, ADR 0018):** Die geholten Aeronautik-Daten (Luftraum/Navaids/Wegpunkte) werden in der DB **persistiert** (`aeronautical_cache`) und überleben einen **Redeploy**; beim Start werden sie **ohne Netz** hydratisiert. Der frühere **periodische Ticker** (`WAYFINDER_OPENAIP_REFRESH`) entfällt — ein OpenAIP-Abruf passiert nur noch **ereignisgesteuert**: Erstbefüllung (Schlüssel vorhanden, aber nichts persistiert), **AOI-Änderung** (Box geändert) oder **expliziter Refresh** (Schlüssel gesetzt/geändert). Fachlicher Grund: Aeronautik-Daten folgen dem **AIRAC-Zyklus** (28 Tage; ADR 0017-Prämisse: statisch halten, bewusst aktualisieren), Dauer-Polling ist Last ohne Nutzen und ein Redeploy soll kein Abruf-Sturm sein. **Schema:** Migration `00017_aeronautical_cache.sql` (`tenant_id` NULL = globaler Fallback, `kind`, `geojson TEXT`, `feature_count`, `fetched_at`; zwei partielle Unique-Indizes wie `view_configs`). **Store:** `AeroCacheRepo.Load`/`Save` (Upsert je (Mandant, kind)) / `Status` (spätestes `fetched_at` + Summe `feature_count`). **`pkg/aeronautical`:** `CacheStore`-Schnittstelle (paket-lokal, DB-entkoppelt); `Service.Hydrate` (Load aus DB, kein Netz), `BootstrapOnce` (hydrieren, nur bei leer + Schlüssel einmal fetchen), `RefreshNow` (erzwungen), `refreshAll` persistiert jeden Erfolg; **`Run`/Ticker entfernt**. Registry: `Start(force)` fetcht nur bei geänderten Inputs oder `force`; ein neuer Mandant mit persistierten Daten **hydratisiert nur** (kein Fetch → Redeploy = kein Fetch-Sturm). **Wiring (`cmd/wayfinder`):** `aeroCacheStore`-Adapter (marshalt FC ↔ TEXT), globaler Service `BootstrapOnce` (keine Goroutine-Schleife), `tenantAeroLifecycle.Apply` (nicht-erzwungen) / `Refresh` (erzwungen, vom Schlüssel-Setzen). **Admin-Status:** `GET …/openaip` liefert zusätzlich `fetched_at`/`feature_count` (best-effort, weggelassen wenn nichts gecacht). **`WAYFINDER_OPENAIP_REFRESH` deprecated/ignoriert** (Warn-Log beim Start). **Alle ADR-0004-Schutz-Eigenschaften bleiben** (best-effort, nie `/ready`-Blockade, Größengrenzen). **Abgrenzung:** Refresh-Buttons + globaler Schlüssel via Platform-Admin-UI + Zeitstempel-Anzeige = AERO-2; AIRAC-Kalender/Change-Impact = AERO-3. | Betreiber-Prämisse (statisch halten, AIRAC), ADR 0017/0018 | **ADR 0018** | `docs/decisions/0018-openaip-persistenter-cache-fetch-once.md`, `pkg/store/migrations/00017_aeronautical_cache.sql`, `pkg/store/aero_cache.go` (`AeroCacheRepo`), `pkg/aeronautical/service.go` (`CacheStore`, `Hydrate`/`HasData`/`BootstrapOnce`/`RefreshNow`/`refreshAll`), `pkg/aeronautical/registry.go` (`Start(force)`, `NewRegistry(store)`), `cmd/wayfinder/aero.go` (`aeroCacheStore`, `tenantAeroLifecycle.Refresh`/`resolve`), `cmd/wayfinder/main.go` (`BootstrapOnce`, Deprecation-Warn, `WithAeroCache`), `pkg/adminapi/adminapi.go` (`TenantAeroLifecycle.Refresh`, `AeroCacheStatusReader`, `WithAeroCache`), `pkg/adminapi/adminapi_openaip.go` (Status-Felder, Force-Refresh bei Schlüssel-Set) | `pkg/aeronautical/persist_test.go` (`TestHydrateLoadsPersistedWithoutFetch`, `TestBootstrapOnceSkipsFetchWhenPersistedDataExists`/`…FetchesAndPersistsWhenEmpty`, `TestRefreshAllPersistsEveryKind`, `TestRegistryForceRefetchesDespitePersistedData`, `TestRegistryNewTenantHydratesWithoutFetch`), `pkg/store/store_integration_test.go` (`TestIntegrationAeroCacheRepo`: Round-Trip/Upsert/global-vs-Mandant/Status-Aggregation/FK-Cascade, **real-PG**), `pkg/adminapi/adminapi_openaip_test.go` (`TestGetTenantOpenAIPReportsCacheStatus`/`…OmitsCacheStatusWhenEmpty`, `TestSetTenantOpenAIPSetsKeyAndRefreshes`), `cmd/wayfinder/main_test.go` (`TestLoadConfigOpenAIPRefreshDeprecated`) |
| FR-AERO-004 | **OpenAIP-Transform-Anreicherung für AoR (ASD-014 Slice 1):** Der OpenAIP→GeoJSON-Transform führt für **Lufträume** zusätzlich drei Felder mit: die stabile OpenAIP-`_id` (`id`) als robuste Referenz für die flughafengebundene AoR-Auswahl (Namen driften pro AIRAC), die **ICAO-Klasse** (`icao_class`, numerisch) und die **Vertikalgrenzen** (`lower`/`upper` als `{value, unit, referenceDatum}`-Objekt; unit 0=m/1=ft/6=FL, referenceDatum 0=GND/1=MSL/2=STD). Der **Reference-Datum wird bewusst erhalten** (nie zu einer skalierten Zahl verrechnet) → datum-vollständig für Höhenband-Filter und ein späteres 3-D-Rendering. **Additiv/rückwärtskompatibel:** nur für `kind==airspace`, fehlende Felder werden weggelassen; Navaid/Waypoint-Output unverändert. Keine neuen Env-Variablen, kein CAT062-Bezug. | ADR 0021 (Datenquellen-Bewertung A/B/C; Auswahl-Semantik Option 1), Betreiber (AoR-Overlays) | **ADR 0021** / ADR 0004 | `pkg/aeronautical/client.go` (`openaipItem` + `_id`/`icaoClass`/`lowerLimit`/`upperLimit`, `openaipLimit.props`, `properties`-Airspace-Block) | `pkg/aeronautical/client_test.go` (`TestFetchEnrichesAirspaceProperties`, `TestEnrichmentFieldsAreAirspaceOnly`) |
| FR-AERO-005 | **AoR-Auswahl pro Mandant (ASD-014 Slice 2):** Der Mandant konfiguriert seinen **Verantwortungsbereich** (Area of Responsibility, ADR 0021 Ebene 2) als **explizite Liste stabiler OpenAIP-Luftraum-`id`s** (Auswahl-Semantik Option 1; die `id` kommt aus FR-AERO-004, weil Luftraum-Namen pro AIRAC driften). Gehalten am **View-Config** (`view_configs.aor_airspace_ids` JSONB, Migration `00021`, nullable = keine AoR), editierbar über die bestehenden Routen `PUT /api/admin/view` / `…/tenants/{id}/view` (kein neuer Endpunkt); **server-validiert** in `validateView` (Anzahl ≤ 500, jede id ≤ 64 Zeichen, keine Steuerzeichen) und **normalisiert** (`normalizeAoRIDs`: Trim, Leere verwerfen, dedup, Reihenfolge erhalten). Die effektive Liste wird über **`whoami`** (`aor_airspace_ids`, `omitempty`) an die ASD-SPA geliefert; das Highlight-Rendering ist Slice 3. **Variante A (whoami-Surfacing):** `pkg/aeronautical` bleibt **unangetastet** (kein Server-Tagging). Additiv/rückwärtskompatibel; keine neuen Env-Variablen, kein CAT062-Bezug. | ADR 0021 (Auswahl-Semantik Option 1), Betreiber (AoR-Overlays) | **ADR 0021** | `pkg/store/migrations/00021_view_configs_aor_airspace_ids.sql`, `pkg/store/view_configs.go` (`ViewConfig.AoRAirspaceIDs`, `viewConfigColumns`, `viewJSONParams`, beide Upserts, `scanViewConfig`), `pkg/adminapi/adminapi.go` (`viewDTO`/`whoamiDTO`-Feld, `validateView`, `normalizeAoRIDs`, `maxAoRAirspaceIDs`/`maxAoRIDLen`, `toViewConfig`/`toViewDTO`, whoami-Handler) | `pkg/store/view_configs_test.go` (`TestViewJSONParams` AoR-Fall, `TestIntegrationViewConfigRepo` Round-Trip/Clear, **real-PG**), `pkg/adminapi/adminapi_test.go` (`TestValidateViewAoRAirspaceIDs`, `TestNormalizeAoRIDs`, `TestWhoamiIncludesAoRAirspaceIDs`, `TestWhoamiOmitsAoRWhenUnset`) |
| FR-AERO-006 | **AoR-Namens-Picker (ASD-014 Slice 4):** Der Admin wählt die Verantwortungsbereich-Lufträume **nach Namen** statt roher IDs. Neuer Endpunkt `GET /api/admin/tenants/{id}/airspaces` (hinter `requireAdmin`) liefert die Luftraum-Liste **des Mandanten** aus dem **vorhandenen** Aeronautik-Cache (`aeronautical.Registry.Serve`) als `[{id, name, type?, icao_class?}]`, nach Name sortiert — **kein neuer Fetch/Datenquelle**; `pkg/adminapi` bleibt transport-agnostisch (Projektion im `cmd/wayfinder`-Adapter, robust gegen int/float64-Properties aus frischem Fetch bzw. Cache-Hydration). Frontend: `AdminTenantDetail.vue` nutzt ein **`v-autocomplete`** (`item-title=name`, `item-value=id`); gewählte, aber nicht (mehr) gecachte IDs werden als synthetische Items erhalten (kein stiller Verlust). Leerer Cache → Hinweis „erst OpenAIP konfigurieren". Server validiert weiter autoritativ. Additiv, kein CAT062-Bezug. | ADR 0021 (AoR-Editor bedienbar machen), Betreiber (Namens-Picker) | **ADR 0021** | `pkg/adminapi/adminapi_airspaces.go` (`AirspaceLister`/`AirspaceOption`/`WithAirspaceLister`/`getTenantAirspaces`), `pkg/adminapi/adminapi.go` (Feld + Route), `cmd/wayfinder/aero.go` (`aeroAirspaceLister`/`projectAirspaces`/`propInt`), `cmd/wayfinder/main.go` (`WithAirspaceLister`), `frontend/src/stores/admin.js` (`loadTenantAirspaces`), `frontend/src/components/admin/AdminTenantDetail.vue` (`v-autocomplete`, `airspaceItems`/`loadAirspaces`) | `pkg/adminapi/adminapi_airspaces_test.go` (Optionen/404/403), `cmd/wayfinder/aero_airspaces_test.go` (`projectAirspaces`/`propInt`), `frontend/src/stores/__tests__/admin.test.js` (`loadTenantAirspaces`), `frontend/src/components/admin/__tests__/adminTenantDetailSave.test.js` (Picker-Wiring); go test/vet/gofmt/golangci-lint + vitest 429 + vite build + dist neu grün |
| FR-ADMIN-010 | **Serverseitige Session-Registry + Session-Limit (Wayfinder 2.0, AP7, ADR 0009 §5):** Bewusste, eng begrenzte Abkehr vom Stateless-Split (ADR 0006) für die Auth-Session, damit **(1)** ein Limit gleichzeitiger Sessions pro Zugang durchsetzbar und **(2)** ein Pausieren/Löschen/Logout **sofort** wirksam wird — beides kann ein rein signiertes Cookie nicht. **Schema** (`00014_sessions.sql`): Tabelle `sessions` (`id` = `base64url(sha256(cookie-token))` — nur der Hash liegt in der DB, ein Dump liefert keine brauchbaren Cookies; `user_id` FK `ON DELETE CASCADE`; `created_at` verankert das absolute Maximum; gleitendes `expires_at`; `client_meta` JSONB) + nullable `users.session_limit` (NULL = Deployment-Default, 0 = unbegrenzt). **Store (`SessionRepo`):** `CreateSession` erzwingt das Limit **atomar** (transaktions-gebundener `pg_advisory_xact_lock` je `user_id` gegen TOCTOU über Replicas + Prune-Expired + Count, Policy `reject`/`evict_oldest`), `ResolveSession` (ein fail-closed-Query: unabgelaufen **und** Zugang aktiv **und** Mandant aktiv — greift Sofort-Pause auch ohne Zeilen-Löschung; stempelt `last_seen_at`), `ExtendSession` (gleitender Renew, gekappt auf `created_at + MaxLifetime`), `DeleteSession`/`DeleteUserSessions`/`DeleteTenantSessions` (echtes Logout + Revoke), `DeleteExpiredSessions` (Janitor). **Cookie/Auth:** das Cookie trägt künftig eine **signierte Session-ID** (`MintSessionID`/`ParseSessionID`, punktfrei → strukturell von Legacy `subject.iat.exp` unterscheidbar); `BuiltinAuthenticator` löst sie gegen die Registry auf (widerrufbar). **Sanfte Übernahme (Rollout):** ein noch gültig signiertes Legacy-Cookie wird auf Signatur akzeptiert und beim nächsten `RenewHandler`-Renew in eine Registry-Session überführt (verankert am Erst-Login `iat`); harter Schnitt jederzeit per `WAYFINDER_SESSION_KEY`-Rotation. **Login/Renew/Logout:** `LoginHandler` eröffnet eine Session mit Limit-Enforcement (per-Zugang-Override schlägt `WAYFINDER_SESSION_LIMIT_DEFAULT`), `reject`-Überschreitung → **429** (Credentials waren gültig, kein 401), `evict_oldest` verdrängt die älteste; `LogoutHandler` löscht die Session serverseitig; `RenewHandler` verlängert die Registry-Zeile (widerrufen/abgelaufen → 401). **Sofort-Revoke:** `adminapi.SessionRevoker` — Pausieren eines Zugangs/Admins (`setUserStatus`/`setAdminStatus`) und eines Mandanten (`setTenantStatus`, Kaskade) tötet die Live-Sessions sofort; Löschen via FK-Cascade. **Impersonation (ADR 0008) bleibt separat & stateless.** **Env:** `WAYFINDER_SESSION_LIMIT_DEFAULT` (0/unset = unbegrenzt, opt-in), `WAYFINDER_SESSION_LIMIT_POLICY` (`reject`\|`evict_oldest`, Default `reject`). **Metriken:** `wayfinder_active_sessions` (Gauge, Live-Count) + `wayfinder_sessions_opened_total`/`_session_logins_rejected_total`/`_sessions_revoked_total`/`_sessions_expired_swept_total`. **Admin-UI (per-Zugang-Limit setzen):** neue Route `PUT /api/admin/tenants/{tid}/users/{uid}/session-limit` (Body `{"limit": <int\|null>}`, hinter `requireAdmin`, `userInTenant`-Scope; `null` = Default, `0` = unbegrenzt, positiv = Kappung, negativ → 400); `userDTO` trägt `session_limit` (ohne omitempty → `null` unterscheidbar von `0`); im Zugangs-Tab (`AdminUsers.vue`) Spalte „Sitzungslimit" (Standard/unbegrenzt/Zahl) + „Limit"-Dialog (Wert setzen oder „Standard verwenden"). Gilt ab dem nächsten Login (keine Rück-Eviktion). **Ehrliche Grenze:** ein DB-Lookup pro Request (Zielgröße unkritisch; Redis-Cache dokumentierte nächste Stufe); im Rollout-Fenster ist ein noch nicht überführtes Legacy-Cookie erst nach Konversion/Ablauf widerrufbar (Key-Rotation erzwingt den harten Schnitt). Keine CAT062-Wirkung (Browser-Rand). | ADR 0009 (AP7), Issue #64, Betreiber-Bedarf (Session-Limit + Sofort-Sperre) | **ADR 0009** | `pkg/store/migrations/00014_sessions.sql`, `pkg/store/sessions.go` (`SessionRepo`), `pkg/store/models.go` (`Session`/`SessionMeta`/`SessionLimitPolicy`, `User.SessionLimit`), `pkg/store/users.go` (`session_limit` in `userColumns`/`scanUser`, `SetSessionLimit`), `pkg/auth/session.go` (`MintSessionID`/`ParseSessionID`), `pkg/auth/authenticator.go` (`SessionResolver`, registry-fähiger `BuiltinAuthenticator`), `pkg/auth/factory.go` (`Config.Sessions`), `pkg/tenant/login.go` (`SessionStore`, `LoginConfig.Sessions`/`SessionLimitDefault`/`SessionLimitPolicy`, Login/Renew/Logout auf Registry, `clientMeta`), `pkg/adminapi/adminapi.go` (`SessionRevoker`/`WithSessionRevoker`/`revokeUserSessions`/`revokeTenantSessions`), `pkg/adminapi/adminapi_users.go` + `adminapi_admins.go` (Pause-Hooks; `setUserSessionLimit`-Handler + `userDTO.SessionLimit`; `UserStore.SetSessionLimit`), `pkg/adminapi/adminapi.go` (`…/session-limit`-Route), `cmd/wayfinder/sessions.go` (`countingRevoker`, `runSessionJanitor`, Metrik-Zähler), `cmd/wayfinder/main.go` (`sessionRepo`-Wiring, Env-Parse, `/metrics`-Serie); Frontend `frontend/src/stores/admin.js` (`setUserSessionLimit`), `frontend/src/components/admin/AdminUsers.vue` (Spalte + Limit-Dialog) | `pkg/store/sessions_integration_test.go` (**real-PG**: Registry-Lebenszyklus + `last_seen`-Touch, gleitender/absoluter Ablauf, Janitor, Limit `reject`+`evict_oldest`, Revoke via Pause-Zugang/Pause-Mandant-Kaskade/Löschen), `pkg/auth/session_id_test.go` (Session-ID Round-Trip/Tamper/Wrong-Key, Legacy-Ablehnung durch `ParseSessionID`, Registry-Authenticator: Resolve/Revoked→401/Legacy-Fallback/Expired-Legacy), `pkg/tenant/login_registry_test.go` (Session eröffnen, per-Zugang-Override, `reject`→429, `evict`-Policy, Extend, Revoked→401, Legacy-Konversion, Logout-Delete), `pkg/adminapi/adminapi_revoke_test.go` (Pause-User/Admin/Tenant revoked, Reaktivieren revoked nicht, nil-Revoker no-op), `pkg/adminapi/adminapi_users_test.go` (`TestSetUserSessionLimit`/`…Validation`/`…CrossTenant`, `TestUserDTOCarriesSessionLimit`, `…ForbidNonAdmin` um `/session-limit` erweitert), `pkg/store/sessions_integration_test.go::TestIntegrationUserSessionLimitColumn` (**real-PG** Round-Trip Set/Clear/negativ/ErrNotFound), `frontend/src/stores/__tests__/admin.test.js` (`setUserSessionLimit` PUT Zahl/`null`); `go test -p 1 ./...` (inkl. real-PG) + `vet`/`gofmt`/`golangci-lint` + `vitest` (207) + `vite build` grün |
| FR-CFG-002 | Ungültige Konfigurationswerte (u. a. ungültiges `WAYFINDER_LOG_LEVEL`) fallen auf Defaults zurück statt abzustürzen | CLAUDE.md Abschnitt 8 (Cloud-nativ) | ADR0001 | `cmd/wayfinder/main.go` (`loadConfig`, `parseLogLevel`) | `cmd/wayfinder/main_test.go` (`TestLoadConfigInvalidLogLevelFallsBackToDefault`, `TestLoadConfig*SecurityEnvVars*`) |
| FR-OPS-003 | Sauberes Herunterfahren (Graceful Shutdown) auf SIGINT/SIGTERM | CLAUDE.md Abschnitt 8 (Cloud-nativ) | ADR0001 | `cmd/wayfinder/main.go` | manuell verifiziert (M1 Schritt 1) |
| FR-DATA-001 | Track-Datenmodell (`Frame`/`FrameTrack`/`FramePlot`) bildet Firefly's `/ws`-Wireformat verlustfrei ab (Feldnamen, Typen, leere Arrays) | M1 Spezifikation, Firefly `crates/firefly-io/src/frame.rs` | ADR0001 | `internal/firefly/frame.go` | `internal/firefly/frame_test.go` |
| FR-DATA-002 | CAT062-Decoder dekodiert I062/245 (Target Identification / Callsign, FRN 10, ICD 2.1.0) — 8 Zeichen als 6-Bit-IA-5-Codes, MSB-first, fremde/ungültige Codes defensiv auf Leerzeichen abgebildet (robuster Decoder, CLAUDE.md Abschnitt 7). Der `Broadcaster` reicht das Callsign als `callsign` ans Frontend durch; `buildLabel` zeigt es als primäre Label-Zeile (Track-Nummer als Fallback). | Fireflys ICD-CAT062.md v2.1.0 (AP7), CLAUDE.md Abschnitt 2 | — | `pkg/cat062/decoder.go` (`decodeTargetIdentification`, `ia5Decode`), `pkg/broadcast/broadcast.go`, `internal/webui/static/app.js` (`buildLabel`) | `pkg/cat062/decoder_test.go::TestDecodeCallsign` |
| FR-DATA-003 | CAT062-Decoder dekodiert das TSE-Bit (Track Service End, I062/080 Oktett 2 Bit 7, ICD 2.2.0): `decodeTrackStatus` liest die FX-Kette oktett-genau (CNF Oktett 1, TSE Oktett 2, CST Oktett 4) und füllt `TrackStatus.Ended`. Der `Broadcaster` reicht es als `ended` (omitempty) durch; das Frontend (`updateTracksLayer`) **filtert** Tracks mit `ended` heraus → Symbol, Label, Vektor und Trail verschwinden sofort, statt auf einen Timeout zu warten. Robuster Decoder: kürzer endende Records werden längen-geschützt gelesen (CLAUDE.md Abschnitt 7). | Fireflys ICD-CAT062.md v2.2.0 / ADR 0016, CLAUDE.md Abschnitt 2 | — | `pkg/cat062/decoder.go` (`decodeTrackStatus`), `pkg/cat062/types.go` (`TrackStatus.Ended`), `pkg/broadcast/broadcast.go`, `internal/webui/static/app.js` (`updateTracksLayer`) | `pkg/cat062/decoder_test.go::TestDecodeTrackEnd` |
| FR-DATA-004 | CAT065-Decoder (Firefly ADR 0018, ICD 2.3.0 §8): `pkg/cat065.DecodeStatusBlock` dekodiert ASTERIX-CAT065-SDPS-Service-Status-Meldungen (Heartbeat) — `[CAT=0x41][LEN][Record]` mit FSPEC, Items I065/010 (SAC/SIC), I065/000 (Message Type), I065/015 (Service-ID), I065/030 (Time of Day, 1/128 s) und I065/040 (NOGO operationell/degradiert). Robuster Decoder: längen-geprüft, kein Panic, fremde/zusätzliche Items (I065/020, I065/050) werden toleriert, falsche Kategorie/abgeschnittene Eingabe werden verworfen. Byte-genau gegen Fireflys Referenz-Dump verifiziert. Der `receiver` dispatcht den gemeinsamen Multicast-Strom am führenden CAT-Oktett (`0x3E` → Track, `0x41` → Status, sonst Decode-Fehler). | CLAUDE.md Abschnitt 2/7, Fireflys ICD-CAT062.md v2.3.0 / ADR 0018 | — | `pkg/cat065/decoder.go`, `pkg/receiver/receiver.go` (`dispatch`, `handleStatus`) | `pkg/cat065/decoder_test.go` (`TestDecodeReferenceHeartbeat`, `TestDecodeTimeOfDay`, `TestDecodeDegradedStatus`, `TestDecodeRejectsWrongCategory`, `TestDecodeRejectsTruncatedInput`), `pkg/receiver/receiver_test.go::TestDispatchRoutesByCategory` |
| FR-DATA-005 | CAT062-Decoder dekodiert das ES-Age-Subfeld von I062/290 (Extended Squitter / ADS-B-Alter, primäres Subfeld-Bit `0x08`, ICD 2.4.0): die Subfeld-Schleife läuft die gesetzten Bits MSB→LSB ab, liest je gesetztem Bit ein Alter-Byte (LSB 1/4 s) und greift PSR (`0x40`) und ES (`0x08`) positionsbasiert heraus — so bleibt der Decoder korrekt, falls Firefly je ein Subfeld dazwischen einfügt (toleranter Decoder, CLAUDE.md Abschnitt 2/7). `UpdateAge.ESAge *float64` ist nur bei ADS-B-Anteil gesetzt (Radar-only → nil). Der `Broadcaster` reicht es als `adsb_age_s` (omitempty) ans Frontend durch. Byte-genau gegen Fireflys Encoder-Test `single_track_with_adsb_hit_matches_reference_dump` verifiziert; additiv (FSPEC unverändert, kein Wire-Bruch). | Fireflys ICD-CAT062.md v2.4.0 / ADR 0019, CLAUDE.md Abschnitt 2 | — | `pkg/cat062/decoder.go` (case 14), `pkg/cat062/types.go` (`UpdateAge.ESAge`), `pkg/broadcast/broadcast.go` (`TrackMessage.AdsbAgeS`) | `pkg/cat062/decoder_test.go` (`TestDecodeAdsbAge`, `TestDecodeNoAdsbAge`), `pkg/broadcast/broadcast_test.go::TestTracksToMessageMapsAdsbAge` |
| FR-DATA-006 | CAT063-Decoder und Sensor-Status-Verdrahtung (Firefly ADR 0022, **UAP-Standardisierung ADR 0032/ICD 3.0.0**): `pkg/cat063.DecodeSensorBlock` dekodiert ASTERIX-CAT063-Sensor-Status-Meldungen (`[CAT=0x3F][LEN][Record]…`) nach der **echten EUROCONTROL-CAT063-UAP** — FSPEC `0xB8` (FRN 1+3+4+5), Items I063/010 (SAC/SIC des **SDPS**, 2 B), I063/030 (Time of Day, 3 B, 1/128 s), I063/050 (SAC/SIC des **Sensors**, 2 B), I063/060 (CON, variabel via FX; Bits `0xC0`: `0x00`=operationell, `0x40`=degradiert, `0x80`=Initialisierung, `0xC0`=nicht verbunden). `SensorStatus.SAC`/`.SIC` = Sensor (I063/050); `.SDPSSAC`/`.SDPSSIC` = SDPS (I063/010). Robuster Decoder: längen-geprüft, kein Panic, falsche Kategorie/abgeschnittene Eingabe verworfen; **vorwärtskompatibel** — kennt die Längen der übrigen Standard-Items (I063/015, I063/070–092) und überspringt RE (FRN 13) / SP (FRN 14) über ihr explizites Längen-Oktett (Fundament für den per-Quelle-Fehlergrund im RE-Feld, Fireflys ADR 0033), unbekannte FRNs werden verworfen statt fehlinterpretiert. Byte-genau gegen Fireflys 3.0.0-Referenz-Dump verifiziert. **Per-Quelle-Fehlergrund (ADR 0020, Fireflys ICD 3.1.0/ADR 0033, additiv):** der Decoder **liest** das I063/RE-Feld (statt es nur zu überspringen) — `[LEN][SUBFIELD=0x80][SRC-REASON]` → `SensorStatus.Reason` ∈ {`unreachable`,`auth`,`rate_limited`,``}; `cat063.DominantReason` verdichtet die degradierten Sensoren auf den dominanten Grund (Priorität `auth`>`rate_limited`>`unreachable`). Der `receiver` dispatcht `0x3F` → `handleSensorStatus` → `sensorStatusHandler(feedID, statuses)`. `pkg/health.Registry.RecordSensors(feedID, active, total, reason)` füllt `FeedSnapshot.SensorsActive`/`.SensorsTotal`/`.DegradedReason`; `FeedSnapshot.Color()` → `"yellow"`, sobald `0 < active < total` (Grund beeinflusst die Farbe nicht). Der Broadcast-Pfad liefert pro Feed eine `FeedStatusMessage{FeedID, Color, SensorsActive, SensorsTotal, DegradedReason}` an genau diesen Feed abonnierte Clients (per-Feed-Scoping, Multi-Tenant); der Admin-Endpoint `/api/admin/feeds/health` trägt `degraded_reason` ebenfalls. Das ASD-Frontend zeigt gelb mit Sensor-Zähler; der `FeedStatusChip` hängt bei bekanntem Grund ein Label an (`SENSOR AUSFALL · NICHT ERREICHBAR`/`· AUTH-FEHLER`/`· RATENLIMIT`) + Tooltip. Kein neuer Env-Var. | CLAUDE.md Abschnitt 2/7, Fireflys ICD-CAT062.md v3.1.0 / ADR 0032+0033, Firefly-Issue #55, Wayfinder #197, Wayfinder ADR 0010/0019/0020 | ADR 0010, ADR 0019, ADR 0020 | `pkg/cat063/decoder.go` (`DecodeSensorBlock`, `decodeRecord`, `reasonFromCode`, `DominantReason`, `fixedItemLen`, `fspecHas`), `pkg/health/registry.go` (`RecordSensors`, `feedEntry.degradedReason`, `FeedSnapshot.DegradedReason`), `pkg/receiver/receiver.go` (`handleSensorStatus`, dispatch `case cat063.Category`), `cmd/wayfinder/main.go` (`sensorStatusHandler`, `broadcastFeedSnapshot`), `pkg/broadcast/broadcast.go` (`FeedStatusMessage.DegradedReason`), `pkg/adminapi/adminapi.go` (`feedHealthDTO.DegradedReason`), `frontend/src/map/engine.js` (WS-Handler), `frontend/src/stores/asd.js` (`feedDegradedReason`), `frontend/src/components/FeedStatusChip.vue` | `pkg/cat063/decoder_test.go` (u.a. `TestDecodeReasonFromReservedExpansion`, `TestDecodeReasonCodes`, `TestOperationalSensorHasNoReason`, `TestDominantReason`, + Standard-UAP-Tests), `frontend/src/stores/__tests__/asd.test.js` (Reason-Aggregation), `frontend/src/components/__tests__/feedStatusChip.test.js` |
| FR-OPS-004 | Feed-Health/Staleness-Erkennung (Firefly ADR 0018): Wayfinder verfolgt die Ankunft der CAT065-Heartbeats und erkennt Staleness (kein CAT065-Heartbeat seit > `WAYFINDER_FEED_STALE_TIMEOUT`, Default 3 s) und unterscheidet ihn von „leerem Himmel" (Feed lebt, aber kein Verkehr). Zustandswechsel werden als `feed_status`-WS-Nachricht an den Browser gepusht; das Frontend zeigt ein Banner. **AP4-Erweiterung:** `pkg/health.Registry` tracked Health **pro Feed** (statt global) + letzten Block-Größe + `SensorsActive`/`SensorsTotal` (für CAT063-Sensor-Status); `GET /api/admin/feeds/health` (admin-only) liefert `{feed_id, color, stale, ever_seen, last_heartbeat_ago_s, track_count_recent, sensors_active, sensors_total}` für alle Feeds; das Admin-Dashboard zeigt Ampel-Chips pro Feed: **grün** = Heartbeat frisch (auch leerer Himmel), **gelb** = Sensor-Teilausfall (CAT063, ADR 0010 / FR-DATA-006), **rot** = kein Heartbeat. **Status-Granularität (UX-Feinschliff):** Die drei Chip-Helfer (Farbe/Label/Titel) sind in **einen** gemeinsamen Presentation-Helper `admin/feedHealth.js` (`describeFeedHealth`) gezogen (zuvor in drei Komponenten dupliziert); **Rot** wird dort — rein anzeige-seitig, die Wire-Farbe bleibt rot — in zwei operativ unterschiedliche Unter-Zustände aufgeschlüsselt: **„nie gestartet"** (`!ever_seen`: nie ein Heartbeat → Zuweisung/Spawn/Quelle prüfen) vs. **„abgerissen"** (`ever_seen && stale`: Heartbeat lief und riss ab, datiert per `last_heartbeat_ago_s`). Grün trägt zusätzlich den Sensor-Anteil (`aktiv/total Radare`), wenn CAT063 vorliegt. Eine **Colorcode-Referenztabelle** (alle Farben + Unter-Zustände + treibende Snapshot-Felder) steht in `docs/TECHNICAL.md §2.5`. | CLAUDE.md Abschnitt 7 (Cloud-nativ/Observability), Fireflys ADR 0018, Issue #67 | — | `pkg/health/health.go` (FeedHealth + LastHeartbeat), `pkg/health/registry.go` (Registry, FeedSnapshot), `cmd/wayfinder/main.go` (feedRegistry, trackHandler, statusHandler, startProbeServer), `cmd/wayfinder/feeds.go` (buildReceivers per-feed closure), `pkg/adminapi/adminapi.go` (FeedHealthSource, getFeedsHealth), `frontend/src/stores/admin.js` (feedsHealth, loadFeedsHealth), `frontend/src/admin/feedHealth.js` (`describeFeedHealth`, Rot-Split), `frontend/src/components/admin/AdminFeeds.vue`, `frontend/src/components/admin/AdminTenants.vue`, `frontend/src/components/admin/AdminTenantDetail.vue`, `docs/TECHNICAL.md §2.5` (Colorcode-Tabelle) | `pkg/health/health_test.go`, `pkg/health/registry_test.go` (9 Tests), `pkg/adminapi/adminapi_test.go` AP4-Block (4 Tests), `frontend/src/stores/__tests__/admin.test.js` AP4-Block (4 Tests), `frontend/src/admin/__tests__/feedHealth.test.js` (8 Tests: alle Farben + Rot-Split) |
| NFR-OBS-003 | `/metrics` exponiert zusätzlich die Feed-Health (Firefly ADR 0018): `wayfinder_cat065_heartbeats_received_total` (Counter) und `wayfinder_feed_stale` (Gauge, 1 wenn stale). | CLAUDE.md Abschnitt 7 (Observability), `docs/ROADMAP.md` Paket #3 | — | `cmd/wayfinder/main.go` (`startProbeServer` `/metrics`), `pkg/metrics/metrics.go` | manuell verifiziert (Endpoint-Komposition); Bausteine durch `pkg/metrics/metrics_test.go` und `pkg/health/health_test.go` abgedeckt |
| NFR-SEC-001 | **Empfangspfad:** Die CAT062-Multicast-Vertrauensgrenze liegt auf der **Netzwerk-Schicht** (dediziertes, abgeschottetes Segment/VLAN; nur Firefly-Sender + autorisierte ASD-Empfänger), kein anwendungsseitiges Krypto auf CAT062 — Spiegel zu Fireflys ADR 0017; der robuste Decoder bleibt die App-Schutzschicht gegen fehlerhafte Datagramme. **Browser-Rand:** TLS + Authentifizierung primär am vorgelagerten Reverse-Proxy/Ingress; ergänzend fail-closed in Wayfinder: strikter konfigurierbarer Origin-Check auf `/ws` (`WAYFINDER_ALLOWED_ORIGINS`, same-origin immer erlaubt, sonst Allowlist), Tenant-Middleware auf `/ws` + `/api/admin/*` (immer aktiv, fail-closed — ADR 0014), optionales TLS (`WAYFINDER_TLS_CERT`/`_KEY`); Health-/Readiness-Probes (`:8080`) bleiben bewusst unauthentifiziert. | CLAUDE.md Abschnitt 7 (Sicherheit), Fireflys ADR 0017, ehem. Issue #7 | ADR 0003 | Empfangspfad: Deployment-Sache (dokumentiert); Browser-Rand: `pkg/ws/handler.go` (`checkOrigin`), `cmd/wayfinder/main.go` (`authMiddleware`, `loadConfig`, TLS) | Browser-Rand: `pkg/ws/handler_test.go` (`TestCheckOrigin*`), `cmd/wayfinder/main_test.go` (`TestAuthMiddleware*`, `TestLoadConfig*SecurityEnvVars*`) |
| NFR-SAFE-001 | **FSPEC-Härtung + Fuzzing der Vertrauensgrenzen-Decoder (Firefly-Cross-Project #235; Spiegel zu Fireflys QW.2/NFR-SAFE-002):** Die drei ASTERIX-Decoder am **unauthentifizierten** Multicast-Rand (`cat062.DecodeDataBlock`, `cat063.DecodeSensorBlock`, `cat065.DecodeStatusBlock`) verarbeiten **jede** Eingabe ohne Panic **und ohne Hänger** (beliebige Bytes ⇒ `(…, nil)` oder `(nil, error)`). Die FX-verkettete FSPEC ist hart auf `maxFSPECOctets = 36` begrenzt (deckt FRN 1…252 — ein Vielfaches jeder realen UAP; CAT062 endet bei FRN 27); Überlänge ⇒ Decode-Fehler statt unbegrenztem Lesen. **Behobener Befund:** in cat063/cat065 lief die FRN-Iteration über einen `uint8`-Zähler — eine feindliche FSPEC ≥ 37 Oktette ließ ihn bei 255 überlaufen (Wrap → **Endlosschleife/DoS** am Feed-Rand). Fix: Cap **+** `int`-Zähler. cat062 war durch seine feste FRN-Liste vor dem Wrap geschützt, erhält den Cap aber ebenfalls (kein unbegrenztes FSPEC-Lesen). Nachweis: drei `go test`-Fuzz-Targets (Seed-Korpus aus den Referenz-Vektoren) + zeitgeboxter CI-Job (`ci.yml` „Fuzz", 30 s je Target) + Endlosschleifen-Regressionstests mit Timeout-Wächter. | CLAUDE.md §7 (robuster Decoder, „Fuzzing des Parsers vorsehen"), Firefly-Issue #235 / NFR-SAFE-002 | NFR-SEC-001 (Decoder = App-Schutzschicht) | `pkg/cat062/fspec.go` (`maxFSPECOctets`, `ErrFSPECTooLong`, `NewFSPEC`), `pkg/cat063/decoder.go` + `pkg/cat065/decoder.go` (`maxFSPECOctets`, FSPEC-Cap, `int`-FRN-Schleife, `fspecHas`) | `pkg/cat062/fuzz_test.go` (`FuzzDecodeDataBlock`, `TestOverlongFSPECIsRejected`), `pkg/cat063/fuzz_test.go` (`FuzzDecodeSensorBlock`, `TestOverlongFSPECReturns`), `pkg/cat065/fuzz_test.go` (`FuzzDecodeStatusBlock`, `TestOverlongFSPECReturns`); CI `.github/workflows/ci.yml` (`fuzz`-Job) |
| NFR-OBS-001 | Strukturierte Logs (`slog`, JSON) sind durchgängig verdrahtet; das Log-Level ist über `WAYFINDER_LOG_LEVEL` (debug/info/warn/error, Default `info`) konfigurierbar, statt hartkodiert zu sein. Wird ein Client beim Broadcast wegen vollem Sende-Channel entfernt (Eviction), wird dies als Warnung geloggt, statt stillschweigend zu passieren — wichtig für die Betriebs-Diagnose bei überlasteten/hängenden Browser-Clients. | CLAUDE.md Abschnitt 7 (Observability), Logging/Observability-Audit (`docs/ROADMAP.md`, Paket #2) | — | `cmd/wayfinder/main.go` (`Config.LogLevel`, `loadConfig`, `parseLogLevel`), `pkg/broadcast/broadcast.go` (`broadcast`) | `cmd/wayfinder/main_test.go` (`TestLoadConfigParsesLogLevel`, `TestLoadConfigLogLevelDefaultsToInfo`, `TestLoadConfigInvalidLogLevelFallsBackToDefault`), `pkg/broadcast/broadcast_test.go::TestBroadcastEvictsClientWithFullSendChannel` |
| NFR-OBS-002 | `/metrics`-Endpoint (Prometheus-Textformat, Port `:8080` neben `/health`/`/ready`) exponiert Betriebskennzahlen: `wayfinder_cat062_blocks_received_total`/`wayfinder_cat062_tracks_received_total` (Counter), `wayfinder_cat062_decode_errors_total` (Counter, aus dem CAT062-Decoder), `wayfinder_tracks_current` (Gauge, letzter empfangener Block), `wayfinder_ws_clients_connected` (Gauge) und `wayfinder_ws_clients_evicted_total` (Counter, Broadcaster-Eviction). **WF2-23.2:** Label-Unterstützung (`metrics.Metric.With`, escaping) + **pro-Mandant-Serien** `wayfinder_tenant_ws_clients_connected{tenant}`/`wayfinder_tenant_tracks_delivered_total{tenant}` (nur stabile `tenant_id`, keine hochkardinalen Labels). | CLAUDE.md Abschnitt 7 (Observability), `docs/ROADMAP.md` Paket #2 / WF2-23 | — | `pkg/metrics/metrics.go` (`Handler`, `Counter`, `Gauge`, `Label`, `With`), `pkg/broadcast/broadcast.go` (`Broadcaster.EvictedCount`, `TenantMetrics`, per-Tenant-Counter), `cmd/wayfinder/main.go` (`startProbeServer`, `/metrics` per-Tenant) | `pkg/metrics/metrics_test.go::TestHandlerRendersPrometheusExpositionFormat`/`TestHandlerRendersLabels`, `pkg/broadcast/broadcast_test.go::TestBroadcastEvictsClientWithFullSendChannel`/`TestBroadcasterTenantMetrics` (race-clean), `pkg/receiver/receiver_test.go::TestReceiverDecodeErrorCountStartsAtZero` |

| FR-MAP-001 | „Radar Dark Mode": konfigurierbares dunkles, kontrastarmes Basis-Theme (`WAYFINDER_MAP_THEME=dark`\|`osm`, Default `dark`, ungültig → Default). `/api/map-config` liefert den passenden MapLibre-Style (CARTO `dark_nolabels`, key-frei, bzw. OSM-Raster) und ein `theme`-Feld; ein explizites `WAYFINDER_MAP_STYLE_URL` gewinnt. Das Frontend wählt eine zur Theme passende Vordergrund-Palette (helle Labels/Vektoren/Trails auf dunklem Grund). | `docs/ROADMAP.md` Paket #13 (ASD-003), CLAUDE.md §7 (Cloud-nativ) | ASD-003 (geplanter ADR) | `cmd/wayfinder/main.go` (`Config.MapTheme`, `loadConfig`, `darkMapStyle`, `mapConfigHandler`), `internal/webui/static/app.js` (`PALETTES`, `main`) | `cmd/wayfinder/main_test.go` (`TestMapConfigHandlerDarkThemeByDefault`, `TestMapConfigHandlerOSMTheme`, `TestMapConfigHandlerCustomStyleURLReportsTheme`, `TestLoadConfigMapTheme`) |

| FR-MAP-002 | Aeronautik-Daten (Lufträume, Navaids, Waypoints) werden **live von OpenAIP** bezogen und dem Frontend als GeoJSON über `/api/airspace`, `/api/navaids`, `/api/waypoints` bereitgestellt (server-seitiger Abruf, ADR 0004). | `docs/ROADMAP.md` Paket #13 (ASD-003) | ADR 0004 | `pkg/aeronautical/client.go` (`Fetch`), `pkg/aeronautical/service.go` (`Register`, `handler`), `cmd/wayfinder/main.go` (`aeroService`) | `pkg/aeronautical/client_test.go` (`TestFetchTransformsNavaids`, `TestFetchToleratesEmptyAndMissingItems`), `pkg/aeronautical/service_test.go` (`TestServiceRefreshAllPopulatesCache`, `TestServiceServesEmptyBeforeFirstRefresh`) |
| FR-AERO-002 | **OpenAIP globaler Schlüssel via UI + Refresh-Buttons (AERO-2, ADR 0018):** Der **globale** Rückfall-Schlüssel wird zur Laufzeit über die Platform-Admin-UI gesetzt (Kopf-Navigation „OpenAIP"), **verschlüsselt** at rest (AES-256-GCM, `pkg/secret`, AAD `openaip:global`) — **kein Klartext-Geheimnis** in der DB. **Verschlüsselung Pflicht (Option A):** ohne `WAYFINDER_SECRET_KEY` liefert `PUT /api/admin/openaip` **503** (Löschen bleibt erlaubt); die Env `WAYFINDER_OPENAIP_API_KEY` bleibt der schlüssellose Deploy-Weg. Ein UI-Schlüssel **gewinnt** über die Env und greift **sofort** (der globale Schlüssel im `tenantAeroLifecycle` ist ein dynamischer Reader, kein statisches Feld); das Setzen/Löschen löst automatisch **Fetch-all** aus. **Refresh-Buttons:** global (`POST /api/admin/openaip/refresh` → `RefreshAll`) und pro Mandant (`POST /api/admin/tenants/{id}/openaip/refresh` → `Refresh`), beide 202 (asynchron). **Zeitstempel-Anzeige:** die Mandanten-Detailseite zeigt „zuletzt geholt / N Objekte" (aus dem AERO-1-Status) + „Jetzt aktualisieren". **Schema:** Migration `00018_platform_settings.sql` (generisches key/value; erster Nutzer `openaip_global_key` = versiegelt). **Store:** `SettingsRepo` (Get/Set/Delete, crypto-frei). **Adapter (`cmd/wayfinder`):** `globalOpenAIP` (Seal/Open, env-Fallback, `Available`/`Configured`/`SetKey`/`effectiveKey`); der Cipher (`WAYFINDER_SECRET_KEY`) wird **einmal** gebaut und mit der Feed-Credential-Versiegelung (ORCH-2c) geteilt. **API:** `GlobalOpenAIPStore`-Interface + `WithGlobalOpenAIP`; `TenantAeroLifecycle.RefreshAll`. **Abgrenzung:** die **Per-Mandant**-Schlüssel bleiben Klartext (mögliche Folgearbeit); AIRAC-Kalender/Change-Impact = AERO-3. | Betreiber-Wunsch (globaler Schlüssel via UI, verschlüsselt, AIRAC-Refresh), ADR 0018 | **ADR 0018** | `pkg/store/migrations/00018_platform_settings.sql`, `pkg/store/settings.go` (`SettingsRepo`), `cmd/wayfinder/aero.go` (`globalOpenAIP`, `tenantAeroLifecycle.globalKey`-Reader/`RefreshAll`), `cmd/wayfinder/main.go` (geteilter Cipher, `newGlobalOpenAIP`, `WithGlobalOpenAIP`), `pkg/adminapi/adminapi.go` (`GlobalOpenAIPStore`/`WithGlobalOpenAIP`, `TenantAeroLifecycle.RefreshAll`, Routen), `pkg/adminapi/adminapi_openaip.go` (`getGlobalOpenAIP`/`setGlobalOpenAIP`/`refreshAllOpenAIP`/`refreshTenantOpenAIP`), `frontend/src/components/admin/AdminGlobalOpenAIP.vue`, `frontend/src/views/AdminView.vue` (OpenAIP-Sektion), `frontend/src/components/admin/AdminTenantDetail.vue` (Refresh-Button + Zeitstempel), `frontend/src/stores/admin.js` (`loadGlobalOpenAIP`/`setGlobalOpenAIPKey`/`refreshAllOpenAIP`/`refreshTenantOpenAIP`) | `pkg/adminapi/adminapi_openaip_test.go` (`TestGetGlobalOpenAIPStatus`, `TestSetGlobalOpenAIPRequiresCipher`, `TestSetGlobalOpenAIPStoresAndFetchAll`, `TestGlobalOpenAIPRefreshAll`, `TestRefreshTenantOpenAIP`), `cmd/wayfinder/aero_test.go` (`TestGlobalOpenAIPSealRoundTrip`/`…NoCipher`/`…DecryptFailureFallsBackToEnv`), `pkg/store/store_integration_test.go` (`TestIntegrationSettingsRepo`, **real-PG**), `frontend/src/stores/__tests__/admin.test.js` (AERO-2-Block: global GET/PUT/503/refresh-all + per-Tenant refresh) |
| FR-AERO-003 | **AIRAC-Kalender + Change-Impact (AERO-3, ADR 0018):** Zwei Betreiber-Hilfen für proaktive Kundeninfo. **(1) AIRAC-Kalender:** `pkg/airac` berechnet **deterministisch** (28-Tage-Raster, Anker 2020-01-02 = „2001", keine externe Quelle) den aktuellen Zyklus + nächsten Stichtag; `GET /api/admin/airac` → `{ident YYNN, effective, next_ident, next_effective, days_until_next}`; die „OpenAIP"-Plattform-Sektion zeigt „Aktuell 2507 (gültig ab …), nächster Wechsel … (in N Tagen)". **(2) Change-Impact:** beim Refresh vergleicht `pkg/aeronautical.diffCollections` die frisch geholten Daten mit dem vorher persistierten Stand (aus dem In-Memory-Cache, **vor** dem Überschreiben) und ermittelt je `kind` `prev_count`/`added`/`removed`; persistiert in `aeronautical_cache` (Migration `00019`, nullable = Erstbefüllung); `GET /api/admin/tenants/{id}/openaip/changes` liefert die Deltas; die Mandanten-Detailseite zeigt „Luftraum 142 → 145 (+5/−2)". **Robustheit:** Features werden über einen **Inhalts-Hash** (SHA-256 des kanonischen Feature-JSON) verglichen — **keine** Annahme über eine stabile OpenAIP-Feature-ID; der Count-Delta ist exakt, `added`/`removed` sind Churn (ein In-Place-Edit = −1/+1). **Ehrliche Grenze:** eine **namentliche** Zuordnung („genau diese Flugplätze") ist **bewusst nicht** enthalten — sie bräuchte eine Live-Verifikation der OpenAIP-Datenform (Egress in der Entwurfs-Umgebung gesperrt). **Schema:** `00019_aeronautical_cache_change_impact.sql` (`prev_feature_count`/`added`/`removed`, nullable). **API:** `AeroChangesReader`-Interface + `WithAeroChanges`; `CacheStore.Save` trägt die `ChangeSummary`. | Betreiber-Wunsch (AIRAC-Awareness + Change-Impact), ADR 0018 | **ADR 0018** | `pkg/airac/airac.go` (`Cycle`/`identFor`), `pkg/aeronautical/geojson.go` (`ChangeSummary`/`featureHash`/`diffCollections`), `pkg/aeronautical/service.go` (`refreshAll`-Diff, `CacheStore.Save`-Signatur), `pkg/store/migrations/00019_aeronautical_cache_change_impact.sql`, `pkg/store/aero_cache.go` (`Save`-Change-Spalten, `Changes`/`AeroCacheChange`), `cmd/wayfinder/aero.go` (`Save`-Mapping, `TenantAeroCacheChanges`), `pkg/adminapi/adminapi_airac.go` (`getAirac`), `pkg/adminapi/adminapi_openaip.go` (`getTenantOpenAIPChanges`), `pkg/adminapi/adminapi.go` (`AeroChangesReader`/`WithAeroChanges`, Routen), `frontend/src/components/admin/AdminGlobalOpenAIP.vue` (AIRAC-Zeile), `frontend/src/components/admin/AdminTenantDetail.vue` (Change-Chips), `frontend/src/stores/admin.js` (`loadAirac`/`loadTenantOpenAIPChanges`) | `pkg/airac/airac_test.go` (Anker/Zyklus-Wechsel/Jahr-Reset/28-Tage-Abstand/Tageszeit), `pkg/aeronautical/persist_test.go` (`TestDiffCollections`, `TestRefreshAllComputesChangeSummary`), `pkg/store/store_integration_test.go` (`TestIntegrationAeroCacheRepo` + Change-Spalten/`Changes`, **real-PG**), `pkg/adminapi/adminapi_openaip_test.go` (`TestGetAiracReturnsAValidCycle`, `TestGetTenantOpenAIPChanges`/`…UnavailableIs404`), `frontend/src/stores/__tests__/admin.test.js` (AERO-3-Block) |
| NFR-OPS-004 | Die Aeronautik-Layer sind **best-effort** und entkoppeln das ASD von OpenAIP: **nicht-blockierender** Abruf, **Last-Good-Cache** bei Fehler (seit AERO-1 sogar über Redeploys hinweg persistent), leere FeatureCollection bevor je etwas gecached wurde; ein OpenAIP-Ausfall beeinflusst weder den Track-Pfad noch `/ready`. Ohne `WAYFINDER_OPENAIP_API_KEY` ist das Feature still aus (Warn-Log). **Seit AERO-1 (ADR 0018): fetch-once/on-demand statt Ticker** — der frühere periodische `WAYFINDER_OPENAIP_REFRESH` ist deprecated/ignoriert (Details siehe FR-AERO-001). | CLAUDE.md §7 (Cloud-nativ/Verfügbarkeit), ADR 0004/0018 | ADR 0004/0018 | `pkg/aeronautical/service.go` (`BootstrapOnce`, `Hydrate`, `refreshAll`, `handler`), `cmd/wayfinder/main.go` (`loadConfig`, Boot-Goroutine) | `pkg/aeronautical/service_test.go` (`TestServiceKeepsLastGoodOnFailure`, `TestServiceDisabledBootstrapReturnsImmediately`), `pkg/aeronautical/persist_test.go` (`TestBootstrapOnceSkipsFetchWhenPersistedDataExists`) |
| NFR-SEC-002 | OpenAIP-Anbindung als **defensiver Konsument**: API-Key **server-seitig** (`WAYFINDER_OPENAIP_API_KEY`, nie an den Browser), Request-Timeout, Antwort-Größenbegrenzung (`maxResponseBytes`), Validierung/Normalisierung der Geometrie (fehlerhafte Items verworfen). Externe Vertrauens-/Größengrenze analog zum robusten CAT062-Decoder. | CLAUDE.md §7 (Sicherheit), ADR 0004 | ADR 0004 | `pkg/aeronautical/client.go` (`maxResponseBytes`, `validGeometry`, Header), `cmd/wayfinder/main.go` (`http.Client{Timeout}`) | `pkg/aeronautical/client_test.go` (`TestFetchNon200ReturnsError`, `TestValidGeometryRejectsBadInput`, `TestFetchTransformsNavaids`) |
| NFR-OBS-004 | `/metrics` exponiert die OpenAIP-Anbindung: `wayfinder_openaip_fetch_success_total`/`wayfinder_openaip_fetch_failures_total` (Counter) und `wayfinder_openaip_cache_age_seconds` (Gauge, -1 wenn nie erfolgreich) — macht eine alternde/ausfallende Quelle beobachtbar. | CLAUDE.md §7 (Observability), ADR 0004 | ADR 0004 | `cmd/wayfinder/main.go` (`startProbeServer` `/metrics`), `pkg/aeronautical/service.go` (`FetchSuccessCount`, `FetchFailureCount`, `CacheAgeSeconds`) | `pkg/aeronautical/service_test.go` (`TestServiceRefreshAllPopulatesCache`, `TestCacheAgeSeconds`) |

| FR-MAP-003 | Luftraumstrukturen (Sektor-/FIR-Grenzen) werden als Karten-Overlay gerendert (Fill/Line/Label, unter den Track-Layern) und sind über ein Layer-Steuerungs-Panel ein-/ausblendbar (Decluttering). Daten aus `/api/airspace`. | `docs/ROADMAP.md` Paket #13 (ASD-003) | ADR 0004 | `internal/webui/static/app.js` (`addAirspaceLayers`, `loadAeronautical`, `setupLayerControl`), `internal/webui/static/index.html` (`#layer-control`) | manuell verifiziert (Rauchtest Endpoints + visuell; kein JS-Test-Harness, vgl. M1.4) |
| FR-MAP-004 | Waypoints und VOR/NDB-Beacons werden als dedizierte, schaltbare Symbol-Layer gerendert: laufzeit-gezeichnete Icons (Waypoint-Dreieck, VOR-Kompassrose, NDB-Ring; Auswahl per `navaid_kind`), Labels aus `ident`/`name`, zoom-abhängige Sichtbarkeit gegen Clutter. Daten aus `/api/navaids` und `/api/waypoints`. | `docs/ROADMAP.md` Paket #13 (ASD-003) | ADR 0004 | `internal/webui/static/app.js` (`addAeronauticalIcons`, `addNavaidLayers`, `addWaypointLayers`, `loadAeronautical`) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness) |

| FR-ASD-001 | **Erweiterter Data Block (ASD-001):** Das Track-Label zeigt den vollständigen ASD-Data-Block: (1) Callsign (I062/245) als primäre Identifikation, Tracknummer als Fallback; (2) Flugfläche im Format `FLnnn` (I062/136, gerundet auf FL-Einheiten), wenn bekannt; (3) Vertikaltendenz-Indikator `▲`/`▼` an der FL-Zeile, wenn der FL-Wert gegenüber dem vorigen Update um mehr als 50 ft (2 LSB) gestiegen oder gefallen ist (kein Indikator = Level/unbekannt); (4) Bodengeschwindigkeit in Knoten (ganzzahlig, aus Vx/Vy, I062/185: `sqrt(Vx²+Vy²) × 1.9438`) als dritte Zeile, wenn > 0. Alle vier Elemente sind in `buildLabel(track, vTrend)` (`app.js`) gebündelt; die FL-History (`state.trackFlHistory`) wird parallel zu `trackHistory` in `updateTracksLayer` gepflegt und bei TSE/Wegfall eines Tracks bereinigt. Kein Backend-Change nötig (alle Felder bereits im WS-JSON). | `docs/ROADMAP.md` Paket #12 (ASD-001), CLAUDE.md §2 (CAT062-Vertrag) | — | `internal/webui/static/app.js` (`buildLabel`, `updateTracksLayer`, `state.trackFlHistory`) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness, vgl. ASD-003) |

| FR-ASD-006 | **ADS-B-Badge im Data-Block (AP9.9):** Das Track-Label zeigt einen ADS-B-Indikator (`◆`, an die Identifikations-Zeile angehängt), wenn der Track **aktuell** einen ADS-B-Anteil hat — d. h. `adsb_age_s` ist vorhanden und nicht älter als `ADSB_FRESH_THRESHOLD_S` (30 s). `isAdsbFresh(adsbAgeS)` kapselt die Frische-Prüfung; Radar-only-Tracks (kein `adsb_age_s`) tragen kein Badge. Das `◆`-Glyph stammt aus demselben Unicode-Block (Geometric Shapes) wie die bereits genutzten Vertikaltendenz-Pfeile `▲`/`▼`, ist also vom Karten-Font-Stack abgedeckt. Kein Backend-Change über FR-DATA-005 hinaus. Operative Bedeutung: der Lotse erkennt sofort, welche Tracks von der hochpräzisen ADS-B-Selbstmeldung gestützt werden (Firefly ADR 0019). **⚠️ Status (WF2-40):** Dieses Data-Block-Badge ging beim Vue-Port **verloren** (`internal/webui/static/app.js` ist toter Referenz-Code; ausgeliefert wird das Vite-Bundle `dist/`) und war im Vue-Frontend nicht vorhanden. **FR-ASD-007** stellt die ADS-B-Kennzeichnung mit **identischer Frische-Schwelle** als Symbol-Form ◆ wieder her und **löst diese Anforderung ab.** | Fireflys ICD-CAT062.md v2.4.0 / ADR 0019, CLAUDE.md §2; Wayfinder#21 | — | abgelöst durch **FR-ASD-007** (zuvor `internal/webui/static/app.js`, `isAdsbFresh`) | abgelöst durch **FR-ASD-007** |
| FR-ASD-007 | **Spur-Herkunft als Symbol-Form (WF2-40):** Das Track-Symbol kodiert die *track-abgeleitete* Überwachungs-Herkunft als **Form** — **◆ ADS-B** (kooperativ, frische Selbstmeldung), **▢ SSR / Mode S** (kooperativer Sekundär-Reply), **○ Primär (PSR)** (reine Skin-Paint ohne ID) — während die **Farbe weiterhin den Track-Zustand** trägt (confirmed/coasting/tentative/filtered; Präzedenz unverändert). `trackProvenance(track)` leitet die Klasse aus bereits dekodierten Vertragsfeldern ab: `adsb_age_s` **vorhanden und frisch** (`isAdsbFresh`, ≤ 30 s) → `adsb`; sonst `icao_addr`/`mode_3a`/Callsign → `ssr`; sonst `psr`. **Ehrliche Grenze:** keine zertifizierte Per-Plot-Provenienz — CAT062 trägt keine explizite Sensor-Quelle pro Plot (offen als WF2-42). Rendering ohne SDF: 12 vorgerenderte (Form×Zustand)-Icons (`addTrackIcons`, Laufzeit-Canvas), Auswahl per datengetriebenem `icon-image`-`concat`-Ausdruck; Track-Layer von `circle` → `symbol` umgestellt, Opazitäts-/Fade-/FL-Semantik unverändert. `provenance` wird in `updateTracksLayer` (live) und `renderSources` (fading) auf jedes Feature gelegt; das Detail-Panel zeigt die Herkunft im Klartext, die Sidebar eine Form-Legende. Kein Backend-/ICD-Change (alle Felder bereits im WS-JSON). **Löst FR-ASD-006 ab** (verlorengegangenes Data-Block-Badge; Frische-Schwelle erhalten, als Form ◆ wiederhergestellt). | `docs/ROADMAP.md` (WF2-40); Fireflys ICD-CAT062.md v2.4.0 / ADR 0019; CLAUDE.md §2 | — | `frontend/src/map/provenance.js` (`trackProvenance`, `isAdsbFresh`, `PROVENANCE_LABELS`), `frontend/src/map/layers.js` (`makeTrackIcon`, `addTrackIcons`, `addTracksLayer`), `frontend/src/map/constants.js` (`TRACK_STATE_COLORS`), `frontend/src/map/tracks.js` + `render.js` (`provenance`-Property), `frontend/src/components/TrackDetailCard.vue`, `frontend/src/components/LayerFilterContent.vue` (Legende) | `frontend/src/map/__tests__/provenance.test.js` (Wahrheitstabelle + Frische, 15 Tests), `frontend/src/map/__tests__/tracks.test.js` (`updateTracksLayer provenance`); Symbol-Optik manuell verifiziert (kein WebGL-Test-Harness, vgl. FR-ASD-001) |
| FR-ASD-005 | **Höhen- und Filter-Tools (ASD-005):** Min/Max-FL-Filter-Panel im `#layer-control` (zwei Number-Inputs + Ausblenden-Checkbox). `isFlFiltered(flightLevelFt)` prüft ob FL außerhalb [minFL, maxFL] liegt; Tracks mit unbekannter FL passieren immer. `flOpacity(ft)` liefert `0.0` (hide) oder `0.15` (dim) für gefilterte Tracks, `undefined` für ungefilterte. `setupFlFilter()` verdrahtet die Inputs und ruft bei Änderung sofort `renderSources()` auf. `fl_opacity` wird in `renderSources()` pro Feature frisch berechnet (nicht im WSS-Handler) damit Schieberbewegungen sofort wirken; `flight_level_ft` ist daher in `liveTrackFeatures`-Properties gespeichert. `filtered: boolean` auf Track-Symbol-Features für die `circle-color`-Expression (blau-grau für gefilterte Tracks). Alle fünf Layer-Opacity-Expressions erweitert um `["has", "fl_opacity"] → ["get", "fl_opacity"]` (Priorität: fade > fl > coasting > normal). | `docs/ROADMAP.md` Paket #15 (ASD-005), CLAUDE.md §7 | — | `internal/webui/static/index.html` (`#fl-filter`-UI), `internal/webui/static/app.js` (`isFlFiltered`, `flOpacity`, `setupFlFilter`, `state.flFilter`, `renderSources`, alle 5 Layer-Paint-Expressions) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness) |
| FR-ASD-004 | **Track-Lebenszyklus & History-Darstellung (ASD-004):** (4a) **History-Dots:** Vergangene Positionen werden als diskrete Kreise (Radius 2 px, Trail-Farbe, Opacity 0.6/0.2 coasting) zusätzlich zur Trail-Linie gerendert — Positionen aller Einträge in `state.trackHistory` als GeoJSON-Points-Layer (`track-history-dots`), der zwischen Trail-Linie und Speed-Vector sitzt. (4b) **Coasting-Abdunkeln:** Coasting-Tracks werden auf allen Layern visuell gedimmt (Track-Kreis opacity 0.5, Label/Vektor opacity 0.35, Trail/Dots opacity 0.2) via MapLibre-`case`-Paint-Expression auf der `coasting`-Feature-Eigenschaft; `state.trackCoasting: Map` speichert den Zustand für Trail/Dot-Features, die keinen direkten Track-Zugriff haben. (4c) **Graceful Fade-Out bei TSE:** TSE-Tracks (`ended=true`) werden statt sofortigem Entfernen in `state.fadingTracks: Map<track_num, {deadline, track}>` eingetragen (Deadline = `Date.now() + 1500 ms`); `renderSources()` mischt sie mit `fade_opacity`-Property (0–1) in alle vier Map-Sources ein; ein `setInterval`-Loop (~50 ms) ruft `tickFade()` → `renderSources()` auf, bis alle Deadlines abgelaufen sind; alle Paint-Expressions prüfen `["has", "fade_opacity"]` und verwenden es direkt (Vorrang vor Coasting-Dimming). `updateTrackHistory` lässt Fading-Track-History bis zur Deadline stehen. Kein Backend-Change nötig. | `docs/ROADMAP.md` Paket #14 (ASD-004), CLAUDE.md §7 (Sicherheit/Betreibbarkeit) | — | `internal/webui/static/app.js` (`HISTORY_DOTS_SOURCE_ID/LAYER_ID`, `FADE_DURATION_MS`, `state.trackCoasting/fadingTracks/liveTrackFeatures/liveVectorFeatures`, `addHistoryDotsLayer`, `addTrailsLayer/addVectorsLayer/addTracksLayer` paint, `updateTrackHistory`, `updateTracksLayer`, `renderSources`, `tickFade`) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness) |

| FR-ASD-002 | **Anti-Garbling — Label-Deconfliction + Drag&Drop-Pinning (ASD-002):** (B1) **Auto-Deconfliction:** Track-Data-Block-Labels werden nie versteckt. `deconflictLabels(allFeatures)` berechnet in Screen-Space für jeden Track (sortiert nach `track_num` für Determinismus) die optimale Label-Position per **greedy 8-Slot-Algorithmus** (`LABEL_SLOTS` rechts-priorisiert, ATC-konform): für jeden Track werden 8 Kandidatenpositionen (`LABEL_SLOT_RADIUS_PX` = 20 px Abstand) gegen bereits platzierte Label-BBoxen und die Kreis-Footprints **aller anderen** Tracks geprüft (`bboxCollides`). Das eigene Symbol ist absichtlich ausgeschlossen, damit das Label neben seinem Punkt sitzen kann. Erster kollisionsfreier Slot gewinnt; wenn alle belegt sind, greift Slot 0 als Fallback — kein Label wird je unterdrückt. Positionen werden via `map.project/unproject` (Screen↔Geo) konvertiert. Labels und Leader Lines gehen in getrennte GeoJSON-Sources (`LABELS_SOURCE_ID` / `LEADER_LINES_SOURCE_ID`), die auf jedem WSS-Update und bei Karten-Bewegung (`map.on("move") + requestAnimationFrame`-Throttle) frisch befüllt werden. `text-allow-overlap: true` + `text-ignore-placement: true` stellen sicher, dass MapLibres eigene Placement-Engine kein Label versteckt. Leader Lines (0.7 px, label-farbig, opacity 0.55) werden gezeichnet, wenn symbol→label-Abstand > `LEADER_THRESHOLD_PX` = 10 px. Alle Opacity-Properties (`fade_opacity`, `fl_opacity`, `coasting`) werden aus den Track-Features durchgereicht. (B2) **Drag&Drop-Pinning:** `setupLabelDrag(map)` verdrahtet `mousedown` auf dem Label-Layer: beim Drag-Start wird `map.dragPan.disable()`, der aktuelle oder initiale Pixel-Offset im `state.labelPins: Map<track_num, {dx, dy}>` gespeichert; `mousemove` aktualisiert den Pin live und ruft `renderSources()` auf; `mouseup` gibt Pan frei. `dblclick` auf ein Label löscht seinen Pin (revert zu Auto-Platzierung). Abgelaufene Tracks werden in `tickFade()` aus `labelPins` entfernt. Kein Backend- oder ICD-Change. | `docs/ROADMAP.md` Paket #16 (ASD-002) | — | `internal/webui/static/app.js` (`LABELS_SOURCE_ID/LAYER_ID`, `LEADER_LINES_SOURCE_ID/LAYER_ID`, `LABEL_SLOTS`, `LABEL_SLOT_RADIUS_PX`, `LABEL_W/H_PX`, `SYMBOL_BBOX_R_PX`, `LEADER_THRESHOLD_PX`, `state.labelPins`, `addLeaderLinesLayer`, `addLabelsLayer`, `bboxCollides`, `deconflictLabels`, `setupLabelDrag`, `renderSources`, `tickFade`) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness) |

| FR-UI-001 | Build-Pipeline: `npm run build` in `frontend/` erzeugt statische Assets unter `internal/webui/dist/`; Go-Backend embedded `dist/` via `//go:embed dist` (ADR 0002) | ADR 0002 | ADR 0002 | `frontend/vite.config.js`, `internal/webui/webui.go` | `npm run build` ✅ (CI-Gate) |
| FR-UI-002 | ASD-Karten-Engine verlustfrei portiert: alle Funktionen aus `app.js` (deconflictLabels, renderSources, tickFade, updateTracksLayer, setupLabelDrag, buildLabel, isFlFiltered, flOpacity, loadAeronautical) sind als ES-Module in `frontend/src/map/` verfügbar; Vitest-Tests schließen die Testlücke für FR-ASD-001/002/004/005 | ADR 0002, CLAUDE.md §6 | ADR 0002 | `frontend/src/map/` | `frontend/src/map/__tests__/` (Vitest) |
| FR-UI-003 | MD3-Sidebar (Navigation Drawer, links) fasst Layer-Toggles (Lufträume/VOR-NDB/Waypoints) und FL-Filter zusammen; räumt den Karten-Scope von überlappenden Panels frei | ADR 0002 | ADR 0002 | `frontend/src/components/LayerSidebar.vue`, `frontend/src/stores/asd.js` | manuell verifiziert (Vitest für Store-Logik) |
| FR-UI-004 | MD3 Top-App-Bar zeigt Titel und Feed-Status-Chip (grün/rot/grau) statt des bisherigen absolut-positionierten `#feed-status`-Div | ADR 0002, FR-OPS-004 | ADR 0002 | `frontend/src/components/FeedStatusChip.vue`, `frontend/src/App.vue` | manuell verifiziert |
| FR-UI-005 | Track-Detail-Panel (MD3 Card/Bottom-Sheet bei Track-Klick): zeigt Callsign, FL, Bodengeschwindigkeit, Mode 3/A, Track-Nummer, Status — neu, bisher nicht im ASD vorhanden | ADR 0002 | ADR 0002 | `frontend/src/components/TrackDetailPanel.vue`, `frontend/src/components/TrackDetailCard.vue` | manuell verifiziert |
| NFR-UI-001 | MD3-Farbsystem (dunkles ASD-Theme): Surface-Farben dunkel (#0b0f14 Hintergrund, #111720 Surface), Primary #5b8fd6 (Radar-Blau), Typographie Roboto; verhindert Auswaschung des WebGL-Karten-Scopes | ADR 0002 | ADR 0002 | `frontend/src/plugins/vuetify.js` | visuell verifiziert |
| NFR-UI-002 | A11y-Grundlinie: Touch-Targets ≥ 44px (Vuetify-Standard), Kontrast-Check gegen dunkles Theme (Vuetify MD3-Garantie), keine focus-ring-Unterdrückung | ADR 0002 | ADR 0002 | Vuetify-Komponentenstandards | visuell verifiziert |

| FR-UI-010 | **~~Kategorie-Filter-Chips mit Track-Zählern (ASD-010)~~ — ENTFERNT (2026-07-02, E2E-Design-Durchgang):** Die drei Status-Chips (Confirmed/Coasting/Tentative) oben zentriert wurden entfernt — der Track-**Status** wird jetzt über die **Symbolik** angezeigt (Farbe + hohl für Coasting, FR-DATA-007/#136), die Chips waren redundant. Auf Freigabe des Projektverantwortlichen wurde die zugehörige **Kategorie-Filterfunktion komplett fallen gelassen** (nicht in die Sidebar verschoben). Ausgebaut: `TrackFilterChips.vue` (gelöscht), Store `trackCounts`/`hiddenCategories`/`setTrackCounts`/`toggleCategoryFilter`, `render.js` `hiddenNums`/`trackCategory`, `engine.js` `updateTrackCounts`, MapCanvas-Watcher. | ASD-010 (Phase 1); Entfernung im E2E-Design-Durchgang | ROADMAP ASD-010 | — (Feature entfernt; kein Code mehr) | `frontend/src/views/__tests__/scopeChrome.test.js` (Regressions-Guard: Chips + Filter-Maschinerie weg); vitest grün |
| FR-UI-011 | **Range-Rings + Scale-Bar + Nord-Orientierung (ASD-012):** (A) **Range-Rings** — konzentrische Kreise **konstanter Boden-Distanz** um den konfigurierten View-Mittelpunkt (`/api/map-config`, tenant-skopierbar via WF2-30/31) als Distanz-Raster für den Lotsen (abgegrenzt von den Sensor-Coverage-Ringen, Paket 6). **Geodätische** Erzeugung (`rangerings.js`): jeder Ring-Vertex liegt in *jede* Richtung in derselben Boden-Distanz — der naive Gleich-Grad-Ansatz würde den Ring in Längsrichtung stauchen (auf Web-Mercator „gequetscht"); im Test bewiesen (gleiche Meter, ungleiche Grad). **Operator-konfigurierbar live:** Abstand (5/10/15 NM) + Anzahl als **reaktiver Pinia-State** (`asd.js` `rangeRingConfig`, Default 10 NM / 5), Sidebar-Toggle + Select/Slider (nur sichtbar wenn aktiv, default aus); MapCanvas-Watcher → `engine.updateRangeRings` regeneriert das Overlay. (B) **Scale-Bar** — MapLibre `ScaleControl{unit:'nautical'}` (absolute Distanzreferenz bei jedem Zoom). (C) **Nord-Orientierung** — MapLibre `NavigationControl{showCompass}` (zeigt Bearing, Klick → Nord-up; freie Kartendrehung bleibt); der alte hand-gebaute Reset-Nord-Button wurde **entfernt** (Declutter). Kein wörtliches „Track-up" (für ein Multi-Track-ASD undefiniert). **Nachtrag (E2E 2026-07-04): Ring-Labels weg vom Nord-Radial.** Die Ring-Beschriftungen lagen ursprünglich alle auf dem 12-Uhr-Radial (Norden), wo sie mit der Kopf-Chrome (Kompass, Header, Feed-Badge) kollidierten. Zwischenschritt: Staffelung über vier Diagonalen — auf Betreiberwunsch (#170) wieder verworfen (wirkte unruhig). **Endstand:** alle Labels einheitlich auf dem **NE-Radial** (45°, `LABEL_BEARING`), je auf ihrem eigenen Ring-Radius (kein Überlappen, da verschiedene Radien); nie nach Norden, hält Abstand zur Kompass-Control (oben links), eine feste Stelle für den Entfernungs-Readout. | `docs/ROADMAP.md` (ASD-012) | — | `frontend/src/map/rangerings.js`, `…/layers.js` (`addRangeRingsLayer`), `…/constants.js` (IDs/Defaults/Palette), `…/engine.js` (Controls, `updateRangeRings`), `…/stores/asd.js` (`rangeRingConfig`), `…/components/LayerFilterContent.vue` (Bedienung), `…/components/MapCanvas.vue` (Watcher), `…/components/MapControls.vue` (Button entfernt) | `frontend/src/map/__tests__/rangerings.test.js` (Distanz/Bearing, **Anti-Squish**, Ring-Schließung/Radius, GeoJSON/Clamp, **Label-Staffelung auf Diagonalen**); Optik (Controls-Platzierung, Ring-Stil, Kompass) **manuell verifiziert** (kein WebGL-Test-Harness, vgl. FR-ASD-001) |

| FR-UI-012 | **Admin-Einstieg in der Navigation Rail (Req 1):** Die Navigation Rail zeigt unten (ans Schienen-Ende gepinnt, Icon `mdi-shield-account`) einen **Admin**-Eintrag, der **ausschließlich** sichtbar ist, wenn die Rolle der aktuellen Identität `admin` ist. Klick navigiert per Vue Router auf den bestehenden `/admin`-View. Die Sichtbarkeit ist **strikt an die server-autoritative Identität gekoppelt** (`/api/admin/whoami`, einmalig in `onMounted` geprüft, **fail-closed**: `user` bzw. 401/403/404 → Eintrag bleibt unsichtbar). Der Store exponiert `isAdmin` (computed `role === 'admin'`). Reine Kosmetik-Gating — die echte Grenze zieht der Server (`RequireRole` auf `/api/admin/*`). Mobile-Layout: identisch gegatetes `v-list-item`. Kein Backend-/ICD-Change. | Req 1 (operative Anforderung 2026-06-23), CLAUDE.md §7 (Browser-Rand absichern) | — (nutzt bestehendes `/admin`-Routing + `RequireRole`) | `frontend/src/stores/admin.js` (`isAdmin`), `frontend/src/components/NavigationRail.vue` (`goAdmin`, `onMounted`-Identity-Probe, Rail-Button + Mobile-`v-list-item`, beide `v-if="isAdmin"`) | `frontend/src/stores/__tests__/admin.test.js` (`isAdmin`-Matrix: `admin` → true, `user`/403 → false); Build grün, 82 Vitest-Tests grün |
| FR-UI-013 | **View-Config-Formular erklärt seine operative Bedeutung (Issue #57, C2.2):** Das View-Editor-Formular in `AdminTenantDetail.vue` (FR-ADMIN-002) trägt **Erläuterungs-Captions**, die einem neuen Administrator den *operativen* Effekt jeder Einstellung verständlich machen — bisher fehlte das beim Zentrum/Zoom-Block, und AOI/FL erklärten ihre Semantik nicht ausreichend. (1) **Zentrum & Zoom**: Caption „legt den Start-Kartenausschnitt der ASD-Karte für alle Clients dieses Mandanten fest". (2) **AOI** (clientseitig aus Zentrum + Radius, `geo.js`): Caption stellt klar, dass die AOI eine **harte serverseitige Daten-Minimierungsgrenze** ist (Tracks außerhalb werden serverseitig verworfen — `broadcast.ViewFilter.admits`), **keine** Anzeigepräferenz. (3) **FL-Band**: Labels mit Einheit „FL min/max (× 100 ft)" (FL100 = 10 000 ft) plus Caption zum **Fail-open**-Verhalten — Tracks ohne gemeldete Flugfläche (keine Mode-C-Höhe) werden immer zugestellt (entspricht `broadcast.ViewFilter.admits`: `FlightLevelFt == nil ⇒ deliver`). Reine UI-Copy, kein Backend-/Schema-/ICD-Change. **Nachtrag (E2E-Fix 2026-07-04): Zentrum & Zoom wirken jetzt tatsächlich.** Die Caption „legt den Start-Kartenausschnitt … fest" war eine **unerfüllte Zusage** — die ASD-Karte zentrierte immer auf die globale `WAYFINDER_MAP_CENTER_*`-Env (Frankfurt), unabhängig von der Mandanten-Ansicht (nur `icao`/`fl_min` kamen aus der View, daher zeigte die Kopfzeile z. B. korrekt `EDDH`, während die Kamera auf Frankfurt saß). Behoben: `whoami` liefert `center_lat`/`center_lon`/`zoom` der effektiven Ansicht (`omitempty`; fehlt eine View-Config → Env-Fallback, nie 0/0); das Frontend öffnet die Karte auf dem Mandanten-Zentrum (Engine-`initMap(initialCenter)`) und der „Neu zentrieren"-Knopf + die Range-Ringe folgen ihm (`effectiveCenter` statt Env); ein Ansicht-Wechsel (z. B. Impersonation auf einen anderen Sektor) re-zielt die Kamera (`applyViewCenter`, No-op bei unverändertem Zentrum). **Nachtrag (#219, Gastmodus-Race 2026-07-07): Re-Zielung im Gastmodus ging verloren.** `initMap` ist asynchron; betritt ein Admin den Read-Only-Gastmodus (ADR 0008), hält der Session-Store beim Karten-Mount noch die veraltete, nicht-impersonierte Sicht (leeres `viewCenter`) — das impersonation-aware `whoami` landet erst *während* des `await initMap`, sodass der `applyViewCenter`-Watcher gegen ein noch `null`es `mapEngine` feuerte und die Ziel-Ansicht (z. B. EDDH) verwarf; Karte **und** „Ansicht zurücksetzen" blieben auf dem globalen Frankfurt-Default gepinnt (gastmodus-spezifisch, da nur der Admin den `adminGate` aus #208 durchläuft). Behoben: `MapCanvas` gleicht nach dem Auflösen von `initMap` die Karte auf die **aktuelle** effektive Sicht ab (`applyViewCenter(session.viewCenter)` + `applyWeatherAOI(session.aoi)`, No-op bei unveränderter Sicht). | Issue #57 (Testlauf-Befund C2.2); **E2E-Befund 2026-07-04** (Karte auf Frankfurt trotz EDDH-Ansicht), **Issue #219** (Gastmodus-Reset auf Frankfurt), CLAUDE.md §1 (Bedienbarkeit ASD) | — (UI-Copy zu FR-ADMIN-002 / FR-OPS-008-AOI) | `frontend/src/components/admin/AdminTenantDetail.vue` (Captions + FL-Labels); **Nachtrag:** `pkg/adminapi/adminapi.go` (`whoamiDTO.CenterLat/CenterLon/Zoom` aus `GetEffective`), `frontend/src/stores/session.js` (`viewCenter`), `frontend/src/map/engine.js` (`initMap(initialCenter)`, `applyViewCenter`, `effectiveCenter`), `frontend/src/components/MapCanvas.vue` (Verdrahtung + Watcher; **#219:** Reconcile nach `initMap`) | `frontend/src/components/admin/__tests__/adminTenantDetailCaptions.test.js` (Caption-/Einheit-/Fail-open-Text-Regression, 4 Tests); **Nachtrag:** `pkg/adminapi/adminapi_test.go` (`TestWhoamiIncludesEffectiveViewCenter`, `TestWhoamiOmitsViewCenterWhenNoConfig`), `frontend/src/stores/__tests__/session.test.js` (`viewCenter` gesetzt/null/Äquator), `frontend/src/components/__tests__/mapCanvasViewCenter.test.js` (Verdrahtung + **#219** Reconcile-Regression); `go test`/`vet`/`gofmt` + `vitest` 416 + `vite build` grün |
| FR-UI-014 | **ASD-Login-Gate + Mandanten-Login (WF2-12.4, ADR 0003/0014):** Der operative Karten-Pfad `/` ist selbst auth-gated — nach ADR 0014 ist die Anmeldung immer aktiv, auch für den Lotsen. Beim Öffnen von `/` löst der ASD die Sitzung über den **rollen-agnostischen** `GET /api/whoami` (hinter der Tenant-Middleware, **nicht** `requireAdmin`) auf: keine Sitzung ⇒ **Login-Maske** statt leerer Karte, gültige Sitzung ⇒ Live-Lagebild; **`MapCanvas`/`/ws` mounten erst nach der Anmeldung** (kein unauthentifizierter WebSocket). Ein **Mandanten-Nutzer** (Rolle `user`) kann sich damit erstmals rein per UI anmelden und **nur** seinen gescopten Strom sehen (Isolation bleibt serverseitig, WF2-21). **Logout** ist UI-bedienbar: Konto-/Abmelden-Menü auf der Karte (für Admins zusätzlich Verknüpfung zur Administration) und Abmelden-Knopf im Admin-Header. Fail-closed: keine Identität ⇒ `401` ⇒ Login, nie eine ungescopte Karte. Wiederverwendbare `LoginCard`, `session`-Store, geteilter `apiFetch`. **Keine CAT062-Wirkung** (Browser-Rand; additiv `GET /api/whoami`). | ADR 0014 (Auth immer an), ADR 0003 (Browser-Rand), UI-Audit-Befund (UI-only-E2E) | **ADR 0003/0014** | `cmd/wayfinder/main.go` (`GET /api/whoami` hinter `tenantMW`), `pkg/adminapi/adminapi.go` (`WhoamiHandler`); `frontend/src/views/AsdView.vue` (Auth-Gate), `frontend/src/components/LoginCard.vue`, `frontend/src/stores/session.js` (`probe`/`login`/`logout`), `frontend/src/stores/admin.js` (`logout`), `frontend/src/views/AdminView.vue` (Logout-Knopf), `frontend/src/api.js` (geteilter `apiFetch`) | `pkg/adminapi/adminapi_test.go::TestWhoamiHandlerIsRoleAgnostic` (RoleUser→200, ohne Identität→401); `frontend/src/stores/__tests__/session.test.js` (probe/login/logout, fail-closed), `admin.test.js` (logout-Reset), `components/__tests__/loginCard.test.js` + `views/__tests__/asdViewAuthGate.test.js` (Struktur-Guards); `go test/vet/gofmt` + `vitest` 199 + `vite build` grün |
| FR-UI-015 | **Sliding-Session + Login-Overlay (WF2-12.5, ADR 0003/0014):** Bei aktiver ASD-Nutzung wird der Lotse **nie** ausgeloggt; eine verlassene Konsole läuft nach dem Idle-Fenster ab; ein Ablauf ist **sichtbar** (Login-Overlay) statt stillem Freeze — für ein ASD ist eine veraltete Lage, die aussieht wie live, gefährlich. **„Aktiv"** = ASD-Karte offen mit lebender WebSocket (**nicht** Maus/Tastatur). **Server:** `POST /api/session/renew` (builtin) re-mintet das Cookie mit frischer TTL für den authentifizierten Principal, hinter der Tenant-Middleware (`401` ohne Sitzung). **Client:** Verlängerung alle 10 min (`RENEW_INTERVAL_MS`) + bei Tab-Fokus (`visibilitychange`) + bei jedem WS-Reconnect, nur solange `authed`. **Overlay:** bei WS-Close prüft der Client die Sitzung (`/api/whoami`); ist sie weg → `LoginCard` „Sitzung abgelaufen"; ein transienter Netz-Abriss bleibt angemeldet (Engine reconnectet selbst). **Standardwerte:** `WAYFINDER_SESSION_TTL`=12h (Sliding-Idle-Fenster), Renew-Takt 10 min; das absolute Maximum ist **opt-in nachgezogen** (FR-UI-016). Keine CAT062-Wirkung (Browser-Rand; additiv `POST /api/session/renew`). | ADR 0014 (Auth immer an), ADR 0003 (Browser-Rand), Betreiber-Wunsch „nicht mitten in der Schicht ausloggen" | **ADR 0003/0014** | `pkg/tenant/login.go` (`RenewHandler`), `cmd/wayfinder/main.go` (Mount hinter `tenantMW`); `frontend/src/stores/session.js` (`renewNow`/`startRenew`/`stopRenew`/`expired`), `frontend/src/views/AsdView.vue` (Renew-Loop, `visibilitychange`, `connection-change`, „Sitzung abgelaufen"), `frontend/src/map/engine.js` (`onConnectionChange`), `frontend/src/components/MapCanvas.vue` (`connection-change`-Event) | `pkg/tenant/login_test.go` (`TestRenewHandlerReissuesCookie`/`…RejectsUnauthenticated`); `frontend/src/stores/__tests__/session.test.js` (renewNow/probe-expired/startRenew-stopRenew Fake-Timer), `views/__tests__/asdViewAuthGate.test.js` (Verlängerung/connection-change/„Sitzung abgelaufen"); `go test/vet/gofmt` + `vitest` 205 + `vite build` grün |
| FR-UI-016 | **Absolutes Sitzungs-Maximum (WF2-12.6, ADR 0003/0014):** Ergänzt die Sliding-Session (FR-UI-015) um eine **harte Obergrenze**: eine Sitzung lebt — egal wie aktiv — **nie** länger als `WAYFINDER_SESSION_MAX_LIFETIME` ab **Erst-Login**; danach Zwang zum Neu-Login. Sicherheits-Härtung (eine offen vergessene/kompromittierte Konsole schließt sich garantiert), in bewusster Abwägung zu „nicht mitten in der Schicht ausloggen" → **opt-in, Default aus** (`0`/leer). **Technik:** Session-Token trägt einen signierten `iat`-Claim (Erst-Login), der über jeden Renew **unverändert** durchgereicht wird; Login **und** Renew kappen die Cookie-Expiry auf `iat + MAX` (so greift das Maximum auch ohne Renew über die normale Ablaufprüfung); der `RenewHandler` verweigert `401` (kein Cookie), sobald `jetzt − iat > MAX`. **Rückwärtskompatibel:** alte Cookies ohne `iat` parsen weiter (`iat=0`) und werden beim ersten Renew **sanft** auf „jetzt" verankert (kein Massen-Logout beim Upgrade). Durchsetzung liegt **nur** im Login/Renew, **nicht** in `ParseSession` → der geteilte Impersonation-Grant (ADR 0008) bleibt unberührt. **Standardwert:** aus; Produktions-Richtwert bei Aktivierung 12h (Schicht); **Probelauf** `30m`. Keine CAT062-Wirkung. | ADR 0014, ADR 0003 (Browser-Rand), Sicherheits-Härtung (Betreiber-Wunsch, Probelauf 30m) | **ADR 0003/0014** | `pkg/auth/session.go` (`Claims`/`iat`, `MintSessionAt`, `ParseSessionClaims`, Legacy-Parse), `pkg/tenant/login.go` (`LoginConfig.MaxLifetime`/`expiry`, `LoginHandler`+`RenewHandler` gekappt/durchgesetzt), `cmd/wayfinder/main.go` (`WAYFINDER_SESSION_MAX_LIFETIME`) | `pkg/auth/session_test.go` (`TestSessionClaimsRoundTrip`/`…MintAtPreservesIssuedAt`/`…LegacyTokenParses`/`…IssuedAtTamperDetected`); `pkg/tenant/login_test.go` (`TestLoginCapsExpiryAtMaxLifetime`, `TestRenewHandlerPreservesIssuedAt`/`…EnforcesMaxLifetime`/`…MaxLifetimeZeroDisabled`/`…LegacyCookieSoftAnchor`); `cmd/wayfinder/tenancy_test.go` (Env-Parse/opt-in-aus); `go test/vet/gofmt` grün |

| FR-UI-018 | **Sidebar-Neugliederung + Coverage-/FL-Kontext (E2E-Findings #114/#115/#116/#121):** Die Lotsen-Sidebar startet **eingeklappt** (nur die 56 px-Icon-Schiene sichtbar, Karte voll; #115). Drei Sektionen statt „Filter": **Layer** (Layer-Schalter + Spurherkunft-Legende), **Filter** (FL-Filter) und ganz unten **Nutzer-Account** (Abmelden; #116). Der FL-Filter zeigt das **zulässige FL-Band der effektiven Ansicht** (`whoami.fl_min/fl_max`) als grauen Hinweis + Platzhalter (#116). Der Schalter **„Radarabdeckung"** ist **deaktiviert**, wenn serverseitig keine Coverage-Sensoren konfiguriert sind (`/api/map-config.coverage_sensor_count = 0` → `asd.coverageAvailable`), statt wirkungslos zu schalten (#114). Beim Ein-/Ausklappen wird MapLibre neu vermessen (`map.resize()` nach der Transition), sonst bleibt ein toter grauer Streifen (#121). **Nachtrag (Design-Aufräumung, #176):** (1) Rail↔Panel-Trennlinie sichtbar (`nav-panel__divider`), (2) **jede** Abschnitts-Überschrift trägt einen Unterstrich (vorher nur die `--spaced`-Variante), (3) die Luftraum-Untergruppen sind **einheitlich blau** ohne farbige Punkte (Kartenfarben in `AIRSPACE_GROUPS` bleiben), (4) der **Sammel-Toggle „Lufträume" entfällt** — die vier Gruppen sind eigenständige Toggles, die Layer-Sichtbarkeit wird abgeleitet (`asd.setAirspaceGroup`: sichtbar ⇔ mind. eine Gruppe aktiv). | E2E-Testlauf-Findings (2026-07-02); #176 | ROADMAP ASD-013 | `frontend/src/components/NavigationRail.vue` (drei Sektionen, `activePanel=null` default, `panel-resize`-Event), `frontend/src/components/LayerFilterContent.vue` (`section`-Prop layers/filters/account, Coverage-Gate, FL-Hinweis), `frontend/src/views/AsdView.vue` (`onPanelResize`→`resize`), `frontend/src/components/MapCanvas.vue` (`resize` exponiert), `frontend/src/map/engine.js` (`setCoverageAvailable` aus `coverage_sensor_count`), `frontend/src/stores/asd.js` (`coverageAvailable`), `frontend/src/stores/session.js` (`flMin`/`flMax`), `pkg/adminapi/adminapi.go` (`whoami` `fl_min`/`fl_max` aus `views.GetEffective`) | `pkg/adminapi/adminapi_test.go::TestWhoamiIncludesEffectiveFLBand`/`TestWhoamiOmitsFLBandWhenUnset`; `frontend` vitest (session `flMin/flMax`) + `vite build` grün |
| FR-UI-019 | **Design System v1 — Tokens & self-hosted Webfonts (ADR 0015):** Design-Tokens als CSS-Custom-Properties (`--wf-*`) für Farbe/Typografie/Spacing/Radius/Elevation, einmalig via `tokens/index.css` importiert; `base.css` bindet die Grund-Schrift an `body` + zwei Signatur-Klassen (`.wf-mono` für tabellarische Zahlen-Readouts, `.wf-overline` für Sektions-Header). **Roboto + Roboto Mono self-hosted** via `@fontsource` (nur Subsets **latin + latin-ext**), in `main.js` importiert — **kein Laufzeit-CDN** (offline/luftgespaltene ATC-Konsole). **Autoritäts-Nähte:** das Vuetify-Theme (`vuetify.js`, `asdDarkTheme`) bleibt Chrome-Ursprung und wird mit `colors.css` im Gleichschritt gepflegt; maßgeblich für die MapLibre-GL-Paint-Ausdrücke bleibt `constants.js` (Domänen-Sektion in `colors.css` ist nur DOM-Spiegel für Legenden). Reines **Fundament** — kein Umstyling in diesem Schritt, sicherheitsrelevante Karten-Engine (`src/map/*`) unberührt (Fortführung ADR 0002). **Daten-Ehrlichkeit:** nur die belegbare Civil-Track-Farbe wird geführt; mil/hostile/neutral, Zuständigkeit, STCA bleiben mangels Wire-Daten ungenutzt. | Neues Claude-Design (Projektverantwortlicher), Reskin-Plan (`docs/STATUS.md`, 2026-07-02) | **ADR 0015** | `frontend/src/design/tokens/{colors,typography,spacing,radius,elevation,index}.css`, `frontend/src/design/base.css`, `frontend/src/main.js` (Font-/Token-Imports), `frontend/package.json` (`@fontsource/roboto`, `@fontsource/roboto-mono`), `internal/webui/dist` (neu eingebettet, Fonts als self-hosted Assets) | `frontend` vitest (**218** grün, keine Regression) + `vite build` grün (Fonts im Bundle, nur latin/latin-ext); `go build ./...` + `go test ./internal/webui/...` (eingebettetes `dist`) grün |
| FR-UI-020 | **ASD-Kopfzeile: ICAO-Kürzel (per-Mandant) + UTC-Uhr (Reskin 3a, ADR 0015):** Oben mittig zeigt die ASD den optionalen **ICAO-Location-Indicator** des Mandanten (Sektor/FIR, z. B. `EDGG·KTG`) und eine **Live-UTC-Uhr** (Wanduhr-Zeitreferenz des Lotsen, getrennt von der CAT062-Datenzeit). **Daten-Ehrlichkeit:** CAT062 trägt keine Sektor-Identität → das ICAO-Kürzel ist **per-Mandant-CONFIG** an der View-Config, kein Track-Feld; ohne Wert bleibt die Kopfzeile ohne ICAO (omitempty). Persistenz: Migration `00015_view_configs_icao.sql` (nullable `icao TEXT`), `store.ViewConfig.ICAO` (Upserts/Scan analog `fl_min/fl_max`), `viewDTO.icao` + `validateView` (Länge ≤ 12) + `normalizeICAO` (trim, leer→NULL), `whoamiDTO.icao` aus der Effektiv-View. Admin pflegt es im View-Editor (`AdminTenantDetail`), die ASD liest es aus `whoami` (session-Store). | Neues Claude-Design + Projektverantwortlicher („Sektor-Header = ICAO-Kürzel") | **ADR 0015** | `pkg/store/migrations/00015_view_configs_icao.sql`, `pkg/store/view_configs.go` (`ICAO`), `pkg/adminapi/adminapi.go` (`viewDTO.ICAO`, `validateView`, `normalizeICAO`, `toViewConfig`/`toViewDTO`, `whoamiDTO.ICAO`, whoami-Handler), `frontend/src/stores/session.js` (`icao`), `frontend/src/components/AsdHeader.vue`, `frontend/src/views/AsdView.vue` (Overlay), `frontend/src/admin/validateView.js`, `frontend/src/components/admin/AdminTenantDetail.vue` (ICAO-Feld) | `pkg/adminapi/adminapi_test.go` (`TestWhoamiIncludesEffectiveICAO`/`…OmitsICAOWhenUnset`, `TestValidateViewICAO`, `TestNormalizeICAO`), `frontend/src/admin/__tests__/validateView.test.js` (ICAO-Parität); vitest/`vite build`/`go test` grün |
| FR-UI-021 | **Schwebende Scope-Legende + Vektor-Readout (Reskin 3b, ADR 0015):** Ausklappbares Legenden-Panel unten links (`ScopeLegend.vue`): **FORM·HERKUNFT** (Glyphen A/F/■/○, auf die Feeds des Mandanten gefiltert) + **FARBE·STATUS** (nur real gerenderte Zustände Bestätigt/Coasting/Tentativ/Ausgefiltert). **Daten-Ehrlichkeit:** keine Ziel-Typ-Farben (Firefly sendet nur Civil) und **kein** Alarm-Block (Safety-Nets sind ein späteres Häppchen). Speed-Vektor-Readout unten rechts („Vektor N min" aus `VECTOR_LOOKAHEAD_S`); native nautische `ScaleControl` von unten-links nach **unten-rechts** verschoben (macht unten-links für die Legende frei). Die Provenienz-Legende liegt jetzt als **eine Quelle** in `map/provenance.js` (`PROVENANCE_LEGEND` + `filterProvenanceLegend`), die Sidebar nutzt sie statt einer Duplik-Liste. **⚠️ Nachtrag (E2E 2026-07-04): Readout unten rechts entfernt.** Das Pill „‹Breite› NM Breite · Vektor ‹N› min" wurde im Akzeptanztest als Verwechslungsquelle erkannt — es sah aus wie ein Maßstabsbalken, war aber nur die Schirmbreite, und stand irreführend neben den Range-Ringen. Ausgebaut: `AsdView.vue`-Overlay + `.vector-readout-overlay`-CSS, `engine.js` `reportViewportWidth` (+ `haversineNM`-Import), `asd.js` `viewportWidthNM`/`setViewportWidth`. Distanz liest der Lotse aus den Range-Ringen; die Vorhalte-Zeit trägt der Geschwindigkeitsvektor am Symbol selbst (`VECTOR_LOOKAHEAD_S` bleibt für die Vektoren). `scopeChrome.test.js` invertiert (Readout-Kette darf nicht zurückkehren). Die `ScopeLegend` unten links bleibt. | Neues Claude-Design (Legende-Screenshot) | **ADR 0015** | `frontend/src/components/ScopeLegend.vue`, `frontend/src/map/provenance.js` (`PROVENANCE_LEGEND`/`filterProvenanceLegend`), `frontend/src/components/LayerFilterContent.vue` (nutzt geteilten Helper), `frontend/src/map/engine.js` (ScaleControl→bottom-right), `frontend/src/views/AsdView.vue` (Legende/Readout-Overlays) | `frontend/src/map/__tests__/provenance.test.js` (`filterProvenanceLegend`-Fälle); vitest 223 + `vite build` grün |
| FR-UI-022 | **Lotsen-Mess-Werkzeuge RBL/DIST/QDM (Häppchen 4, ADR 0015):** Client-seitige Karten-Geometrie, kein Feed-Bedarf. **RBL** (Range/Bearing-Line, auf der Karte ziehen), **DIST** (Distanz/Peilung zwischen zwei angeklickten Tracks), **QDM** (Peilung Track→Zielpunkt). Reine Großkreis-Geometrie in `map/tools.js` (`haversineNM`/`bearingDeg`/Formatter/`measureText`); MapLibre-Mess-Controller `map/measure.js` als **eigenständiges Overlay** (Source + Linien-/Punkt-Layer + Event-Handler, entkoppelt von `engine.js`; Track-Pick via `queryRenderedFeatures`); `stores/tools.js` (`activeTool`/`readout`/`hint`, toggelndes `selectTool`); **seit Häppchen 3** liegen die Werkzeug-Buttons RBL/DIST/QDM als **Rail-Icons** in `NavigationRail.vue` (Toggle → `selectTool`, Design-Mockup; zusätzlich **Zoom +/−** in der Rail, delegiert über `AsdView`→`MapCanvas.zoomIn/zoomOut`), und `MeasureStatus.vue` zeigt über der Karte **nur noch** Hinweis + Live-Readout und hält die Tastenkürzel **R/D/Q/Esc**. Distanz/Peilung als Vue-Readout (kein MapLibre-Symbol → keine Glyph-Abhängigkeit). Im Werkzeug-Modus öffnet ein Track-Klick **kein** Detail-Panel (`AsdView`). **PROBE** bewusst ausgelassen (Inhalt undefiniert). | Neues Claude-Design (Rail-Werkzeuge) | **ADR 0015** | `frontend/src/map/tools.js`, `frontend/src/map/measure.js`, `frontend/src/stores/tools.js`, `frontend/src/components/NavigationRail.vue` (Rail-Werkzeuge + Zoom, Häppchen 3), `frontend/src/components/MeasureStatus.vue` (Hinweis/Readout + Tastenkürzel), `frontend/src/components/MapControls.vue` (Zoom entfernt, Recenter/Vollbild bleiben), `frontend/src/components/MapCanvas.vue` (Controller-Wiring + `zoomIn`/`zoomOut`), `frontend/src/views/AsdView.vue` (Track-Klick-Guard + Zoom-Delegation) | `frontend/src/map/__tests__/tools.test.js` (10 Geometrie-Tests), `frontend/src/stores/__tests__/tools.test.js` (5 Store-Tests), `frontend/src/components/__tests__/railTools.test.js` (Rail-Werkzeuge/Zoom-Verdrahtung); vitest grün + `vite build` grün |
| FR-UI-023 | **Design-Template-Angleichung (ADR 0015 Nachtrag-2):** Der Claude-Design-Export (`ASD.zip`) ist das **verbindliche Template** (wie Material Design für die Komponenten). Angeglichen: **(Farben)** Surface-Hierarchie zurück auf **Near-Black** (`#070b12`…, Lockstep `colors.css`+`vuetify.js`), Map-Hintergrund `#070b12`, CARTO-Raster bleibt (bewusst, echte Geografie), zarter **Cyan-Mittglow**. **(Symbolgröße)** Track-Symbole waren ~40 % zu klein (24 px-Canvas@pr2 deckelte auf 12 CSS-px) → Canvas 32 px, Zeichen-Geometrie = Template-CSS × 2 (Raute 12, Quadrat 8, Kreis-Ø 9); History-Dot 1.6, Deconfliction-BBox 8→9. **(Symbol-Korrektheit)** PSR ist in **jedem** Zustand ein **hohler Ring** (Zustand = Ringfarbe), der fehlende **Cyan-Auswahl-Ring** (r=11) ist ergänzt (eigener MapLibre-Circle-Layer, an die Selektion gepinnt), die Legende zeichnet dieselben **SVG-Marken** wie die Karte (PSR hohl). **(Fonts)** Karten-Datenblöcke jetzt **Roboto Mono** — Wayfinder **hostet die Glyph-PBFs selbst** (`/glyphs/{fontstack}/{range}.pbf`, `go:embed`, fontnik-generiert), kein Font-CDN mehr auf der Karte (air-gap-Schritt); zusätzlich GL-`letter-spacing 0.02`/`line-height 1.25`; Overline-Header 10 px/700. **(Chrome)** Track-Detail oben-rechts (292), Nav-Panel 248, Rail-Brand-Kachel 30×30, Legende 232/0.96/Radius-md. **Daten-Ehrlichkeit unverändert:** Militär/Hostile/Alarme bleiben mangels Wire-Daten draußen; Offline-`@fontsource`-UI-Fonts bleiben. | Design-Export als Template (Projektverantwortlicher); Audit 2026-07-02 | **ADR 0015 Nachtrag-2** | `frontend/src/design/tokens/{colors,typography}.css`, `frontend/src/plugins/vuetify.js`, `cmd/wayfinder/main.go` (`darkMapStyle`, `glyphs`, `/glyphs`-Mount), `internal/webui/webui.go` (`GlyphsHandler`, `go:embed glyphs`) + `internal/webui/glyphs/Roboto Mono Medium/*.pbf`, `frontend/src/map/{layers,constants,provenance,render,engine}.js`, `frontend/src/components/{ScopeLegend,MapCanvas,NavigationRail,AsdHeader,MeasureStatus,TrackDetailPanel}.vue`, `frontend/src/views/AsdView.vue` | `frontend/src/map/__tests__/trackSymbology.test.js` (Geometrie 32-px-Canvas, PSR-hohl, Auswahl-Ring, History-Dot), `internal/webui/webui_test.go` (`TestGlyphsHandlerServesPBF`/`…NotFound`), `cmd/wayfinder/main_test.go` (Style-glyphs), `docs/decisions/0015-design-system-tokens.md` (Nachtrag-2); vitest **280** + `go test ./...` (28 Pakete) + `vite build`/gofmt/vet grün |

| FR-UI-024 | **ICAO-Flugplatzsuche im View-Editor (Betreiberwunsch 2026-07-04):** Beim Setzen der Standard-Ansicht eines Mandanten tippt der Admin einen **ICAO-Code oder Flughafennamen** (z. B. `EDDH` / `Hamburg`) in ein Suchfeld; die Treffer erscheinen als Liste, und die Auswahl eines Treffers **füllt automatisch** Zentrum Breite/Länge **sowie** ICAO-Kürzel (Kopfzeile) und QNH-Flugplatz — alles editierbar, nichts persistiert bis „Ansicht speichern". Killt die Fehlerklasse hand-getippter Koordinaten (z. B. `0/0`). **Offline-Verzeichnis:** `pkg/airport` bettet per `go:embed` eine kompakte, aus dem **public-domain OurAirports-Datensatz** erzeugte ICAO→{Name,Lat,Lon}-Tabelle ein (`airports.tsv`, ~22 000 Plätze inkl. Kleinflugplätze) — **kein** externer Dienst, **kein** API-Key (Air-Gap-Linie wie das Font-/Glyph-Selbsthosting; ICAO aus `icao_code`, Fallback `ident`/`gps_code` nur bei exakt 4 Buchstaben, schließt FAA-Localcodes aus). Admin-Endpoint `GET /api/admin/airports?q=` (hinter `requireAdmin`) rankt exakter ICAO → ICAO-Präfix → Namens-Substring; Query < 2 Zeichen → leere Liste. | Betreiberwunsch (2026-07-04, ICAO-Autofill statt Hand-Koordinaten) | — | `pkg/airport/airport.go` (`Search`/`Lookup`, `go:embed airports.tsv`), `pkg/airport/gen/gen.go` (Generator, `//go:build ignore`; OurAirports → TSV), `pkg/adminapi/adminapi.go` (`getAirports` + Route), `frontend/src/stores/admin.js` (`searchAirports`), `frontend/src/components/admin/AdminTenantDetail.vue` (`v-autocomplete`, `onAirportSearch`/`onAirportPick`) | `pkg/airport/airport_test.go` (Ranking/Limit/Lookup/Embedded-Daten), `pkg/adminapi/adminapi_test.go` (`TestGetAirports*`: ICAO-Treffer, kurze Query→`[]`, 401 ohne Identity), `frontend/src/stores/__tests__/admin.test.js` (`searchAirports`), `frontend/src/components/admin/__tests__/adminTenantDetailCaptions.test.js` (Verdrahtung); go test/vet/gofmt/golangci-lint + vitest 339 + vite build grün |
| FR-UI-025 | **AoR-Kartendarstellung + Editor (ASD-014 Slice 3, ADR 0021):** Die ASD-Karte **hebt den Verantwortungsbereich** des Mandanten (CTR/TMA) hervor — eine eigene Linien-Ebene über der Airspace-Quelle, gefiltert auf die Feature-`id`s aus `whoami.aor_airspace_ids` (Akzentfarbe `#00e676`, distinkt zu allen Airspace-Gruppen). `session.aorAirspaceIds` speist `engine.updateAoR(ids)`; `MapCanvas` reconcilet nach `initMap` (gleicher #219-Race) und watcht die Liste; Legenden-Toggle „Verantwortungsbereich (AoR)" (Default an, `layerVisibility.aor`). **Editor:** `AdminTenantDetail.vue` bietet ein Chips-Feld (`v-combobox`) für die stabilen OpenAIP-Luftraum-`id`s; Load/`buildViewDto` runden über die bestehende `saveTenantView` (kein neuer Endpunkt), `normalizeAorIds` (Trim/Dedup) client-seitig; `validateView.js` zieht die Server-Grenzen nach (≤ 500 / ≤ 64 Zeichen / keine Steuerzeichen). **Ehrliche Grenze:** ID-Eingabe (Chips) — **Namens-Picker in FR-AERO-006 (Slice 4) nachgezogen**. Rein Frontend, kein CAT062-Bezug, `pkg/aeronautical` unangetastet. | ADR 0021 (Ebene 2, Auswahl-Semantik Option 1), Betreiber (AoR-Overlays), FR-AERO-004/005 | **ADR 0021** | `frontend/src/stores/session.js` (`aorAirspaceIds`), `frontend/src/map/constants.js` (`AIRSPACE_AOR_LAYER_ID`/`_COLOR`), `frontend/src/map/layers.js` (`addAirspaceAoRLayer`/`aorFilter`), `frontend/src/map/engine.js` (`updateAoR`, Layer + `aor`-Sichtbarkeit), `frontend/src/components/MapCanvas.vue` (Reconcile + Watcher), `frontend/src/stores/asd.js` (`layerVisibility.aor`), `frontend/src/components/LayerFilterContent.vue` (Toggle + Swatch), `frontend/src/components/admin/AdminTenantDetail.vue` (`v-combobox`, `normalizeAorIds`, DTO/Load), `frontend/src/admin/validateView.js` (AoR-Grenzen) | `frontend/src/stores/__tests__/session.test.js` (`aorAirspaceIds` + Default `[]`), `frontend/src/admin/__tests__/validateView.test.js` (AoR-Grenzen), `frontend/src/components/__tests__/mapCanvasViewCenter.test.js` (Engine-/MapCanvas-Wiring), `frontend/src/components/admin/__tests__/adminTenantDetailSave.test.js` (Editor-Wiring/DTO); vitest 427 + vite build + eingebettetes `dist` neu; Go unberührt grün |
| FR-PROFILE-001 | **View-Profile — Per-Nutzer-Store (VP-1, ADR 0023):** Grundlage für persönliche Anzeige-Profile: neue Tabelle `user_view_profiles` (Migration 00022; `id, user_id` FK ON DELETE CASCADE, `name`, `settings JSONB` **opak**, `is_default`, Timestamps) + `ViewProfileRepo` (List/Create/Update/Delete/SetDefault/GetDefault). **Invarianten im Store erzwungen:** max. **3** Profile/Nutzer (Transaktion + per-Nutzer `pg_advisory_xact_lock` gegen Races → `ErrProfileLimit`) und **höchstens ein** Default (partieller Unique-Index `WHERE is_default`). **Streng per-`user_id` gescopt** (jede Query `WHERE id AND user_id` → fremdes Profil = `ErrNotFound`, keine Cross-User-Leckage). `settings` wird verbatim gespeichert/zurückgeliefert (Backend interpretiert es nie → neuer UI-Toggle ohne Migration), nil → `{}` (`normalizeSettings`). **Kein CAT062-Bezug.** Getrennt von `view_configs` (Karten-Rahmung, unangetastet). | Betreiberwunsch (2026-07-08, „bis zu 3 benannte Ansichten + Default beim Login"), **ADR 0023** | **ADR 0023** | `pkg/store/migrations/00022_user_view_profiles.sql`, `pkg/store/view_profiles.go` (`ViewProfileRepo`, `MaxViewProfilesPerUser`, `ErrProfileLimit`, `normalizeSettings`) | `pkg/store/view_profiles_test.go` (`normalizeSettings`), `pkg/store/view_profiles_integration_test.go` (`TestIntegrationViewProfilesCRUD`: CRUD, Cap→`ErrProfileLimit`, Single-Default-Invariante, Cross-User-Isolation, nil→`{}`); `go build`/`vet`/`gofmt` grün, Store-Integrationstests grün gegen echte DB |
| FR-PROFILE-002 | **View-Profile — user-gescopte REST-API (VP-2, ADR 0023):** Fünf Endpunkte hinter **`tenantMW`+`pwGate`** (jeder authentifizierte Nutzer, **kein** Admin-Gate): `GET /api/view-profiles` (Liste), `POST /api/view-profiles` (anlegen, `make_default`), `PUT /api/view-profiles/{id}` (umbenennen+`settings`), `DELETE /api/view-profiles/{id}`, `POST /api/view-profiles/{id}/default`. **Streng auf `id.UserID` aus der Session gescopt** — der Handler liest die User-ID **nie** aus dem Body; Store-Queries sind `WHERE id AND user_id` → fremdes Profil = **404** (keine Leckage). **Validierung** (`validateViewProfile`, rein/testbar): Name getrimmt, nicht leer, ≤ 60 Zeichen; `settings` **muss ein JSON-Objekt** sein (kein Array/Scalar) und ≤ 16 KiB; leer → `{}`; Toggle-Schlüssel **nicht** interpretiert (opak). Cap→**409**, ungültig→**422**, kaputtes JSON→**400**, nil-Store→**404**. `ViewProfilesHandler()` (Sub-Mux) in `main.go` gemountet; `ViewProfileStore`-Interface + `WithViewProfiles`-Builder (nil-safe). **Kein CAT062-Bezug.** | **ADR 0023**, FR-PROFILE-001 | **ADR 0023** | `pkg/adminapi/adminapi_view_profiles.go` (`ViewProfileStore`, `WithViewProfiles`, DTO, `validateViewProfile`, 5 Handler, `ViewProfilesHandler`), `pkg/adminapi/adminapi.go` (`profiles`-Feld), `cmd/wayfinder/main.go` (Repo-Wiring + Route-Mount) | `pkg/adminapi/adminapi_view_profiles_test.go` (`validateViewProfile`, DTO nil→`{}`, List/Create/Update/Delete/SetDefault, Validierung 422/400, Cap→409, fremd→404, kein-Identity→401, nil-Store→404, Scoping auf Session-UserID); `go build`/`vet`/`gofmt`/`golangci-lint` (0 issues) grün |
| FR-PROFILE-003 | **View-Profile — Frontend-Store + Capture/Apply (VP-3, ADR 0023):** Pinia-`profiles`-Store (`load`/`saveCurrent`/`update`/`rename`/`overwrite`/`remove`/`setDefault`/`apply`, `canCreate`≤3, `defaultProfile`) gegen die VP-2-API (`apiFetch`); lokaler Zustand nach jedem Call konsistent gehalten (u. a. **genau ein** `is_default` lokal). **Reine, testbare** Serialisierung in `profileSettings.js`: `captureSettings(asd)` schnappschusst die **Anzeige-Präferenzen** des asd-Stores (Layer-Sichtbarkeit, Airspace-Gruppen, Range-Ring-Config, History-Dauer, FL-Filter; **kein** Karten-Zentrum/Zoom — Option A) in ein versioniertes Objekt; `applySettings(asd, settings)` spielt es **tolerant** zurück (unbekannte/fehlende Keys übersprungen, Zahlen validiert, `airspace`-Layer aus den Gruppen abgeleitet statt direkt gesetzt) → Karte folgt über die bestehenden MapCanvas-Watcher. Noch **keine UI** (VP-4) und **kein** Import in Komponenten → `dist` unverändert. **Kein CAT062-Bezug.** | **ADR 0023**, FR-PROFILE-002 | **ADR 0023** | `frontend/src/stores/profiles.js` (`useProfilesStore`, `MAX_PROFILES`), `frontend/src/stores/profileSettings.js` (`captureSettings`/`applySettings`/`SETTINGS_VERSION`) | `frontend/src/stores/__tests__/profileSettings.test.js` (Capture/Apply, Toleranz, `airspace`-Ableitung, FL-Coercion, Round-Trip), `frontend/src/stores/__tests__/profiles.test.js` (load/save/rename/remove/setDefault/apply gegen gemocktes `fetch`, `canCreate`); vitest 504 grün |
| FR-PROFILE-004 | **View-Profile — UI-Umschalter + Speichern-Dialog (VP-4, ADR 0023):** `ViewProfileMenu.vue` im ASD-Header-Cluster (neben Feed-Chip + Ereignis-Glocke): ein Button (Label = aktives Profil) öffnet ein `v-menu` mit der **Profilliste** — Klick **wendet an** (`store.apply`), Stern-Icon **setzt Default** (`store.setDefault`, spiegelt `is_default`), Stift **benennt um** (`store.rename`), Papierkorb **löscht** (`store.remove`). „**Aktuelle Ansicht speichern…**" öffnet einen `v-dialog` (Name + „Als Standard beim Login") → `store.saveCurrent(name, makeDefault)`; bei erreichtem Cap (3) ist der Eintrag **deaktiviert** (`store.canCreate`, Hinweis „Maximal 3 Profile"). Lädt die Profile `onMounted`. Verdrahtet den VP-3-Store; **kein** Backend-/CAT062-Bezug; `dist` neu gebaut. Nur die Bedienung — **Apply-on-Login** ist VP-5. | **ADR 0023**, FR-PROFILE-003, Betreiberwunsch (benennen/abrufen/Default) | **ADR 0023** | `frontend/src/components/ViewProfileMenu.vue`, `frontend/src/views/AsdView.vue` (Import + Mount im Header-Cluster) | `frontend/src/components/__tests__/viewProfileMenu.test.js` (Source-Guard: Store-Verdrahtung, `load` on mount, apply/setDefault/remove/saveCurrent/rename, Default-Stern, Cap-Gating, AsdView-Mount); vitest 510 + vite build + eingebettetes `dist` neu; Go unberührt |
| FR-PROFILE-005 | **View-Profile — Apply-on-Login (VP-5, ADR 0023):** Nach dem Login wird das **Default-Profil** des Nutzers automatisch angewandt. `profiles`-Store: `applyDefaultOnce()` (Guard `defaultApplied`) wendet das Profil mit `is_default` **genau einmal pro App-Load** an (`applySettings` → asd-Store) und setzt `activeId`; ohne Default **latcht** der Guard nicht (retrybar, sobald die Liste geladen ist). `ViewProfileMenu.vue` triggert es **erst wenn die Karte bereit ist** (`asd.mapLoaded`, damit die Toggles über die Live-MapCanvas-Watcher greifen) — nach `store.load()` und via `watch(mapLoaded)`, je nachdem was zuletzt fertig ist. **Orthogonal** zur Tenant-Karten-Rahmung (Zentrum/AOI unberührt); kein Default → heutiges Verhalten; überschreibt **keine** spätere manuelle Wahl (Once-Guard). **Kein Backend-/CAT062-Bezug.** **Schließt das Feature View-Profile ab (VP-1…VP-5).** | **ADR 0023**, FR-PROFILE-004, Betreiberwunsch (Default beim Login) | **ADR 0023** | `frontend/src/stores/profiles.js` (`applyDefaultOnce`, `defaultApplied`), `frontend/src/components/ViewProfileMenu.vue` (`watch(asd.mapLoaded)`, `tryApplyDefault`) | `frontend/src/stores/__tests__/profiles.test.js` (`applyDefaultOnce`: einmalig, überschreibt spätere manuelle Wahl nicht, retrybar ohne Default), `frontend/src/components/__tests__/viewProfileMenu.test.js` (mapLoaded-Gating-Verdrahtung); vitest 513 + vite build + eingebettetes `dist` neu; Go unberührt |
| FR-UI-028 | **Selektions-Umrandung des Datenblock-Labels (ASD-011b, Designkonzept):** Bei Selektion eines Tracks bekommt sein Datenblock-Label zusätzlich zum Symbol-Halo (FR-ASD-007/#183) eine **abgerundete Rahmen-Box** in **neutralem Hellton** (`#f2f7fc`, distinkt zu Cyan-Chrome und Zustands-/Alarmfarben), sodass Symbol **und** Block gemeinsam als „ausgewählt" lesbar sind — angeglichen an das Claude-Design-Template (selektiertes Label umrandet). Die Box wird in `deconflictLabels` aus der bereits berechneten Label-Screen-Bbox (`LABEL_W_PX`/`LABEL_H_PX` + Padding) als **abgerundeter Ring** erzeugt (reine, testbare `roundedRectRing`), jeder Ringpunkt per **`map.unproject`** zurückprojiziert → die Box sitzt **pixelgenau** um das Label (nutzt denselben exakten Round-Trip wie der Label-Drag-Fix) und folgt ihm bei jedem Render. Eigene Line-Ebene (`SELECTION_LABEL_LAYER_ID`, `line-join/-cap: round`) über den Labels; nur **0/1** Feature (das selektierte Label). Rein Frontend, **kein CAT062-Bezug**. **Zuschnitt:** nur die Selektions-Umrandung — „**alle** Labels boxen" + Alarmfarben (STCA/EMG/DUP) bleiben separate Häppchen (STCA bräuchte Wire-Daten I062/340). | Designkonzept (Claude-Export, Betreiber 2026-07-08), FR-UI-023 (Template verbindlich) | — | `frontend/src/map/constants.js` (`SELECTION_LABEL_*`), `frontend/src/map/deconflict.js` (`roundedRectRing`, `selectionBoxFeatures`, `selectedTrackNum`-Param), `frontend/src/map/layers.js` (`addSelectionLabelLayer`), `frontend/src/map/engine.js` (Layer-Registrierung), `frontend/src/map/render.js` (Source-Set + `selectedTrackNum`-Durchreichung) | `frontend/src/map/__tests__/deconflict.test.js` (`roundedRectRing` Bounds/Radius-Clamp; `selection outline (ASD-011b)`: nur selektierter Track, Box rahmt Label-Bbox exakt, geschlossener Ring); vitest 489 + vite build + eingebettetes `dist` neu; Go unberührt grün |
| FR-UI-027 | **Alarm-/Ereignis-Panel (ASD-013):** Ein zuschaltbares Ereignis-Panel (Glocken-Icon oben rechts mit Ungesehen-Badge) protokolliert betrieblich relevante Zustandsübergänge, die der Lotse sonst leicht verpasst: **Feed-Ausfall/-Degradation/-Erholung** (Aggregat-`feedStatus`-Wechsel), **Verbindungsverlust/-wiederherstellung** (WebSocket-Lebenszyklus) und **Track erschienen/beendet** (Live-Set-Diff bzw. I062/080 TSE). Alle Ereignisse werden **client-seitig aus dem bereits konsumierten WS-Strom abgeleitet** (kein Wire-Change) und sind damit **automatisch mandanten-skopiert** (der Strom ist es, WF2-21). Die Ableitung sind **reine, testbare** Funktionen (`map/events.js`: `feedStatusEvent`/`connectionEvent`/`trackLifecycleEvents` + `SEVERITY_META`); ein Pinia-Ring-Puffer (`stores/events.js`, `MAX_EVENTS=200`, neueste zuerst, Ungesehen-Zähler) hält die Historie; die Engine speist die Ableitung im WS-Handler (Feed vor/nach `setFeedHealth`, Track-Batch, Connection). **Rausch-Vermeidung:** die erste Frame nach einem (Re)Connect **primet** nur die Track-Baseline (kein „erschienen"-Flut fürs Anfangsbild); ein bloßer Scan-Ausfall ohne TSE erzeugt **kein** „beendet" (nur die autoritative Track-Ende-Signalisierung zählt). **Ehrliche Grenze:** keine Wire-Alarme (STCA/Militär/Hostile mangels Feld draußen, vgl. ASD-006/#18) — nur beobachtbare Zustandsübergänge. | ASD-013 (ROADMAP), Betreiber-Situationsbewusstsein | — | `frontend/src/map/events.js` (reine Ableitung + Severities), `frontend/src/stores/events.js` (Ring-Puffer-Store), `frontend/src/components/EventPanel.vue` (Panel), `frontend/src/map/engine.js` (WS-Handler-Verdrahtung, Prime-Logik), `frontend/src/views/AsdView.vue` (Glocke + Badge + Panel-Toggle) | `frontend/src/map/__tests__/events.test.js` (Ableitung: Feed/Connection-Transitionen, Track-Diff/TSE, Severities), `frontend/src/stores/__tests__/events.test.js` (Ring-Puffer/Cap/unseen/clear/markSeen), `frontend/src/components/__tests__/eventPanel.test.js` (Panel-/AsdView-/Engine-Verdrahtung); vitest 485 + vite build + eingebettetes `dist` neu; Go unberührt grün |
| FR-UI-029 | **ASD-Chrome-Politur + Ereignis→Track-Sprung (Betreiber-Video/Foto 2026-07-08):** Vier gemeldete UI-Mängel behoben und eine Bedien-Erweiterung ergänzt. **(1) Sidebar-Reflow beim Auf-/Zuklappen:** Das ausklappende Nav-Panel baute die Schrift sichtbar auf bzw. stauchte sie beim Einklappen — Ursache war die `flex:1`-Panelbreite, die während der Drawer-Breiten-Animation mitwuchs und den Text neu umbrach. Fix: **feste Panelbreite** (`.nav-panel` = offene Drawer-Breite − Rail, 248/304 px je Band); der Inhalt liegt sofort final, `.nav-two-col overflow:hidden` clippt den Überhang → sauberer Wisch-Reveal statt Neu-Layout. **(2) Blitzender Scrollbalken:** folgt aus (1) — keine transiente Höhe mehr; zusätzlich `.nav-panel__body overflow-x:hidden`. **(3) Überlappende Icons:** Der Profil-Schalter + die Ereignis-Glocke stapelten als zwei Extra-Zeilen im Top-Right-Cluster, sodass die Map-Controls (`top:100px`) auf ihnen saßen. Fix: Profil + Glocke teilen **eine** kompakte Aktionszeile (`.cluster-actions`); `MapControls` auf `top:140px` und `TrackDetailPanel` auf `top:220px` nachgezogen (gleiche Controls→Detail-Distanz wie zuvor). **(4) Profil nur als Icon:** `ViewProfileMenu` ist jetzt ein Icon-Button (`mdi-bookmark-multiple-outline`) mit **Hover-Tooltip** (aktiver Profilname) statt sichtbarem Label — hält den Lotsen-Scope aufgeräumt. **(5) Ereignis→Track:** Ein Klick auf eine **noch aktive** „Track N erschienen"-Zeile selektiert den Track (Detail-Panel + ASD-007-Halo, Kamera-`easeTo`). Der Store spiegelt das Live-Track-Set (`liveTrackNums`, aus `liveTrackFeatures` je Batch); nur Zeilen, deren Track noch am Schirm ist, sind klickbar (Fadenkreuz-Affordanz); die Engine liefert `selectTrackByNum` (No-op → Panel bleibt offen, wenn der Track weg ist). Ring-Puffer bleibt `MAX_EVENTS=200` (≫50) mit vorhandenem Scroll. **Kein CAT062-/Backend-Bezug** (reine Frontend-Chrome). | Betreiber-Video/Foto (2026-07-08) | — (Politur zu FR-UI-018/005, FR-UI-027, FR-PROFILE) | `frontend/src/components/NavigationRail.vue` (feste Panelbreite + `overflow-x`), `frontend/src/components/MapControls.vue` (`top:140px`), `frontend/src/components/TrackDetailPanel.vue` (`top:220px`), `frontend/src/components/ViewProfileMenu.vue` (Icon-only + Tooltip), `frontend/src/views/AsdView.vue` (`.cluster-actions`, `onEventSelectTrack`), `frontend/src/components/EventPanel.vue` (`isSelectable`/`select-track`), `frontend/src/components/MapCanvas.vue` (`selectTrackByNum`), `frontend/src/map/engine.js` (`selectTrackByNum`/`syncLiveTrackNums`), `frontend/src/stores/asd.js` (`liveTrackNums`/`setLiveTrackNums`) | `frontend/src/stores/__tests__/asd.test.js` (`liveTrackNums`-Set), `frontend/src/components/__tests__/eventPanel.test.js` (Selektierbarkeit/`select-track`/Engine-`selectTrackByNum`), `frontend/src/components/__tests__/viewProfileMenu.test.js` (Icon-only + Tooltip), `frontend/src/components/__tests__/layerSidebarCleanup.test.js` (feste Panelbreite, `overflow-x`); vitest 525 + vite build + eingebettetes `dist` neu; `go build ./...` grün |
| FR-UI-026 | **Erweitertes Track-Detail-Panel (ASD-011):** Das Detail-Panel eines angeklickten Tracks zeigt zusätzlich zu Callsign/FL/Bodengeschwindigkeit/Mode 3-A/Track-Nr./Status (FR-UI-005) nun **Vertikaltendenz** (Steigend/Sinkend/Gleichbleibend aus der bereits berechneten ASD-001b-Tendenz), **Kurs über Grund** (aus Vx/Vy, I062/185), **Position WGS84** (I062/105, Dezimalgrad mit Hemisphäre), **Sensor-Aktualität** (Chips je Technologie mit Update-Alter + Frische-Farbe aus I062/290, ICD 2.6.0), **ICAO-Adresse** (I062/380, 6-stellig Hex), **Positionsgenauigkeit** (I062/500, ±m) und **System (SAC/SIC)** (I062/010). Die Formatierer sind **reine, testbare** Funktionen in `map/trackDetail.js`; die Felder werden in `updateTracksLayer` auf die Feature-Properties gelegt (latitude/longitude/icao_addr/accuracy/sac/sic/per-Technologie-Alter/vertical_trend), sodass das Panel sie direkt aus `store.selectedTrack` liest — **kein** Backend-/CAT062-Bezug (alle Felder bereits im WS-JSON). **Ehrliche Grenze:** PSR erscheint nicht in „Sensor-Aktualität" (kein sauberes Per-Track-`psr_age`-Frische-Signal); der Primär-nur-Fall wird über die „Herkunft"-Zeile (FR-ASD-007) getragen. | ASD-011 (ROADMAP, Detail-Panel-Ausbau) | — | `frontend/src/map/trackDetail.js` (`formatLatLon`/`formatHeading`/`formatIcao`/`formatAccuracy`/`formatAge`/`verticalTrendLabel`/`sensorAgeList`), `frontend/src/map/tracks.js` (Properties gebacken), `frontend/src/components/TrackDetailCard.vue` (neue Zeilen + Formatierer) | `frontend/src/map/__tests__/trackDetail.test.js` (Formatierer, 28 Fälle), `frontend/src/map/__tests__/tracks.test.js` (`extended detail fields (ASD-011)`: Properties + Vertikaltendenz); vitest 456 + vite build + eingebettetes `dist` neu; Go unberührt grün |

| FR-DATA-013 | **CAT062-Flugplan-Korrelation I062/390 (Firefly FPL.2 / FR-TRK-048, ICD 3.7.0, additiv):** Der CAT062-Decoder liest das **Compound-Item I062/390** (FRN 21, 3. FSPEC-Oktett Bit `0x02`) — das Ergebnis der zentralen Flugplan-Korrelation (Fireflys ADR 0038/0039): FX-verkettetes Primary Subfield + Subfelder aufsteigend, **subfeld-getrieben** (dieselbe robuste Machinerie wie I062/380, #238). Dekodiert **CSN** (#2, 7 Okt. ASCII, getrimmt → Plan-Callsign), **DEP** (#7, 4 Okt. ICAO-Locator) und **DST** (#8, 4 Okt.); bekannte fixe Subfelder (#1/#3–#6/#9–#11/#13/#14) werden **längen-übersprungen** (Vorwärts-Kompatibilität für Fireflys additives Feldsatz-Wachstum), das **variable** #12 (TOD, repetitiv) wird **abgelehnt** statt fehl-geparst. → `DecodedTrack.PlanCallsign`/`.PlanDeparture`/`.PlanDestination` (nur bei korreliertem Track; unkorreliert = alle nil). **ASD-Nutzung:** Detail-Panel-Zeilen „Plan-Callsign" + „Route (ADEP → ADES)"; **Callsign-Mismatch** — der gefilte Plan-Callsign (CSN) kann von der gesendeten Kennung (I062/245) abweichen — wird im Panel hervorgehoben (Warn-Farbe) **und** am Label mit einem dezenten „≠"-Marker signalisiert. WS-JSON: `plan_callsign`/`plan_departure`/`plan_destination` (flach, `omitempty`). Byte-genau gegen Fireflys ICD §4.10-Referenz-Vektoren (`43 80 …` DLH123 EDDF→EDDM; `40 …` BAW22 nur-Callsign). Additiv, kein Wire-/ICD-Bruch (unkorrelierter Track byte-identisch). **Scope-Abgrenzung:** die **manuelle Korrelation** (Kommando-API `POST/DELETE /correlation`, Rückkanal Wayfinder→Firefly) ist ein **separates** Häppchen mit eigenem ADR (architektonischer Neubau — heute ist Wayfinder reiner Multicast-Konsument); ebenso `identity_conflict` (nur in Fireflys WS-Pfad, nicht über CAT062 verfügbar). | Fireflys ICD-CAT062.md v3.7.0 (§4.10), Firefly FPL.2 / FR-TRK-048 / ADR 0038/0039, Wayfinder-Issue #245 | — | `pkg/cat062/decoder.go` (FRN 21-Case, `decodeFlightPlanData`, `i390SubfieldLen`, `compoundSubfieldPresent` generalisiert), `pkg/cat062/types.go` (Plan-Felder), `pkg/broadcast/broadcast.go` (`TrackMessage.PlanCallsign`/`PlanDeparture`/`PlanDestination`), `frontend/src/map/tracks.js` (Plan-Feature-Properties), `frontend/src/map/label.js` (≠-Mismatch-Marker), `frontend/src/map/trackDetail.js` (`formatPlanRoute`/`isPlanCallsignMismatch`), `frontend/src/components/TrackDetailCard.vue` (Plan-Zeilen + Mismatch-Highlight) | `pkg/cat062/flightplan_test.go` (`TestDecodeFlightPlanFull`/`…CallsignOnly`/`…SkipsUnknownFixedSubfield`/`…RejectsVariableSubfield`/`…FullRecord`/`…AbsenceUnchanged`/`…Truncated`), `pkg/broadcast/broadcast_test.go` (`TestTracksToMessageMapsFlightPlan`), `frontend/src/map/__tests__/label.test.js` (≠-Fälle), `trackDetail.test.js` (`formatPlanRoute`/`isPlanCallsignMismatch`), `tracks.test.js` (`updateTracksLayer flight plan #245`) |
| FR-DATA-012 | **CAT062-Kinematik-Kette I062/200 + I062/210 (Firefly VERT.3 / FR-TRK-043, ICD 3.6.0, additiv):** Der CAT062-Decoder liest den **qualitativen Bewegungszustand** und die **berechnete Beschleunigung** — zwei feste Items an ihren Standard-UAP-Positionen: **I062/210** (FRN 8, 2. FSPEC-Oktett Bit `0x80`; 2 Oktette, `Ax`/`Ay` je i8 × 0,25 m/s², Ost/Nord, Encoder-Clamp auf i8-Feldbereich −32,0…+31,75) und **I062/200** (FRN 15, 3. FSPEC-Oktett Bit `0x80`; 1 Oktett, drei unabhängige 2-Bit-Achsen **TRANS**/**LONG**/**VERT** + ADF/Spare). Jede Achse mit Wert 3 = **unbestimmt** ⇒ nil-Pointer; I062/200 entfällt ganz, wenn alle drei unbestimmt sind (Absenz = „kein Trend-Anspruch", nie „alles konstant"). → `DecodedTrack.MotionCourse`/`.MotionSpeed`/`.MotionVertical` (getypte Strings `CourseTrend`/`SpeedTrend`/`VerticalTrend`) + `.AccelAxMS2`/`.AccelAyMS2`. **ASD-Nutzung:** (1) **Kurven-Indikator** →/← an der Label-Identitätszeile aus TRANS (nur bei Rechts-/Linkskurve). (2) Detail-Panel: Kurventrend, Geschwindigkeitstrend, Beschleunigungs-Betrag (√(Ax²+Ay²)). (3) Die qualitative VERT-Achse fährt additiv als `vertical_motion` mit — der **Steig-/Sinkpfeil bleibt aber quantitativ aus I062/220** (#241), VERT wird im Panel nicht doppelt gezeigt. WS-JSON: `course_trend`/`speed_trend`/`vertical_motion` (nur bestimmte Achsen) + `accel_ax_ms2`/`accel_ay_ms2`. Byte-genau gegen Fireflys ICD §4.9-Referenz-Vektoren (`04 FE`/`7F 80`/`54`/`B0`). Additiv, kein Wire-/ICD-Bruch (Track ohne Kinematik byte-identisch). | Fireflys ICD-CAT062.md v3.6.0 (§4.9), Firefly VERT.3 / FR-TRK-043, Wayfinder-Issue #242 | — | `pkg/cat062/decoder.go` (FRN 8/15-Cases, `courseTrend`/`speedTrend`/`verticalTrend`, `uapOrder`), `pkg/cat062/types.go` (`CourseTrend`/`SpeedTrend`/`VerticalTrend`-Enums, Kinematik-Felder), `pkg/broadcast/broadcast.go` (`TrackMessage.CourseTrend`/`SpeedTrend`/`VerticalMotion`/`AccelAxMs2`/`AccelAyMs2`, `enumStr`), `frontend/src/map/tracks.js` (Kinematik-Feature-Properties), `frontend/src/map/label.js` (Kurven-Indikator →/←), `frontend/src/map/trackDetail.js` (`formatCourseTrend`/`formatSpeedTrend`/`formatVerticalMotion`/`formatAcceleration`), `frontend/src/components/TrackDetailCard.vue` (Kinematik-Zeilen) | `pkg/cat062/kinematics_test.go` (`TestDecodeAccelerationReferenceVectors`/`…ModeOfMovementReferenceVectors` inkl. unbestimmte Achse/`…ModeOfMovementAllUndetermined`/`…KinematicsFullRecord`/`…KinematicsAbsenceUnchanged`/`…AccelerationTruncated`), `pkg/broadcast/broadcast_test.go` (`TestTracksToMessageMapsKinematics`), `frontend/src/map/__tests__/label.test.js` (→/←-Fälle), `trackDetail.test.js` (Formatter), `tracks.test.js` (`updateTracksLayer kinematics chain #242`) |
| FR-DATA-011 | **CAT062-Vertikal-Kette I062/130 + I062/135 + I062/220 (Firefly VERT.2 / FR-TRK-042, ICD 3.5.0, additiv):** Der CAT062-Decoder liest die **gefilterte Vertikal-Lösung** des Trackers — drei feste 2-Oktett-Items an ihren Standard-UAP-Positionen im **dritten** FSPEC-Oktett (FRN 18/19/20, Bits `0x10`/`0x08`/`0x04`): **I062/130** (Geometric Altitude, WGS-84, i16 × 6,25 ft), **I062/135** (Barometric Altitude, gefiltert — **Bit 16 = QNH-Bit**, Bits 15–1 = **15-Bit-Zweierkomplement** × 25 ft; die eine subtile Stelle: echte 15-Bit-Vorzeichen-Erweiterung, nicht i16) und **I062/220** (Rate of Climb/Descent, i16 × 6,25 ft/min, positiv = steigen) → `DecodedTrack.GeometricAltitudeFt`/`.BarometricAltitudeFt` + `.BaroQNHCorrected`/`.RateOfClimbDescentFtMin` (alle optional; Absenz = kein frischer Schätzwert ≤ 30 s, nie eine Stale-Behauptung). **ASD-Nutzung:** (1) Label-**Anzeige-Höhe bevorzugt I062/135** (geglättet statt springendes I062/136) mit **A/FL-Kennzeichnung** — QNH-korrigiert → `A030` (echte Altitude), sonst → `FL350` (Druckhöhe); Fallback auf I062/136. (2) **Steig-/Sinkpfeil** ▲/▼ jetzt **primär aus I062/220** (±300 ft/min-Totband gegen Flattern, Firefly liefert die Rate bereits Kalman-geglättet), Fallback auf die bisherige FL-Differenz-Heuristik. (3) Detail-Panel: barometrische Höhe (mit QNH-Referenz), Steig-/Sinkrate, geometrische Höhe. Der QNH-Flag reist als `*bool` nur mit vorhandener baro. Höhe (kein Stray-`false`). Byte-genau gegen Fireflys ICD §4.8-Referenz-Vektoren (`06 40`/`FF 9C`/`05 78`/`80 78`/`FF F0`/`01 E0`/`FF 40`). Additiv, kein Wire-/ICD-Bruch (Track ohne Vertikal-Daten byte-identisch; I062/136 bleibt gemessen daneben). | Fireflys ICD-CAT062.md v3.5.0 (§4.8), Firefly VERT.2 / FR-TRK-042 / PR #74, Wayfinder-Issue #241 | — | `pkg/cat062/decoder.go` (FRN 18/19/20-Cases, `uapOrder`), `pkg/cat062/types.go` (Vertikal-Felder), `pkg/broadcast/broadcast.go` (`TrackMessage.GeometricAltitudeFt`/`BarometricAltitudeFt`/`QNHCorrected`/`RocdFtMin`, QNH-Pointer-Semantik), `frontend/src/map/tracks.js` (rocd-getriebener `vTrend` + Totband, Feature-Properties), `frontend/src/map/label.js` (A/FL-Anzeige-Höhe), `frontend/src/map/trackDetail.js` (`formatGeometricAltitude`/`formatBarometricAltitude`/`formatRateOfClimb`), `frontend/src/components/TrackDetailCard.vue` (Vertikal-Zeilen) | `pkg/cat062/vertical_test.go` (`TestDecodeGeometricAltitudeReferenceVectors`/`…BarometricAltitudeReferenceVectors` inkl. QNH-Bit + 15-Bit-ZK/`…RateOfClimbDescentReferenceVectors`/`…VerticalChainFullRecord`/`…VerticalAbsenceUnchanged`/`…BarometricTruncated`), `pkg/broadcast/broadcast_test.go` (`TestTracksToMessageMapsVerticalChain`), `frontend/src/map/__tests__/label.test.js` (A/FL-Fälle), `trackDetail.test.js` (Formatter), `tracks.test.js` (`updateTracksLayer vertical chain #241`: rocd-Totband + Fallback) |
| FR-DATA-010 | **I062/380 Mode-S-DAPs, subfeld-getrieben (Firefly FEP.2, ICD 3.4.0, additiv):** Der CAT062-Decoder liest I062/380 (Aircraft Derived Data) **subfeld-getrieben** statt „feste 4 Bytes (ADR)" — FX-verkettete Subfeld-Spec (bis 4 Oktette, gecappt) + Subfelder in aufsteigender Nummer: **ADR** (#1, ICAO-Adresse), **MHG** (#3, u16 × 360/2¹⁶°), **SAL** (#6, Selected Altitude, 13-Bit-Zweierkomplement × 25 ft; SAS/Source maskiert), **IAR** (#26, IAS, u16 × 1 kt), **MAC** (#27, Mach, u16 × 0,008) → `DecodedTrack.SelectedAltitudeFt`/`.MagneticHeadingDeg`/`.IndicatedAirspeedKt`/`.MachNumber`. **Korrektheitskritisch:** der alte Decoder las nur die ADR und ignorierte die FX-Kette — ein DAP-tragender Track hätte den restlichen Record desynchronisiert; jetzt exakt konsumiert. Bekannte fixe Subfelder werden längen-übersprungen, ein variables/unbekanntes (#8/#9/#25) **abgelehnt** (robuster Decoder, kein Mis-Parse). **ASD-Nutzung:** Label zeigt „S&lt;selFL&gt;" neben der Ist-Flugfläche; `TrackDetailCard` zeigt Selected Altitude mit **Level-Bust-Hinweis** bei Abweichung ≥ 300 ft, plus MHG/IAS/Mach. Byte-genau gegen Fireflys ICD §4.7-Referenz-Dump (ADR 0x3C65AC, MHG 270°, SAL 35 000 ft, IAS 250 kt, Mach 0,784). Additiv, kein Wire-/ICD-Bruch (DAP-loser Track byte-identisch). | Fireflys ICD-CAT062.md v3.4.0 (§4.7), Firefly FEP.2, Wayfinder-Issue #238 | — | `pkg/cat062/decoder.go` (`decodeAircraftDerivedData`, `i380Present`, `i380SubfieldLen`), `pkg/cat062/types.go` (DAP-Felder), `pkg/broadcast/broadcast.go` (`TrackMessage.SelectedAltitudeFt`/`MagneticHeadingDeg`/`IasKt`/`Mach`), `frontend/src/map/trackDetail.js` (`formatSelectedAltitude`/`isLevelBust`/`formatMagneticHeading`/`formatIas`/`formatMach`), `frontend/src/map/label.js` (SEL-Suffix), `frontend/src/map/tracks.js`, `frontend/src/components/TrackDetailCard.vue` (DAP-Zeilen + Level-Bust) | `pkg/cat062/dap_test.go` (`TestDecodeModeSDaps`/`…BackwardCompatAdrOnly`/`…SelectedAltitudeNegative`/`…RejectsVariableSubfield`), `frontend/src/map/__tests__/trackDetail.test.js` (Formatter + `isLevelBust`), `frontend/src/map/__tests__/label.test.js` (SEL-Suffix), `frontend/src/map/__tests__/tracks.test.js` (DAP-Properties) |
| FR-DATA-009 | **CAT063 Registrierungs-Bias I063/080 + I063/081 (Firefly REG.3 / ADR 0034, ICD 3.3.0, additiv):** Der CAT063-Decoder dekodiert den **angewandten** Sensor-Registrierungs-Bias — I063/080 **SRB** (Slant Range Bias, i16 × 1/128 NM → Meter) und I063/081 **SAB** (Azimuth Bias, i16 × 360/2¹⁶°) — nach `SensorStatus.RangeBiasM`/`.AzimuthBiasDeg` (**nil = keine Korrektur**, nie 0; die FRNs 7/8 wurden bisher nur längen-übersprungen). **Per-Sensor-Detailpfad neu (Umfang-Option „voller Chip-Ausbau"):** `health.Registry` führt je Feed eine `[]SensorDetail`-Liste (SAC/SIC, operational, reason, bias) → `FeedSnapshot.Sensors`; der WS-`FeedStatusMessage.sensors[]` und der Admin-`/api/admin/feeds/health`-`sensors[]` tragen sie. **ASD-Nutzung:** Der operative **FeedStatusChip** wird zum Menü mit ausklappbarer Per-Sensor-Zeile „SIC 2 · Δr +145 m · Δθ +0,30°" (nur degradierte/biased Sensoren); `AdminFeeds` zeigt dasselbe je Feed. Gesundheitssignal: ein plötzlich wachsender Bias kündigt ein Kalibrier-/Hardware-Problem an. Byte-genau gegen Fireflys `sensor_with_bias_matches_reference_dump`. **Bewusst kein Prometheus-Metrik** (Metrik-Kardinalitäts-Regel, NFR-SEC-003/WF2-23). Additiv, kein Wire-/ICD-Bruch. | Fireflys ICD-CAT062.md v3.3.0 (§9), Firefly REG.3 / ADR 0034, Wayfinder-Issue #237 | — | `pkg/cat063/decoder.go` (SRB/SAB-Decode, `SensorStatus.RangeBiasM`/`.AzimuthBiasDeg`), `pkg/health/registry.go` (`SensorDetail`, `FeedSnapshot.Sensors`, `RecordSensors`), `cmd/wayfinder/main.go` (Handler + `broadcastFeedSnapshot`), `pkg/broadcast/broadcast.go` (`FeedSensor`, `FeedStatusMessage.Sensors`), `pkg/adminapi/adminapi.go` (`feedSensorDTO`, `feedHealthDTO.Sensors`), `frontend/src/admin/feedHealth.js` (`formatSensorBias`/`describeSensor`/`sensorNeedsAttention`), `frontend/src/stores/asd.js` (`feedSensors`/`sensorDetails`), `frontend/src/map/engine.js`, `frontend/src/components/FeedStatusChip.vue`, `frontend/src/components/admin/AdminFeeds.vue` | `pkg/cat063/bias_test.go` (`TestDecodeRegistrationBias`/`TestDecodeNegativeBias`/`TestDecodeNoBias`), `pkg/health/registry_test.go` (`TestRegistryRecordSensorsStoresDetail`), `frontend/src/admin/__tests__/feedHealth.test.js` (`formatSensorBias`/`describeSensor`/`sensorNeedsAttention`), `frontend/src/stores/__tests__/asd.test.js` (`sensorDetails #237`) |
| FR-DATA-008 | **I062/080 Vertrauens-Flags MON + SPI (Firefly ICD 3.2.0 / QW.3, additiv):** Der CAT062-Decoder liest die beiden Bits aus **Oktett 1** von I062/080 — **MON** (`0x80`, Monosensor: nur eine Quelle im Frische-Fenster, keine Kreuzprüfung → anfälliger für Ghosts/Bias) und **SPI** (`0x40`, Ident-Puls „squawk ident" auf der letzten Meldung) — nach `TrackStatus.Monosensor`/`.SPI`; der Broadcaster reicht sie als `mono`/`spi` (`omitempty`) an die SPA. **ASD-Nutzung:** MON **dezent** als „*"-Marker an der Label-Identitätszeile + Detail-Panel-Zeile „Monosensor · nur eine Quelle"; SPI als amberfarbener **Ident-Highlight-Ring** um das Track-Symbol (gefilterter Circle-Layer auf der Track-Quelle, `spi==true`; **kein Timer** — folgt dem Draht-Flag, das die ~15–30 s des Transponder-Pulses trägt) + Detail-Panel-Zeile „Ident aktiv". Additiv, kein Wire-/ICD-Bruch (es werden nur bisher konstant-0 Bits gesetzt); ein Decoder, der die Bits ignoriert, verhält sich unverändert. | Fireflys ICD-CAT062.md v3.2.0 (§4.1), Firefly FR-TRK-036 / QW.3, Wayfinder-Issue #236 | — | `pkg/cat062/types.go` (`TrackStatus.Monosensor`/`.SPI`), `pkg/cat062/decoder.go` (`decodeTrackStatus`), `pkg/broadcast/broadcast.go` (`TrackMessage.Monosensor`/`.SPI`, `json:"mono"`/`"spi"`), `frontend/src/map/label.js` (MON-Marker), `frontend/src/map/tracks.js` (Feature-Properties `mono`/`spi`), `frontend/src/map/layers.js` (`addSpiHighlightLayer`) + `constants.js` (`SPI_HIGHLIGHT_LAYER_ID`/`_COLOR`), `frontend/src/map/engine.js` (Layer-Verdrahtung), `frontend/src/components/TrackDetailCard.vue` (MON/SPI-Zeilen) | `pkg/cat062/status_test.go` (`TestDecodeMonAndSpi`, `TestDecodeNoMonNoSpi`), `frontend/src/map/__tests__/label.test.js` (MON-Marker-Fälle), `frontend/src/map/__tests__/tracks.test.js` (`updateTracksLayer MON/SPI flags`) |
| FR-DATA-007 | **Per-Technologie-Provenienz A/F + Feed-Status-Fix (E2E-Findings #117/#118/#119; ICD 2.6.0):** Der CAT062-Decoder liest I062/290 positionsbasiert über das Primary-Subfeld und füllt neben `PSRAge`/`ESAge` nun **`SSRAge`/`MDSAge`/`FLARMAge`** (ICD 2.6.0, Firefly ADR 0027; tolerant gegen unbekannte Bits). Die WS-`TrackMessage` trägt `ssr_age_s`/`mds_age_s`/`flarm_age_s` (`omitempty`). Das Frontend leitet daraus die Herkunft ab — **FLARM erstmals distinkt** — und zeichnet für **FLARM** einen eigenen Glyph (`F`); die Track-Formen folgen seit #136 (Häppchen 2) dem Design (◆ ADS-B, ■ SSR, ● PSR, Coasting hohl); neu berechnet je Update (kein Caching → kein „ADS-B bis Reload"-Effekt, #118). Der **Feed-Status** wird aus dem Wire-Feld `color` (green/yellow/red) auf Chip-Zustände gemappt und **worst-wins über alle Feeds aggregiert** (zuvor las das Frontend ein nicht existentes `state`-Feld → dauerhaft „FEED ?", #117); Reset bei WS-(Re)Connect. | E2E-Testlauf-Findings (2026-07-02); Firefly ICD 2.6.0 / ADR 0027 | Firefly ADR 0027 (from-firefly #90) | `pkg/cat062/types.go` (`SSRAge`/`MDSAge`/`FLARMAge`), `pkg/cat062/decoder.go` (I062/290 0x20/0x10/0x04), `pkg/broadcast/broadcast.go` (`SSRAgeS`/`MDSAgeS`/`FlarmAgeS`), `frontend/src/map/provenance.js` (`PROVENANCE_FLARM`, `trackProvenance`), `frontend/src/map/layers.js` (Provenienz-Glyphen in `makeTrackIcon`/`addTrackIcons`), `frontend/src/map/tracks.js` (Provenienz je Update), `frontend/src/stores/asd.js` (`feedHealth`/`feedStatus` worst-wins, `setFeedHealth`/`resetFeedHealth`), `frontend/src/map/engine.js` (`feed_status.color`) | `pkg/cat062/decoder_test.go::TestDecodePerTechnologyAges`/`TestDecodeUpdateAgesUnknownBit`; `frontend/src/stores/__tests__/asd.test.js` (color-Mapping + worst-wins + Reset), `frontend/src/map/__tests__/provenance.test.js` (FLARM-Klasse); `go test`/`vitest`/`vite build` grün |

| FR-ORCH-009 | **Quell-Abdeckung als Zentrum+Radius + Mandanten-Übernahme, Feed-Refetch, Auto-Dismiss (E2E-Findings #109/#111/#112/#113):** Der Feed-Quellen-Dialog gibt flächen-begrenzte Quellen (`adsb_opensky`/`flarm_aprs`) als **Zentrum + Radius** ein (konsistent mit der Standard-Ansicht); die Query-BBox wird beim Speichern per `radiusNmToBbox` abgeleitet, ein gespeicherter Bereich per `bboxToRadius` zurückgewandelt — der `FIREFLY_SOURCES`-Wire-Vertrag (BBox) bleibt unverändert (#109). Dropdown **„Aus Mandant übernehmen"** füllt Zentrum+Radius aus der Standard-Ansicht des gewählten Mandanten (`loadTenantView`; #113). Nach dem Speichern der Quellen wird der Feed-Katalog **neu geladen**, sodass der abgeleitete Sensor-Mix sofort erscheint (#112). Erfolgs-Badges (`admin.notice`) **verschwinden nach 5 s** automatisch; Fehler bleiben sichtbar (#111). | E2E-Testlauf-Findings (2026-07-02) | ADR 0011 (Feed-Lifecycle) | `frontend/src/components/admin/AdminFeeds.vue` (Zentrum/Radius-Felder, `tenantOptions`/`applyTenantArea`, `buildSourcesPayload`/`toFormSource` via `admin/geo.js`, `loadFeeds` nach Speichern), `frontend/src/stores/admin.js` (`notice`-Auto-Dismiss-Watcher) | `frontend` vitest grün (`admin`-Store) + `vite build`; manuell im E2E-Runbook Teil 5 verifiziert |

| FR-UI-009 | **Karten-Controls rechts (ASD-009):** `MapControls.vue` rendert zwei schwebende Button-Gruppen rechts über dem MapLibre-Canvas (`position: absolute`, z-index 10). Gruppe 1: Zoom-In/Zoom-Out. Gruppe 2: Recenter (flyTo konfiguriertem Zentrum), Nord-up-Reset (easeTo bearing=0), Fullscreen-Toggle. Controls emittieren Events; `MapCanvas.vue` delegiert an `mapEngine`-Methoden (`zoomIn`, `zoomOut`, `recenter`, `resetNorth`), die in `engine.js` neu hinzugefügt wurden. Kein Vue-Framework-Eingriff in den Map-Engine-State. | ASD-009 (Mockup-Analyse, Phase 1) | ROADMAP ASD-009 | `frontend/src/components/MapControls.vue`, `frontend/src/components/MapCanvas.vue`, `frontend/src/map/engine.js` | visuell verifiziert (Build grün) |

| FR-UI-008 | **Navigation Rail + Sekundär-Panel (ASD-008):** `NavigationRail.vue` ersetzt die monolithische `LayerSidebar.vue`. Auf Desktop ist die Schiene permanent sichtbar (56 px, Icons + Tooltips, `rail`-Prop); Klick auf ein Rail-Icon expandiert sie auf 240 px und zeigt das Sekundär-Panel (Layer-Toggles + FL-Filter); erneuter Klick auf dasselbe Icon kollabiert zurück. Collapse-Button unten für explizites Einklappen. Auf Mobile bleibt das Hamburger-Menü mit Temporary-Drawer erhalten. App.vue importiert `NavigationRail.vue` statt `LayerSidebar.vue`. | ASD-008 (Mockup-Analyse, Phase 1) | ROADMAP ASD-008, `docs/design/color-tokens.md` | `frontend/src/components/NavigationRail.vue`, `frontend/src/App.vue` | visuell verifiziert (Build + 39 Vitest-Tests grün) |

| FR-UI-007 | **Farbschema ASD-007 (Mockup-Ableitung):** MD3-Chrome-Theme auf Cyan-Primary umgestellt (`#23d3e6`); Hintergrundhierarchie `#070b12`→`#0e1622`→`#16202e`→`#1c2c3e`; Semantikfarben (error `#ff4338`, warning `#ffb02e`, success `#3ecf6b`, info `#3d9be0`) ATC-konventionell; ASD-Domain-Farben in `TRACK_COLORS` (friendlyCivil `#41c4e8`, friendlyMilitary `#ffa726`, hostile `#ff4338`, unknown `#ffd23e`, neutral `#43c66b`) + Palette-Update (`label`/`vector`/`trail`/`airspaceFillColor`/`airways`). Vollständig dokumentiert in `docs/design/color-tokens.md`. | ASD-007 Mockup-Analyse (2026-06-17) | `docs/design/color-tokens.md`, ROADMAP ASD-007 | `frontend/src/plugins/vuetify.js` (`asdDarkTheme.colors`), `frontend/src/map/constants.js` (`TRACK_COLORS`, `PALETTES`) | visuell verifiziert |

| FR-CFG-003 | Optionale Konfigurationsdatei `wayfinder.yaml`: `loadYAMLFile(path, cfg, logger)` liest die Datei (Felder: `map.center_lat/lon/zoom`, `openaip.radius_km`) und überschreibt die Hardcoded-Defaults; Env-Vars gewinnen danach immer (12-Factor). Pfad: Standard `wayfinder.yaml` im Arbeitsverzeichnis, überschreibbar via `WAYFINDER_CONFIG_FILE`. Fehlende Datei ist nicht-fatal (kein Crash, kein Log-Fehler). Ungültiges YAML: Warn-Log + Fallback auf Defaults. Partielle Dateien (nur einzelne Felder gesetzt) lassen übrige Defaults unberührt. | ADR 0002 (Konfigurationsdatei-Konzept), CLAUDE.md §7 (12-Factor) | ADR 0002 | `cmd/wayfinder/main.go` (`yamlFileConfig`, `loadYAMLFile`, `loadConfig`) | `cmd/wayfinder/main_test.go` (`TestLoadYAMLFileAppliesValues`, `TestLoadYAMLFileMissingFileIsNonFatal`, `TestLoadYAMLFileInvalidYAMLIsNonFatal`, `TestLoadYAMLFilePartialOverride`, `TestLoadYAMLFileEnvVarWins`) |

| FR-TEN-001 | **Mandantenfähigkeit nach Hybrid-Modell (Wayfinder 2.0, ADR 0005):** Wayfinder wird zur mandantenfähigen Plattform. Konzeptuelles Datenmodell: **Tenant** (isolierte Organisation), **User** (genau ein Tenant, Rollen `user`/`admin`), **Feed** (ein Upstream-CAT062/065-Strom mit Metadaten inkl. Sensor-Mix als **Feed-Eigenschaft**), **Subscription** (Tenant ↔ erlaubte Feeds), **ViewConfig** (Zentrum/Radius/AOI/FL-Band/Layer), **Entitlement** (Feature-Flags als Daten). Der ASD-Kern bleibt erhalten und wird zur mandanten-skopierten Sicht. 12-Factor-Grenze: fachliche Mandanten-Config in der DB, Infra-Secrets (DB-URL, Stream-Endpunkt, TLS, OIDC) bleiben ENV. Multi-Tenant ist der einzige Betriebsmodus; der frühere Single-Tenant-Rückfall entfällt (ADR 0014). | Konzept `docs/design/wayfinder-2.0-konzept.md`, ROADMAP §0/§1 (WF2-00), CLAUDE.md §1/§7 | **ADR 0005** | **WF2-10.2 ✅ (Teil):** `pkg/store/models.go` (`Tenant`/`User`/`Role`), `pkg/store/tenants.go`/`users.go` (Repos; `UserRepo.GetBySubject` = Identität→Mandant), Schema `00001_init.sql`+`00002_view_config_user_unique.sql`; **WF2-10.3a ✅** `feeds.go`/`subscriptions.go`/`entitlements.go`, **WF2-10.3b ✅** `view_configs.go` (BBox/ViewConfig, Tenant-Default + Nutzer-Override, `GetEffective`) — **Persistenz-Schicht WF2-10 komplett (alle 6 Tabellen)**; **WF2-13 ✅** Admin-Bootstrap (`cmd/wayfinder/bootstrap.go` `runBootstrap`/`bootstrapCommand`: idempotentes Anlegen erster Tenant/Admin/Credential, Subcommand `wayfinder bootstrap`) + `/admin`-Rollen-Gate (`pkg/tenant/authz.go` `RequireRole`); Abo-Durchsetzung WF2-20/21 | **WF2-10.2 ✅** `pkg/store/repo_test.go` (DB-frei) + `pkg/store/store_integration_test.go` (real PostgreSQL 16 via `scripts/pg-test.sh`); **WF2-13 ✅** `cmd/wayfinder/bootstrap_test.go` (DB-frei `validate`) + `bootstrap_integration_test.go` (real-PG: create/idempotent/Passwort-Update/Cross-Tenant-Konflikt) + `pkg/tenant/authz_test.go` (RequireRole: admin erlaubt, user/ohne-Identität → 403); Isolations-Durchsetzungstests WF2-22 |
| NFR-SEC-003 | **Cross-Tenant-Daten-Isolation (Wayfinder 2.0, ADR 0005):** Ein Mandant darf **niemals** die Luftlage eines anderen sehen, außer explizit freigegeben. Verbindliche Prinzipien: **server-seitige Autorisierung pro Subscription** (Broadcaster von all-to-all auf prädikat-gefilterte Zustellung `feed ∈ subs ∧ AOI ∧ FL-Band ∧ Kategorie`, kein Browser-seitiges Filtern), **fail-closed** (ohne gültigen Tenant-Kontext keine Daten), **Pflicht-Negativtests** als Gate (Mandant A bekommt nie einen Track von Mandant B; Property-/Fuzz-Tests gegen das Prädikat), mandanten-bewusste Autorisierung am Browser-Rand (nimmt die in ADR 0003 vertagte „Autorisierungs-ADR" auf) und eine **Audit-Spur** (welcher Tenant sah welchen Scope). | Konzept §5.5/§6, ADR 0005, ADR 0003 (Ehrliche Grenze, vertagte Autorisierung), CLAUDE.md §7 (Sicherheit) | **ADR 0005** | **Abo-Datenschicht WF2-10.3a ✅:** `pkg/store/subscriptions.go` (`ListFeedsByTenant`/`IsSubscribed` — die Daten, gegen die geprüft wird); **HTTP-Rand-Enforcement WF2-12.1 ✅** (`pkg/tenant.Middleware`: subject→tenant fail-closed, 401 ohne gültigen Mandanten); **Stream-Enforcement Feed-Ebene WF2-21.1 ✅** (`broadcast.Scope`/`broadcastTracks`: Track nur an Clients mit `scope.AllowsFeed(feed_id)`, leerer Scope = nichts; `ws.ScopeResolver` fail-closed `403`; `newScopeResolver` via `subscriptions.ListFeedIDsByTenant`); **Sicht-Filter AOI/FL-Band WF2-21.2 ✅** (`broadcast.ViewFilter`/`Scope.filterView`: harte Datensparsamkeits-Grenze server-seitig, **fail-open** bei fehlendem Attribut; `resolveViewFilter` via `view_configs.GetEffective`, FL→Fuß); Lebenszyklus bleibt client-seitig, Klassifizierung später (WF2-40); **Audit-Spur WF2-23.1 ✅** (`cmd/wayfinder.logScopeAudit`: strukturiertes `slog`-Event `component=audit`/`event=ws_connect` mit tenant_id/user_id/subject/feeds/aoi/fl beim `/ws`-Connect, 12-Factor/keine DB) **+ Pro-Tenant-Metriken WF2-23.2 ✅** (`tenant`-gelabelte `/metrics`-Serien, nur stabile `tenant_id`) → **Stufe 2 (mandanten-isolierter Datenstrom) komplett**; **Ingest-Rand-Isolation (E2E-Fix):** `pkg/receiver` filtert jedes Datagramm per **Ziel-Gruppe** (`ipv4.PacketConn`/`FlagDst` → `acceptsGroup`) — auf **einem Host** binden mehrere Feed-Empfänger denselben Port (Allocator: eine Gruppe je Feed, Port fix; `feed_alloc.go`) mit **Wildcard-Bind**, sodass jeder Socket **alle** beigetretenen Gruppen empfängt; ohne Filter tagged ein Empfänger fremde Feed-Tracks mit **seiner** feed_id → Cross-Tenant-Leck **vor** dem Scope-Filter (der korrekt per feed_id filtert, aber mit falsch etikettierten Daten gefüttert wurde). Der Fix verwirft fremde Gruppen am Socket | **WF2-10.3a ✅** (Daten) `…::TestIntegrationSubscriptionRepoIsolation`; **WF2-21.1 ✅ Pflicht-Negativtest** `…::TestBroadcastFeedIsolation` (A bekommt nie B's Feed) + `TestScopeAllowsFeed` + Resolver fail-closed; **WF2-21.2 ✅** `TestViewFilterAdmits` (AOI/FL/**fail-open**) + `TestBroadcastViewScoping` + `TestResolveViewFilter` (DB-frei) + `TestIntegrationResolveViewFilter` (real-PG); **WF2-22 ✅ Property-/Fuzz-Gate** `pkg/broadcast/isolation_test.go`: `TestFilterViewMatchesOracle` (50k Iter, differenziell vs. unabhängiges Oracle, beide Richtungen), `TestBroadcasterIsolationProperty` (Ende-zu-Ende: jeder empfangene Track ∈ Client-Scope), `FuzzScopeFilter` (755k execs, 0 Fehler); **WF2-23.1 ✅** `cmd/wayfinder/scope_test.go::TestScopeResolverEmitsAudit` (Audit-Event mit Scope-Feldern, JSON-geprüft); **Ingest-Rand-Isolation** `pkg/receiver/receiver_test.go::TestAcceptsGroup` (eigene Gruppe akzeptiert, fremde verworfen, nil-Dst→Fallback) |

| FR-TEN-002 | **Konfig-/Identitäts-Persistenz (Wayfinder 2.0, ADR 0006):** Datastore = **PostgreSQL**; Zugriff über **`pgx` + `sqlc`** (typsicheres Go aus handgeschriebenem SQL, kein schweres ORM — auditierbar); Schema-Migrationen versioniert/eingebettet via **minimalen In-House-Runner** (`pkg/store/migrate.go`, eingebettete SQL, je Migration eine Transaktion, idempotent; `goose` verworfen wg. transitivem `modernc.org/sqlite`-Ballast — ADR 0006 Nachtrag). Schema: `tenants`, `users` (Rolle user/admin), `feeds` (globaler Katalog, `sensor_mix` JSONB als Feed-Eigenschaft), `subscriptions` (Tenant↔Feed), `view_configs` (tenant-Default + optionale Nutzer-Übersteuerung), `entitlements` (Feature-Flags als Daten). **Stateless-Split:** alle Mandanten-/Konfig-/Identitäts-Daten in der DB, **kein durabler node-lokaler Zustand**; Infra/Secrets (`WAYFINDER_DB_URL` etc.) bleiben ENV (12-Factor). Cache: vorerst In-Proc-TTL, Redis zurückgestellt bis gemessener Bedarf. | ADR 0006, ADR 0005 (Datenmodell), Konzept §6.2/§11.3 | **ADR 0006** | **WF2-10.1 ✅** `pkg/store/store.go` (`Open`/`DSNFromEnv`), `pkg/store/migrate.go` (Runner), `pkg/store/migrations/00001_init.sql` (Schema); Repositories (pgx/sqlc) folgen WF2-10.2/10.3, Hot-Reload WF2-30 | **WF2-10.1 ✅** `pkg/store/store_test.go` (DB-frei: Migrations-Parsing, Schema-Drift, DSN); Schema-Apply/Repository-Integration folgt WF2-10.3 (CI/Testcontainers) |
| FR-TEN-003 | **Feature-Entitlement-Service (Wayfinder 2.0, WF2-50, ADR 0005 §4):** Pro-Mandant-Feature-Flags **als Daten** (`entitlements`-Tabelle aus WF2-10), entkoppelt von Billing (WF2-51 ruht). `pkg/feature` kapselt einen **fail-closed** Service über `store.EntitlementRepo`: `HasFeature(ctx, tenantID, key) bool` und `Effective(...)` sind **Default-Deny**; ein **unbekannter Katalog-Key** oder **jeder Store-Fehler** evaluiert zu `false` **plus Warn-Log und Metrik** (`wayfinder_feature_check_failclosed_total{reason}`) — ein Tor öffnet nie bei Unsicherheit. Getypter Katalog (`stca`, `multi_feed`, `premium_layers`, `airspaces`, `range_rings`, `history_dots`, `vor_ndb`, `waypoints` — 8 Keys ab AP2) mit `IsKnown`-Validierung; `Set` weist unbekannte Keys ab (`ErrUnknownFeature` → 400). **Admin-Verwaltung** (admin = Billing-Grenze): `GET`/`PUT /api/admin/tenants/{tenantID}/entitlements[/{key}]`, cross-tenant nur `admin` (`requireAdmin`). **SPA:** `whoami` trägt die effektiven Flags (`features`), der Admin-Store exponiert `hasFeature()` — UI-Gating kosmetisch (Layer-Steuerelemente in `LayerFilterContent.vue`), Durchsetzung serverseitig. Gate-Formel: `!isAuthorized || hasFeature(key)` (Nicht-Admin-Nutzer sehen alle Layer). Kein Schema-Change, keine neue Env-Var. **Nachtrag (E2E 2026-07-04): operator-lesbarer Katalog.** Jeder Katalog-Eintrag trägt neben der Beschreibung ein **Label** (Fachbegriff, z. B. „QNH", „Range Rings"); die Admin-UI zeigt Label + klartextliche Kurzbeschreibung statt des rohen Keys, **ohne interne Doku-Kürzel** (ASD-/WF2-/WX-/ADR — per `TestNoInternalDocRefs` erzwungen). `entitlementDTO` um `label` erweitert, `feature.Label(key)` ergänzt, Copy deutsch. Reine Anzeige — Keys/Wire/DB unverändert. **Nachtrag (#175): reservierte Keys markiert.** `stca` und `premium_layers` sind katalogisiert, aber ohne Verbraucher (No-Op-Toggle); sie tragen jetzt ein `Reserved`-Flag (`feature.Reserved`, `entitlementDTO.reserved`), das Admin-Panel zeigt sie **deaktiviert** + „noch nicht aktiv" (Präzedenz #114). Keys bleiben im Katalog (fail-closed unverändert); Verdrahten hebt das Flag auf. | Konzept §6/§11, ROADMAP §1 (WF2-50), ADR 0005 §4; #175 | **ADR 0005** | `pkg/feature/feature.go` (`Service`, `HasFeature`, `Effective`, `Set`, `ErrUnknownFeature`), `pkg/feature/catalog.go` (`Key`, `STCA`/`MultiFeed`/`PremiumLayers`/`Airspaces`/`RangeRings`/`HistoryDots`/`VorNdb`/`Waypoints` — 8 Keys, AP2), `pkg/adminapi/adminapi.go` (`EntitlementService`, `listTenantEntitlements`/`setTenantEntitlement`, `whoami.features`), `cmd/wayfinder/main.go` (Service-Konstruktion + `/metrics`), `frontend/src/stores/admin.js` (`features`/`hasFeature`), `frontend/src/stores/asd.js` (`historyDots` in `layerVisibility`), `frontend/src/map/engine.js` (`historyDots`-Gruppe in `setLayerVisibility`), `frontend/src/components/LayerFilterContent.vue` (Layer-Gate via `!admin.isAuthorized \|\| admin.hasFeature(key)`, History-Dots-Switch) | `pkg/feature/*_test.go` (DB-frei: Default-Deny, **beide Fail-Closed-Pfade** mit Zähler+Warn-Log, Katalog-Overlay, `Set`-Validierung; **8-Key-Len-Check nach AP2**), `pkg/adminapi/adminapi_test.go` (admin GET/PUT, unknown-key 400, unknown-tenant 404, **Cross-Tenant 403 auf beiden Routen**, `whoami.features`), `pkg/adminapi/adminapi_integration_test.go` (**real-PG** Round-Trip set→list + whoami), `frontend/src/stores/__tests__/admin.test.js` (`hasFeature`, **AP2-Block**: alle 5 neuen Keys, fail-closed Default, Nicht-Admin-Gate) |
| FR-TEN-004 | **Feed-Sensorklassen-Katalog & entitlement-gegatete Abos (Wayfinder 2.0, WF2-41, ADR 0005 §6.4):** (A) **Sensorklassen-Katalog** `pkg/sensorclass` — kontrolliertes Vokabular (`PSR`, `SSR`, `MODE_S`, `ADS-B`, `MLAT`, `FLARM`); `Parse` mappt gängige Legacy-Schreibweisen kanonisch (`ads-b`/`ADS_B`/`1090ES`→`ADS-B`, `Mode A/C`→`SSR`, …), `Canonicalize` normalisiert+dedupliziert und **weist unbekannte Tokens ab** (`*UnknownClassError`). Erzwungen am **Chokepoint** `FeedRepo.Create` → Feed-Metadaten enthalten nie Tippfehler (kein Schema-Change, `sensor_mix` bleibt JSONB). Read-only-Surfacing `GET /api/admin/sensor-classes`. (B) **Abos binden an Feeds** (ADR 0005 §6.4) mit **harter Invariante**: ein Mandant **ohne** `multi_feed`-Entitlement (WF2-50) hält **höchstens einen** Feed; ein zweiter *distinkter* Grant → **409 Conflict**, **bevor** `Subscribe` die DB erreicht (fail-early); idempotenter Re-Grant desselben Feeds bleibt 204; admin muss erst das Entitlement setzen. | Konzept §6.4, ROADMAP §1 (WF2-41), ADR 0005 | **ADR 0005** | `pkg/sensorclass/sensorclass.go` (`Class`, `Parse`/`Canonicalize`/`IsKnown`/`All`/`Describe`, `UnknownClassError`), `pkg/store/feeds.go` (`Create`-Validierung), `pkg/adminapi/adminapi.go` (`grantSubscription`-Gate auf `feature.MultiFeed`, `getSensorClasses`), `cmd/wayfinder/feedcmd.go` | `pkg/sensorclass/*_test.go` (Legacy-Wahrheitstabelle, Round-Trip, Dedup, unknown→Fehler), `pkg/adminapi/adminapi_test.go` (1./2. Feed, **409 ohne `multi_feed` + `Subscribe` nicht erreicht**, erlaubt mit `multi_feed`, idempotenter Re-Grant, `sensor-classes`), `pkg/store/feeds_subscriptions_integration_test.go` (**real-PG**: unknown→Fehler, Legacy→kanonisch), `pkg/adminapi/adminapi_integration_test.go` (**real-PG** Grant-Gating-Round-Trip) |
| NFR-SEC-004 | **Identitäts-/Session-Sicherheit (Wayfinder 2.0, ADR 0006; Multi-Tenant-only ADR 0014):** `WAYFINDER_AUTH_MODE` ∈ {proxy, builtin}. **proxy (primär):** OIDC/oauth2-proxy am Ingress; Wayfinder **validiert** den weitergereichten Token (Issuer/Audience/Signatur) und mappt `subject → users.subject → tenant_id`, kein Passwort-Speicher (Muster aus ADR 0003). **builtin (Default):** eingebaute Nutzer mit **argon2id**-Hashes (Standalone/Onboarding); Default, wenn `WAYFINDER_AUTH_MODE` unkonfiguriert ist (zero-touch, ADR 0011). **Multi-Tenant ist der einzige Betriebsmodus (ADR 0014):** `WAYFINDER_DB_URL` ist Pflicht und die Authentifizierung am Browser-Rand ist immer aktiv; der frühere `none`-Modus (degenerierter Single-Tenant) entfällt. **Tenant-Kontext fail-closed** (keine gültige Zuordnung ⇒ keine Daten, NFR-SEC-003). Session möglichst zustandslos (proxy hält Session; builtin: signiertes kurzlebiges Cookie, Signing-Key aus ENV). Secrets ausschließlich in ENV. | ADR 0006, ADR 0003 (Proxy-Primär-Muster), ADR 0014 (Multi-Tenant-only), CLAUDE.md §7 | **ADR 0006** | **WF2-11.1 ✅** `pkg/auth` (`auth.go` Mode/ParseMode/Authenticator, `password.go` argon2id, `session.go` HMAC-Session, `authenticator.go` Builtin), **WF2-11.2 ✅** `proxy.go` (`ProxyAuthenticator`, go-oidc-Validierung Issuer/Audience/Signatur/Ablauf, Bearer-Token) — **AuthN-Paket komplett (proxy + builtin)**; **WF2-12.1 ✅** `pkg/auth/factory.go` (`NewAuthenticator`, fail-closed-Config) + `pkg/tenant` (`Identity`/Context, `Middleware` subject→user→tenant fail-closed); **WF2-12.2 ✅** `cmd/wayfinder/main.go` (`setupTenancy`: DB-Open+Migrate+Authenticator, `tenant.Middleware` immer auf `/ws`; `WAYFINDER_DB_URL` ist Pflicht (ADR 0014), fehlt sie bricht `setupTenancy` den Start ab); **WF2-12.3 ✅** builtin-Login (`pkg/store/credentials.go` `CredentialRepo` + Migration `00003`; `pkg/tenant/login.go` `LoginHandler`/`LogoutHandler`: `auth.VerifyPassword`→`MintSession`-HttpOnly-Cookie, Timing-Angleich gegen User-Enumeration) + `main.go` `/api/login`+`/api/logout` (nur builtin-Modus) — **WF2-12 komplett** | **WF2-11/12 ✅** `pkg/auth/{password,session,auth,proxy,factory}_test.go` + `pkg/tenant/{tenant,login}_test.go` (argon2, Session, **OIDC valid + alle Ablehnungen**, Factory, Middleware fail-closed, **Login: Erfolg setzt gültige Cookie, alle Fehler→401/400 ohne Cookie**) + **`cmd/wayfinder/tenancy_test.go`** + **`pkg/store/credentials_integration_test.go`** (real-PG) |
| FR-SEC-005 | **Cross-Tenant Read-Only-Impersonation (admin „View as Tenant X", WF2-34, ADR 0008):** Ein `admin` kann zur Support-Diagnose die ASD-Sicht eines Ziel-Mandanten **read-only** einsehen — eine **bewusste, auditierte, zeitlich befristete, kontrollierte Ausnahme** zu **NFR-SEC-003**. **Mechanismus:** signierter **HttpOnly-Grant-Cookie** `wf_impersonation` (HMAC via wiederverwendetem Session-Key, TTL `WAYFINDER_IMPERSONATION_TTL`, Default 30m); der `/ws`-Lesepfad löst **Feed-Scope UND View** gegen den Ziel-Mandanten auf. **Fail-closed:** gültiger Grant + Nicht-`admin` → **lauter Handshake-Reject + Audit** (`impersonation_denied`); fehlend/ungültig/abgelaufen → Default-Pfad (**byte-identisch** → WF2-22 bleibt gültig). Die Session ist vom Ziel-Mandanten **entkoppelt** (`scope.TenantID=0`) → **ausgeschlossen** aus Pro-Tenant-Billing/SLA-Metriken (§6) und Live-Rescope (§4, Snapshot-v1). Die **Identität wird nie überschrieben**; Schreibpfade nutzen immer den echten Mandanten → strukturell read-only. Endpoints `GET`/`POST`/`DELETE /api/admin/impersonation` (Status / Mint nur `admin` / Clear). Audit-Events `impersonation_start`/`_denied`/`_end` + `impersonated_tenant_id` am `ws_connect`; Metrik `wayfinder_impersonation_sessions_total`. **Frontend (Häppchen 3):** `admin`-only Banner/Steuerleiste — inaktiv ein dezenter „Als Mandant ansehen"-Einstieg, aktiv ein prominenter Warn-Banner „Sie betrachten X — nur Lesen" mit **Tenant-Switcher** + **Beenden**; WS-**Reconnect** bei Start/Wechsel/Exit (neuer Cookie greift sofort); Reload-fest über den `GET`-Status (HttpOnly-Cookie ist nicht JS-lesbar); **kein** farbiger Viewport-Rahmen (Entscheidung). **Stand:** Häppchen 1 (Grant-Kern) + 2 (Backend) + 3 (Frontend) ✅ — WF2-34 komplett. **Nachtrag (ADR 0008, E2E-Fix):** Der Grant gilt mit identischer fail-closed-Semantik auch auf den **Read-only-REST-Endpunkten** der Karte — `impersonationReadMW` stempelt den effektiven Lese-Mandanten in den Kontext (`tenant.WithReadTenant`, Identity unberührt); `/api/whoami` löst Features/Sensor-Klassen/FL-Band/ICAO gegen den Ziel-Mandanten auf (+ `impersonated_tenant_id`, omitempty), `/api/airspace|navaids|waypoints` bedienen Cache **und** Feature-Gate des Ziel-Mandanten (`readTenantOf`), `/api/weather/qnh` dessen Flugplatz. Gültiger Grant von Nicht-`admin`/auf fehlenden Mandanten → 403 + Audit; `/api/admin/*` und alle Schreibpfade unverändert (echte Identität). | ADR 0008, Anforderung 2 (2026-06-23), CLAUDE.md §7 (Browser-Rand/Sicherheit) | **ADR 0008** | `pkg/impersonation/impersonation.go` (`MintGrant`/`parseGrant`/`Resolve`, `TenantChecker`, `CookieName`, `ErrDenied`/`ErrUnknownTenant`), `cmd/wayfinder/main.go` (`newScopeResolver`-Override + `scope.TenantID=0`, `logScopeAudit`-Felder, `WAYFINDER_IMPERSONATION_TTL`, Metrik, Routen), `cmd/wayfinder/impersonation.go` (`impersonationStatusHandler`/`startImpersonationHandler`/`stopImpersonationHandler`, `tenantExistsChecker`, `impersonationGrantCookie`, `logImpersonationDenied`, **Nachtrag:** `impersonationReadMW`/`readTenantOf`), `pkg/tenant/tenant.go` (`WithReadTenant`/`ReadTenant`/`ImpersonatedTenant`), `pkg/adminapi/adminapi.go` (`whoami` liest den effektiven Lese-Mandanten, `whoamiDTO.ImpersonatedTenantID`), `cmd/wayfinder/main.go` (`impReadMW`-Wiring auf whoami/Aero/QNH); **Frontend:** `frontend/src/stores/impersonation.js` (`active`/`tenantId`/`reconnectNonce`, `loadStatus`/`start`/`stop`), `frontend/src/components/ImpersonationBar.vue`, `frontend/src/map/engine.js` (`reconnect()` + Close-Guard), `frontend/src/components/MapCanvas.vue` (Reconnect-Watcher), **B2:** `frontend/src/components/admin/AdminTenantDetail.vue` („Als Mandant ansehen“-Button → `imp.start` + Sprung zur Karte; Fehler-Alert bei fehlgeschlagenem Mint) | `pkg/impersonation/impersonation_test.go` (11: Grant-Krypto Round-Trip/tampered/wrong-key/expired + **Resolve-Matrix** inkl. `user`→Denied), `cmd/wayfinder/scope_test.go` (Override **active** = Ziel-Feeds + `TenantID=0`, **denied** für Nicht-`admin`, **unknown-tenant**, **expired→Default**, **disabled ohne Key**), `cmd/wayfinder/impersonation_test.go` (Endpoints: Mint 204+HttpOnly/Strict, 404 unknown, 400 bad-body, 500 fail-closed, Clear MaxAge<0, **Status active/inactive**); **Frontend:** `frontend/src/stores/__tests__/impersonation.test.js` (loadStatus ohne Reconnect, start/stop bumpen `reconnectNonce`, Fehlerpfade); **Nachtrag:** `pkg/tenant/readtenant_test.go` (Marker-Semantik, Identity unberührt), `cmd/wayfinder/impersonation_read_test.go` (Passthrough ohne/mit stale Grant, Ziel-Stempel, Nicht-`admin`→403, unbekannter Mandant→403, DB-Fehler→500, `readTenantOf`), `pkg/adminapi/adminapi_impersonation_test.go` (whoami: Features/Sensor-Klassen/View gegen Ziel-Mandant + Offenlegung; Normalpfad byte-identisch), `frontend/src/components/admin/__tests__/adminTenantDetailViewAs.test.js` (B2-Einstieg: Button, Grant-Mint + Karten-Sprung, Fehlerpfad) |

| FR-FEED-001 | **Cloud-Ingest & FeedSource (Wayfinder 2.0, ADR 0007):** `FeedSource`-Abstraktion mit zwei Implementierungen — **`MulticastFeedSource`** (direkter Multicast-Beitritt, On-Prem/Dev, heutiges `pkg/receiver`-Verhalten) und **`StreamFeedSource`** (Konsum vom NATS-Bus, Cloud), umschaltbar via `WAYFINDER_FEED_SOURCE=multicast\|nats`. **Ingest-Gateway** als eigener Minimal-Dienst (`cmd/wayfinder-ingest`): tritt der/den CAT062/065-Multicast-Gruppe(n) bei und republisht **jedes Datagramm unverändert** (Roh-ASTERIX, kein Decode im Gateway) auf **Subject pro Feed** (`wayfinder.feed.<feed_id>`); realisiert Multi-Feed (Hybrid-Modell). Der Decoder→Broadcaster-Pfad bleibt quell-unabhängig; CAT-Oktett-Dispatch wie bisher. | ADR 0007, ADR 0005 (Hybrid-Feeds), Konzept §5.3/§6.3 | **ADR 0007** | **WF2-20.1 ✅ (feed_id-Durchstich):** `receiver.Config.FeedID` → `broadcast.TrackBatch` → `TrackMessage.feed_id`. **WF2-20.2 ✅ (Multi-Feed):** `cmd/wayfinder/feeds.go` `resolveFeeds` (DB-`feeds`-Katalog → N `feedConfig`; leer/kein-DB → ENV-Fallback) + `buildReceivers` (N Receiver je feed_id), `main()`-Reorder (DB vor Receiver, per-Feed-Listen-Skip, Decode-Fehler-Summe); Feed-CLI `cmd/wayfinder/feedcmd.go` (`feed add`/`feed list`). NATS-`StreamFeedSource` folgt WF2-53 | **WF2-20.1 ✅** `TestHandleTracksStampsFeedID`, `TestTracksToMessage`. **WF2-20.2 ✅** `cmd/wayfinder/feeds_test.go` (`resolveFeeds` Fallback+Mapping, `buildReceivers`) + `feeds_integration_test.go::TestIntegrationFeedCatalogue` (real-PG: `feed add/list` + Katalog→feedConfigs, distinkte DB-IDs) + E2E-Rauchtest (Server `feeds resolved count=2`, leer→1) |
| NFR-SCALE-001 | **Cloud-Fan-out & Skalierung (Wayfinder 2.0, ADR 0007):** Zielumgebung Public Cloud + Kubernetes. Transport = **NATS JetStream**: Core-Subject-Fan-out (jede zustandslose Instanz subscribt die Subjects ihrer Feeds und erhält jedes Datagramm — „jede Instanz sieht alles"), JetStream nur als kurzer Late-Join-Puffer (**kein** System-of-Record; revisionssicherer Replay bleibt Fireflys SDPS-005). N zustandslose Wayfinder-Instanzen hinter Service/Ingress (WF2-52); NATS-Cluster (3 Knoten) oder Managed; TLS + Credentials (NKey/JWT); Secrets (`WAYFINDER_NATS_URL`/`_CREDS`) in ENV. Bus trägt Roh-Datagramme (einziger Decode-Punkt erhalten). | ADR 0007, ADR 0006 (Stateless-Split), CLAUDE.md §7 (Cloud-nativ) | **ADR 0007** | folgt WF2-52 (Stateless-Skalierung), WF2-53 (Gateway+Bus produktiv/HA) | folgt WF2-52/53 (Pilot/Benchmark Latenz/Last/HA) |
| FR-ORCH-001 | **Feed-Quell-Datenmodell (Orchestrierung, ORCH-1 / ADR 0012):** Ein Feed trägt eine generische, Firefly-agnostische **Quell-Konfiguration** (`feeds.source_config`, JSONB-Array) — die Liste der Live-Eingänge, aus denen die dem Feed gewidmete Firefly-Instanz ihre System-Tracks rechnen soll. Geschlossenes Quell-Vokabular (`adsb_opensky`, `flarm_aprs`, `radar_asterix`), additiv erweiterbar ohne Schema-Bruch. Pro-Art-Validierung am Schreib-Rand (wie WF2-41 Sensor-Mix): flächengebundene Internet-Quellen (`adsb_opensky`/`flarm_aprs`) **erfordern** eine WGS84-`bbox` und tragen **keine** Sensor-Identität; ein echter Radar (`radar_asterix`) **erfordert** `sac`/`sic` (0..255) und hat eine optionale `bbox`; `cred_ref` ist nur ein **Verweis** auf ein Pro-Feed-Secret (nie der Klartext, nie an den Browser — ADR 0012 §6, Secret-Wert folgt ORCH-2). **Poll-Intervall (UX-5, Firefly ADR 0029):** eine `adsb_opensky`-Quelle trägt optional `poll_interval_secs` (nur ADS-B — FLARM ist Push, Radar hat eigene Scan-Periode; Bereich 5..3600 s, sonst `400` mit Index; fehlt → Firefly-Default 10 s), das Wayfinder additiv nach `FIREFLY_SOURCES` durchreicht (Kontrakt v1.4.0); es lässt den Betreiber die OpenSky-Rate-Limit-Grenze (HTTP 429) respektieren. Daraus abgeleitet die **grobe äußere** `feeds.coverage_bbox` (Union der Quell-BBoxen + Marge, lat/lon-geklemmt) — die generische Geo-Grenze, die Wayfinder an Firefly übergibt (`FIREFLY_COVERAGE_BBOX`), **getrennt** von der präzisen inneren Mandanten-AOI (WF2-21.2, bleibt autoritative Isolations-/Billing-Grenze; coarse-outer vs. precise-inner, ADR 0012 §3). Dedizierte Repo-Accessoren (nicht in der schlanken `Feed`-Zeile/DTO, analog OpenAIP-Key-Isolation ONB-6). **Admin-API (ORCH-1b):** `GET/PUT /api/admin/feeds/{id}/sources` hinter `requireAdmin`; `PUT` validiert server-seitig (geschlossenes Vokabular, Per-Art-Regeln → `400` mit Quell-**Index**, kein Teil-Write), leitet die `coverage_bbox` bei fehlender Angabe selbst ab (Default-Marge 50 km) oder übernimmt eine explizit gesetzte (Operator-Override, WGS84-validiert), und liest die gespeicherte Konfig kanonisch zurück. **Frontend (ORCH-1c):** Quell-Builder im „Feeds"-Tab (`AdminFeeds.vue`) — je Feed ein „Quellen"-Dialog mit editierbarer Quell-Liste (Typ-Dropdown; BBox-Eingabe für Flächenquellen, SAC/SIC für Radar; **Zugangsdaten quelltyp-abhängig (UX-4):** OpenSky Client-ID/Secret für ADS-B (Pflicht), APRS-IS Rufzeichen/Passcode optional für FLARM, **keine** für Radar; die `cred_ref` wird automatisch vergeben (kein Hand-Handle mehr) und bei deaktiviertem Secret-Store erscheint ein klarer Hinweis auf `WAYFINDER_SECRET_KEY` statt eines toten Feldes; **Poll-Intervall (UX-5):** nur bei ADS-B ein optionales Feld „Poll-Intervall (s)" + Infobox zum OpenSky-Rate-Limit (429), leer = Firefly-Default), baut den Payload typ-abhängig (Flächenquelle ohne sac/sic; `poll_interval_secs` nur bei ADS-B und nur wenn gesetzt), Coverage server-abgeleitet und nach dem Speichern read-only angezeigt; Store-Actions `loadFeedSources`/`saveFeedSources`. Gating kosmetisch (Server erzwingt). **Stand:** ORCH-1 komplett (1a Schema + Store, 1b Admin-API, 1c Frontend) ✅. | ADR 0012 (ORCH-1), Betreiber-Wunsch „pro Kunde eigener Sensor-Mix + Ausschnitt", Firefly ADR 0029 (Poll-Intervall) | **ADR 0012** | `pkg/store/migrations/00010_feed_source_config.sql`, `pkg/store/feed_sources.go` (`SourceType`/`Source`/`SourceConfig`, `Validate` inkl. `poll_interval_secs`-Bereich, `CoverageBBox`, `FeedRepo.GetSourceConfig`/`SetSourceConfig`, `InvalidSourceError`); `pkg/dockerbackend/sources.go` (`fireflySource.PollIntervalSecs`-Passthrough); `pkg/adminapi/adminapi_feeds.go` (`getFeedSources`/`putFeedSources`/`validateBBox`/`feedSourcesDTO`/`defaultCoverageMarginKm`), `pkg/adminapi/adminapi.go` (`FeedStore` um `Get`/`SetSourceConfig`, Routen); `frontend/src/stores/admin.js` (`loadFeedSources`/`saveFeedSources`), `frontend/src/components/admin/AdminFeeds.vue` (Quell-Builder-Dialog, Poll-Feld+Infobox) | `pkg/store/feed_sources_test.go` (`TestSourceConfigValidate` Vokabular/Per-Art-Regeln/BBox-Bereiche/Index + `poll_interval_secs` gültig/nur-ADS-B/Bereich, `TestCoverageBBox*` Union/keine-BBox/Marge/Pol-Clamp), `pkg/dockerbackend/sources_test.go` (`TestFireflySourcesEnvPollIntervalPassthrough`), `pkg/store/feeds_subscriptions_integration_test.go::TestIntegrationFeedSourceConfig` (**real-PG**: Default leer, Round-Trip cred_ref/sac/sic + Coverage, Reject ohne Teil-Write, Clear, ErrNotFound), `pkg/adminapi/adminapi_feeds_test.go` (`TestGetFeedSources*`, `TestPutFeedSourcesRoundTripAndDerivesCoverage`/`…ExplicitCoverageOverrides`/`…InvalidIsRejected`/`…UnknownIs404`, `TestFeedSourcesRoutesForbidNonAdmin`), `frontend/src/stores/__tests__/admin.test.js` (ORCH-1c-Block), `frontend/src/components/admin/__tests__/adminFeedsPollInterval.test.js` (5 Tests: Gate/Infobox/Payload/Round-Trip/Bounds) |
| NFR-SEC-004 | **Quell-Credential-Isolation (Orchestrierung, ADR 0012 §6):** Quell-Zugangsdaten (OpenSky-Client-Credentials, FLARM-/Radar-Zugänge) werden als **Pro-Feed-Secrets** geführt — in der Quell-Konfig steht nur eine `cred_ref` (ein Verweis/Handle), **nie** der Klartext; das Secret wird nie in ein DTO geschrieben, nie an den Browser gereicht und erst dem `InstanceBackend` beim Start gereicht (analog OpenAIP-Key-Isolation ONB-6). **Stand:** Datenmodell trägt `cred_ref` (ORCH-1a) ✅; Secret-Speicher + Übergabe an die Control-Plane folgen ORCH-2. | ADR 0012 §6, ADR 0011 (ONB-6 Key-Isolation), CLAUDE.md §7 (Sicherheit) | **ADR 0012** | `pkg/store/feed_sources.go` (`Source.CredRef` als Referenz, Validierung non-blank/Länge) | `pkg/store/feed_sources_test.go` (cred_ref-Validierung); Secret-Speicher folgt ORCH-2 |
| FR-ORCH-002 | **Tracker-Instanz-Abstraktion (Orchestrierung, ORCH-2 / ADR 0012 §4):** Wayfinder bekommt eine backend-agnostische Abstraktion, um **pro Feed eine Firefly-Instanz** zu betreiben — Grundlage für „Feed zuweisen ⇒ Instanz startet". `instance.Backend` (`Start`/`Stop`/`Status`, **idempotent**, nebenläufigkeitssicher; Instanz-Identität = `feed_id`) wird injiziert, sodass Control-Plane (ORCH-2c) und Reconciler (ORCH-3) **ohne** echtes Spawnen unit-testbar sind (Spiegel zum `feedmanager`-Muster). `instance.Spec` ist die **generische, Firefly-agnostische** Launch-Spezifikation aus einem Feed (Multicast-Endpoint, grobe `Coverage`-BBox, Quell-Liste, **Secret-Referenzen** statt Werten); `SpecFromFeed` leitet sie rein ab (sammelt distinkte, sortierte `cred_ref`-Handles — deterministisch für den Drift-Vergleich des Reconcilers). Die genauen Firefly-**Eingangs**-Env-Namen (`FIREFLY_SOURCES` etc.) bleiben bewusst aus der `Spec` heraus — sie sind cross-project (ORCH-5) und werden erst im Backend-Adapter (ORCH-2b) übersetzt. `MemoryBackend` ist Test-Double **und** Einzelhost-Dev-Platzhalter. **Docker-Adapter (ORCH-2b):** `dockerbackend.Backend` implementiert `instance.Backend` über Docker-Container — ein Container je Feed, Identität via Label `wayfinder.feed_id`, Drift-Erkennung via `wayfinder.spec_hash`-Label (gleicher Hash → idempotenter No-op/Restart, geänderter → Replace), Orphan-fähiges `RunningFeeds` (listet auch gestoppte managed Container). Die **gesamte Lebenszyklus-Logik ist hinter ein schmales `ContainerClient`-Interface gekapselt** und mit einem Fake voll unit-getestet; **die einzige Datei mit dem schweren Docker-SDK-Import** (`client.go`) ist eine dünne Übersetzung (kein Unit-Test, braucht Daemon). Injection-sicher (Container-Name nur aus numerischer Feed-ID, kein Shell-Aufruf — SDK). `fireflyEnv` setzt die Ausgabe-Env (`FIREFLY_CAT062_GROUP/PORT`, `FIREFLY_COVERAGE_BBOX`) und **immer** `FIREFLY_SOURCES` (Quell-Eingangs-Kontrakt, ORCH-5a, FR-ORCH-006) — bei einem Feed **ohne** Quellen die explizite leere Liste `[]`: der gespawnte Firefly läuft mit leerem Himmel + CAT065-Heartbeat statt einer Platzhalter-Szene (Firefly ADR 0030; das frühere `FIREFLY_MODE=live`/`FIREFLY_SCENE` entfällt). Die Credential-**Werte** (Auflösung der `cred_env`-Namen) injiziert die Control-Plane separat (ORCH-5b). **Binary-Verdrahtung:** `cmd/wayfinder-orchestrator` wählt das Backend über `WAYFINDER_ORCHESTRATOR_BACKEND` (`memory` Default / `docker`); `docker` erfordert `WAYFINDER_FIREFLY_IMAGE`, optional `WAYFINDER_FIREFLY_NETWORK` (Default `host`, Multicast); das frühere `WAYFINDER_FIREFLY_SCENE` entfiel (Firefly ADR 0030). **Stand:** ORCH-2a ✅; Docker-Adapter (2b) ✅; Secret-Auflösung + Änderungs-Trigger (2c 3/3) folgen. | ADR 0012 §4/§6 (`InstanceBackend`, Least-Privilege-Control-Plane), Betreiber-Wunsch „Auto-Start der Tracker" | **ADR 0012** | `pkg/instance/instance.go` (`Status`, `Feed`, `Spec`, `SpecFromFeed`, `Spec.Validate`/`Endpoint`, `Backend`), `pkg/instance/memory.go` (`MemoryBackend`); `pkg/dockerbackend/backend.go` (`Backend`, `ContainerClient`, `fireflyEnv`, `specHash`, Labels), `pkg/dockerbackend/client.go` (SDK-Adapter); `cmd/wayfinder-orchestrator/main.go` (`newBackend`, Backend-Env) | `pkg/instance/instance_test.go` (`TestSpecFromFeed*` Ableitung/Secret-Refs distinkt+sortiert/Dedup, `TestSpecValidate`/`…Endpoint`, `TestMemoryBackendLifecycle`/`…RejectsInvalidSpec`/`…StartHookFailure`/`…Concurrent` unter `-race`), `pkg/dockerbackend/backend_test.go` (`TestStartCreatesAndRunsContainer`/`…IsIdempotentOnSameSpec`/`…RestartsStoppedContainer`/`…ReplacesDriftedContainer`, `TestStopRemovesContainer`/`…UnknownFeedIsNoop`, `TestStatusFailedForExitedContainer`, `TestRunningFeedsListsManaged`, `TestStartRejectsInvalidSpec`, `TestFireflyEnvMapsSpec` unter `-race`), `cmd/wayfinder-orchestrator/main_test.go` (`TestLoadConfigBackendDefaultsToMemory`/`…DockerRequiresImage`/`…UnknownBackendErrors`) |
| NFR-SEC-004 | **Quell-Credential-Isolation (Orchestrierung, ADR 0012 §6):** Quell-Zugangsdaten (OpenSky-Client-Credentials, FLARM-/Radar-Zugänge) werden als **Pro-Feed-Secrets** geführt — in der Quell-Konfig steht nur eine `cred_ref` (ein Verweis/Handle), **nie** der Klartext; das Secret wird nie in ein DTO geschrieben, nie an den Browser gereicht und erst dem `InstanceBackend` beim Start gereicht (analog OpenAIP-Key-Isolation ONB-6). **Verschlüsselter Secret-Speicher (ORCH-2c 3a):** Die Werte liegen **AES-256-GCM-verschlüsselt** in `feed_secrets` (`pkg/secret.Cipher`: zufälliger Nonce je Seal, Auth-Tag → Manipulation wird beim Open erkannt; `ErrDecrypt` nie Teil-Klartext); ein DB-Leak allein liefert **keine** Klartext-Credentials. **AAD-Identitäts-Bindung (Hardening):** Seal/Open binden jeden Blob per **Additional Authenticated Data** an seine `(feed_id, cred_ref)`-Identität (`orchestrator.credAAD`, NUL-getrennt, eindeutig); ein in der DB **verschobener/replayter** Blob (Angreifer mit Schreibzugriff) scheitert beim Entschlüsseln unter einer anderen Identität — fail-closed, Defense-in-depth über die reine At-Rest-Vertraulichkeit hinaus. Der Schlüssel ist deployment-verwaltet (`WAYFINDER_SECRET_KEY`, base64-32-Byte), **nie** in der DB. `store.SecretRepo` ist crypto-agnostisch (speichert nur den opaken Blob; `Set`/`Get`/`Delete`/`ListRefs`, Cascade beim Feed-Löschen). `orchestrator.SecretResolver` liest den Blob und entschlüsselt ihn — nur in der getrennten Control-Plane, nie am Browser-Rand. **Write-only Admin-API (ORCH-2c 3a-API):** `GET/PUT/DELETE /api/admin/feeds/{id}/secrets[/{ref…}]` (requireAdmin); der Wert geht nur **rein** (PUT `{value}`) und wird vom `orchestrator.SecretSealer` (Schreib-Gegenstück zum Resolver) **vor** der Speicherung versiegelt — der `GET` meldet nur, **welche** Refs gesetzt sind (`{secrets:[{ref,configured}]}`), **nie** einen Wert. Der Server hält den Schlüssel (`WAYFINDER_SECRET_KEY`) nur zum Seal beim Schreiben; ohne Schlüssel sind die Routen **deaktiviert** (503), nie unverschlüsselt speichernd. Das `{ref…}`-Trailing-Wildcard erlaubt `cred_ref`s mit Slash (z. B. `secret/opensky`). **Stand:** Datenmodell `cred_ref` (ORCH-1a) ✅; `Spec.SecretRefs` nur Referenzen (ORCH-2a) ✅; verschlüsselter Speicher + Resolver (ORCH-2c 3a) ✅; write-only Admin-API + Sealer + Frontend (ORCH-2c 3a-API) ✅; **Container-Injection (ORCH-5b-1, Variante A) ✅:** der Orchestrator löst die `cred_ref`s nur in der Least-Privilege-Control-Plane auf (`WAYFINDER_SECRET_KEY` → `SecretResolver`, `StoreDesiredState.WithSecretResolver`) und injiziert den Klartext als separates `FIREFLY_SOURCE_<i>_SECRET`-Env in den gespawnten Container (nie ins `FIREFLY_SOURCES`-JSON, nie geloggt); fehlt Key/Secret → Quelle läuft anonym (WARN, kein Abbruch). Rotation ändert den Spec-Hash → Restart. | ADR 0012 §6, ADR 0011 (ONB-6 Key-Isolation), CLAUDE.md §7 (Sicherheit) | **ADR 0012** | `pkg/secret/secret.go` (`Cipher`, `Seal`/`Open`, `KeyFromBase64`, `ErrDecrypt`), `pkg/store/migrations/00011_feed_secrets.sql`, `pkg/store/feed_secrets.go` (`SecretRepo`), `pkg/orchestrator/secret.go` (`SecretResolver`/`SecretReader`, **`SecretSealer`/`SecretWriter`**), `pkg/orchestrator/desired.go` (`FeedSecretResolver`/`WithSecretResolver`, best-effort `resolveSecrets`), `pkg/instance/instance.go` (`Spec.ResolvedSecrets`), `pkg/dockerbackend/sources.go` (`fireflySourcesEnv`, Cred-Envs), `pkg/adminapi/adminapi_secrets.go` (`SecretService`, write-only Handler), `cmd/wayfinder/main.go` (`WAYFINDER_SECRET_KEY` → Cipher → Sealer), `cmd/wayfinder-orchestrator/main.go` (`WAYFINDER_SECRET_KEY` → Cipher → Resolver), `frontend/.../AdminFeeds.vue` + `stores/admin.js` (`loadFeedSecrets`/`setFeedSecret`/`deleteFeedSecret`) | `pkg/secret/secret_test.go` (Round-Trip, nicht-deterministisch, falscher Schlüssel/Manipulation/Garbage → ErrDecrypt, Key-Länge, KeyFromBase64, **`TestAADMustMatch`** Identitäts-Bindung), `pkg/store/feeds_subscriptions_integration_test.go::TestIntegrationSecretRepo` (**real-PG**: Set/Get/ListRefs sortiert/Upsert/Delete/ErrNotFound/Feed-Cascade), `pkg/orchestrator/secret_test.go` (Resolver + **`…SealerStoresCiphertextAndRoundTrips`** + **`…AADBindsToFeedIdentity`** Relocate-Schutz), `pkg/orchestrator/desired_test.go` (Resolver-Pfad: aufgelöst/anonym-Fallback/ohne-Resolver), `pkg/dockerbackend/sources_test.go` (kein Wert-Leak ins JSON), `pkg/adminapi/adminapi_secrets_test.go` (configured-Liste/kein-Leak/PUT-Round-Trip+Slash-Ref/Wert-Pflicht/zu-lang/Delete+404/unbekannter-Feed/503-ohne-Key/403-Non-Admin), `frontend/.../admin.test.js` (Secret-Store-Actions inkl. 503) |
| FR-ORCH-003 | **Reconciler (Orchestrierung, ORCH-3 / ADR 0012 §5):** Cloud-natives Operator-Muster — statt imperativ beim ersten Abo zu starten und beim letzten zu stoppen, hält der Reconciler **Soll = Ist**: er berechnet periodisch (und auf Anstoß) die Soll-`instance.Spec`s (Feeds mit ≥ 1 aktivem Abo, je aus ihrer Quell-Konfig abgeleitet) und treibt das `instance.Backend` darauf zu. **Idempotent & crash-fest:** jeder Soll-Spec wird ge-`Start`et (Backend ist idempotent → No-op bei gleichem Spec, Re-Apply bei geändertem, Recovery eines abgestürzten/fehlgeschlagenen); jede laufende Instanz, deren Feed nicht mehr Soll ist, wird ge-`Stop`t (Orphan-Cleanup); Ist wird **beobachtet** (`Backend.RunningFeeds`), nicht erinnert → übersteht Neustart. Ein Per-Feed-Fehler bricht den Lauf **nicht** ab (gesammelt via `errors.Join`, geloggt, nächster Tick heilt). `Run(ctx, interval)` reconciled sofort + je Tick bis ctx-Cancel. Instanz-Identität = `feed_id`. **DesiredState** ist injiziert; der **Store-Adapter** (ORCH-2c, 1. Stück) `orchestrator.StoreDesiredState` leitet das Soll aus dem Katalog ab: alle Feeds mit ≥ 1 Abo (`SubscriptionRepo.ListSubscribedFeeds`, DISTINCT) × je Quell-Konfig (`FeedRepo.GetSourceConfig`) → `instance.SpecFromFeed`; ein Lese-Fehler **bricht den Lauf ab** (kein Teil-Soll → keine fälschlich verwaisten Instanzen). Voll testbar gegen `MemoryBackend` + Fakes. **Getrennter Prozess (ORCH-2c, 2/3, 🔒 ADR 0012 §6):** Die Control-Plane läuft als **eigenes Binary `cmd/wayfinder-orchestrator`** — nur dieser Prozess erhält später das Container-Start-Privileg, der browser-zugewandte Server **nie** (Least-Privilege; ein Browser-Rand-Exploit kann keine Container starten). Verdrahtet DB + `StoreDesiredState` + Reconciler + (vorerst) `MemoryBackend`; **migriert nicht** (ein Migrator = der Hauptserver); Env `WAYFINDER_DB_URL`/`WAYFINDER_ORCHESTRATOR_INTERVAL`(15s)/`WAYFINDER_LOG_LEVEL`; Modi `--once` (ein Lauf, Exit-Codes 2/1) + Schleife mit Graceful-Shutdown. **Stand:** Reconciler-Kern ✅; Store-`DesiredState`-Adapter (1/3) ✅; getrennter Control-Plane-Prozess (2/3) ✅; verschlüsselter Secret-Speicher/-Resolver + write-only API (2c 3a/3a-API) ✅; **Änderungs-Trigger (2c 3b) ✅** (FR-ORCH-004); echter Docker-Adapter (2b) ✅; Container-Secret-Injection folgt (ORCH-5). | ADR 0012 §5/§6 (Reconciler + getrennte Least-Privilege-Control-Plane) | **ADR 0012** | `pkg/reconciler/reconciler.go` (`DesiredState`, `Reconciler`, `Reconcile`, `Run`); `pkg/instance/instance.go` (`Backend.RunningFeeds`), `pkg/instance/memory.go`; `pkg/orchestrator/desired.go` (`StoreDesiredState`, `SubscribedFeedLister`/`SourceConfigReader`), `pkg/store/subscriptions.go` (`ListSubscribedFeeds`); `cmd/wayfinder-orchestrator/main.go` (`loadConfig`, `run`, `--once`/Loop, getrenntes Binary) | `pkg/reconciler/reconciler_test.go` (`TestReconcileStartsDesired`/`…StopsOrphans`/`…IsIdempotent`/`…ReappliesChangedSpec`/`…RecoversCrashedInstance`/`…DesiredStateErrorAborts`/`…PerFeedErrorDoesNotAbort`, `TestRunReconcilesUntilCancelled` unter `-race`), `pkg/orchestrator/desired_test.go` (`TestDesiredSpecsBuildsSpecsFromSubscribedFeeds`/`…Empty`/`…ListErrorAborts`/`…SourceConfigErrorAborts`), `pkg/store/feeds_subscriptions_integration_test.go::TestIntegrationSubscriptionRepoIsolation` (**real-PG**: `ListSubscribedFeeds` DISTINCT + Drop bei letztem Unsubscribe), `cmd/wayfinder-orchestrator/main_test.go` (`TestLoadConfig*`: Defaults/DSN-Pflicht/`--once`/Interval+Fallback/LogLevel/unknown-flag) |
| FR-ORCH-004 | **Änderungs-getriebener Reconcile (Orchestrierung, ORCH-2c 3b / ADR 0012 §5):** Der Reconciler konvergiert nicht nur im Intervall-Takt, sondern **sofort** bei einer relevanten Katalog-Änderung — ein neu angelegter/abonnierter/umkonfigurierter Feed startet „seine" Firefly-Instanz ohne Wartezeit bis zum nächsten Tick. **DB-Seite (Migration `00012`):** Statement-Level-Trigger auf `feeds` und `subscriptions` rufen `pg_notify('wayfinder_reconcile','')` — DB-seitig statt App-seitig, fängt **jeden** Schreiber (adminapi + manuelles SQL); leerer Payload, da der Reconciler das **volle** Soll neu berechnet; `feed_secrets` bewusst noch nicht (Secret-Werte sind erst mit der Container-Injection spec-relevant, ORCH-5). **Orchestrator-Seite:** `orchestrator.Listener` hält eine **dedizierte** Verbindung (`LISTEN` bindet an eine Session), wandelt jede Notification in ein Reconcile-Signal und feuert **nach jedem (Re-)Connect** ein Resync-Signal (fängt Änderungen, die während einer Verbindungslücke passierten — der Reconciler liest ohnehin das volle Soll, ein Extra-Signal ist immer sicher); Reconnect mit fixem Backoff. **Coalescing:** der `Run`-`select` erhält einen `trigger`-Fall; das Signal-Senden ist **nicht-blockierend** auf einen gepufferten Size-1-Channel, sodass ein Schwung schneller Änderungen zu **einem** anstehenden Reconcile kollabiert. Das Intervall bleibt **Sicherheitsnetz** (verpasste Notify/Selbstheilung). `trigger` ist nil-bar (Memory/Tests → nur Intervall). **Stand:** ✅ (Trigger + Listener + Migration, real-PG-verifiziert). | ADR 0012 §5 (Reconciler „periodisch **und auf Anstoß**"), Betreiber-Erwartung „Zuweisung wirkt sofort" | **ADR 0012** | `pkg/store/migrations/00012_reconcile_notify.sql` (Trigger-Funktion + `feeds`/`subscriptions`-Trigger), `pkg/orchestrator/listen.go` (`Listener`, `ReconcileChannel`, Resync-on-(re)connect, nicht-blockierendes `signalReconcile`), `pkg/reconciler/reconciler.go` (`Run` um `trigger`-Channel), `cmd/wayfinder-orchestrator/main.go` (Trigger-Channel + Listener-Goroutine) | `pkg/reconciler/reconciler_test.go::TestRunReconcilesOnTrigger` (Trigger reconciled sofort, riesiges Intervall), `pkg/orchestrator/listen_integration_test.go::TestIntegrationListenerSignalsOnChange` (**real-PG**: Resync-on-connect, `feeds`-Insert/Delete + `subscriptions`-Insert → Signal) |
| FR-ORCH-005 | **Automatische Multicast-Endpoint-Allokation (Orchestrierung, ORCH-4 / ADR 0012):** Jede orchestrierte Firefly-Instanz sendet auf **ihrem eigenen** Multicast-Endpoint; zwei Feeds auf derselben `(Gruppe, Port)` würden sich **auf der Leitung übersprechen** — ein Mandant sähe fremde Datagramme schon auf Netzebene, *bevor* das Scoped-Fan-out greift (defense-in-depth unter NFR-SEC-003). Der Allocator vergibt daher **eine Gruppe pro Feed** aus einem konfigurierten /24 (variiert das letzte Oktett, fester Port) — die **niedrigste freie** Gruppe je neuem Feed. **Race-sicher:** der `feeds_endpoint_unique`-Constraint (Migration `00013`, `UNIQUE(multicast_group, port)`) ist die Wahrheit; `CreateAutoAllocated` liest die belegten Oktette, nimmt das niedrigste freie und fügt ein — gewinnt ein paralleler Create dasselbe (`ErrEndpointTaken`), wird neu gelesen und das nächste freie probiert; voller Pool → `ErrPoolExhausted`. Endpoints **außerhalb** des Pools (Legacy/manuell) verkleinern den Pool nicht. **Pro-Feed-Netz-Isolation:** je Feed eine eigene Gruppe → IGMP/Switches prunen pro Feed (ein Empfänger sieht nur Pakete der beigetretenen Gruppen). **Admin-API (ORCH-4):** `POST /api/admin/feeds` macht `multicast_group`/`port` **optional** — beide weglassen ⇒ Auto-Vergabe (zugewiesener Endpoint zurückgegeben + live beigetreten); beide setzen ⇒ manueller Override (validiert, Kollision → **409**); nur eines gesetzt → **400**; Pool erschöpft → **507**. **Config (12-Factor):** `WAYFINDER_FEED_GROUP_BASE` (Default `239.255.0`), `WAYFINDER_FEED_PORT` (`8600`), `WAYFINDER_FEED_OCTET_MIN`/`_MAX` (`1`/`254`) → ~254 Feeds out-of-the-box, auf /16 weitbar; ungültige Kombi fällt auf `DefaultMulticastPool` zurück. **Frontend:** Schalter „Multicast-Endpoint automatisch zuweisen" (Default an) im Feed-Anlegen-Dialog; manuelle Eingabe als Aufklapp-Option. **Keine CAT062-Schnittstellen-Wirkung** (der Endpoint ist nur die interne Wire-Adresse). **Stand:** ✅ (Migration + Allocator + API + Frontend, real-PG-verifiziert). | ADR 0012 (Auto-Orchestrierung), NFR-SEC-003 (Isolation als defense-in-depth), Betreiber-Wunsch „keine Endpoint-Handarbeit" | **ADR 0012** | `pkg/store/migrations/00013_feeds_endpoint_unique.sql`, `pkg/store/feed_alloc.go` (`MulticastPool`/`Validate`, `DefaultMulticastPool`, `FeedRepo.WithMulticastPool`/`CreateAutoAllocated`/`lowestFreeOctet`, `ErrPoolExhausted`/`ErrEndpointTaken`/`isEndpointUnique`), `pkg/store/feeds.go` (`Create` mappt Endpoint-Unique → `ErrEndpointTaken`, `FeedRepo.pool`); `pkg/adminapi/adminapi.go` (`FeedStore.CreateAutoAllocated`), `pkg/adminapi/adminapi_feeds.go` (`createFeed` optionaler Endpoint, 409/507); `cmd/wayfinder/main.go` (`feedPool`, `WAYFINDER_FEED_*`, `WithMulticastPool`-Verdrahtung); `frontend/src/components/admin/AdminFeeds.vue` (Auto-Schalter), `frontend/src/stores/admin.js` (`createFeed` 409/507) | `pkg/store/feed_alloc_test.go` (`TestMulticastPoolValidate`/`…Group`, `TestIntegrationFeedAutoAllocate` **real-PG**: Sequenz/Skip-belegt/Manueller-Dup→ErrEndpointTaken/Pool-Erschöpfung/außerhalb-Pool), `pkg/adminapi/adminapi_feeds_test.go` (`TestCreateFeedAutoAllocatesEndpoint`/`…PartialEndpointRejected`/`…EndpointTakenIs409`/`…PoolExhaustedIs507`), `frontend/src/stores/__tests__/admin.test.js` (Auto-Allokation/409-Endpoint/507-Pool) |
| FR-ORCH-006 | **Quell-Eingangs-Übersetzung (Orchestrierung, ORCH-5 / ADR 0012; Firefly ADR 0023):** Der Docker-Adapter übersetzt die generische Quell-Liste eines Feeds (`store.SourceConfig`) in Fireflys env-getriebenen **Eingangs-Kontrakt** — die einzige Wayfinder-Seite des Cross-Project-Drahts neben dem CAT062-Ausgabe-Vertrag. **ORCH-5a (Rendering):** `fireflySourcesEnv` rendert `spec.Sources` deterministisch als `FIREFLY_SOURCES`-JSON-Array (Eintrag: `type`, `bbox` mit `min_lat…`-Feldnamen aus `store.BBox`, `sac`/`sic`, `cred_env`); `fireflyEnv` setzt `FIREFLY_SOURCES` **immer** — beim quellenlosen Feed die explizite leere Liste `[]` (leerer Himmel, Firefly ADR 0030; das frühere `FIREFLY_MODE=live`/Scene-Fallback entfällt). Je Quelle mit `cred_ref` wird ein `cred_env`-**Name** (`FIREFLY_SOURCE_<i>_SECRET`) referenziert — der Credential-**Wert** steht **nie** im JSON-Blob. Deterministische Reihenfolge → stabiler Spec-Hash. **ORCH-5b (Cred-Auflösung, Variante A):** Die Auflösung passiert in der **Control-Plane**: `StoreDesiredState.WithSecretResolver` löst — wenn ein Deployment-Key da ist — je Spec die `SecretRefs` über den `orchestrator.SecretResolver` zu Klartext-Werten auf und legt sie in `Spec.ResolvedSecrets`; `fireflySourcesEnv` gibt dann je aufgelöster Quelle ein separates `FIREFLY_SOURCE_<i>_SECRET=<wert>`-Env aus (Wert **nie** im JSON-Blob, **nie** geloggt). **Best-effort:** fehlt der Key oder ein Secret (kein Eintrag/falscher Key/manipuliert), läuft die Quelle **anonym** weiter (kein `cred_env`) — WARN, **kein** Reconcile-Abbruch. Weil der Wert ins Env fließt, ändert eine Secret-Rotation den Spec-Hash → der Reconciler startet die Instanz mit dem neuen Wert neu. Der Klartext lebt nur im Least-Privilege-Orchestrator (`WAYFINDER_SECRET_KEY` nur dort), nie am Browser-Rand. **ORCH-5b-2 (UI, UX-2):** Im Admin (Feed → Quellen → Secret) gibt der Betreiber **zwei** Felder ein — **Client-ID + Client-Secret** (OAuth2, Firefly ADR 0024; OpenSky hat Basic Auth abgeschaltet) — die vor dem PUT zu **einem** `client_id:client_secret`-Secret kombiniert werden (`frontend/src/admin/credential.js`: `validateCredential`/`combineCredential`, rein/testbar). Die Client-ID darf keinen Doppelpunkt tragen (Firefly splittet am **ersten** `:`), das Client-Secret schon; ungültige Paare blockieren das Speichern. Write-only bleibt: der Server meldet nur, **ob** ein Wert gesetzt ist, nie den Wert. Der Wire-Vertrag (ein String, ein `:`) ist davon unberührt — nur die Feld-Semantik/Beschriftung. **Schnittstellen-Wirkung:** Eingangs-Seite (Firefly-Kontrakt v1.1.0); CAT062-Ausgabe unberührt. **Stand:** ORCH-5a ✅; ORCH-5b-1 (Backend/Cred-Auflösung) ✅; ORCH-5b-2 (UI-Zwei-Felder, UX-2; OAuth2-Labels ADR 0024) ✅. **Nachtrag (E2E 2026-07-05, #198/#199):** (a) `cred_ref` wird beim **Quell-Typwechsel neu abgeleitet** (`ensureCredRef` unbedingt statt nur-bei-leer) und im Payload **nur** gesendet, wenn er auflösbar ist (Pflicht-Credential ODER Secret hinterlegt/gültig getippt) — kein Dangling-`…-adsb_opensky`-Ref mehr an anonymen FLARM-Quellen (behebt das Orchestrator-Log-Rauschen „secret unresolved"). (b) Ein Klick auf „Speichern" (`submitSources`) persistiert **Quelle und getipptes Secret atomar**; ein **Pflicht-Credential** ohne hinterlegtes/eingegebenes Secret **blockiert** das Speichern, und die Erfolgsmeldung ist ehrlich (ein Secret-Fehler wird im Dialog angezeigt) — vorher fiel das getippte Secret beim Haupt-Button **still** weg (grüner Toast trotz fehlendem Pflicht-Zugang). | ADR 0012 (Auto-Orchestrierung), Firefly ADR 0023 (`FIREFLY_SOURCES`-Kontrakt), `docs/cross-project/todo-for-firefly.md` (#35); #198/#199 | **ADR 0012** | `pkg/dockerbackend/sources.go` (`fireflySource`-DTO, `fireflySourcesEnv`, `credEnvName`), `pkg/dockerbackend/backend.go` (`fireflyEnv` setzt `FIREFLY_SOURCES` + Cred-Envs), `pkg/instance/instance.go` (`Spec.ResolvedSecrets`), `pkg/orchestrator/desired.go` (`FeedSecretResolver`, `WithSecretResolver`, `resolveSecrets` best-effort), `cmd/wayfinder-orchestrator/main.go` (`WAYFINDER_SECRET_KEY` → `SecretResolver`), `frontend/src/admin/credential.js`, `frontend/src/components/admin/AdminFeeds.vue` (Zwei-Felder-Dialog, `saveSecret` kombiniert) | `pkg/dockerbackend/sources_test.go` (`TestFireflySourcesEnvEmpty`/`…AdsbWithResolvedCredential` (kein Wert-Leak)/`…UnresolvedCredentialIsAnonymous`/`…RadarAndAnonymous`/`…CredByIndex`/`…Deterministic`), `pkg/dockerbackend/backend_test.go::TestFireflyEnvMapsSpec`, `pkg/orchestrator/desired_test.go` (`TestDesiredSpecsResolvesSecretsWhenResolverWired`/`…UnresolvableSecretIsAnonymous`/`…NoResolverLeavesSecretsUnresolved`), `cmd/wayfinder-orchestrator/main_test.go::TestLoadConfigSecretKey`, `frontend/src/admin/__tests__/credential.test.js` (validate/combine: leer/Doppelpunkt-in-Client-ID/Client-Secret-mit-Doppelpunkt) |
| FR-ORCH-007 | **End-to-End-Abnahme-Harness (Orchestrierung, ORCH-5c / ADR 0012):** Die ganze Auto-Orchestrierungs-Kette ist in einem lauffähigen Single-Host-Stack zusammengesteckt und abnehmbar: **Feed zuweisen → Orchestrator spawnt Firefly → CAT062/UDP-Multicast → Tracks im ASD → Abbestellen räumt auf.** `Dockerfile.orchestrator` baut das Control-Plane-Binary getrennt vom Server (ADR 0012 §6); `docker-compose.orchestrated.yml` fährt `db` + `wayfinder` (migriert + empfängt) + `orchestrator` (`BACKEND=docker`, mountet **nur hier** den Docker-Socket, `WAYFINDER_FIREFLY_IMAGE`, gemeinsamer `WAYFINDER_SECRET_KEY`); alle host-vernetzt (Multicast), Postgres an den Host veröffentlicht. `scripts/e2e-orchestrated.sh` seedet den Katalog direkt in Postgres (Tenant+Feed+Subscription) und assertet **Prüfpunkt 1** (Container mit Label `wayfinder.feed_id` spawnt), **2** (Env trägt Endpoint + `FIREFLY_SOURCES` — beim quellenlosen Feed die explizite leere Liste `[]`, Firefly ADR 0030; ohne `FIREFLY_SOURCE_0_SECRET`), **5** (`empty`: `wayfinder_cat065_heartbeats_received_total` > 0 — der leere Himmel beweist die Kette per Heartbeat; `opensky-anon`: `wayfinder_cat062_tracks_received_total` > 0, best-effort) und **8** (Unsubscribe → Orphan-Cleanup), in zwei Modi (`empty` offline / `opensky-anon`). Die credential-bezogenen Prüfpunkte **3/4/6/7** (Secret-Injection, kein Leak, Anonym-Fallback, Rotation→Restart) sind der manuelle authentifizierte Lauf (`docs/E2E-ABNAHME.md`). **Stand:** Harness + Skript ✅ (Sandbox-verifiziert: `docker compose config`, `go build`, `bash -n`); realer DinD-Lauf auf Linux-Docker-Host. | ADR 0012 (Auto-Orchestrierung), CLAUDE.md §7 (Betreibbarkeit) | **ADR 0012** | `Dockerfile.orchestrator`, `docker-compose.orchestrated.yml`, `scripts/e2e-orchestrated.sh`, `docs/E2E-ABNAHME.md` | Sandbox: `docker compose -f docker-compose.orchestrated.yml config` (gültig), `go build ./cmd/...`, `bash -n scripts/e2e-orchestrated.sh`; voller Lauf: Runbook A/B (automatisiert) + C (manuell) |
| FR-ORCH-008 | **Orchestrator emittiert den zugewiesenen Feed (E2E-Testlauf-Härtung, Issues #104/#102):** Die je Feed gespawnte Firefly-Instanz sendet ihren Feed tatsächlich. `dockerbackend.fireflyEnv` setzt **immer** `FIREFLY_CAT062_ENABLED=true` (Fireflys Default ist *aus* — sonst läuft der Tracker, sendet aber weder CAT062 noch CAT065-Heartbeat) und einen **pro Feed eindeutigen** `FIREFLY_PORT` (`fireflyHTTPPort` = Basis `18080` + Feed-ID, außerhalb 8080/8081/5432): die Instanz teilt sich per `network_mode: host` den Port-Raum; Fireflys HTTP-Server bindet sonst auf 8080 (Wayfinder-Probe-Port) und stürzt mit *address already in use* in eine Crash-Loop (im realen Multipass-Lauf beobachtet). **Sensor-Mix ableiten (#102):** `SourceConfig.DerivedSensorMix` leitet den Sensor-Mix eines Feeds aus den Quell-Typen ab (`adsb_opensky→ADS-B`, `flarm_aprs→FLARM`, `radar_asterix→SSR`); `SetSourceConfig` schreibt ihn atomar mit — kein hand-gepflegtes Label mehr, das driften kann. | E2E-Testlauf (Multipass) Befund, Issues #104/#102 | **ADR 0012** | `pkg/dockerbackend/backend.go` (`fireflyEnv` ENABLED+PORT, `fireflyHTTPPort`), `pkg/store/feed_sources.go` (`DerivedSensorMix`, `SetSourceConfig` schreibt `sensor_mix`) | `pkg/dockerbackend/backend_test.go` (`TestFireflyEnvMapsSpec` ENABLED+PORT, `TestFireflyHTTPPortIsCollisionFree`), `pkg/store/feed_sources_test.go::TestDerivedSensorMix` |
| FR-ORCH-010 | **`radar_asterix`-Standort serialisieren (Issue #91, Firefly-Eingangs-Kontrakt v1.3.0):** Eine `radar_asterix`-Quelle trägt jetzt den **Radar-Standort** — `lat`/`lon` (WGS84, **Pflicht**: CAT048 ist polar *relativ zum Radar* und trägt den Standort nicht; ohne ihn kann Firefly die Polar-Plots nicht ins Tracking-Frame heben und die Instanz bricht beim Start ab) plus optional `height_m` (m über WGS84-Ellipsoid) und `listen` (UDP-`group:port` für den ASTERIX-Eingang). `store.Source` bekommt die Felder; `Source.validate` verlangt `lat`/`lon` (Bereiche geprüft) für `radar_asterix` und **verbietet** sie bei flächen-begrenzten Quellen — Ablehnung am Schreib-Rand statt Container-Crash. `dockerbackend.fireflySource` reicht die Felder deterministisch in `FIREFLY_SOURCES` durch (`radar_asterix` trägt **kein** `cred_env`). Admin-UI (`AdminFeeds.vue`) bietet im Quellen-Dialog Radar-Standort-Felder. Wire-Vertrag additiv (nur `radar_asterix` betroffen). | Firefly-Issue #91 / ADR 0028, `docs/source-input-contract.md` v1.3.0 | **ADR 0012** | `pkg/store/feed_sources.go` (`Source` Lat/Lon/HeightM/Listen, `validate`), `pkg/dockerbackend/sources.go` (`fireflySource` + `fireflySourcesEnv`), `frontend/src/components/admin/AdminFeeds.vue` | `pkg/store/feed_sources_test.go` (radar mit/ohne lat/lon, Bereichs-Checks, Flächenquelle-mit-Standort abgelehnt), `pkg/dockerbackend/sources_test.go::TestFireflySourcesEnvRadarAndAnonymous` (lat/lon/listen serialisiert), `pkg/adminapi/adminapi_feeds_test.go` (Round-Trip/404 mit Radar-Standort) |
| FR-ORCH-011 | **Quell-Typ `adsb_aggregator` — auth-freier ADS-B-Bezug (Issue #201, Firefly-Eingangs-Kontrakt v1.5.0 / ADR 0031):** Ein Feed kann ADS-B über einen **Community-Aggregator** (adsb.lol Default, adsb.fi) beziehen — der zweite Bezugsweg **neben** OpenSky, für Umgebungen mit Datacenter-IP-Sperre (Codespaces-Diagnose 2026-07-05) oder ohne OAuth2-Client. Der Typ ist **flächen-begrenzt** (`bbox` Pflicht, Zentrum+Radius-Editor wie die übrigen Flächenquellen), **gepollt** (`poll_interval_secs` gilt jetzt für `adsb_opensky` **und** `adsb_aggregator`, Bereich 5..3600 s) und **auth-frei** (kein `CREDENTIAL`-Eintrag im UI → kein `cred_ref`, keine Secret-Felder; dank #198/#199 entsteht kein Dangling-Ref). Neues Feld `provider` (nur dieser Typ; geschlossenes Vokabular `adsb_lol`/`adsb_fi`, unbekannt/falscher Typ → `400` mit Quell-Index — ein unbekannter Provider ließe die Firefly-Instanz beim Start abbrechen; airplanes.live bewusst zurückgestellt bis die Radius-Einheit verifiziert ist, Firefly ADR 0031). `dockerbackend.fireflySource` reicht `provider` additiv nach `FIREFLY_SOURCES` durch; Sensor-Mix-Ableitung → ADS-B. UI: Typ „ADS-B (Community-Aggregator)" + Anbieter-Select mit menschenlesbaren Labels (adsb.lol/adsb.fi — snake_case bleibt Wire-intern) + Höflichkeits-Infobox (~1 req/s öffentlich, Default 10 s). Wire-Vertrag additiv; CAT062-Ausgabe unberührt. | Firefly-Issue #53 / ADR 0031, `docs/source-input-contract.md` v1.5.0, Issue #201 | **ADR 0012** | `pkg/store/feed_sources.go` (`SourceADSBAggregator`, `isPolled`, `knownAggregatorProviders`, `Source.Provider`, `validate`), `pkg/dockerbackend/sources.go` (`fireflySource.Provider`), `frontend/src/components/admin/AdminFeeds.vue` (Typ-Eintrag, `AGG_PROVIDERS`-Select, `isPolledType`-Poll-Feld, Payload/Round-Trip) | `pkg/store/feed_sources_test.go` (aggregator gültig mit/ohne provider, poll_interval, ohne bbox / unbekannter provider / provider-auf-Fremdtyp abgelehnt), `pkg/dockerbackend/sources_test.go::TestFireflySourcesEnvAggregatorProviderPassthrough`, `frontend/.../adminFeedsAggregator.test.js` (6 Tests: Vokabular/Provider-Labels/Gate/Payload/Round-Trip/auth-frei), `adminFeedsPollInterval.test.js` (auf gepollte Typen verallgemeinert) |
| FR-ORCH-012 | **Lokale ASTERIX-über-UDP-Quelltypen `adsb_asterix` + `mlat_asterix` (Issues #239/#240, Firefly-Eingangs-Kontrakt v1.6.0/v1.7.0):** Ein Feed kann jetzt zwei **lokale Push-Quellen** beziehen — eine **eigene ADS-B-Bodenstation** als ASTERIX **CAT021/UDP** (`adsb_asterix`, Firefly FEP.3) und ein **WAM/MLAT-System** als **CAT020 (Zielmeldungen) + CAT019 (Systemstatus) über UDP** (`mlat_asterix`, FEP.5). Sie sind die **Produktions-Bezugswege** für ADS-B bzw. unabhängige kooperative Überwachung — Push statt Poll, lokal statt Internet-REST — neben OpenSky/Aggregator und dem polaren `radar_asterix`. Beide bilden eine **dritte Formkategorie**: das Bodensystem berechnet die Position selbst (CAT021 sind geodätische Selbstmeldungen, CAT020 vom MLAT-System berechnet), daher **kein `bbox`, kein `lat`/`lon`/`height_m`, kein `cred_ref`** (roher UDP-Strom, Vertrauensgrenze = Netz-Isolation) — nur ein optionaler `listen`-Endpoint (`group:port`), optionale `sac`/`sic` (Stations-/System-Identität, 0..255) und eine optionale `sensor_id` (Plot-Tag; absent → Fireflys Default 230/240). `store.Source` bekommt `sensor_id`; `Source.validate` lehnt `sensor_id` auf jedem Nicht-UDP-Typ ab und verbietet für die UDP-Typen `bbox`/`lat`/`lon`/`height_m`/`cred_ref` (Bereichs-Check SAC/SIC, non-negative `sensor_id`) — Ablehnung am Schreib-Rand statt Container-Crash. `dockerbackend.fireflySource` reicht `sensor_id` additiv in `FIREFLY_SOURCES` durch. Sensor-Mix-Ableitung: `adsb_asterix→ADS-B`, `mlat_asterix→MLAT`. UI: zwei Typ-Einträge („ADS-B (Bodenstation, CAT021/UDP)" / „WAM/MLAT (CAT020/019/UDP)") mit dedizierter Formular-/Payload-Kategorie (`ASTERIX_UDP_TYPES`/`isAsterixUdpType`): Listen-Endpoint + SAC/SIC + Sensor-ID, **keine** Fläche/Credentials, Infobox. **Betriebshinweis (Kontrakt-Punkt 4):** trägt ein Feed **ausschließlich** UDP-Push-Quellen, fehlt die Union-bbox für Fireflys System-Referenzpunkt (betrifft nur I062/100, das Wayfinder nicht rendert — gerendert wird WGS84 I062/105); der Betreiber setzt dann `FIREFLY_SYSTEM_REF_*` an der Firefly-Instanz — kein Auto-Wert ableitbar, da die Quelle keinen Standort trägt. Wire-Vertrag additiv (bestehende Typen unverändert); CAT062-Ausgabe unberührt. | Issues #239/#240 (`from-firefly`), Firefly FEP.3/FEP.5, `docs/source-input-contract.md` v1.6.0/v1.7.0 | **ADR 0012** | `pkg/store/feed_sources.go` (`SourceADSBASTERIX`/`SourceMLATASTERIX`, `isASTERIXUDP`, `Source.SensorID`, `sensorClassBySourceType`, `validate`), `pkg/dockerbackend/sources.go` (`fireflySource.SensorID`, `fireflySourcesEnv`), `frontend/src/components/admin/AdminFeeds.vue` (Typ-Einträge, `ASTERIX_UDP_TYPES`/`isAsterixUdpType`, Formular-/Payload-Zweig, `blankSource`/`toFormSource`) | `pkg/store/feed_sources_test.go` (adsb_asterix/mlat_asterix gültig mit/ohne sac/sic/sensor_id, bbox/location/cred_ref/sensor_id-auf-Fremdtyp abgelehnt, SAC-Bereich, negative sensor_id; `TestDerivedSensorMix` um ADS-B/MLAT), `pkg/dockerbackend/sources_test.go::TestFireflySourcesEnvAsterixUDP` (listen/sac/sic/sensor_id serialisiert, bbox/lat/lon/cred_env weggelassen), `frontend/.../adminFeedsAsterixUdp.test.js` (6 Tests: Vokabular/Kategorie/Formular/Payload/Round-Trip/auth-frei) |
| FR-ORCH-013 | **Manuelle Flugplan-Korrelation — Command-Rückkanal Wayfinder→Firefly (ADR 0024, Issue #245 Teil B):** Der Lotse kann Fireflys **automatische** Flugplan-Korrelation als **Bedienhandlung** korrigieren — einen Plan auf einen Track pinnen (`POST /correlation {track_number, callsign}`), einen Track auf **unkorreliert** zwingen (`POST /correlation {track_number}`), oder einen Pin lösen (`DELETE /correlation/{N}`); manuell schlägt Automatik (Fireflys ADR 0038/0039). **Neuer Rückkanal:** Wayfinder ist bisher reiner Multicast-Konsument; Teil B baut erstmals einen Command-Weg zur je-Feed-gespawnten Firefly-Instanz. **Ratifizierte Entscheidungen (ADR 0024):** (E1) **Server-direkt** — der browser-zugewandte Server ruft Fireflys `/correlation` synchron (422/409-Rückmeldung fürs Kontextmenü); kreuzt ADR 0012 §6 nicht (gegatete Datenaktion, kein Container-Start). (E2) **deployment-weites** `WAYFINDER_FIREFLY_COMMAND_TOKEN` (Bearer; Muster wie `WAYFINDER_SECRET_KEY`; Per-Feed-Token als spätere Härtung). (E3) **Gating** `IsSubscribed(tenant, track.feed_id)` — kein scope-loser Admin (ADR 0022), **nicht** unter Read-only-Impersonation (ADR 0008; erste Feed-Schreib-Aktion). (E4) portable Adressierungs-Abstraktion (Host-Netz `127.0.0.1:<18080+feed_id>` heute, K8s-Naht offen). (E5) Transport nach `pkg/weather`-Muster (Timeout, `io.LimitReader`, best-effort). **Ausdrücklich außen vor:** `identity_conflict` (nur Fireflys WS-Pfad, nicht über CAT062). **Stand:** ADR 0024 akzeptiert ✅; Umsetzung in vier Häppchen (je eigene Freigabe): **(1) Command-Client + Adressierung + Config ✅** (`pkg/fireflycmd`: `Client` mit `Correlate`/`SetUncorrelated`/`ClearOverride`/`ListOverrides` gegen Fireflys `POST/DELETE/GET /correlation`, typisierte Fehler `ErrUnknownCallsign`(422)/`ErrNoFlightPlans`(409)/`ErrUnauthorized`(401)/`ErrUnreachable`, Bearer-Header, `io.LimitReader`, Timeout; `HostLoopbackAddresser` über die **verschobene** Single-Source-of-Truth `instance.FireflyHTTPPort`; `TokenEnvVar`-Konstante — noch **nicht** in den Server verdrahtet); (2) Server-Endpoint + Gating + 422/409-Mapping, **(2) Server-Endpoint + Gating ✅** (`pkg/correlationapi`: `POST /api/correlation` `{feed_id, track_number, callsign?}` + `DELETE /api/correlation/{feedID}/{trackNumber}`, hinter `tenantMW`+`pwGate`, **nicht** admin-gegated; drei Gating-Schleusen `authorize`: authentifiziert → sonst 401, **nicht** unter Read-only-Impersonation `tenant.ImpersonatedTenant` → sonst 403, `IsSubscribed(Identity.TenantID, feed_id)` → sonst 403 (deckt scope-losen Admin ab); Fehler-Mapping `ErrUnknownCallsign`→422/`ErrNoFlightPlans`→409/`ErrUnreachable`+`ErrUnauthorized`→502; leeres Token → 503; Audit-Log je Kommando; `WAYFINDER_FIREFLY_COMMAND_TOKEN` in `cmd/wayfinder` verdrahtet); (3) Frontend-Kontextmenü, (4) `fireflyEnv`-Token-Injektion + Doku. Keine CAT062-Ausgabe-Wirkung. | ADR 0024, Issue #245 Teil B (`from-firefly`), Fireflys ADR 0038/0039 | **ADR 0024** | H1 ✅: `pkg/fireflycmd/fireflycmd.go`, `pkg/instance/instance.go` (`FireflyHTTPPort` geteilte Quelle), `pkg/dockerbackend/backend.go` (delegiert); **H2 ✅:** `pkg/correlationapi/correlationapi.go` (`Service`, `Commander`/`SubscriptionChecker`-Interfaces, `authorize`, `SetHandler`/`ClearHandler`), `cmd/wayfinder/main.go` (Config `FireflyCommandToken`, `fireflycmd`+`correlationapi` verdrahtet, Routen); H3–4: `frontend/…` Kontextmenü, `pkg/dockerbackend` `fireflyEnv` | H1 ✅: `pkg/fireflycmd/fireflycmd_test.go`, `pkg/instance/instance_test.go::TestFireflyHTTPPort`; **H2 ✅:** `pkg/correlationapi/correlationapi_test.go` (AuthZ-Tabelle: unauth→401, Nicht-Abonnent/Impersonation-trotz-Abo/scope-loser-Admin→403, Subs-Fehler→500, Feature-aus→503, Body-Validierung→400, 422/409/502-Mapping, Commander-nie-aufgerufen bei Ablehnung), `cmd/wayfinder/main_test.go::TestLoadConfigParsesFireflyCommandToken`; H3–4: Frontend-Tests |
| FR-UI-017 | **ASD-Karte: entitlement-gegatete Layer + dynamische Spurherkunft-Legende (Issues #106/#107):** Das Layer-/Filter-Panel zeigt einem Lotsen **nur** die per Admin-Konfig freigeschalteten Layer — gegated über die **role-agnostische** Session-Identität (`GET /api/whoami` → `features`), die auch für einen reinen Mandanten-Nutzer gefüllt ist (zuvor gated die Karte über den admin-gegateten `whoami` und zeigte dem Lotsen alles). Fail-open beim Laden/anonym/Admin-Betrachter. Die **Spurherkunft-Legende** zeigt nur die Herkünfte, die die abonnierten Feeds liefern — abgeleitet aus `whoami.sensor_classes` (Union der Sensor-Klassen der abonnierten Feeds); leere Menge → volle Legende (Fallback, nie leer). Rein kosmetisch; der Server erzwingt Scope/Zugriff unabhängig (NFR-SEC-003). | E2E-Testlauf-Befund, Issues #106/#107; WF2-40 (Legende), WF2-50 (Entitlements) | ADR 0009 | `pkg/adminapi/adminapi.go` (`whoamiDTO.SensorClasses`, `effectiveSensorClasses`), `frontend/src/stores/session.js` (`features`/`hasFeature`/`sensorClasses`), `frontend/src/components/LayerFilterContent.vue` (`showLayer`, dynamische `provenanceLegend`) | `frontend/src/stores/__tests__/session.test.js` (whoami-Felder/Defaults), vitest (207) + Frontend-Build grün |
| NFR-SEC-005 | **Wetter-Vertrauensgrenze — defensive Wetter-Konsumenten (ADR 0016):** Die drei Wetter-Features (DWD-Radar-Overlay, QNH-Infobox, DWD-Warnungen) führen Wayfinders erste allgemeine **ausgehende Internet-Abhängigkeit außerhalb von OpenAIP** ein — zu **DWD** (`maps.dwd.de`) und **NOAA** (`aviationweather.gov`). Verbindliche Prinzipien (analog NFR-SEC-002 / robuster CAT062-Decoder): **(1) Backend-Proxy als einziger Egress-Punkt** — der Browser spricht nur denselben Origin an (kein Browser→Drittanbieter, kein CORS-Rand, per-Tenant gatebar). **(2) Best-effort/graceful degradation** — nie `/ready` blockieren, nie den Start blockieren; Fehler → Last-Good-Cache bzw. leere/transparente Antwort (HTTP 200); nicht-blockierender periodischer Refresh. **(3) Misstrauischer Konsument** — Request-Timeout, Antwort-Größenbegrenzung (`io.LimitReader`), tolerantes Parsen (fehlerhafte Einzel-Records verworfen), **kein Panic** auf Eingabe, **Fuzzing** des METAR-/GeoJSON-Parsers. **(4) Feature still aus** ohne konfigurierte URL (`Enabled` aus gesetzter URL abgeleitet) und zusätzlich per Feature-Entitlement pro Mandant (`weather_radar`/`qnh`/`weather_warnings`, ADR 0014). **(5) QNH-Datenwahrheit** — QNH nur aus echtem METAR (NOAA `altim`), **nie** aus DWD-PMSL/MOSMIX (andere physikalische Größe); kein erfundener Wert im UI. **(6) Lizenz/Attribution** — DWD frei unter GeoNutzV/CC BY 4.0 („© Deutscher Wetterdienst"), NOAA Public Domain; Attribution sichtbar in Karte/Legende. **Stand:** Fundament (ADR 0016 + Feature-Keys) ✅ Häppchen 0; Implementierung Radar/QNH/Warnungen folgt Häppchen A/B/C. | CLAUDE.md §7 (Sicherheit), ADR 0016 | **ADR 0016** | `docs/decisions/0016-ausgehende-wetter-feeds-dwd-noaa.md`, `pkg/feature/catalog.go` (Keys `weather_radar`/`qnh`/`weather_warnings` folgen A/B/C); `pkg/weathertiles`/`pkg/weather` (Häppchen A/B/C) | Häppchen-weise: Tile-Proxy/METAR-/GeoJSON-Decoder gegen Referenz-Vektoren + Fuzzing (A/B/C) |

| FR-WX-001 | **DWD-Wetter-Radar-Overlay (WX-A, ADR 0016):** Der Lotse kann ein DWD-Niederschlags-/Radar-Bild als Karten-Overlay unter die Luftlage legen (Gewitterzellen/Niederschlag sichtbar). **Backend-Tile-Proxy:** MapLibre fordert XYZ-Kacheln von Wayfinder (`/api/weather/radar/{z}/{x}/{y}.png`); der Server übersetzt jede Kachel in einen DWD-GeoServer-WMS-`GetMap` in **EPSG:3857** (WMS 1.1.1, unzweideutige Achsenreihenfolge), cacht das PNG für die Radar-Kadenz (~5 min) und liefert es same-origin. **Best-effort/misstrauisch (NFR-SEC-005):** Request-Timeout, `io.LimitReader`-Größenlimit (4 MiB/Kachel), Nicht-Bild-Antwort (GeoServer-`ServiceException`-XML) wird verworfen, jeder Fehlerpfad → **transparente** Kachel (HTTP 200), nie ein Absturz, nie `/ready`-Blockade. **Gating (ADR 0017 / Häppchen 2):** **default-an** (öffentliche DWD-WMS-URL als Default), abschaltbar per `WAYFINDER_DWD_RADAR_ENABLED=false`; `map-config.weather_radar_available` deaktiviert sonst den Schalter; per-Tenant über Entitlement `weather_radar` in der Sidebar (`showLayer`). **Attribution:** „© Deutscher Wetterdienst" an der Raster-Source (GeoNutzV/CC BY 4.0). Z-Order: über der Basiskarte, unter Aeronautik/Tracks. Kein CAT062-Eingriff, Firefly-unabhängig. | Betreiber-Wunsch (Wetter im ASD), ADR 0016 | **ADR 0016** | `pkg/weathertiles/` (`tiles.go` XYZ→EPSG:3857-BBox + transparente Fallback-Kachel, `client.go` WMS-GetMap-Client mit Größen-/Content-Type-Schutz, `service.go` TTL-Cache + `TileHandler`), `cmd/wayfinder/main.go` (Config `WAYFINDER_DWD_*`, Service, Route hinter `tenantMW`, `map-config.weather_radar_available`, Metriken), `pkg/feature/catalog.go` (`weather_radar`), `frontend/src/map/constants.js`/`layers.js` (`addWeatherRadarLayer`, Raster-Source)/`engine.js` (Z-Order + `groups`-Eintrag + Verfügbarkeit), `frontend/src/stores/asd.js` (`weatherRadar`/`weatherRadarAvailable`), `frontend/src/components/LayerFilterContent.vue` (Toggle + Hinweis) | `pkg/weathertiles/weathertiles_test.go` (BBox-Referenzwerte, `validTile`, WMS-URL-Encoding, Cache/TTL-Refetch mit injizierter Uhr, Last-Good→transparent-Fallback, Nicht-Bild-Ablehnung, ungültige Koordinaten, disabled, Handler-Pfad+`.png`-Trim, transparente Kachel = valides PNG), `pkg/feature/catalog_test.go` (Key im Katalog), `frontend/src/stores/__tests__/asd.test.js` (Default aus/Verfügbarkeit/Toggle) |

| FR-WX-002 | **QNH-Infobox (WX-B, ADR 0016; per-Tenant CBD-3, ADR 0017):** Der Lotse sieht das aktuelle **QNH** (Höhenmesser-Einstellung, hPa) **seines** Flugplatzes in der ASD-Kopfzeile — der Flugplatz wird **pro Mandant** in der Admin-UI gesetzt (`view_configs.qnh_icao`, echter 4-stelliger ICAO). **Datenwahrheit (NFR-SEC-005):** QNH kommt **ausschließlich** aus einem echten **METAR** (NOAA/AWC-Feld `altim`, hilfsweise `Qxxxx`/`Axxxx` aus `rawOb`), **nie** aus DWD-PMSL/MOSMIX (andere physikalische Größe); Werte außerhalb 850–1100 hPa werden verworfen; **kein erfundener Wert** — fehlt ein METAR, wird nichts angezeigt, ein zu altes wird als **stale** ausgegraut/markiert. **Backend-Poller (`pkg/weather`):** pollt die **Vereinigung der Mandanten-Flugplätze** (dynamisch aus der DB via `DistinctQNHICAOs`, `StationsProvider`) periodisch (Default 15 min); ein `Refresh()`-Kick nach einer Ansichts-Änderung holt einen frisch gesetzten Flugplatz sofort; cacht je Station das Last-Good, dient `GET /api/weather/qnh` **tenant-scoped** (nur der eigene Flugplatz, `SnapshotFor`) als `{stations:[{icao,qnh_hpa,obs_time,stale}],primary}`. Defensiv: distinktiver User-Agent (Pflicht gegen AWC-403), Timeout, `io.LimitReader`, toleranter Decode (fehlerhafte Records übersprungen), best-effort (Fehler → Last-Good, nie `/ready`-Blockade). **Connected-by-default:** NOAA-Quelle default-an (`WAYFINDER_QNH_ENABLED`, Default true; `false` = Opt-out); `WAYFINDER_METAR_STATIONS` bleibt deprecated globaler Fallback. **Gating (zwei Ebenen):** Quelle default-an, Anzeige nur mit gesetztem `qnh_icao` **und** Entitlement `qnh` (`session.hasFeature`). QNH ganzzahlig (Cockpit/ATC-Konvention). Kein CAT062-Eingriff, Firefly-unabhängig. | Betreiber-Wunsch (QNH im ASD), ADR 0016/0017 | **ADR 0016/0017** | `pkg/weather/` (`client.go` NOAA-METAR-Client + `altim`/`Q`/`A`-Parsing + Plausibilitätsgrenze, `service.go` Poll-Loop + dynamischer `StationsProvider` + `Refresh`-Kick + Per-Station-Cache + Stale-Logik + `SnapshotFor`/`TenantHandler`), `pkg/store/view_configs.go` (`qnh_icao`-Spalte + `DistinctQNHICAOs`, Migration `00016`), `pkg/adminapi/adminapi.go` (`qnh_icao` in `viewDTO` + `validICAOCode`/`normalizeQNHICAO`), `cmd/wayfinder/main.go` (`WAYFINDER_QNH_ENABLED`, DB-Provider, tenant-aware Route, Refresh im rescope), `pkg/feature/catalog.go` (`qnh`), `frontend/src/components/admin/AdminTenantDetail.vue` (Feld „QNH-Flugplatz"), `frontend/src/admin/validateView.js` (ICAO-Format), `frontend/src/stores/weather.js` + `AsdHeader.vue` (QNH-Span, `qnh`-gegated) | `pkg/weather/weather_test.go` (Provider-Union + tenant-scoped `SnapshotFor`, `TenantHandler`, `Refresh`-Kick, Quelle aus/an, Q/A-Parsing inkl. inHg + Plausibilität, `altim`-vor-`rawOb`, Fetch skippt fehlerhafte Records, Snapshot-Reihenfolge/primary/Stale/Rundung, Last-Good), `pkg/adminapi/adminapi_test.go` (`validateViewQNHICAO`, `normalizeQNHICAO`), `cmd/wayfinder/main_test.go` (`QNHConnectedByDefault`/`OptOut`/`MetarStationsFallback`), `frontend/src/admin/__tests__/validateView.test.js` (ICAO-Format-Parität) |

| FR-WX-003 | **DWD-Wetterwarnungen-Overlay (WX-C, ADR 0016):** Der Lotse kann amtliche DWD-Warnpolygone (Gewitter, Sturm, Schnee/Eis …) als Karten-Overlay einblenden, nach **Warnstufe** (`wf_level` 1–4) eingefärbt. **Backend-Proxy (`pkg/weatherwarnings`):** holt den DWD-GeoServer-**WFS** (`dwd:Warnungen_Gemeinden_vereinigt`, `outputFormat=application/json`, WGS84 — kein Achsenreihenfolge-Problem), **normalisiert** je Feature die CAP-Attribute case-insensitiv auf eine stabile Form (`wf_level` aus `SEVERITY`, plus `headline`/`event`/`expires`), verwirft Features mit ungültiger Geometrie, cacht das Last-Good und dient `GET /api/weather/warnings.geojson`. Periodischer Refresh (~5 min). Defensiv (NFR-SEC-005): Timeout, `io.LimitReader` (16 MiB), toleranter Decode, best-effort (Fehler → leere/Last-Good-Collection, HTTP 200, nie `/ready`-Blockade). **Gating (ADR 0017 / Häppchen 2):** **default-an** (öffentliche DWD-WFS-URL als Default), abschaltbar per `WAYFINDER_DWD_WARN_ENABLED=false`; `map-config.weather_warnings_available` deaktiviert sonst den Schalter; per-Tenant über Entitlement `weather_warnings`. Frontend: `fill`+`line`-Layer (Severity-Farbrampe), über dem Radar-Raster, unter Aeronautik/Tracks; 5-min-Refresh via `setData`. Attribution „© Deutscher Wetterdienst". Kein CAT062-Eingriff, Firefly-unabhängig. | Betreiber-Wunsch (Wetterwarnungen im ASD), ADR 0016 | **ADR 0016** | `pkg/weatherwarnings/` (`geojson.go` normalisierte FeatureCollection + `validGeometry`, `client.go` WFS-GetFeature-Client + `normaliseProps`/`severityLevel`, `service.go` Refresh-Loop + `atomic`-Cache + `Handler`), `cmd/wayfinder/main.go` (Config `WAYFINDER_DWD_WARN_*`, Service + `go Run`, Route hinter `tenantMW`, `map-config.weather_warnings_available`, Metriken `source="dwd_warnings"`), `pkg/feature/catalog.go` (`weather_warnings`), `frontend/src/map/constants.js`/`layers.js` (`addWeatherWarningsLayer`/`updateWeatherWarnings`, Severity-Farbrampe)/`engine.js` (Z-Order + `groups` + Fetch/Refresh + Verfügbarkeit), `frontend/src/stores/asd.js` (`weatherWarnings`/`weatherWarningsAvailable`), `frontend/src/components/LayerFilterContent.vue` (Toggle + Hinweis) | `pkg/weatherwarnings/weatherwarnings_test.go` (`severityLevel`, `normaliseProps` case-insensitiv, WFS-URL-Encoding, Fetch parst + verwirft ungültige Geometrie + re-serialisiert, Non-200-Fehler, leer-vor-erstem-Fetch, Refresh-Cache + Last-Good bei Fehler, disabled, CacheAge), `pkg/feature/catalog_test.go` (Key), `frontend/src/stores/__tests__/asd.test.js` (Default aus/Verfügbarkeit) |

| NFR-OPS-005 | **Connected-by-default-Posture (ADR 0017):** Wayfinder ist ein System zur **Informationsbereitstellung/Lagedarstellung**, **nicht** zur Steuerung von Flugbewegungen — es ist für den **vernetzten** Betrieb ausgelegt; ein abgeschottetes/offline Netz ist keine Design-Randbedingung. **Externe Kontext-Quellen** (Basiskarte, DWD-Wetter/Warnungen, NOAA-QNH, OpenAIP-Aeronautik) sind **default-an**, einzeln abschaltbar über explizite `WAYFINDER_..._ENABLED=false`-Schalter (bzw. self-hosted Kartenstil / kein OpenAIP-Schlüssel). **Abgrenzung:** der **CAT062/065/063-Multicast-Eingang** bleibt netz-isoliert (NFR-SEC-001, ADR 0003) — die Posture betrifft **ausschließlich ausgehende Kontext-Quellen**. **Schutz-Eigenschaften bleiben:** best-effort (nie `/ready`-Blockade, Last-Good/leer bei Ausfall), robuster/misstrauischer Decoder, Größenlimits. Die ausgehenden **Egress-Ziele** (`maps.dwd.de`, `aviationweather.gov`, `api.core.openaip.net`, Karten-Tile-CDNs; HTTPS/443) sind dokumentierte Betriebsvoraussetzung. Self-hosted Fonts/Glyphen (ADR 0015) bleiben — Begründung nun **Robustheit/keine Laufzeit-CDN-Abhängigkeit** statt Air-Gap. **Zwei Ebenen:** „Quelle default-an" ≠ „jeder Mandant sieht alles" — die Sichtbarkeit steuert weiter das Feature-Entitlement pro Mandant (default-deny). **Stand:** Fundament (ADR 0017 + Netzwerk-Anforderungen-Doku) ✅ Häppchen 1; **DWD-Radar + Warnungen default-an** (`WAYFINDER_DWD_RADAR_ENABLED`/`_WARN_ENABLED`, Default true) ✅ Häppchen 2; **QNH (NOAA) default-an + per-Tenant-Flugplatz** (`WAYFINDER_QNH_ENABLED`, Default true; `view_configs.qnh_icao`) ✅ Häppchen 3; OpenAIP (AERO) folgt. | CLAUDE.md §7 (Betreibbarkeit), Betreiber-Prämisse (2026-07-03), ADR 0017 | **ADR 0017** | `docs/decisions/0017-connected-by-default.md`, `docs/decisions/0004-*`/`0016-*` (Nachtrag-Verweise), `docs/INSTALLATION.md §8.0` (Egress-Ziele), `docs/BETRIEB.md §6.4` (Abgrenzung Feed-Isolation) | Doku/Entscheidung (kein Code-Verhalten geändert; `go build` grün); die per-Feature-Default-Flips werden mit ihren Häppchen getestet (2/3/AERO) |
| NFR-SEC-006 | **Admin ohne eigenes ASD + pfad-unabhängiges Passwort-Gate (ADR 0022, Issue #208):** (1) Ein Plattform-Admin hat **kein eigenes Lagebild**: der `/ws`-Scope-Resolver lehnt den Handshake eines Admins ohne **aktives** Read-Only-Impersonation-Grant (Gastmodus, ADR 0008) **fail-closed** ab (403 + Audit-Event `ws_admin_denied`) — auch bei abgelaufenem Grant und bei platform-weit deaktivierter Impersonation (kein `WAYFINDER_SESSION_KEY`); der frühere Fallback „leeres eigenes Bild" (TenantID 0) entfällt. Der einzige Lagebild-Zugang des Admins ist der Gastmodus über einen echten Mandanten (Augen-Icon, #209). (2) Solange `must_change_password` gesetzt ist (ONB-1-Seed), weisen **alle operativen Daten-Pfade** — `/ws`, `/api/airspace|navaids|waypoints`, `/api/weather/*`, `/api/airports.geojson`, `/api/runways.geojson` — mit `403 password_change_required` ab (`tenant.RequirePasswordChanged`), unabhängig davon, über welche URL sich der Principal anmeldet; erreichbar bleiben Login/Logout, `GET /api/whoami` (meldet den Flag), `POST /api/session/renew` und die adminapi-Allowlist. (3) Frontend-Führung: `/` leitet einen authentifizierten Admin (bzw. must-change-Principal) nach `/admin` um (`adminGate` hält den Spinner, bis entschieden ist — kein totes `/ws`); Gastmodus-„Beenden" und TTL-Ablauf führen nach `/admin` zurück; kein „Zur Lage"-Shortcut mehr. Altstand durch Migration 00007 bereinigt (admin XOR tenant, CHECK-Constraint). Kein CAT062-Einfluss. | Issue #208, CLAUDE.md §7 (Sicherheit), ADR 0011 (ONB-1) | **ADR 0022** (+ ADR 0008) | `pkg/tenant/authz.go` (`RequirePasswordChanged`), `cmd/wayfinder/main.go` (`newScopeResolver` Admin-Ablehnung + `pwGate`-Mounts), `frontend/src/views/AsdView.vue` (`adminGate`, TTL-Drop-Recheck), `frontend/src/stores/session.js` (`mustChangePassword`), `frontend/src/components/ImpersonationBar.vue` (`exitGuestMode` → `/admin`), `frontend/src/views/AdminView.vue` (Shortcut entfernt) | `pkg/tenant/authz_test.go::TestRequirePasswordChanged`, `cmd/wayfinder/scope_test.go` (`TestScopeResolverAdminWithoutGrantRejected`, `…ExpiredFallsBack` User-only, `…DisabledWithoutKey` Admin-Ablehnung), `frontend/src/views/__tests__/asdAdminGate.test.js` (6 Fälle) |

Das Register wird bei jedem Meilenstein ergänzt.
