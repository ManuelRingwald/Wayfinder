# Anforderungs-Register

Hier werden alle Anforderungen (funktional und nicht-funktional) dokumentiert und rückverfolgt zu:
- Design-Entscheidungen (ADRs)
- Code-Implementierung
- Test-Verifikation

| ID | Anforderung | Quelle | Design | Implementierung | Tests |
|----|-------------|--------|--------|-----------------|-------|
| FR-OPS-001 | Liveness-Probe (`/healthz`) signalisiert, dass der Prozess läuft | CLAUDE.md Abschnitt 8 (Cloud-nativ) | ADR0001 | `internal/server/server.go` | `internal/server/server_test.go` |
| FR-OPS-002 | Readiness-Probe (`/readyz`) signalisiert Betriebsbereitschaft (später: Firefly-Verbindung) | CLAUDE.md Abschnitt 8 (Cloud-nativ) | ADR0001 | `internal/server/server.go` | `internal/server/server_test.go` |
| FR-CFG-001 | Konfiguration (Multicast-Gruppe/-Port, Probe-/Map-/Sicherheits-Einstellungen, Log-Level via `WAYFINDER_LOG_LEVEL`, sowie Multi-Tenancy via `WAYFINDER_DB_URL`/`WAYFINDER_AUTH_MODE`/`WAYFINDER_OIDC_*`/`WAYFINDER_SESSION_*`) über Env-Vars mit dokumentierten Defaults (INSTALLATION §7, TECHNICAL §6) | CLAUDE.md Abschnitt 8 (12-Factor) | ADR0001, ADR 0006 | `cmd/wayfinder/main.go` (`Config`, `loadConfig`, `authConfig`) | `cmd/wayfinder/main_test.go` (`TestLoadConfig*`), `cmd/wayfinder/tenancy_test.go` (`TestLoadConfigTenancyEnvVars`) |
| FR-ADMIN-001 | **Tenant-skopiertes Admin-API (Wayfinder 2.0, WF2-31):** REST unter `/api/admin/*`, hinter `RequireRole(tenant_admin, super_admin)`; die Mandanten-ID kommt **immer aus der Identity** (Middleware), nie aus Pfad/Body → ein Admin kann nur die **eigene** Mandanten-Config berühren (Isolation per Konstruktion, NFR-SEC-003). Endpunkte: `GET/PUT /api/admin/view` (Tenant-Default-Sicht: Zentrum/Zoom/AOI/FL/Layer, **server-validiert**: Lat/Lon/Zoom-Bereiche, AOI wohlgeformt, `fl_min ≤ fl_max`), `GET /api/admin/subscriptions` (eigene abonnierte Feeds), `GET /api/admin/feeds` (Katalog, read-only), `GET /api/admin/whoami` (Rollen-Probe der SPA, in WF2-32 von `/admin` hierher verschoben). **WF2-31b — super_admin-Provisioning (cross-tenant):** `GET /api/admin/tenants`, `GET/POST /api/admin/tenants/{tenantID}/subscriptions`, `DELETE /api/admin/tenants/{tenantID}/subscriptions/{feedID}` — Ziel-`tenant_id` aus dem **Pfad**, Doppel-Gate (`RequireRole` + in-handler `requireSuper`, sonst `403`); super_admin ist die **einzige** cross-tenant-schreibende Rolle (Billing-Grenze). | Konzept §6, ROADMAP §1 (WF2-31) | ADR 0005/0006 | `pkg/adminapi/adminapi.go` (`Handler`, `validateView`, `requireSuper`, `TenantStore`, DTOs), `cmd/wayfinder/main.go` (`/api/admin/`-Wiring hinter `tenantMW`+`RequireRole`) | `pkg/adminapi/adminapi_test.go` (DB-frei: Tenant-Scoping aus Identity, Validierung, **Cross-Tenant-Negativtest `TestCrossTenantRoutesForbidTenantAdmin` = tenant_admin → 403**, Grant/Revoke-Targeting, 401/404/405) + `adminapi_integration_test.go::TestIntegrationAdminAPI` (real-PG: View-Round-Trip + super_admin grant→list→revoke) + `TestWhoamiReportsIdentity`/`TestWhoamiUnauthorizedWithoutIdentity` |
| FR-ADMIN-002 | **Admin-UI (Wayfinder 2.0, WF2-32):** Bedienbare Admin-Oberfläche unter der Browser-Route `/admin` (Vue-3-SPA, `vue-router` **History-Mode**), die das WF2-31/31b-API konsumiert. **Eigenständige View, kein Overlay:** Navigieren zu `/admin` unmountet die ASD-Karte vollständig (MapLibre + WebSocket + Timer frei; `MapCanvas.onUnmounted → mapEngine.destroy()`). **View-Editor** (Zentrum/Zoom/AOI/FL-Band/Layer) mit **Client-Validierungs-Parität** zur Server-`validateView` (`src/admin/validateView.js`) **vor** dem PUT; **Abos/Feeds** read-only; **super_admin-Provisioning** (Mandant wählen → grant/revoke) hinter `isSuperAdmin`-Gate. Rollen-Gating der UI ist **kosmetisch** — der Server erzwingt jede Grenze unabhängig (`requireSuper → 403`, Tenant-ID aus Identity); Client-Validierung ist UX, **nie** die Sicherheitsgrenze (Defense-in-Depth). **SPA-History-Fallback** im Backend (`internal/webui/webui.go`): unbekannte Pfade → `index.html` (Deep-Links überleben Reload), API-Surface über speziellere Mux-Pattern **nie** beschattet. **AP3 (ADR 0009) — mandantenzentrierter Neuschnitt:** die alten Top-Level-Tabs sind durch ein **Master/Detail** ersetzt — eine **Mandanten-Übersicht** (`GET /api/admin/overview`: Status, aktive Features, Feeds, Zugangs-Anzahl je Mandant in einem Aggregat-Call) und pro Mandant eine **zentrale Detail-Konfiguration** (Status, Sicht, Features, Feeds, Zugänge). Die Detailseite bearbeitet die Sicht **jedes** Mandanten über neue cross-tenant Routen `GET/PUT /api/admin/tenants/{id}/view` (gleiche Server-`validateView`). **Center+Radius (NM) ↔ AOI-Bbox** wird **clientseitig** umgerechnet (`src/admin/geo.js`: `radiusNmToBbox`/`bboxToRadius`) — das Backend bleibt AOI-basiert (WF2-21.2 unberührt). `AdminProvisioning`/`AdminUsers` nehmen einen optionalen `tenantId`-Prop und werden eingebettet. | Konzept §6, ROADMAP §1 (WF2-32), ADR 0009 (AP3), Projektverantwortlicher (History-Mode + voller Komponenten-Austausch + mandantenzentriert) | ADR 0001 (Vue/Vuetify), ADR 0009 | `frontend/src/router/index.js`, `frontend/src/App.vue` + `views/AsdView.vue` + `views/AdminView.vue` (Master/Detail), `frontend/src/stores/admin.js` (`loadOverview`/`loadTenantView`/`saveTenantView`/`loadTenantEntitlements`/`setTenantEntitlement`), `frontend/src/admin/geo.js` (AP3), `frontend/src/admin/validateView.js`, `frontend/src/components/admin/{AdminTenants,AdminTenantDetail,AdminProvisioning,AdminUsers}.vue`, `internal/webui/webui.go` (SPA-Fallback), `pkg/adminapi/adminapi.go` (`whoami`, `getOverview`, `getTenantView`/`putTenantView`) | `frontend/src/admin/__tests__/validateView.test.js` (Validierungs-Parität), `frontend/src/admin/__tests__/geo.test.js` (AP3 Center/Radius↔Bbox, Round-Trip, Pol-Clamp), `frontend/src/stores/__tests__/admin.test.js` (whoami/Rollen, `saveView`-DTO, grant/revoke, 403-Pfad; **AP3** overview/tenant-view/entitlement-Actions; gemocktes `fetch`), `pkg/adminapi/adminapi_test.go` (**AP3** overview-Aggregat, tenant-view get/put/404/invalid, cross-tenant 403), `pkg/adminapi/adminapi_integration_test.go` (**real-PG** AP3 overview + tenant-view Round-Trip), `internal/webui/webui_test.go` (SPA-Fallback `/admin`→Shell, echte Assets nicht beschattet, Root→`index.html`) |
| FR-ADMIN-003 | **Live-Apply von Sicht-/Abo-Änderungen (Wayfinder 2.0, WF2-33):** Ändert ein Admin die View (`PUT /api/admin/view`) oder die Feed-Grants (`POST`/`DELETE …/subscriptions`), werden die **aktiven** `/ws`-Streams des betroffenen Mandanten **live re-skopiert** — ohne Reconnect. **Thread-Safety per Konstruktion:** Der Broadcaster ist ein Single-Goroutine-Actor; der Scope-Tausch ist ein Kommando durch denselben `Run`-Loop (`rescopeChan`/`ApplyScopes`), in dem auch die Track-Auswertung läuft → **kein Lock am heißen Pfad, keine Race**, Run-Loop nie blockiert (gepuffert, ctx-begrenzt). **Zwei-Phasen:** Snapshot über immutable Client-Identität (`ClientsForTenant`, nie der mutable Scope) → Auflösung **off-Run** pro distinct User (`resolveScope`, identisch zum Connect-Pfad → respektiert User-Overrides) → Apply in `Run` (Disconnect-Race via `clients.Load`-Guard übersprungen). **Shrink:** kein Delete-Signal — der Server sendet außenliegende Tracks schlicht nicht mehr, das Frontend coastet sie über den Client-Timeout aus (keep it simple). Bei Validierungsfehler (`400`) wird **nicht** re-skopiert. Ergänzt die Durchsetzung von **NFR-SEC-003** (Isolations-Rand jetzt auch live). | ROADMAP §1 (WF2-33), Projektverantwortlicher (Thread-Safety + Shrink-Leitplanken) | ADR 0005 | `pkg/broadcast/broadcast.go` (`Scope.UserID`, immutable Client-Identity, `ClientsForTenant`/`ApplyScopes`/`applyScopes`/`rescopeChan` + Run-Case), `cmd/wayfinder/main.go` (`resolveScope`, `rescopeTenant`, Verdrahtung), `pkg/adminapi/adminapi.go` (`RescopeFunc` + `triggerRescope` auf `putView`/`grant`/`revoke`) | `pkg/broadcast/rescope_test.go` (`TestApplyScopesShrinkAOILive`, `…GrantAndRevokeFeedLive`, `…OnlyTargetClients`, `…SkipsUnknownClient`, `TestClientsForTenantSnapshot`, **`TestRescopeRaceUnderLoad` unter `-race`**), `cmd/wayfinder/rescope_test.go` (`TestResolveScopeBuildsScope`, `TestRescopeTenantAppliesLive` Ende-zu-Ende), `pkg/adminapi/adminapi_test.go` (`TestPutViewTriggersRescope`, `TestPutViewInvalidDoesNotRescope`, `TestGrantTriggersRescope`, `TestRevokeTriggersRescope`) |
| FR-ADMIN-004 | **Zugangs-Verwaltung (Wayfinder 2.0, AP6, ADR 0009):** Der `admin` verwaltet pro Mandant einen oder mehrere **Zugänge** (Login-Konten, Rolle `user`) und kann ganze Mandanten pausieren — ohne DB-Konsole. **Schema** (`00005_user_status.sql`): `users.status` (`active`/`paused`, CHECK, Default `active`, nicht-breaking); Mandanten-Pause nutzt das vorhandene `tenants.status`. **Store:** getypter `Status` (`StatusActive`/`StatusPaused`, `Valid()`-Guard, geteilt von `User`+`Tenant`); `UserRepo.SetStatus`/`Delete` (Credentials/View-Overrides via `ON DELETE CASCADE`), `TenantRepo.SetStatus`. **Login-Enforcement (fail-closed):** ein **pausierter Zugang** — oder ein Zugang unter einem **pausierten Mandanten** — kann sich **nicht** anmelden, auch mit korrektem Passwort; gleiches generisches 401 (keine paused/active-Enumeration, Timing-uniform); ein Tenant-Lookup-Fehler gilt als suspendiert. **API** (cross-tenant, hinter `requireAdmin`): `GET`/`POST /api/admin/tenants/{id}/users`, `PATCH`/`DELETE …/users/{uid}`, `PUT …/users/{uid}/password`, `PATCH /api/admin/tenants/{id}` (Mandant-Pause). Neue Konten sind **immer** Rolle `user`; Passwort min. 8 Zeichen; doppelter Subject → 409; User-ID aus fremdem Mandanten → 404 (Ressourcen-Hierarchie ehrlich). **Sofort-Wirkung** auf laufende Sessions ist **AP7** (Session-Registry); AP6 gated nur neue Logins. **Frontend:** `AdminUsers.vue`-Tab (admin-only) mit Mandanten-Wähler, Zugangs-Liste (Status-Chip), Anlegen/Pausieren/Passwort/Löschen-Dialogen + Mandant-Pause; Gating kosmetisch (Server erzwingt). | ADR 0009 (AP6), Issue #63 | **ADR 0009** | `pkg/store/migrations/00005_user_status.sql`, `pkg/store/models.go` (`Status`, `User.Status`/`Tenant.Status`), `pkg/store/users.go` (`SetStatus`/`Delete`), `pkg/store/tenants.go` (`SetStatus`), `pkg/tenant/login.go` (`TenantLookup`, Status-Cascade), `pkg/adminapi/adminapi_users.go` (`UserStore`/`CredentialStore`, CRUD-Handler, `setTenantStatus`), `frontend/src/stores/admin.js` (`loadTenantUsers`/`createUser`/`setUserStatus`/`deleteUser`/`setUserPassword`/`setTenantStatus`), `frontend/src/components/admin/AdminUsers.vue`, `frontend/src/views/AdminView.vue` (Tab) | `pkg/store/store_integration_test.go::TestIntegrationUserStatusLifecycle` (**real-PG**: Default-active, Pause/Reaktivieren, Tenant-Pause, Delete kaskadiert Credential, `ErrNotFound`), `pkg/store/repo_test.go::TestStatusValid`, `pkg/tenant/login_test.go::TestLoginEnforcesStatus` (paused-Account/paused-Tenant/Lookup-Fehler→401, nil-Tenants skip), `pkg/adminapi/adminapi_users_test.go` (CRUD, 400/404/409, **`TestAccessRoutesForbidNonAdmin` = user→403 + keine Mutation**, `TestUserCrossTenantMismatch`), `frontend/src/stores/__tests__/admin.test.js` (AP6-Actions, 409-Pfad) |
| FR-ADMIN-005 | **Zero-Touch-Onboarding: Auto-Admin + Konto-Selbstverwaltung (ONB-1, ADR 0011):** Eine frische Instanz ist **ohne Terminal-Schritt** benutzbar. **Boot-Auto-Seed:** In `builtin`-Modus mit DB legt Wayfinder beim Start einen Standard-Mandanten (`default`) + Standard-Admin (`admin`/`admin`) an — **nur** wenn kein aktiver Admin existiert (`CountActiveAdmins == 0`), idempotent, `runBootstrap` wiederverwendet. Der Seed-Admin trägt `must_change_password=true`. **Schema** (`00006_user_must_change_password.sql`): additive Spalte `users.must_change_password` (Default `false`, nicht-breaking). **Pflichtwechsel-Gate (fail-closed):** Trägt das eingeloggte Konto das Flag, weist das Admin-API **jede** Route außer `GET /api/admin/whoami`, `GET /api/admin/me`, `PUT /api/admin/me/password` mit **403 `password_change_required`** ab; das Flag wird vom `tenant.Identity` getragen (kein DB-Lookup), das SPA zeigt die Pflichtwechsel-Maske. **Selbstverwaltung** (`/api/admin/me/*`, rollen-unabhängig): `GET` (eigenes Konto), `PUT …/password` (aktuelles Passwort prüfen → 401 bei falsch; setzt Flag zurück), `DELETE` (eigenes Konto, **„letzter aktiver Admin"-Guard** → 409). **Session-Key-Komfort:** builtin ohne `WAYFINDER_SESSION_KEY` → flüchtiger Zufalls-Schlüssel + Warnung (fester Schlüssel für Produktion). **Deployment:** `docker-compose.onboarding.yml` (Postgres + Wayfinder builtin) → `docker compose up` genügt. | ADR 0011 (ONB-0/1), Betreiber-Wunsch „nur Container starten" | **ADR 0011** | `pkg/store/migrations/00006_user_must_change_password.sql`, `pkg/store/models.go` (`User.MustChangePassword`), `pkg/store/users.go` (`SetMustChangePassword`, `CountActiveAdmins`, `userColumns`/`scanUser`), `pkg/tenant/tenant.go` (`Identity.MustChangePassword`, Middleware), `cmd/wayfinder/seed.go` (`autoSeedDefaultAdmin`), `cmd/wayfinder/main.go` (Seed-Aufruf in `setupTenancy`, Session-Key-Auto-Gen), `pkg/adminapi/adminapi.go` (`ServeHTTP`-Gate, `passwordChangeAllowlist`, whoami-Flag, `/me`-Routen), `pkg/adminapi/adminapi_me.go` (`getMe`/`putMePassword`/`deleteMe`), `pkg/adminapi/adminapi_users.go` (`UserStore`/`CredentialStore` erweitert), `frontend/src/stores/admin.js` (`mustChangePassword`, `changeOwnPassword`), `frontend/src/views/AdminView.vue` (Pflichtwechsel-Maske) | `pkg/store/users_onboarding_integration_test.go::TestIntegrationMustChangePasswordAndAdminCount` (**real-PG**: Default-false, Toggle, `CountActiveAdmins` zählt nur aktive Admins), `cmd/wayfinder/seed_integration_test.go::TestIntegrationAutoSeed` (**real-PG**: Erst-Seed setzt Flag+Default-PW, Re-Seed no-op, rotiertes PW/Flag nicht überschrieben), `pkg/adminapi/adminapi_me_test.go` (Gate blockt/erlaubt, `getMe`-Flag, `putMePassword` Erfolg/401/zu-kurz, `deleteMe` Last-Admin-409/Erfolg), `frontend/src/stores/__tests__/admin.test.js` (ONB-1: `mustChangePassword`, `changeOwnPassword` PUT+Reload+401) |
| FR-ADMIN-006 | **Strikte Admin/Nutzer-Trennung + Plattform-Admin-Verwaltung (ONB-3, ADR 0011):** Plattform-Admins und Mandanten-Nutzer sind sauber getrennt. Ein **Admin ist global** (kein Mandant), ein **Nutzer gehört genau einem Mandanten**. **Schema** (`00007_admin_tenant_nullable.sql`): `users.tenant_id` nullable, bestehende Admins entkoppelt, **CHECK-Constraint** `admin XOR tenant` (admin ⇒ `tenant_id IS NULL`, user ⇒ `NOT NULL`). **Store:** getrennte Konstruktoren `UserRepo.Create` (Nutzer) / `CreateAdmin` (Admin, tenant-los), `ListAdmins`; `scanUser` liest NULL → `TenantID 0`. **Bootstrap/Seed:** `runBootstrap` verzweigt nach Rolle (Admin ohne `-tenant`); Auto-Seed legt tenant-losen Admin + Komfort-Mandant `default` an. **Login:** Mandanten-Pause-Kaskade wird für tenant-lose Admins übersprungen (keine Selbst-Aussperrung). **API (global, hinter `requireAdmin`):** `GET/POST /api/admin/admins`, `PATCH/DELETE /api/admin/admins/{id}`, `PUT …/{id}/password`. Die per-Mandant-Route `POST /api/admin/tenants/{id}/users` lehnt `role:"admin"` mit **400** ab. **„Letzter aktiver Admin"-Guard** (`wouldOrphanAdmins` → `CountActiveAdmins`): Pausieren/Löschen des letzten aktiven Admins → **409**. **Frontend:** dedizierte „Plattform-Administratoren"-Verwaltung (`AdminPlatformAdmins.vue`) mit eigener Header-Navigation; Store-Actions `loadAdmins`/`createAdmin`/`setAdminStatus`/`deleteAdmin`/`setAdminPassword` (409-Guard in verständliche Meldung übersetzt). | ADR 0011 (ONB-3), Betreiber-Wunsch „saubere Welten-Trennung" | **ADR 0011** | `pkg/store/migrations/00007_admin_tenant_nullable.sql`, `pkg/store/models.go` (`User.TenantID` 0=tenant-los), `pkg/store/users.go` (`Create`/`CreateAdmin`/`ListAdmins`/`scanUser`), `pkg/tenant/login.go` (Cascade-Skip für `TenantID==0`), `pkg/adminapi/adminapi_admins.go` (CRUD + `wouldOrphanAdmins`/`adminByID`), `pkg/adminapi/adminapi_users.go` (`UserStore` erweitert, `createUser` lehnt admin-Rolle ab), `pkg/adminapi/adminapi.go` (`/admins`-Routen), `cmd/wayfinder/bootstrap.go` (`provisionAccount`, rollen-abhängig), `cmd/wayfinder/seed.go` (tenant-loser Admin + Default-Mandant), `frontend/src/stores/admin.js` (Admin-Actions), `frontend/src/components/admin/AdminPlatformAdmins.vue`, `frontend/src/views/AdminView.vue` (Header-Navigation) | `pkg/store/store_integration_test.go::TestIntegrationAdminTenantSeparation` (**real-PG**: tenant-loser Admin, `ListAdmins`, CHECK lehnt beide Halb-Zustände ab), `cmd/wayfinder/bootstrap_integration_test.go::TestIntegrationBootstrap` (**real-PG**: Admin-Welt tenant-los + keine Tenant-Erzeugung, Nutzer-Welt mit Mandant, Welten-Konflikt), `cmd/wayfinder/seed_integration_test.go::TestIntegrationAutoSeed`, `pkg/adminapi/adminapi_admins_test.go` (CRUD, Last-Admin-Guard 409, Nicht-Admin-ID → 404, requireAdmin → 403), `pkg/adminapi/adminapi_users_test.go::TestCreateUserRejectsAdminRole`, `pkg/tenant/login_test.go` (tenant-loser Admin nicht ausgesperrt), `frontend/src/stores/__tests__/admin.test.js` (ONB-3-Block) |
| FR-ADMIN-007 | **Mandanten-Lebenszyklus über die Oberfläche (ONB-4, ADR 0011):** Der `admin` legt Mandanten **anlegen/löschen** ohne Terminal — der letzte rein-CLI-Provisionierungsschritt entfällt. **Store:** `TenantRepo.Delete` (einzeiliges `DELETE`, atomar via `ON DELETE CASCADE` auf Zugänge/Credentials/Abos/Entitlements/View-Konfig); `ErrNotFound` bei fehlender Zeile. **API (hinter `requireAdmin`):** `POST /api/admin/tenants` `{slug, name?}` → 201 (Slug DNS-label-artig validiert, `name` Default = `slug`, doppelter Slug → 409 via `GetBySlug`-Pre-Check); `DELETE /api/admin/tenants/{id}` → 204. **Guard B (Betreiber-Entscheidung):** Ein Mandant mit noch vorhandenen **Zugängen** wird **fail-closed mit 409** abgelehnt — der kaskadierende Löschvorgang ist ein bewusster Zweischritt (erst Konten entfernen), kein versehentlicher Ein-Klick-Verlust von Lotsen-Logins. Der frühere ADR-Guard „Mandant mit aktiven Admins" ist nach ONB-3 gegenstandslos (Admins sind tenantlos). **Frontend:** „Mandant anlegen"-Dialog in der Übersicht, „Mandant löschen" (mit Bestätigung) auf der Detailseite; Store-Actions `createTenant`/`deleteTenant` (409-Guard verständlich übersetzt). | ADR 0011 (ONB-4), Betreiber-Wunsch „nur Container starten" + Guard B | **ADR 0011** | `pkg/store/tenants.go` (`Delete`), `pkg/adminapi/adminapi_tenants.go` (`createTenant`/`deleteTenant`, Slug-Validierung, Guard B), `pkg/adminapi/adminapi.go` (`TenantStore` um `Create`/`Delete`/`GetBySlug` erweitert, Routen), `frontend/src/stores/admin.js` (`createTenant`/`deleteTenant`), `frontend/src/components/admin/AdminTenants.vue` (Anlegen), `frontend/src/components/admin/AdminTenantDetail.vue` (Löschen) | `pkg/store/store_integration_test.go::TestIntegrationTenantDeleteCascades` (**real-PG**: Cascade auf User/Credential/Abo, Feed überlebt, Re-Delete → ErrNotFound), `pkg/adminapi/adminapi_users_test.go` (`TestCreateTenant`/`…DefaultsNameToSlug`/`…Validation`/`…DuplicateSlug`, `TestDeleteTenantEmptySucceeds`/`…WithAccountsRefused`/`…UnknownIs404`, `TestTenantLifecycleRoutesForbidNonAdmin`), `frontend/src/stores/__tests__/admin.test.js` (ONB-4-Block) |
| FR-ADMIN-008 | **Feed-Lebenszyklus + Live-Receiver-Join/-Leave (ONB-5, ADR 0011):** Der `admin` legt Daten-Feeds **anlegen/löschen** ohne Terminal und ohne Neustart — der Server tritt der Multicast-Gruppe eines neuen Feeds **sofort** bei und verlässt die Gruppe eines gelöschten Feeds **sofort**. **Feed-Manager (`pkg/feedmanager`):** ein Supervisor hält je Feed-ID eine laufende Receiver-Goroutine (mutex-geschützte Map `feedID→cancel`), `Start`/`Stop`/`StopAll`/`Running`; die `Factory` ist injiziert → UDP-frei unit-testbar. **Prompte Beendigung:** `receiver.Run` setzt bei ctx-Cancel eine vergangene Read-Deadline (Watchdog), sodass ein blockiertes `ReadFromUDP` **sofort** zurückkehrt und die IGMP-Mitgliedschaft auch bei totem Feed prompt freigegeben wird. **Store:** `FeedRepo.Delete` (atomar, `ON DELETE CASCADE` auf Abos), `GetByName` (Dup-Pre-Check); Migration `00008_feeds_name_unique.sql` (UNIQUE `name`). **API (hinter `requireAdmin`):** `POST /api/admin/feeds` `{name, multicast_group, port, region?, sensor_mix?}` → 201 (Multicast-IPv4 + Port 1..65535 validiert, `sensor_mix` gegen Vokabular, doppelter Name → 409); **Create ist atomar über Katalog + Live-Join** — scheitert der Beitritt, wird die Katalogzeile zurückgerollt. `DELETE /api/admin/feeds/{id}` → 204 (Receiver verlässt Gruppe, dann Zeile; **Guard C**: kein Blockieren bei Abos — Grants kaskadieren). **Decode-Error-Metrik** ist prozessweit-monoton über Feed-Churn (Hook `OnDecodeError`, nicht Summe über die lebende Receiver-Menge). **Health-Registry:** `Forget(feedID)` beim Löschen, damit kein Phantom-Feed (ewig stale) im Dashboard bleibt; der Staleness-Monitor iteriert die **lebende** Feed-Menge (`feedManager.Running`). **Entkopplung:** `adminapi.FeedLifecycle` ist eine Primitiv-Param-Schnittstelle (kein Import der Transport-Schicht); main.go verdrahtet einen Adapter über Feed-Manager + Health-Registry. **Resilienz:** ein einzelner Receiver-Fehler beendet nicht mehr den ganzen Server (Manager vergisst den Feed, loggt). **Frontend:** „Feeds"-Eintrag in der Admin-Header-Navigation öffnet `AdminFeeds.vue` (Katalog-Tabelle mit `multicast_group:port`, Sensor-Mix, Gesundheits-Chip; Anlegen-Dialog + Lösch-Bestätigung); Store-Actions `createFeed`/`deleteFeed` (409-Duplikatname verständlich übersetzt). | ADR 0011 (ONB-5), Betreiber-Wunsch „nur Container starten" | **ADR 0011** | `pkg/feedmanager/feedmanager.go` (`Manager`/`Start`/`Stop`/`StopAll`/`Running`/`Factory`), `pkg/receiver/receiver.go` (Cancel-Watchdog in `Run`, `OnDecodeError`), `pkg/store/feeds.go` (`Delete`/`GetByName`), `pkg/store/migrations/00008_feeds_name_unique.sql`, `pkg/health/registry.go` (`Forget`), `pkg/adminapi/adminapi_feeds.go` (`createFeed`/`deleteFeed`/`validateMulticast`), `pkg/adminapi/adminapi.go` (`FeedStore`+`Create`/`Delete`/`GetByName`, `FeedLifecycle`, Routen, `feedDTO`+Multicast/Port), `cmd/wayfinder/feeds.go` (`newReceiverFactory`, `feedLifecycle`-Adapter), `cmd/wayfinder/main.go` (Feed-Manager-Wiring, churn-stabiler Decode-Counter, Monitor über lebende Menge), `frontend/src/stores/admin.js` (`createFeed`/`deleteFeed`), `frontend/src/components/admin/AdminFeeds.vue`, `frontend/src/views/AdminView.vue` (Feeds-Navigation) | `pkg/feedmanager/feedmanager_test.go` (UDP-freie Fake-Receiver: Start/Stop/Idempotenz/Listen-Fehler/Self-Error-Forget/StopAll/Base-Ctx, **`-race`**), `pkg/receiver/receiver_test.go` (Cancel-Pfad), `pkg/store/store_integration_test.go::TestIntegrationFeedDeleteCascades`/`TestIntegrationFeedNameUnique` (**real-PG**), `pkg/adminapi/adminapi_feeds_test.go` (`TestCreateFeed`/`…Validation`/`…DuplicateName`/`…InvalidSensorMix`/`…RollsBackOnJoinFailure`, `TestDeleteFeedSucceeds`/`…UnknownIs404`, `TestFeedLifecycleRoutesForbidNonAdmin`, `…WithoutLifecycle`), `cmd/wayfinder/feeds_test.go::TestNewReceiverFactory`, `frontend/src/stores/__tests__/admin.test.js` (ONB-5-Block) |
| FR-ADMIN-009 | **OpenAIP pro Mandant (ONB-6, ADR 0011 F3):** Jeder Mandant nutzt einen **eigenen optionalen OpenAIP-API-Schlüssel** und ruft Luftraum-/Navaid-/Wegpunkt-Daten gegen **seine eigene Area of Interest** (aus der Mandanten-View-Konfig: AOI-Box, sonst Box um das View-Zentrum) ab — statt eines prozess-globalen Schlüssels und einer global gecachten Region (Mandanten-Isolations-Leck). Mandanten **ohne** eigenen Schlüssel fallen auf den globalen `WAYFINDER_OPENAIP_API_KEY` zurück (abwärtskompatibel). **Schema:** Migration `00009_tenant_openaip_key.sql` (`tenants.openaip_api_key TEXT` nullable). **Store:** `TenantRepo.GetOpenAIPKey`/`SetOpenAIPKey` (Schlüssel isoliert gelesen, **nie** Teil der allgemeinen `Tenant`-Zeile/DTO). **Registry (`pkg/aeronautical`):** ein Supervisor hält je `tenantID` einen laufenden `Service` (eigener Client/Schlüssel + eigene BBox), `Start` (idempotent auf unveränderte Schlüssel+BBox)/`Stop`/`StopAll`/`Serve` (Fallback auf den globalen Cache, wenn kein eigener läuft); injizierte `ClientFactory` → transport-entkoppelt. **Endpunkte mandanten-aufgelöst:** `/api/airspace`, `/api/navaids`, `/api/waypoints` liegen im Multi-Mandanten-Betrieb **hinter der Tenant-Middleware** und liefern den Cache **des Mandanten des Requests** (kein Identity → leere Collection, graceful); Single-Tenant unverändert (globaler Cache, unauthentifiziert). **API (hinter `requireAdmin`):** `GET /api/admin/tenants/{id}/openaip` → `{configured: bool}` (**nie der Schlüssel selbst**); `PUT …/openaip` `{api_key: string|null}` → 204 (leer/Whitespace/`null` = löschen → globaler Fallback), **Live-Apply** (re)startet die Mandanten-Refresh sofort. **Live-AOI:** eine View-Änderung (`putView`/`putTenantView`) löst zusätzlich `triggerAeroApply` aus → Re-Fetch gegen die neue AOI; **Mandant-Löschen** (ONB-4) ruft `triggerAeroStop`. **Metriken:** `wayfinder_openaip_fetch_*` summieren über die globale + alle Mandanten-Instanzen (monoton über Mandanten-Churn). **Alle ADR-0004-Schutz-Eigenschaften bleiben** (Schlüssel server-seitig, best-effort/Last-Good-Cache, nicht-blockierender Start, Größengrenzen, „kein Schlüssel ⇒ Feature still aus" — jetzt pro Mandant). **Frontend:** Abschnitt „OpenAIP-Konfiguration" in `AdminTenantDetail.vue` (Status gesetzt/nicht gesetzt, Schlüssel nie angezeigt; Setzen/Löschen); Store-Action `setTenantOpenAIPKey`. | ADR 0011 (ONB-6 / F3), ADR 0004 (OpenAIP-Datenquelle), Mandanten-Isolation | **ADR 0011 / ADR 0004** | `pkg/store/migrations/00009_tenant_openaip_key.sql`, `pkg/store/tenants.go` (`GetOpenAIPKey`/`SetOpenAIPKey`), `pkg/aeronautical/registry.go` (`Registry`/`Start`/`Stop`/`Serve`/`Register`/`ClientFactory`/`FetchSuccessCount`), `pkg/aeronautical/service.go` (`Serve`), `pkg/adminapi/adminapi_openaip.go` (`getTenantOpenAIP`/`setTenantOpenAIP`), `pkg/adminapi/adminapi.go` (`TenantStore`+`Get`/`SetOpenAIPKey`, `TenantAeroLifecycle`, Routen, View-Apply), `pkg/adminapi/adminapi_tenants.go` (`deleteTenant` → `triggerAeroStop`), `cmd/wayfinder/aero.go` (`newAeroClientFactory`, `tenantAeroLifecycle`-Adapter, `aeroBBoxFromView`), `cmd/wayfinder/main.go` (Registry-Wiring, Boot-Apply, tenant-aware Endpunkt-Registrierung, summierte Metrik), `frontend/src/stores/admin.js` (`setTenantOpenAIPKey`), `frontend/src/components/admin/AdminTenantDetail.vue` (OpenAIP-Abschnitt) | `pkg/store/store_integration_test.go::TestIntegrationTenantOpenAIPKey` (**real-PG**: Get/Set/Clear-Roundtrip, nil-Default, ErrNotFound), `pkg/aeronautical/registry_test.go` (Per-Tenant-Cache, Empty-Key-Fallback, Idempotenz, Stop-Fallback, Count-Aggregation, Handler-Auflösung/`-race`), `pkg/adminapi/adminapi_openaip_test.go` (`TestGetTenantOpenAIPReportsConfigured`/`…NeverLeaksKey`, `TestSetTenantOpenAIPSetsKeyAndApplies`/`…NullClearsKey`/`…UnknownTenantIs404`/`…InvalidBodyIs400`, `TestOpenAIPRoutesForbidNonAdmin`, `TestDeleteTenantStopsAero`), `frontend/src/stores/__tests__/admin.test.js` (ONB-6-Block) |
| FR-CFG-002 | Ungültige Konfigurationswerte (u. a. ungültiges `WAYFINDER_LOG_LEVEL`) fallen auf Defaults zurück statt abzustürzen | CLAUDE.md Abschnitt 8 (Cloud-nativ) | ADR0001 | `cmd/wayfinder/main.go` (`loadConfig`, `parseLogLevel`) | `cmd/wayfinder/main_test.go` (`TestLoadConfigInvalidLogLevelFallsBackToDefault`, `TestLoadConfig*SecurityEnvVars*`) |
| FR-OPS-003 | Sauberes Herunterfahren (Graceful Shutdown) auf SIGINT/SIGTERM | CLAUDE.md Abschnitt 8 (Cloud-nativ) | ADR0001 | `cmd/wayfinder/main.go` | manuell verifiziert (M1 Schritt 1) |
| FR-DATA-001 | Track-Datenmodell (`Frame`/`FrameTrack`/`FramePlot`) bildet Firefly's `/ws`-Wireformat verlustfrei ab (Feldnamen, Typen, leere Arrays) | M1 Spezifikation, Firefly `crates/firefly-io/src/frame.rs` | ADR0001 | `internal/firefly/frame.go` | `internal/firefly/frame_test.go` |
| FR-DATA-002 | CAT062-Decoder dekodiert I062/245 (Target Identification / Callsign, FRN 10, ICD 2.1.0) — 8 Zeichen als 6-Bit-IA-5-Codes, MSB-first, fremde/ungültige Codes defensiv auf Leerzeichen abgebildet (robuster Decoder, CLAUDE.md Abschnitt 7). Der `Broadcaster` reicht das Callsign als `callsign` ans Frontend durch; `buildLabel` zeigt es als primäre Label-Zeile (Track-Nummer als Fallback). | Fireflys ICD-CAT062.md v2.1.0 (AP7), CLAUDE.md Abschnitt 2 | — | `pkg/cat062/decoder.go` (`decodeTargetIdentification`, `ia5Decode`), `pkg/broadcast/broadcast.go`, `internal/webui/static/app.js` (`buildLabel`) | `pkg/cat062/decoder_test.go::TestDecodeCallsign` |
| FR-DATA-003 | CAT062-Decoder dekodiert das TSE-Bit (Track Service End, I062/080 Oktett 2 Bit 7, ICD 2.2.0): `decodeTrackStatus` liest die FX-Kette oktett-genau (CNF Oktett 1, TSE Oktett 2, CST Oktett 4) und füllt `TrackStatus.Ended`. Der `Broadcaster` reicht es als `ended` (omitempty) durch; das Frontend (`updateTracksLayer`) **filtert** Tracks mit `ended` heraus → Symbol, Label, Vektor und Trail verschwinden sofort, statt auf einen Timeout zu warten. Robuster Decoder: kürzer endende Records werden längen-geschützt gelesen (CLAUDE.md Abschnitt 7). | Fireflys ICD-CAT062.md v2.2.0 / ADR 0016, CLAUDE.md Abschnitt 2 | — | `pkg/cat062/decoder.go` (`decodeTrackStatus`), `pkg/cat062/types.go` (`TrackStatus.Ended`), `pkg/broadcast/broadcast.go`, `internal/webui/static/app.js` (`updateTracksLayer`) | `pkg/cat062/decoder_test.go::TestDecodeTrackEnd` |
| FR-DATA-004 | CAT065-Decoder (Firefly ADR 0018, ICD 2.3.0 §8): `pkg/cat065.DecodeStatusBlock` dekodiert ASTERIX-CAT065-SDPS-Service-Status-Meldungen (Heartbeat) — `[CAT=0x41][LEN][Record]` mit FSPEC, Items I065/010 (SAC/SIC), I065/000 (Message Type), I065/015 (Service-ID), I065/030 (Time of Day, 1/128 s) und I065/040 (NOGO operationell/degradiert). Robuster Decoder: längen-geprüft, kein Panic, fremde/zusätzliche Items (I065/020, I065/050) werden toleriert, falsche Kategorie/abgeschnittene Eingabe werden verworfen. Byte-genau gegen Fireflys Referenz-Dump verifiziert. Der `receiver` dispatcht den gemeinsamen Multicast-Strom am führenden CAT-Oktett (`0x3E` → Track, `0x41` → Status, sonst Decode-Fehler). | CLAUDE.md Abschnitt 2/7, Fireflys ICD-CAT062.md v2.3.0 / ADR 0018 | — | `pkg/cat065/decoder.go`, `pkg/receiver/receiver.go` (`dispatch`, `handleStatus`) | `pkg/cat065/decoder_test.go` (`TestDecodeReferenceHeartbeat`, `TestDecodeTimeOfDay`, `TestDecodeDegradedStatus`, `TestDecodeRejectsWrongCategory`, `TestDecodeRejectsTruncatedInput`), `pkg/receiver/receiver_test.go::TestDispatchRoutesByCategory` |
| FR-DATA-005 | CAT062-Decoder dekodiert das ES-Age-Subfeld von I062/290 (Extended Squitter / ADS-B-Alter, primäres Subfeld-Bit `0x08`, ICD 2.4.0): die Subfeld-Schleife läuft die gesetzten Bits MSB→LSB ab, liest je gesetztem Bit ein Alter-Byte (LSB 1/4 s) und greift PSR (`0x40`) und ES (`0x08`) positionsbasiert heraus — so bleibt der Decoder korrekt, falls Firefly je ein Subfeld dazwischen einfügt (toleranter Decoder, CLAUDE.md Abschnitt 2/7). `UpdateAge.ESAge *float64` ist nur bei ADS-B-Anteil gesetzt (Radar-only → nil). Der `Broadcaster` reicht es als `adsb_age_s` (omitempty) ans Frontend durch. Byte-genau gegen Fireflys Encoder-Test `single_track_with_adsb_hit_matches_reference_dump` verifiziert; additiv (FSPEC unverändert, kein Wire-Bruch). | Fireflys ICD-CAT062.md v2.4.0 / ADR 0019, CLAUDE.md Abschnitt 2 | — | `pkg/cat062/decoder.go` (case 14), `pkg/cat062/types.go` (`UpdateAge.ESAge`), `pkg/broadcast/broadcast.go` (`TrackMessage.AdsbAgeS`) | `pkg/cat062/decoder_test.go` (`TestDecodeAdsbAge`, `TestDecodeNoAdsbAge`), `pkg/broadcast/broadcast_test.go::TestTracksToMessageMapsAdsbAge` |
| FR-DATA-006 | CAT063-Decoder und Sensor-Status-Verdrahtung (Firefly ADR 0022, ICD 2.5.0): `pkg/cat063.DecodeSensorBlock` dekodiert ASTERIX-CAT063-Sensor-Status-Meldungen (`[CAT=0x3F][LEN][Record]…`) — FSPEC `0xE0`, Items I063/010 (SAC/SIC, 2 B), I063/030 (Time of Day, 3 B, 1/128 s), I063/060 (NOGO, 1 B; Bits `0xC0`: `0x00`=operationell, `0x40`=degradiert, `0x80`=nicht verbunden, `0xC0`=nicht initialisiert). Robuster Decoder: längen-geprüft, kein Panic, falsche Kategorie/abgeschnittene Eingabe verworfen. Byte-genau gegen 9 Referenz-Vektoren aus Fireflys ICD §9 verifiziert. Der `receiver` dispatcht `0x3F` → `handleSensorStatus` → `sensorStatusHandler(feedID, statuses)`. `pkg/health.Registry.RecordSensors(feedID, active, total)` füllt `FeedSnapshot.SensorsActive`/`.SensorsTotal`, womit `FeedSnapshot.Color()` → `"yellow"` zurückgibt, sobald `0 < active < total`. Der Broadcast-Pfad liefert pro Feed eine `FeedStatusMessage{FeedID, Color, SensorsActive, SensorsTotal}` an Clients, die genau diesen Feed abonniert haben (per-Feed-Scoping, Multi-Tenant-tauglich). Das Frontend zeigt gelb mit Sensor-Zähler. Kein neuer Env-Var. | CLAUDE.md Abschnitt 2/7, Fireflys ICD-CAT062.md v2.5.0 / ADR 0022, Wayfinder Issue #72, Wayfinder ADR 0010 | ADR 0010 | `pkg/cat063/decoder.go` (`DecodeSensorBlock`, `decodeRecord`, `fspecHas`), `pkg/health/registry.go` (`RecordSensors`, `feedEntry.sensorsActive/Total`), `pkg/receiver/receiver.go` (`handleSensorStatus`, `sensorStatusHandler`, dispatch `case cat063.Category`), `cmd/wayfinder/feeds.go` (`buildReceivers` 5. Argument), `cmd/wayfinder/main.go` (`sensorStatusHandler`, `broadcastFeedSnapshot`), `pkg/broadcast/broadcast.go` (`FeedStatusMessage.FeedID/SensorsActive/SensorsTotal`, FeedID-Scoping), `internal/webui/static/index.html` + `app.js` (gelbes Banner) | `pkg/cat063/decoder_test.go` (9 Tests: Single/Two-Sensors/TimeOfDay/Degraded/NotConnected/EmptyBlock/WrongCategory/Truncated/LENExceeds), `cmd/wayfinder/feeds_test.go` (5-Argument-Interface) |
| FR-OPS-004 | Feed-Health/Staleness-Erkennung (Firefly ADR 0018): Wayfinder verfolgt die Ankunft der CAT065-Heartbeats und erkennt Staleness (kein CAT065-Heartbeat seit > `WAYFINDER_FEED_STALE_TIMEOUT`, Default 3 s) und unterscheidet ihn von „leerem Himmel" (Feed lebt, aber kein Verkehr). Zustandswechsel werden als `feed_status`-WS-Nachricht an den Browser gepusht; das Frontend zeigt ein Banner. **AP4-Erweiterung:** `pkg/health.Registry` tracked Health **pro Feed** (statt global) + letzten Block-Größe + `SensorsActive`/`SensorsTotal` (für CAT063-Sensor-Status); `GET /api/admin/feeds/health` (admin-only) liefert `{feed_id, color, stale, ever_seen, last_heartbeat_ago_s, track_count_recent, sensors_active, sensors_total}` für alle Feeds; das Admin-Dashboard zeigt Ampel-Chips pro Feed: **grün** = Heartbeat frisch (auch leerer Himmel), **gelb** = Sensor-Teilausfall (CAT063, ADR 0010 / FR-DATA-006), **rot** = kein Heartbeat. | CLAUDE.md Abschnitt 7 (Cloud-nativ/Observability), Fireflys ADR 0018, Issue #67 | — | `pkg/health/health.go` (FeedHealth + LastHeartbeat), `pkg/health/registry.go` (Registry, FeedSnapshot), `cmd/wayfinder/main.go` (feedRegistry, trackHandler, statusHandler, startProbeServer), `cmd/wayfinder/feeds.go` (buildReceivers per-feed closure), `pkg/adminapi/adminapi.go` (FeedHealthSource, getFeedsHealth), `frontend/src/stores/admin.js` (feedsHealth, loadFeedsHealth), `frontend/src/components/admin/AdminTenants.vue`, `frontend/src/components/admin/AdminTenantDetail.vue` | `pkg/health/health_test.go`, `pkg/health/registry_test.go` (9 Tests), `pkg/adminapi/adminapi_test.go` AP4-Block (4 Tests), `frontend/src/stores/__tests__/admin.test.js` AP4-Block (4 Tests) |
| NFR-OBS-003 | `/metrics` exponiert zusätzlich die Feed-Health (Firefly ADR 0018): `wayfinder_cat065_heartbeats_received_total` (Counter) und `wayfinder_feed_stale` (Gauge, 1 wenn stale). | CLAUDE.md Abschnitt 7 (Observability), `docs/ROADMAP.md` Paket #3 | — | `cmd/wayfinder/main.go` (`startProbeServer` `/metrics`), `pkg/metrics/metrics.go` | manuell verifiziert (Endpoint-Komposition); Bausteine durch `pkg/metrics/metrics_test.go` und `pkg/health/health_test.go` abgedeckt |
| NFR-SEC-001 | **Empfangspfad:** Die CAT062-Multicast-Vertrauensgrenze liegt auf der **Netzwerk-Schicht** (dediziertes, abgeschottetes Segment/VLAN; nur Firefly-Sender + autorisierte ASD-Empfänger), kein anwendungsseitiges Krypto auf CAT062 — Spiegel zu Fireflys ADR 0017; der robuste Decoder bleibt die App-Schutzschicht gegen fehlerhafte Datagramme. **Browser-Rand:** TLS + Authentifizierung primär am vorgelagerten Reverse-Proxy/Ingress; ergänzend fail-closed in Wayfinder: strikter konfigurierbarer Origin-Check auf `/ws` (`WAYFINDER_ALLOWED_ORIGINS`, same-origin immer erlaubt, sonst Allowlist), optionale Token-Middleware (`WAYFINDER_AUTH_TOKEN`, Bearer-Header oder `?token=`, Default-aus mit Warn-Log), optionales TLS (`WAYFINDER_TLS_CERT`/`_KEY`); Health-/Readiness-Probes (`:8080`) bleiben bewusst unauthentifiziert. | CLAUDE.md Abschnitt 7 (Sicherheit), Fireflys ADR 0017, ehem. Issue #7 | ADR 0003 | Empfangspfad: Deployment-Sache (dokumentiert); Browser-Rand: `pkg/ws/handler.go` (`checkOrigin`), `cmd/wayfinder/main.go` (`authMiddleware`, `loadConfig`, TLS) | Browser-Rand: `pkg/ws/handler_test.go` (`TestCheckOrigin*`), `cmd/wayfinder/main_test.go` (`TestAuthMiddleware*`, `TestLoadConfig*SecurityEnvVars*`) |
| NFR-OBS-001 | Strukturierte Logs (`slog`, JSON) sind durchgängig verdrahtet; das Log-Level ist über `WAYFINDER_LOG_LEVEL` (debug/info/warn/error, Default `info`) konfigurierbar, statt hartkodiert zu sein. Wird ein Client beim Broadcast wegen vollem Sende-Channel entfernt (Eviction), wird dies als Warnung geloggt, statt stillschweigend zu passieren — wichtig für die Betriebs-Diagnose bei überlasteten/hängenden Browser-Clients. | CLAUDE.md Abschnitt 7 (Observability), Logging/Observability-Audit (`docs/ROADMAP.md`, Paket #2) | — | `cmd/wayfinder/main.go` (`Config.LogLevel`, `loadConfig`, `parseLogLevel`), `pkg/broadcast/broadcast.go` (`broadcast`) | `cmd/wayfinder/main_test.go` (`TestLoadConfigParsesLogLevel`, `TestLoadConfigLogLevelDefaultsToInfo`, `TestLoadConfigInvalidLogLevelFallsBackToDefault`), `pkg/broadcast/broadcast_test.go::TestBroadcastEvictsClientWithFullSendChannel` |
| NFR-OBS-002 | `/metrics`-Endpoint (Prometheus-Textformat, Port `:8080` neben `/health`/`/ready`) exponiert Betriebskennzahlen: `wayfinder_cat062_blocks_received_total`/`wayfinder_cat062_tracks_received_total` (Counter), `wayfinder_cat062_decode_errors_total` (Counter, aus dem CAT062-Decoder), `wayfinder_tracks_current` (Gauge, letzter empfangener Block), `wayfinder_ws_clients_connected` (Gauge) und `wayfinder_ws_clients_evicted_total` (Counter, Broadcaster-Eviction). **WF2-23.2:** Label-Unterstützung (`metrics.Metric.With`, escaping) + **pro-Mandant-Serien** `wayfinder_tenant_ws_clients_connected{tenant}`/`wayfinder_tenant_tracks_delivered_total{tenant}` (nur stabile `tenant_id`, keine hochkardinalen Labels). | CLAUDE.md Abschnitt 7 (Observability), `docs/ROADMAP.md` Paket #2 / WF2-23 | — | `pkg/metrics/metrics.go` (`Handler`, `Counter`, `Gauge`, `Label`, `With`), `pkg/broadcast/broadcast.go` (`Broadcaster.EvictedCount`, `TenantMetrics`, per-Tenant-Counter), `cmd/wayfinder/main.go` (`startProbeServer`, `/metrics` per-Tenant) | `pkg/metrics/metrics_test.go::TestHandlerRendersPrometheusExpositionFormat`/`TestHandlerRendersLabels`, `pkg/broadcast/broadcast_test.go::TestBroadcastEvictsClientWithFullSendChannel`/`TestBroadcasterTenantMetrics` (race-clean), `pkg/receiver/receiver_test.go::TestReceiverDecodeErrorCountStartsAtZero` |

| FR-MAP-001 | „Radar Dark Mode": konfigurierbares dunkles, kontrastarmes Basis-Theme (`WAYFINDER_MAP_THEME=dark`\|`osm`, Default `dark`, ungültig → Default). `/api/map-config` liefert den passenden MapLibre-Style (CARTO `dark_nolabels`, key-frei, bzw. OSM-Raster) und ein `theme`-Feld; ein explizites `WAYFINDER_MAP_STYLE_URL` gewinnt. Das Frontend wählt eine zur Theme passende Vordergrund-Palette (helle Labels/Vektoren/Trails auf dunklem Grund). | `docs/ROADMAP.md` Paket #13 (ASD-003), CLAUDE.md §7 (Cloud-nativ) | ASD-003 (geplanter ADR) | `cmd/wayfinder/main.go` (`Config.MapTheme`, `loadConfig`, `darkMapStyle`, `mapConfigHandler`), `internal/webui/static/app.js` (`PALETTES`, `main`) | `cmd/wayfinder/main_test.go` (`TestMapConfigHandlerDarkThemeByDefault`, `TestMapConfigHandlerOSMTheme`, `TestMapConfigHandlerCustomStyleURLReportsTheme`, `TestLoadConfigMapTheme`) |

| FR-MAP-002 | Aeronautik-Daten (Lufträume, Navaids, Waypoints) werden **live von OpenAIP** bezogen und dem Frontend als GeoJSON über `/api/airspace`, `/api/navaids`, `/api/waypoints` bereitgestellt (server-seitiger Abruf, ADR 0004). | `docs/ROADMAP.md` Paket #13 (ASD-003) | ADR 0004 | `pkg/aeronautical/client.go` (`Fetch`), `pkg/aeronautical/service.go` (`Register`, `handler`), `cmd/wayfinder/main.go` (`aeroService`) | `pkg/aeronautical/client_test.go` (`TestFetchTransformsNavaids`, `TestFetchToleratesEmptyAndMissingItems`), `pkg/aeronautical/service_test.go` (`TestServiceRefreshAllPopulatesCache`, `TestServiceServesEmptyBeforeFirstRefresh`) |
| NFR-OPS-004 | Die Aeronautik-Layer sind **best-effort** und entkoppeln das ASD von OpenAIP: periodischer, **nicht-blockierender** Refresh (`WAYFINDER_OPENAIP_REFRESH`, Default 24 h), **Last-Good-Cache** bei Fehler, leere FeatureCollection bevor je etwas gecached wurde; ein OpenAIP-Ausfall beeinflusst weder den Track-Pfad noch `/ready`. Ohne `WAYFINDER_OPENAIP_API_KEY` ist das Feature still aus (Warn-Log). | CLAUDE.md §7 (Cloud-nativ/Verfügbarkeit), ADR 0004 | ADR 0004 | `pkg/aeronautical/service.go` (`Run`, `refreshAll`, `handler`), `cmd/wayfinder/main.go` (`loadConfig`, eigene Goroutine) | `pkg/aeronautical/service_test.go` (`TestServiceKeepsLastGoodOnFailure`, `TestServiceDisabledRunReturnsImmediately`) |
| NFR-SEC-002 | OpenAIP-Anbindung als **defensiver Konsument**: API-Key **server-seitig** (`WAYFINDER_OPENAIP_API_KEY`, nie an den Browser), Request-Timeout, Antwort-Größenbegrenzung (`maxResponseBytes`), Validierung/Normalisierung der Geometrie (fehlerhafte Items verworfen). Externe Vertrauens-/Größengrenze analog zum robusten CAT062-Decoder. | CLAUDE.md §7 (Sicherheit), ADR 0004 | ADR 0004 | `pkg/aeronautical/client.go` (`maxResponseBytes`, `validGeometry`, Header), `cmd/wayfinder/main.go` (`http.Client{Timeout}`) | `pkg/aeronautical/client_test.go` (`TestFetchNon200ReturnsError`, `TestValidGeometryRejectsBadInput`, `TestFetchTransformsNavaids`) |
| NFR-OBS-004 | `/metrics` exponiert die OpenAIP-Anbindung: `wayfinder_openaip_fetch_success_total`/`wayfinder_openaip_fetch_failures_total` (Counter) und `wayfinder_openaip_cache_age_seconds` (Gauge, -1 wenn nie erfolgreich) — macht eine alternde/ausfallende Quelle beobachtbar. | CLAUDE.md §7 (Observability), ADR 0004 | ADR 0004 | `cmd/wayfinder/main.go` (`startProbeServer` `/metrics`), `pkg/aeronautical/service.go` (`FetchSuccessCount`, `FetchFailureCount`, `CacheAgeSeconds`) | `pkg/aeronautical/service_test.go` (`TestServiceRefreshAllPopulatesCache`, `TestCacheAgeSeconds`) |

| FR-MAP-003 | Luftraumstrukturen (Sektor-/FIR-Grenzen) werden als Karten-Overlay gerendert (Fill/Line/Label, unter den Track-Layern) und sind über ein Layer-Steuerungs-Panel ein-/ausblendbar (Decluttering). Daten aus `/api/airspace`. | `docs/ROADMAP.md` Paket #13 (ASD-003) | ADR 0004 | `internal/webui/static/app.js` (`addAirspaceLayers`, `loadAeronautical`, `setupLayerControl`), `internal/webui/static/index.html` (`#layer-control`) | manuell verifiziert (Rauchtest Endpoints + visuell; kein JS-Test-Harness, vgl. M1.4) |
| FR-MAP-004 | Waypoints und VOR/NDB-Beacons werden als dedizierte, schaltbare Symbol-Layer gerendert: laufzeit-gezeichnete Icons (Waypoint-Dreieck, VOR-Kompassrose, NDB-Ring; Auswahl per `navaid_kind`), Labels aus `ident`/`name`, zoom-abhängige Sichtbarkeit gegen Clutter. Daten aus `/api/navaids` und `/api/waypoints`. | `docs/ROADMAP.md` Paket #13 (ASD-003) | ADR 0004 | `internal/webui/static/app.js` (`addAeronauticalIcons`, `addNavaidLayers`, `addWaypointLayers`, `loadAeronautical`) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness) |

| FR-ASD-001 | **Erweiterter Data Block (ASD-001):** Das Track-Label zeigt den vollständigen ASD-Data-Block: (1) Callsign (I062/245) als primäre Identifikation, Tracknummer als Fallback; (2) Flugfläche im Format `FLnnn` (I062/136, gerundet auf FL-Einheiten), wenn bekannt; (3) Vertikaltendenz-Indikator `▲`/`▼` an der FL-Zeile, wenn der FL-Wert gegenüber dem vorigen Update um mehr als 50 ft (2 LSB) gestiegen oder gefallen ist (kein Indikator = Level/unbekannt); (4) Bodengeschwindigkeit in Knoten (ganzzahlig, aus Vx/Vy, I062/185: `sqrt(Vx²+Vy²) × 1.9438`) als dritte Zeile, wenn > 0. Alle vier Elemente sind in `buildLabel(track, vTrend)` (`app.js`) gebündelt; die FL-History (`state.trackFlHistory`) wird parallel zu `trackHistory` in `updateTracksLayer` gepflegt und bei TSE/Wegfall eines Tracks bereinigt. Kein Backend-Change nötig (alle Felder bereits im WS-JSON). | `docs/ROADMAP.md` Paket #12 (ASD-001), CLAUDE.md §2 (CAT062-Vertrag) | — | `internal/webui/static/app.js` (`buildLabel`, `updateTracksLayer`, `state.trackFlHistory`) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness, vgl. ASD-003) |

| FR-ASD-006 | **ADS-B-Badge im Data-Block (AP9.9):** Das Track-Label zeigt einen ADS-B-Indikator (`◆`, an die Identifikations-Zeile angehängt), wenn der Track **aktuell** einen ADS-B-Anteil hat — d. h. `adsb_age_s` ist vorhanden und nicht älter als `ADSB_FRESH_THRESHOLD_S` (30 s). `isAdsbFresh(adsbAgeS)` kapselt die Frische-Prüfung; Radar-only-Tracks (kein `adsb_age_s`) tragen kein Badge. Das `◆`-Glyph stammt aus demselben Unicode-Block (Geometric Shapes) wie die bereits genutzten Vertikaltendenz-Pfeile `▲`/`▼`, ist also vom Karten-Font-Stack abgedeckt. Kein Backend-Change über FR-DATA-005 hinaus. Operative Bedeutung: der Lotse erkennt sofort, welche Tracks von der hochpräzisen ADS-B-Selbstmeldung gestützt werden (Firefly ADR 0019). **⚠️ Status (WF2-40):** Dieses Data-Block-Badge ging beim Vue-Port **verloren** (`internal/webui/static/app.js` ist toter Referenz-Code; ausgeliefert wird das Vite-Bundle `dist/`) und war im Vue-Frontend nicht vorhanden. **FR-ASD-007** stellt die ADS-B-Kennzeichnung mit **identischer Frische-Schwelle** als Symbol-Form ◆ wieder her und **löst diese Anforderung ab.** | Fireflys ICD-CAT062.md v2.4.0 / ADR 0019, CLAUDE.md §2; Wayfinder#21 | — | abgelöst durch **FR-ASD-007** (zuvor `internal/webui/static/app.js`, `isAdsbFresh`) | abgelöst durch **FR-ASD-007** |
| FR-ASD-007 | **Spur-Herkunft als Symbol-Form (WF2-40):** Das Track-Symbol kodiert die *track-abgeleitete* Überwachungs-Herkunft als **Form** — **◆ ADS-B** (kooperativ, frische Selbstmeldung), **▢ SSR / Mode S** (kooperativer Sekundär-Reply), **○ Primär (PSR)** (reine Skin-Paint ohne ID) — während die **Farbe weiterhin den Track-Zustand** trägt (confirmed/coasting/tentative/filtered; Präzedenz unverändert). `trackProvenance(track)` leitet die Klasse aus bereits dekodierten Vertragsfeldern ab: `adsb_age_s` **vorhanden und frisch** (`isAdsbFresh`, ≤ 30 s) → `adsb`; sonst `icao_addr`/`mode_3a`/Callsign → `ssr`; sonst `psr`. **Ehrliche Grenze:** keine zertifizierte Per-Plot-Provenienz — CAT062 trägt keine explizite Sensor-Quelle pro Plot (offen als WF2-42). Rendering ohne SDF: 12 vorgerenderte (Form×Zustand)-Icons (`addTrackIcons`, Laufzeit-Canvas), Auswahl per datengetriebenem `icon-image`-`concat`-Ausdruck; Track-Layer von `circle` → `symbol` umgestellt, Opazitäts-/Fade-/FL-Semantik unverändert. `provenance` wird in `updateTracksLayer` (live) und `renderSources` (fading) auf jedes Feature gelegt; das Detail-Panel zeigt die Herkunft im Klartext, die Sidebar eine Form-Legende. Kein Backend-/ICD-Change (alle Felder bereits im WS-JSON). **Löst FR-ASD-006 ab** (verlorengegangenes Data-Block-Badge; Frische-Schwelle erhalten, als Form ◆ wiederhergestellt). | `docs/ROADMAP.md` (WF2-40); Fireflys ICD-CAT062.md v2.4.0 / ADR 0019; CLAUDE.md §2 | — | `frontend/src/map/provenance.js` (`trackProvenance`, `isAdsbFresh`, `PROVENANCE_LABELS`), `frontend/src/map/layers.js` (`makeTrackIcon`, `addTrackIcons`, `addTracksLayer`), `frontend/src/map/constants.js` (`TRACK_STATE_COLORS`), `frontend/src/map/tracks.js` + `render.js` (`provenance`-Property), `frontend/src/components/TrackDetailCard.vue`, `frontend/src/components/LayerFilterContent.vue` (Legende) | `frontend/src/map/__tests__/provenance.test.js` (Wahrheitstabelle + Frische, 15 Tests), `frontend/src/map/__tests__/tracks.test.js` (`updateTracksLayer provenance`); Symbol-Optik manuell verifiziert (kein WebGL-Test-Harness, vgl. FR-ASD-001) |
| FR-ASD-005 | **Höhen- und Filter-Tools (ASD-005):** Min/Max-FL-Filter-Panel im `#layer-control` (zwei Number-Inputs + Ausblenden-Checkbox). `isFlFiltered(flightLevelFt)` prüft ob FL außerhalb [minFL, maxFL] liegt; Tracks mit unbekannter FL passieren immer. `flOpacity(ft)` liefert `0.0` (hide) oder `0.15` (dim) für gefilterte Tracks, `undefined` für ungefilterte. `setupFlFilter()` verdrahtet die Inputs und ruft bei Änderung sofort `renderSources()` auf. `fl_opacity` wird in `renderSources()` pro Feature frisch berechnet (nicht im WSS-Handler) damit Schieberbewegungen sofort wirken; `flight_level_ft` ist daher in `liveTrackFeatures`-Properties gespeichert. `filtered: boolean` auf Track-Symbol-Features für die `circle-color`-Expression (blau-grau für gefilterte Tracks). Alle fünf Layer-Opacity-Expressions erweitert um `["has", "fl_opacity"] → ["get", "fl_opacity"]` (Priorität: fade > fl > coasting > normal). | `docs/ROADMAP.md` Paket #15 (ASD-005), CLAUDE.md §7 | — | `internal/webui/static/index.html` (`#fl-filter`-UI), `internal/webui/static/app.js` (`isFlFiltered`, `flOpacity`, `setupFlFilter`, `state.flFilter`, `renderSources`, alle 5 Layer-Paint-Expressions) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness) |
| FR-ASD-004 | **Track-Lebenszyklus & History-Darstellung (ASD-004):** (4a) **History-Dots:** Vergangene Positionen werden als diskrete Kreise (Radius 2 px, Trail-Farbe, Opacity 0.6/0.2 coasting) zusätzlich zur Trail-Linie gerendert — Positionen aller Einträge in `state.trackHistory` als GeoJSON-Points-Layer (`track-history-dots`), der zwischen Trail-Linie und Speed-Vector sitzt. (4b) **Coasting-Abdunkeln:** Coasting-Tracks werden auf allen Layern visuell gedimmt (Track-Kreis opacity 0.5, Label/Vektor opacity 0.35, Trail/Dots opacity 0.2) via MapLibre-`case`-Paint-Expression auf der `coasting`-Feature-Eigenschaft; `state.trackCoasting: Map` speichert den Zustand für Trail/Dot-Features, die keinen direkten Track-Zugriff haben. (4c) **Graceful Fade-Out bei TSE:** TSE-Tracks (`ended=true`) werden statt sofortigem Entfernen in `state.fadingTracks: Map<track_num, {deadline, track}>` eingetragen (Deadline = `Date.now() + 1500 ms`); `renderSources()` mischt sie mit `fade_opacity`-Property (0–1) in alle vier Map-Sources ein; ein `setInterval`-Loop (~50 ms) ruft `tickFade()` → `renderSources()` auf, bis alle Deadlines abgelaufen sind; alle Paint-Expressions prüfen `["has", "fade_opacity"]` und verwenden es direkt (Vorrang vor Coasting-Dimming). `updateTrackHistory` lässt Fading-Track-History bis zur Deadline stehen. Kein Backend-Change nötig. | `docs/ROADMAP.md` Paket #14 (ASD-004), CLAUDE.md §7 (Sicherheit/Betreibbarkeit) | — | `internal/webui/static/app.js` (`HISTORY_DOTS_SOURCE_ID/LAYER_ID`, `FADE_DURATION_MS`, `state.trackCoasting/fadingTracks/liveTrackFeatures/liveVectorFeatures`, `addHistoryDotsLayer`, `addTrailsLayer/addVectorsLayer/addTracksLayer` paint, `updateTrackHistory`, `updateTracksLayer`, `renderSources`, `tickFade`) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness) |

| FR-ASD-002 | **Anti-Garbling — Label-Deconfliction + Drag&Drop-Pinning (ASD-002):** (B1) **Auto-Deconfliction:** Track-Data-Block-Labels werden nie versteckt. `deconflictLabels(allFeatures)` berechnet in Screen-Space für jeden Track (sortiert nach `track_num` für Determinismus) die optimale Label-Position per **greedy 8-Slot-Algorithmus** (`LABEL_SLOTS` rechts-priorisiert, ATC-konform): für jeden Track werden 8 Kandidatenpositionen (`LABEL_SLOT_RADIUS_PX` = 20 px Abstand) gegen bereits platzierte Label-BBoxen und die Kreis-Footprints **aller anderen** Tracks geprüft (`bboxCollides`). Das eigene Symbol ist absichtlich ausgeschlossen, damit das Label neben seinem Punkt sitzen kann. Erster kollisionsfreier Slot gewinnt; wenn alle belegt sind, greift Slot 0 als Fallback — kein Label wird je unterdrückt. Positionen werden via `map.project/unproject` (Screen↔Geo) konvertiert. Labels und Leader Lines gehen in getrennte GeoJSON-Sources (`LABELS_SOURCE_ID` / `LEADER_LINES_SOURCE_ID`), die auf jedem WSS-Update und bei Karten-Bewegung (`map.on("move") + requestAnimationFrame`-Throttle) frisch befüllt werden. `text-allow-overlap: true` + `text-ignore-placement: true` stellen sicher, dass MapLibres eigene Placement-Engine kein Label versteckt. Leader Lines (0.7 px, label-farbig, opacity 0.55) werden gezeichnet, wenn symbol→label-Abstand > `LEADER_THRESHOLD_PX` = 10 px. Alle Opacity-Properties (`fade_opacity`, `fl_opacity`, `coasting`) werden aus den Track-Features durchgereicht. (B2) **Drag&Drop-Pinning:** `setupLabelDrag(map)` verdrahtet `mousedown` auf dem Label-Layer: beim Drag-Start wird `map.dragPan.disable()`, der aktuelle oder initiale Pixel-Offset im `state.labelPins: Map<track_num, {dx, dy}>` gespeichert; `mousemove` aktualisiert den Pin live und ruft `renderSources()` auf; `mouseup` gibt Pan frei. `dblclick` auf ein Label löscht seinen Pin (revert zu Auto-Platzierung). Abgelaufene Tracks werden in `tickFade()` aus `labelPins` entfernt. Kein Backend- oder ICD-Change. | `docs/ROADMAP.md` Paket #16 (ASD-002) | — | `internal/webui/static/app.js` (`LABELS_SOURCE_ID/LAYER_ID`, `LEADER_LINES_SOURCE_ID/LAYER_ID`, `LABEL_SLOTS`, `LABEL_SLOT_RADIUS_PX`, `LABEL_W/H_PX`, `SYMBOL_BBOX_R_PX`, `LEADER_THRESHOLD_PX`, `state.labelPins`, `addLeaderLinesLayer`, `addLabelsLayer`, `bboxCollides`, `deconflictLabels`, `setupLabelDrag`, `renderSources`, `tickFade`) | manuell verifiziert (Rauchtest + visuell; kein JS-Test-Harness) |

| FR-UI-001 | Build-Pipeline: `npm run build` in `frontend/` erzeugt statische Assets unter `internal/webui/dist/`; Go-Backend embedded `dist/` via `//go:embed dist` (ADR 0002) | ADR 0002 | ADR 0002 | `frontend/vite.config.js`, `internal/webui/webui.go` | `npm run build` ✅ (CI-Gate) |
| FR-UI-002 | ASD-Karten-Engine verlustfrei portiert: alle Funktionen aus `app.js` (deconflictLabels, renderSources, tickFade, updateTracksLayer, setupLabelDrag, buildLabel, isFlFiltered, flOpacity, loadAeronautical) sind als ES-Module in `frontend/src/map/` verfügbar; Vitest-Tests schließen die Testlücke für FR-ASD-001/002/004/005 | ADR 0002, CLAUDE.md §6 | ADR 0002 | `frontend/src/map/` | `frontend/src/map/__tests__/` (Vitest) |
| FR-UI-003 | MD3-Sidebar (Navigation Drawer, links) fasst Layer-Toggles (Lufträume/VOR-NDB/Waypoints) und FL-Filter zusammen; räumt den Karten-Scope von überlappenden Panels frei | ADR 0002 | ADR 0002 | `frontend/src/components/LayerSidebar.vue`, `frontend/src/stores/asd.js` | manuell verifiziert (Vitest für Store-Logik) |
| FR-UI-004 | MD3 Top-App-Bar zeigt Titel und Feed-Status-Chip (grün/rot/grau) statt des bisherigen absolut-positionierten `#feed-status`-Div | ADR 0002, FR-OPS-004 | ADR 0002 | `frontend/src/components/FeedStatusChip.vue`, `frontend/src/App.vue` | manuell verifiziert |
| FR-UI-005 | Track-Detail-Panel (MD3 Card/Bottom-Sheet bei Track-Klick): zeigt Callsign, FL, Bodengeschwindigkeit, Mode 3/A, Track-Nummer, Status — neu, bisher nicht im ASD vorhanden | ADR 0002 | ADR 0002 | `frontend/src/components/TrackDetailPanel.vue`, `frontend/src/components/TrackDetailCard.vue` | manuell verifiziert |
| NFR-UI-001 | MD3-Farbsystem (dunkles ASD-Theme): Surface-Farben dunkel (#0b0f14 Hintergrund, #111720 Surface), Primary #5b8fd6 (Radar-Blau), Typographie Roboto; verhindert Auswaschung des WebGL-Karten-Scopes | ADR 0002 | ADR 0002 | `frontend/src/plugins/vuetify.js` | visuell verifiziert |
| NFR-UI-002 | A11y-Grundlinie: Touch-Targets ≥ 44px (Vuetify-Standard), Kontrast-Check gegen dunkles Theme (Vuetify MD3-Garantie), keine focus-ring-Unterdrückung | ADR 0002 | ADR 0002 | Vuetify-Komponentenstandards | visuell verifiziert |

| FR-UI-010 | **Kategorie-Filter-Chips mit Track-Zählern (ASD-010):** `TrackFilterChips.vue` rendert drei `v-chip`s (Confirmed / Coasting / Tentative) top-center über dem Canvas mit Live-Zählern aus Pinia `trackCounts`. Klick togglet `hiddenCategories` im Store. Engine.js zählt nach jedem WS-Frame die Kategorien und schreibt sie via `setTrackCounts`. `renderSources()` baut `hiddenNums`-Set aus `hiddenCategories` und filtert alle Feature-Typen (Symbole, Vektoren, Dots, Trails). `MapCanvas.vue` watcht `hiddenCategories.size` und triggert `updateFlFilter()` für sofortigen Re-Render. | ASD-010 (Mockup-Analyse, Phase 1) | ROADMAP ASD-010 | `frontend/src/components/TrackFilterChips.vue`, `frontend/src/components/MapCanvas.vue`, `frontend/src/stores/asd.js` (`trackCounts`, `hiddenCategories`, `setTrackCounts`, `toggleCategoryFilter`), `frontend/src/map/render.js` (`hiddenNums`), `frontend/src/map/engine.js` (`updateTrackCounts`) | visuell verifiziert (Build grün, 39 Vitest-Tests grün) |
| FR-UI-011 | **Range-Rings + Scale-Bar + Nord-Orientierung (ASD-012):** (A) **Range-Rings** — konzentrische Kreise **konstanter Boden-Distanz** um den konfigurierten View-Mittelpunkt (`/api/map-config`, tenant-skopierbar via WF2-30/31) als Distanz-Raster für den Lotsen (abgegrenzt von den Sensor-Coverage-Ringen, Paket 6). **Geodätische** Erzeugung (`rangerings.js`): jeder Ring-Vertex liegt in *jede* Richtung in derselben Boden-Distanz — der naive Gleich-Grad-Ansatz würde den Ring in Längsrichtung stauchen (auf Web-Mercator „gequetscht"); im Test bewiesen (gleiche Meter, ungleiche Grad). **Operator-konfigurierbar live:** Abstand (5/10/15 NM) + Anzahl als **reaktiver Pinia-State** (`asd.js` `rangeRingConfig`, Default 10 NM / 5), Sidebar-Toggle + Select/Slider (nur sichtbar wenn aktiv, default aus); MapCanvas-Watcher → `engine.updateRangeRings` regeneriert das Overlay. (B) **Scale-Bar** — MapLibre `ScaleControl{unit:'nautical'}` (absolute Distanzreferenz bei jedem Zoom). (C) **Nord-Orientierung** — MapLibre `NavigationControl{showCompass}` (zeigt Bearing, Klick → Nord-up; freie Kartendrehung bleibt); der alte hand-gebaute Reset-Nord-Button wurde **entfernt** (Declutter). Kein wörtliches „Track-up" (für ein Multi-Track-ASD undefiniert). | `docs/ROADMAP.md` (ASD-012) | — | `frontend/src/map/rangerings.js`, `…/layers.js` (`addRangeRingsLayer`), `…/constants.js` (IDs/Defaults/Palette), `…/engine.js` (Controls, `updateRangeRings`), `…/stores/asd.js` (`rangeRingConfig`), `…/components/LayerFilterContent.vue` (Bedienung), `…/components/MapCanvas.vue` (Watcher), `…/components/MapControls.vue` (Button entfernt) | `frontend/src/map/__tests__/rangerings.test.js` (9: Distanz/Bearing, **Anti-Squish**, Ring-Schließung/Radius, GeoJSON/Clamp/Label); Optik (Controls-Platzierung, Ring-Stil, Kompass) **manuell verifiziert** (kein WebGL-Test-Harness, vgl. FR-ASD-001) |

| FR-UI-012 | **Admin-Einstieg in der Navigation Rail (Req 1):** Die Navigation Rail zeigt unten (ans Schienen-Ende gepinnt, Icon `mdi-shield-account`) einen **Admin**-Eintrag, der **ausschließlich** sichtbar ist, wenn die Rolle der aktuellen Identität `tenant_admin` oder `super_admin` ist. Klick navigiert per Vue Router auf den bestehenden `/admin`-View. Die Sichtbarkeit ist **strikt an die server-autoritative Identität gekoppelt** (`/api/admin/whoami`, einmalig in `onMounted` geprüft, **fail-closed**: Operator bzw. 401/403/404 → Eintrag bleibt unsichtbar). Der Store exponiert `isAdmin` (computed `role === 'tenant_admin' || role === 'super_admin'`). Reine Kosmetik-Gating — die echte Grenze zieht der Server (`RequireRole` auf `/api/admin/*`). Mobile-Layout: identisch gegatetes `v-list-item`. Kein Backend-/ICD-Change. | Req 1 (operative Anforderung 2026-06-23), CLAUDE.md §7 (Browser-Rand absichern) | — (nutzt bestehendes `/admin`-Routing + `RequireRole`) | `frontend/src/stores/admin.js` (`isAdmin`), `frontend/src/components/NavigationRail.vue` (`goAdmin`, `onMounted`-Identity-Probe, Rail-Button + Mobile-`v-list-item`, beide `v-if="isAdmin"`) | `frontend/src/stores/__tests__/admin.test.js` (`isAdmin`-Matrix: `tenant_admin`/`super_admin` → true, Operator/403 → false); Build grün, 82 Vitest-Tests grün |

| FR-UI-009 | **Karten-Controls rechts (ASD-009):** `MapControls.vue` rendert zwei schwebende Button-Gruppen rechts über dem MapLibre-Canvas (`position: absolute`, z-index 10). Gruppe 1: Zoom-In/Zoom-Out. Gruppe 2: Recenter (flyTo konfiguriertem Zentrum), Nord-up-Reset (easeTo bearing=0), Fullscreen-Toggle. Controls emittieren Events; `MapCanvas.vue` delegiert an `mapEngine`-Methoden (`zoomIn`, `zoomOut`, `recenter`, `resetNorth`), die in `engine.js` neu hinzugefügt wurden. Kein Vue-Framework-Eingriff in den Map-Engine-State. | ASD-009 (Mockup-Analyse, Phase 1) | ROADMAP ASD-009 | `frontend/src/components/MapControls.vue`, `frontend/src/components/MapCanvas.vue`, `frontend/src/map/engine.js` | visuell verifiziert (Build grün) |

| FR-UI-008 | **Navigation Rail + Sekundär-Panel (ASD-008):** `NavigationRail.vue` ersetzt die monolithische `LayerSidebar.vue`. Auf Desktop ist die Schiene permanent sichtbar (56 px, Icons + Tooltips, `rail`-Prop); Klick auf ein Rail-Icon expandiert sie auf 240 px und zeigt das Sekundär-Panel (Layer-Toggles + FL-Filter); erneuter Klick auf dasselbe Icon kollabiert zurück. Collapse-Button unten für explizites Einklappen. Auf Mobile bleibt das Hamburger-Menü mit Temporary-Drawer erhalten. App.vue importiert `NavigationRail.vue` statt `LayerSidebar.vue`. | ASD-008 (Mockup-Analyse, Phase 1) | ROADMAP ASD-008, `docs/design/color-tokens.md` | `frontend/src/components/NavigationRail.vue`, `frontend/src/App.vue` | visuell verifiziert (Build + 39 Vitest-Tests grün) |

| FR-UI-007 | **Farbschema ASD-007 (Mockup-Ableitung):** MD3-Chrome-Theme auf Cyan-Primary umgestellt (`#23d3e6`); Hintergrundhierarchie `#070b12`→`#0e1622`→`#16202e`→`#1c2c3e`; Semantikfarben (error `#ff4338`, warning `#ffb02e`, success `#3ecf6b`, info `#3d9be0`) ATC-konventionell; ASD-Domain-Farben in `TRACK_COLORS` (friendlyCivil `#41c4e8`, friendlyMilitary `#ffa726`, hostile `#ff4338`, unknown `#ffd23e`, neutral `#43c66b`) + Palette-Update (`label`/`vector`/`trail`/`airspaceFillColor`/`airways`). Vollständig dokumentiert in `docs/design/color-tokens.md`. | ASD-007 Mockup-Analyse (2026-06-17) | `docs/design/color-tokens.md`, ROADMAP ASD-007 | `frontend/src/plugins/vuetify.js` (`asdDarkTheme.colors`), `frontend/src/map/constants.js` (`TRACK_COLORS`, `PALETTES`) | visuell verifiziert |

| FR-CFG-003 | Optionale Konfigurationsdatei `wayfinder.yaml`: `loadYAMLFile(path, cfg, logger)` liest die Datei (Felder: `map.center_lat/lon/zoom`, `openaip.radius_km`) und überschreibt die Hardcoded-Defaults; Env-Vars gewinnen danach immer (12-Factor). Pfad: Standard `wayfinder.yaml` im Arbeitsverzeichnis, überschreibbar via `WAYFINDER_CONFIG_FILE`. Fehlende Datei ist nicht-fatal (kein Crash, kein Log-Fehler). Ungültiges YAML: Warn-Log + Fallback auf Defaults. Partielle Dateien (nur einzelne Felder gesetzt) lassen übrige Defaults unberührt. | ADR 0002 (Konfigurationsdatei-Konzept), CLAUDE.md §7 (12-Factor) | ADR 0002 | `cmd/wayfinder/main.go` (`yamlFileConfig`, `loadYAMLFile`, `loadConfig`) | `cmd/wayfinder/main_test.go` (`TestLoadYAMLFileAppliesValues`, `TestLoadYAMLFileMissingFileIsNonFatal`, `TestLoadYAMLFileInvalidYAMLIsNonFatal`, `TestLoadYAMLFilePartialOverride`, `TestLoadYAMLFileEnvVarWins`) |

| FR-TEN-001 | **Mandantenfähigkeit nach Hybrid-Modell (Wayfinder 2.0, ADR 0005):** Wayfinder wird zur mandantenfähigen Plattform. Konzeptuelles Datenmodell: **Tenant** (isolierte Organisation), **User** (genau ein Tenant, Rollen `operator`/`tenant-admin`), **Feed** (ein Upstream-CAT062/065-Strom mit Metadaten inkl. Sensor-Mix als **Feed-Eigenschaft**), **Subscription** (Tenant ↔ erlaubte Feeds), **ViewConfig** (Zentrum/Radius/AOI/FL-Band/Layer), **Entitlement** (Feature-Flags als Daten). Der ASD-Kern bleibt erhalten und wird zur mandanten-skopierten Sicht. 12-Factor-Grenze: fachliche Mandanten-Config in der DB, Infra-Secrets (DB-URL, Stream-Endpunkt, TLS, OIDC) bleiben ENV. Single-Tenant (ein Default-Tenant, ein Feed) bleibt als degenerierter Fall lauffähig. | Konzept `docs/design/wayfinder-2.0-konzept.md`, ROADMAP §0/§1 (WF2-00), CLAUDE.md §1/§7 | **ADR 0005** | **WF2-10.2 ✅ (Teil):** `pkg/store/models.go` (`Tenant`/`User`/`Role`), `pkg/store/tenants.go`/`users.go` (Repos; `UserRepo.GetBySubject` = Identität→Mandant), Schema `00001_init.sql`+`00002_view_config_user_unique.sql`; **WF2-10.3a ✅** `feeds.go`/`subscriptions.go`/`entitlements.go`, **WF2-10.3b ✅** `view_configs.go` (BBox/ViewConfig, Tenant-Default + Nutzer-Override, `GetEffective`) — **Persistenz-Schicht WF2-10 komplett (alle 6 Tabellen)**; **WF2-13 ✅** Admin-Bootstrap (`cmd/wayfinder/bootstrap.go` `runBootstrap`/`bootstrapCommand`: idempotentes Anlegen erster Tenant/Admin/Credential, Subcommand `wayfinder bootstrap`) + `/admin`-Rollen-Gate (`pkg/tenant/authz.go` `RequireRole`); Abo-Durchsetzung WF2-20/21 | **WF2-10.2 ✅** `pkg/store/repo_test.go` (DB-frei) + `pkg/store/store_integration_test.go` (real PostgreSQL 16 via `scripts/pg-test.sh`); **WF2-13 ✅** `cmd/wayfinder/bootstrap_test.go` (DB-frei `validate`) + `bootstrap_integration_test.go` (real-PG: create/idempotent/Passwort-Update/Cross-Tenant-Konflikt) + `pkg/tenant/authz_test.go` (RequireRole: admin erlaubt, operator/ohne-Identität → 403); Isolations-Durchsetzungstests WF2-22 |
| NFR-SEC-003 | **Cross-Tenant-Daten-Isolation (Wayfinder 2.0, ADR 0005):** Ein Mandant darf **niemals** die Luftlage eines anderen sehen, außer explizit freigegeben. Verbindliche Prinzipien: **server-seitige Autorisierung pro Subscription** (Broadcaster von all-to-all auf prädikat-gefilterte Zustellung `feed ∈ subs ∧ AOI ∧ FL-Band ∧ Kategorie`, kein Browser-seitiges Filtern), **fail-closed** (ohne gültigen Tenant-Kontext keine Daten), **Pflicht-Negativtests** als Gate (Mandant A bekommt nie einen Track von Mandant B; Property-/Fuzz-Tests gegen das Prädikat), mandanten-bewusste Autorisierung am Browser-Rand (nimmt die in ADR 0003 vertagte „Autorisierungs-ADR" auf) und eine **Audit-Spur** (welcher Tenant sah welchen Scope). | Konzept §5.5/§6, ADR 0005, ADR 0003 (Ehrliche Grenze, vertagte Autorisierung), CLAUDE.md §7 (Sicherheit) | **ADR 0005** | **Abo-Datenschicht WF2-10.3a ✅:** `pkg/store/subscriptions.go` (`ListFeedsByTenant`/`IsSubscribed` — die Daten, gegen die geprüft wird); **HTTP-Rand-Enforcement WF2-12.1 ✅** (`pkg/tenant.Middleware`: subject→tenant fail-closed, 401 ohne gültigen Mandanten); **Stream-Enforcement Feed-Ebene WF2-21.1 ✅** (`broadcast.Scope`/`broadcastTracks`: Track nur an Clients mit `scope.AllowsFeed(feed_id)`, leerer Scope = nichts; `ws.ScopeResolver` fail-closed `403`; `newScopeResolver` via `subscriptions.ListFeedIDsByTenant`); **Sicht-Filter AOI/FL-Band WF2-21.2 ✅** (`broadcast.ViewFilter`/`Scope.filterView`: harte Datensparsamkeits-Grenze server-seitig, **fail-open** bei fehlendem Attribut; `resolveViewFilter` via `view_configs.GetEffective`, FL→Fuß); Lebenszyklus bleibt client-seitig, Klassifizierung später (WF2-40); **Audit-Spur WF2-23.1 ✅** (`cmd/wayfinder.logScopeAudit`: strukturiertes `slog`-Event `component=audit`/`event=ws_connect` mit tenant_id/user_id/subject/feeds/aoi/fl beim `/ws`-Connect, 12-Factor/keine DB) **+ Pro-Tenant-Metriken WF2-23.2 ✅** (`tenant`-gelabelte `/metrics`-Serien, nur stabile `tenant_id`) → **Stufe 2 (mandanten-isolierter Datenstrom) komplett** | **WF2-10.3a ✅** (Daten) `…::TestIntegrationSubscriptionRepoIsolation`; **WF2-21.1 ✅ Pflicht-Negativtest** `…::TestBroadcastFeedIsolation` (A bekommt nie B's Feed) + `TestScopeAllowsFeed` + Resolver fail-closed; **WF2-21.2 ✅** `TestViewFilterAdmits` (AOI/FL/**fail-open**) + `TestBroadcastViewScoping` + `TestResolveViewFilter` (DB-frei) + `TestIntegrationResolveViewFilter` (real-PG); **WF2-22 ✅ Property-/Fuzz-Gate** `pkg/broadcast/isolation_test.go`: `TestFilterViewMatchesOracle` (50k Iter, differenziell vs. unabhängiges Oracle, beide Richtungen), `TestBroadcasterIsolationProperty` (Ende-zu-Ende: jeder empfangene Track ∈ Client-Scope), `FuzzScopeFilter` (755k execs, 0 Fehler); **WF2-23.1 ✅** `cmd/wayfinder/scope_test.go::TestScopeResolverEmitsAudit` (Audit-Event mit Scope-Feldern, JSON-geprüft) |

| FR-TEN-002 | **Konfig-/Identitäts-Persistenz (Wayfinder 2.0, ADR 0006):** Datastore = **PostgreSQL**; Zugriff über **`pgx` + `sqlc`** (typsicheres Go aus handgeschriebenem SQL, kein schweres ORM — auditierbar); Schema-Migrationen versioniert/eingebettet via **minimalen In-House-Runner** (`pkg/store/migrate.go`, eingebettete SQL, je Migration eine Transaktion, idempotent; `goose` verworfen wg. transitivem `modernc.org/sqlite`-Ballast — ADR 0006 Nachtrag). Schema: `tenants`, `users` (Rolle operator/tenant_admin/super_admin), `feeds` (globaler Katalog, `sensor_mix` JSONB als Feed-Eigenschaft), `subscriptions` (Tenant↔Feed), `view_configs` (tenant-Default + optionale Nutzer-Übersteuerung), `entitlements` (Feature-Flags als Daten). **Stateless-Split:** alle Mandanten-/Konfig-/Identitäts-Daten in der DB, **kein durabler node-lokaler Zustand**; Infra/Secrets (`WAYFINDER_DB_URL` etc.) bleiben ENV (12-Factor). Cache: vorerst In-Proc-TTL, Redis zurückgestellt bis gemessener Bedarf. | ADR 0006, ADR 0005 (Datenmodell), Konzept §6.2/§11.3 | **ADR 0006** | **WF2-10.1 ✅** `pkg/store/store.go` (`Open`/`DSNFromEnv`), `pkg/store/migrate.go` (Runner), `pkg/store/migrations/00001_init.sql` (Schema); Repositories (pgx/sqlc) folgen WF2-10.2/10.3, Hot-Reload WF2-30 | **WF2-10.1 ✅** `pkg/store/store_test.go` (DB-frei: Migrations-Parsing, Schema-Drift, DSN); Schema-Apply/Repository-Integration folgt WF2-10.3 (CI/Testcontainers) |
| FR-TEN-003 | **Feature-Entitlement-Service (Wayfinder 2.0, WF2-50, ADR 0005 §4):** Pro-Mandant-Feature-Flags **als Daten** (`entitlements`-Tabelle aus WF2-10), entkoppelt von Billing (WF2-51 ruht). `pkg/feature` kapselt einen **fail-closed** Service über `store.EntitlementRepo`: `HasFeature(ctx, tenantID, key) bool` und `Effective(...)` sind **Default-Deny**; ein **unbekannter Katalog-Key** oder **jeder Store-Fehler** evaluiert zu `false` **plus Warn-Log und Metrik** (`wayfinder_feature_check_failclosed_total{reason}`) — ein Tor öffnet nie bei Unsicherheit. Getypter Katalog (`stca`, `multi_feed`, `premium_layers`, `airspaces`, `range_rings`, `history_dots`, `vor_ndb`, `waypoints` — 8 Keys ab AP2) mit `IsKnown`-Validierung; `Set` weist unbekannte Keys ab (`ErrUnknownFeature` → 400). **Admin-Verwaltung** (super_admin = Billing-Grenze): `GET`/`PUT /api/admin/tenants/{tenantID}/entitlements[/{key}]`, cross-tenant nur super_admin (`requireSuper`). **SPA:** `whoami` trägt die effektiven Flags (`features`), der Admin-Store exponiert `hasFeature()` — UI-Gating kosmetisch (Layer-Steuerelemente in `LayerFilterContent.vue`), Durchsetzung serverseitig. Gate-Formel: `!isAuthorized || hasFeature(key)` (Nicht-Admin-Nutzer sehen alle Layer). Kein Schema-Change, keine neue Env-Var. | Konzept §6/§11, ROADMAP §1 (WF2-50), ADR 0005 §4 | **ADR 0005** | `pkg/feature/feature.go` (`Service`, `HasFeature`, `Effective`, `Set`, `ErrUnknownFeature`), `pkg/feature/catalog.go` (`Key`, `STCA`/`MultiFeed`/`PremiumLayers`/`Airspaces`/`RangeRings`/`HistoryDots`/`VorNdb`/`Waypoints` — 8 Keys, AP2), `pkg/adminapi/adminapi.go` (`EntitlementService`, `listTenantEntitlements`/`setTenantEntitlement`, `whoami.features`), `cmd/wayfinder/main.go` (Service-Konstruktion + `/metrics`), `frontend/src/stores/admin.js` (`features`/`hasFeature`), `frontend/src/stores/asd.js` (`historyDots` in `layerVisibility`), `frontend/src/map/engine.js` (`historyDots`-Gruppe in `setLayerVisibility`), `frontend/src/components/LayerFilterContent.vue` (Layer-Gate via `!admin.isAuthorized \|\| admin.hasFeature(key)`, History-Dots-Switch) | `pkg/feature/*_test.go` (DB-frei: Default-Deny, **beide Fail-Closed-Pfade** mit Zähler+Warn-Log, Katalog-Overlay, `Set`-Validierung; **8-Key-Len-Check nach AP2**), `pkg/adminapi/adminapi_test.go` (super_admin GET/PUT, unknown-key 400, unknown-tenant 404, **Cross-Tenant 403 auf beiden Routen**, `whoami.features`), `pkg/adminapi/adminapi_integration_test.go` (**real-PG** Round-Trip set→list + whoami), `frontend/src/stores/__tests__/admin.test.js` (`hasFeature`, **AP2-Block**: alle 5 neuen Keys, fail-closed Default, Nicht-Admin-Gate) |
| FR-TEN-004 | **Feed-Sensorklassen-Katalog & entitlement-gegatete Abos (Wayfinder 2.0, WF2-41, ADR 0005 §6.4):** (A) **Sensorklassen-Katalog** `pkg/sensorclass` — kontrolliertes Vokabular (`PSR`, `SSR`, `MODE_S`, `ADS-B`, `MLAT`, `FLARM`); `Parse` mappt gängige Legacy-Schreibweisen kanonisch (`ads-b`/`ADS_B`/`1090ES`→`ADS-B`, `Mode A/C`→`SSR`, …), `Canonicalize` normalisiert+dedupliziert und **weist unbekannte Tokens ab** (`*UnknownClassError`). Erzwungen am **Chokepoint** `FeedRepo.Create` → Feed-Metadaten enthalten nie Tippfehler (kein Schema-Change, `sensor_mix` bleibt JSONB). Read-only-Surfacing `GET /api/admin/sensor-classes`. (B) **Abos binden an Feeds** (ADR 0005 §6.4) mit **harter Invariante**: ein Mandant **ohne** `multi_feed`-Entitlement (WF2-50) hält **höchstens einen** Feed; ein zweiter *distinkter* Grant → **409 Conflict**, **bevor** `Subscribe` die DB erreicht (fail-early); idempotenter Re-Grant desselben Feeds bleibt 204; super_admin muss erst das Entitlement setzen. | Konzept §6.4, ROADMAP §1 (WF2-41), ADR 0005 | **ADR 0005** | `pkg/sensorclass/sensorclass.go` (`Class`, `Parse`/`Canonicalize`/`IsKnown`/`All`/`Describe`, `UnknownClassError`), `pkg/store/feeds.go` (`Create`-Validierung), `pkg/adminapi/adminapi.go` (`grantSubscription`-Gate auf `feature.MultiFeed`, `getSensorClasses`), `cmd/wayfinder/feedcmd.go` | `pkg/sensorclass/*_test.go` (Legacy-Wahrheitstabelle, Round-Trip, Dedup, unknown→Fehler), `pkg/adminapi/adminapi_test.go` (1./2. Feed, **409 ohne `multi_feed` + `Subscribe` nicht erreicht**, erlaubt mit `multi_feed`, idempotenter Re-Grant, `sensor-classes`), `pkg/store/feeds_subscriptions_integration_test.go` (**real-PG**: unknown→Fehler, Legacy→kanonisch), `pkg/adminapi/adminapi_integration_test.go` (**real-PG** Grant-Gating-Round-Trip) |
| NFR-SEC-004 | **Identitäts-/Session-Sicherheit (Wayfinder 2.0, ADR 0006):** `WAYFINDER_AUTH_MODE` ∈ {proxy, builtin, none}. **proxy (primär):** OIDC/oauth2-proxy am Ingress; Wayfinder **validiert** den weitergereichten Token (Issuer/Audience/Signatur) und mappt `subject → users.subject → tenant_id`, kein Passwort-Speicher (Muster aus ADR 0003). **builtin (optional):** eingebaute Nutzer mit **argon2id**-Hashes für Standalone, Default-aus. **none:** degenerierter Single-Tenant-Fall mit Warn-Log. **Tenant-Kontext fail-closed** (keine gültige Zuordnung ⇒ keine Daten, NFR-SEC-003). Session möglichst zustandslos (proxy hält Session; builtin: signiertes kurzlebiges Cookie, Signing-Key aus ENV). Secrets ausschließlich in ENV. | ADR 0006, ADR 0003 (Proxy-Primär-Muster), CLAUDE.md §7 | **ADR 0006** | **WF2-11.1 ✅** `pkg/auth` (`auth.go` Mode/ParseMode/Authenticator, `password.go` argon2id, `session.go` HMAC-Session, `authenticator.go` None/Builtin), **WF2-11.2 ✅** `proxy.go` (`ProxyAuthenticator`, go-oidc-Validierung Issuer/Audience/Signatur/Ablauf, Bearer-Token) — **AuthN-Paket komplett (alle 3 Modi)**; **WF2-12.1 ✅** `pkg/auth/factory.go` (`NewAuthenticator`, fail-closed-Config) + `pkg/tenant` (`Identity`/Context, `Middleware` subject→user→tenant fail-closed); **WF2-12.2 ✅** `cmd/wayfinder/main.go` (`setupTenancy`: DB-Open+Migrate+Authenticator, `tenant.Middleware` auf `/ws` nur wenn `WAYFINDER_DB_URL` gesetzt — sonst Single-Tenant, ADR 0005 §7); **WF2-12.3 ✅** builtin-Login (`pkg/store/credentials.go` `CredentialRepo` + Migration `00003`; `pkg/tenant/login.go` `LoginHandler`/`LogoutHandler`: `auth.VerifyPassword`→`MintSession`-HttpOnly-Cookie, Timing-Angleich gegen User-Enumeration) + `main.go` `/api/login`+`/api/logout` (nur builtin-Modus) — **WF2-12 komplett** | **WF2-11/12 ✅** `pkg/auth/{password,session,auth,proxy,factory}_test.go` + `pkg/tenant/{tenant,login}_test.go` (argon2, Session, **OIDC valid + alle Ablehnungen**, Factory, Middleware fail-closed, **Login: Erfolg setzt gültige Cookie, alle Fehler→401/400 ohne Cookie**) + **`cmd/wayfinder/tenancy_test.go`** + **`pkg/store/credentials_integration_test.go`** (real-PG) |
| FR-SEC-005 | **Cross-Tenant Read-Only-Impersonation (super_admin „View as Tenant X", WF2-34, ADR 0008):** Ein `super_admin` kann zur Support-Diagnose die ASD-Sicht eines Ziel-Mandanten **read-only** einsehen — eine **bewusste, auditierte, zeitlich befristete, kontrollierte Ausnahme** zu **NFR-SEC-003**. **Mechanismus:** signierter **HttpOnly-Grant-Cookie** `wf_impersonation` (HMAC via wiederverwendetem Session-Key, TTL `WAYFINDER_IMPERSONATION_TTL`, Default 30m); der `/ws`-Lesepfad löst **Feed-Scope UND View** gegen den Ziel-Mandanten auf. **Fail-closed:** gültiger Grant + Nicht-`super_admin` → **lauter Handshake-Reject + Audit** (`impersonation_denied`); fehlend/ungültig/abgelaufen → Default-Pfad (**byte-identisch** → WF2-22 bleibt gültig). Die Session ist vom Ziel-Mandanten **entkoppelt** (`scope.TenantID=0`) → **ausgeschlossen** aus Pro-Tenant-Billing/SLA-Metriken (§6) und Live-Rescope (§4, Snapshot-v1). Die **Identität wird nie überschrieben**; Schreibpfade nutzen immer den echten Mandanten → strukturell read-only. Endpoints `GET`/`POST`/`DELETE /api/admin/impersonation` (Status / Mint nur `super_admin` / Clear). Audit-Events `impersonation_start`/`_denied`/`_end` + `impersonated_tenant_id` am `ws_connect`; Metrik `wayfinder_impersonation_sessions_total`. **Frontend (Häppchen 3):** `super_admin`-only Banner/Steuerleiste — inaktiv ein dezenter „Als Mandant ansehen"-Einstieg, aktiv ein prominenter Warn-Banner „Sie betrachten X — nur Lesen" mit **Tenant-Switcher** + **Beenden**; WS-**Reconnect** bei Start/Wechsel/Exit (neuer Cookie greift sofort); Reload-fest über den `GET`-Status (HttpOnly-Cookie ist nicht JS-lesbar); **kein** farbiger Viewport-Rahmen (Entscheidung). **Stand:** Häppchen 1 (Grant-Kern) + 2 (Backend) + 3 (Frontend) ✅ — WF2-34 komplett. | ADR 0008, Anforderung 2 (2026-06-23), CLAUDE.md §7 (Browser-Rand/Sicherheit) | **ADR 0008** | `pkg/impersonation/impersonation.go` (`MintGrant`/`parseGrant`/`Resolve`, `TenantChecker`, `CookieName`, `ErrDenied`/`ErrUnknownTenant`), `cmd/wayfinder/main.go` (`newScopeResolver`-Override + `scope.TenantID=0`, `logScopeAudit`-Felder, `WAYFINDER_IMPERSONATION_TTL`, Metrik, Routen), `cmd/wayfinder/impersonation.go` (`impersonationStatusHandler`/`startImpersonationHandler`/`stopImpersonationHandler`, `tenantExistsChecker`, `impersonationGrantCookie`, `logImpersonationDenied`); **Frontend:** `frontend/src/stores/impersonation.js` (`active`/`tenantId`/`reconnectNonce`, `loadStatus`/`start`/`stop`), `frontend/src/components/ImpersonationBar.vue`, `frontend/src/map/engine.js` (`reconnect()` + Close-Guard), `frontend/src/components/MapCanvas.vue` (Reconnect-Watcher) | `pkg/impersonation/impersonation_test.go` (11: Grant-Krypto Round-Trip/tampered/wrong-key/expired + **Resolve-Matrix** inkl. Operator/TenantAdmin→Denied), `cmd/wayfinder/scope_test.go` (Override **active** = Ziel-Feeds + `TenantID=0`, **denied** für Nicht-super, **unknown-tenant**, **expired→Default**, **disabled ohne Key**), `cmd/wayfinder/impersonation_test.go` (Endpoints: Mint 204+HttpOnly/Strict, 404 unknown, 400 bad-body, 500 fail-closed, Clear MaxAge<0, **Status active/inactive**); **Frontend:** `frontend/src/stores/__tests__/impersonation.test.js` (loadStatus ohne Reconnect, start/stop bumpen `reconnectNonce`, Fehlerpfade) |

| FR-FEED-001 | **Cloud-Ingest & FeedSource (Wayfinder 2.0, ADR 0007):** `FeedSource`-Abstraktion mit zwei Implementierungen — **`MulticastFeedSource`** (direkter Multicast-Beitritt, On-Prem/Dev, heutiges `pkg/receiver`-Verhalten) und **`StreamFeedSource`** (Konsum vom NATS-Bus, Cloud), umschaltbar via `WAYFINDER_FEED_SOURCE=multicast\|nats`. **Ingest-Gateway** als eigener Minimal-Dienst (`cmd/wayfinder-ingest`): tritt der/den CAT062/065-Multicast-Gruppe(n) bei und republisht **jedes Datagramm unverändert** (Roh-ASTERIX, kein Decode im Gateway) auf **Subject pro Feed** (`wayfinder.feed.<feed_id>`); realisiert Multi-Feed (Hybrid-Modell). Der Decoder→Broadcaster-Pfad bleibt quell-unabhängig; CAT-Oktett-Dispatch wie bisher. | ADR 0007, ADR 0005 (Hybrid-Feeds), Konzept §5.3/§6.3 | **ADR 0007** | **WF2-20.1 ✅ (feed_id-Durchstich):** `receiver.Config.FeedID` → `broadcast.TrackBatch` → `TrackMessage.feed_id`. **WF2-20.2 ✅ (Multi-Feed):** `cmd/wayfinder/feeds.go` `resolveFeeds` (DB-`feeds`-Katalog → N `feedConfig`; leer/kein-DB → ENV-Fallback) + `buildReceivers` (N Receiver je feed_id), `main()`-Reorder (DB vor Receiver, per-Feed-Listen-Skip, Decode-Fehler-Summe); Feed-CLI `cmd/wayfinder/feedcmd.go` (`feed add`/`feed list`). NATS-`StreamFeedSource` folgt WF2-53 | **WF2-20.1 ✅** `TestHandleTracksStampsFeedID`, `TestTracksToMessage`. **WF2-20.2 ✅** `cmd/wayfinder/feeds_test.go` (`resolveFeeds` Fallback+Mapping, `buildReceivers`) + `feeds_integration_test.go::TestIntegrationFeedCatalogue` (real-PG: `feed add/list` + Katalog→feedConfigs, distinkte DB-IDs) + E2E-Rauchtest (Server `feeds resolved count=2`, leer→1) |
| NFR-SCALE-001 | **Cloud-Fan-out & Skalierung (Wayfinder 2.0, ADR 0007):** Zielumgebung Public Cloud + Kubernetes. Transport = **NATS JetStream**: Core-Subject-Fan-out (jede zustandslose Instanz subscribt die Subjects ihrer Feeds und erhält jedes Datagramm — „jede Instanz sieht alles"), JetStream nur als kurzer Late-Join-Puffer (**kein** System-of-Record; revisionssicherer Replay bleibt Fireflys SDPS-005). N zustandslose Wayfinder-Instanzen hinter Service/Ingress (WF2-52); NATS-Cluster (3 Knoten) oder Managed; TLS + Credentials (NKey/JWT); Secrets (`WAYFINDER_NATS_URL`/`_CREDS`) in ENV. Bus trägt Roh-Datagramme (einziger Decode-Punkt erhalten). | ADR 0007, ADR 0006 (Stateless-Split), CLAUDE.md §7 (Cloud-nativ) | **ADR 0007** | folgt WF2-52 (Stateless-Skalierung), WF2-53 (Gateway+Bus produktiv/HA) | folgt WF2-52/53 (Pilot/Benchmark Latenz/Last/HA) |
| FR-ORCH-001 | **Feed-Quell-Datenmodell (Orchestrierung, ORCH-1 / ADR 0012):** Ein Feed trägt eine generische, Firefly-agnostische **Quell-Konfiguration** (`feeds.source_config`, JSONB-Array) — die Liste der Live-Eingänge, aus denen die dem Feed gewidmete Firefly-Instanz ihre System-Tracks rechnen soll. Geschlossenes Quell-Vokabular (`adsb_opensky`, `flarm_aprs`, `radar_asterix`), additiv erweiterbar ohne Schema-Bruch. Pro-Art-Validierung am Schreib-Rand (wie WF2-41 Sensor-Mix): flächengebundene Internet-Quellen (`adsb_opensky`/`flarm_aprs`) **erfordern** eine WGS84-`bbox` und tragen **keine** Sensor-Identität; ein echter Radar (`radar_asterix`) **erfordert** `sac`/`sic` (0..255) und hat eine optionale `bbox`; `cred_ref` ist nur ein **Verweis** auf ein Pro-Feed-Secret (nie der Klartext, nie an den Browser — ADR 0012 §6, Secret-Wert folgt ORCH-2). Daraus abgeleitet die **grobe äußere** `feeds.coverage_bbox` (Union der Quell-BBoxen + Marge, lat/lon-geklemmt) — die generische Geo-Grenze, die Wayfinder an Firefly übergibt (`FIREFLY_COVERAGE_BBOX`), **getrennt** von der präzisen inneren Mandanten-AOI (WF2-21.2, bleibt autoritative Isolations-/Billing-Grenze; coarse-outer vs. precise-inner, ADR 0012 §3). Dedizierte Repo-Accessoren (nicht in der schlanken `Feed`-Zeile/DTO, analog OpenAIP-Key-Isolation ONB-6). **Admin-API (ORCH-1b):** `GET/PUT /api/admin/feeds/{id}/sources` hinter `requireAdmin`; `PUT` validiert server-seitig (geschlossenes Vokabular, Per-Art-Regeln → `400` mit Quell-**Index**, kein Teil-Write), leitet die `coverage_bbox` bei fehlender Angabe selbst ab (Default-Marge 50 km) oder übernimmt eine explizit gesetzte (Operator-Override, WGS84-validiert), und liest die gespeicherte Konfig kanonisch zurück. **Frontend (ORCH-1c):** Quell-Builder im „Feeds"-Tab (`AdminFeeds.vue`) — je Feed ein „Quellen"-Dialog mit editierbarer Quell-Liste (Typ-Dropdown; BBox-Eingabe für Flächenquellen, SAC/SIC für Radar, optional Credential-Referenz), baut den Payload typ-abhängig (Flächenquelle ohne sac/sic), Coverage server-abgeleitet und nach dem Speichern read-only angezeigt; Store-Actions `loadFeedSources`/`saveFeedSources`. Gating kosmetisch (Server erzwingt). **Stand:** ORCH-1 komplett (1a Schema + Store, 1b Admin-API, 1c Frontend) ✅. | ADR 0012 (ORCH-1), Betreiber-Wunsch „pro Kunde eigener Sensor-Mix + Ausschnitt" | **ADR 0012** | `pkg/store/migrations/00010_feed_source_config.sql`, `pkg/store/feed_sources.go` (`SourceType`/`Source`/`SourceConfig`, `Validate`, `CoverageBBox`, `FeedRepo.GetSourceConfig`/`SetSourceConfig`, `InvalidSourceError`); `pkg/adminapi/adminapi_feeds.go` (`getFeedSources`/`putFeedSources`/`validateBBox`/`feedSourcesDTO`/`defaultCoverageMarginKm`), `pkg/adminapi/adminapi.go` (`FeedStore` um `Get`/`SetSourceConfig`, Routen); `frontend/src/stores/admin.js` (`loadFeedSources`/`saveFeedSources`), `frontend/src/components/admin/AdminFeeds.vue` (Quell-Builder-Dialog) | `pkg/store/feed_sources_test.go` (`TestSourceConfigValidate` Vokabular/Per-Art-Regeln/BBox-Bereiche/Index, `TestCoverageBBox*` Union/keine-BBox/Marge/Pol-Clamp), `pkg/store/feeds_subscriptions_integration_test.go::TestIntegrationFeedSourceConfig` (**real-PG**: Default leer, Round-Trip cred_ref/sac/sic + Coverage, Reject ohne Teil-Write, Clear, ErrNotFound), `pkg/adminapi/adminapi_feeds_test.go` (`TestGetFeedSources*`, `TestPutFeedSourcesRoundTripAndDerivesCoverage`/`…ExplicitCoverageOverrides`/`…InvalidIsRejected`/`…UnknownIs404`, `TestFeedSourcesRoutesForbidNonAdmin`), `frontend/src/stores/__tests__/admin.test.js` (ORCH-1c-Block: `loadFeedSources`/`saveFeedSources`/400-mit-Index) |
| NFR-SEC-004 | **Quell-Credential-Isolation (Orchestrierung, ADR 0012 §6):** Quell-Zugangsdaten (OpenSky-Client-Credentials, FLARM-/Radar-Zugänge) werden als **Pro-Feed-Secrets** geführt — in der Quell-Konfig steht nur eine `cred_ref` (ein Verweis/Handle), **nie** der Klartext; das Secret wird nie in ein DTO geschrieben, nie an den Browser gereicht und erst dem `InstanceBackend` beim Start gereicht (analog OpenAIP-Key-Isolation ONB-6). **Stand:** Datenmodell trägt `cred_ref` (ORCH-1a) ✅; Secret-Speicher + Übergabe an die Control-Plane folgen ORCH-2. | ADR 0012 §6, ADR 0011 (ONB-6 Key-Isolation), CLAUDE.md §7 (Sicherheit) | **ADR 0012** | `pkg/store/feed_sources.go` (`Source.CredRef` als Referenz, Validierung non-blank/Länge) | `pkg/store/feed_sources_test.go` (cred_ref-Validierung); Secret-Speicher folgt ORCH-2 |
| FR-ORCH-002 | **Tracker-Instanz-Abstraktion (Orchestrierung, ORCH-2 / ADR 0012 §4):** Wayfinder bekommt eine backend-agnostische Abstraktion, um **pro Feed eine Firefly-Instanz** zu betreiben — Grundlage für „Feed zuweisen ⇒ Instanz startet". `instance.Backend` (`Start`/`Stop`/`Status`, **idempotent**, nebenläufigkeitssicher; Instanz-Identität = `feed_id`) wird injiziert, sodass Control-Plane (ORCH-2c) und Reconciler (ORCH-3) **ohne** echtes Spawnen unit-testbar sind (Spiegel zum `feedmanager`-Muster). `instance.Spec` ist die **generische, Firefly-agnostische** Launch-Spezifikation aus einem Feed (Multicast-Endpoint, grobe `Coverage`-BBox, Quell-Liste, **Secret-Referenzen** statt Werten); `SpecFromFeed` leitet sie rein ab (sammelt distinkte, sortierte `cred_ref`-Handles — deterministisch für den Drift-Vergleich des Reconcilers). Die genauen Firefly-**Eingangs**-Env-Namen (`FIREFLY_SOURCES` etc.) bleiben bewusst aus der `Spec` heraus — sie sind cross-project (ORCH-5) und werden erst im Backend-Adapter (ORCH-2b) übersetzt. `MemoryBackend` ist Test-Double **und** Einzelhost-Dev-Platzhalter. **Docker-Adapter (ORCH-2b):** `dockerbackend.Backend` implementiert `instance.Backend` über Docker-Container — ein Container je Feed, Identität via Label `wayfinder.feed_id`, Drift-Erkennung via `wayfinder.spec_hash`-Label (gleicher Hash → idempotenter No-op/Restart, geänderter → Replace), Orphan-fähiges `RunningFeeds` (listet auch gestoppte managed Container). Die **gesamte Lebenszyklus-Logik ist hinter ein schmales `ContainerClient`-Interface gekapselt** und mit einem Fake voll unit-getestet; **die einzige Datei mit dem schweren Docker-SDK-Import** (`client.go`) ist eine dünne Übersetzung (kein Unit-Test, braucht Daemon). Injection-sicher (Container-Name nur aus numerischer Feed-ID, kein Shell-Aufruf — SDK). `fireflyEnv` setzt heute die ratifizierte Ausgabe-Env (`FIREFLY_CAT062_GROUP/PORT`, `FIREFLY_COVERAGE_BBOX`) + optional `FIREFLY_SCENE` als Platzhalter-Quelle; die echte Quell-Eingangs-Env (`FIREFLY_SOURCES`) + Secret-Auflösung folgen ORCH-5/2c-3. **Binary-Verdrahtung:** `cmd/wayfinder-orchestrator` wählt das Backend über `WAYFINDER_ORCHESTRATOR_BACKEND` (`memory` Default / `docker`); `docker` erfordert `WAYFINDER_FIREFLY_IMAGE`, optional `WAYFINDER_FIREFLY_NETWORK` (Default `host`, Multicast) + `WAYFINDER_FIREFLY_SCENE`. **Stand:** ORCH-2a ✅; Docker-Adapter (2b) ✅; Secret-Auflösung + Änderungs-Trigger (2c 3/3) folgen. | ADR 0012 §4/§6 (`InstanceBackend`, Least-Privilege-Control-Plane), Betreiber-Wunsch „Auto-Start der Tracker" | **ADR 0012** | `pkg/instance/instance.go` (`Status`, `Feed`, `Spec`, `SpecFromFeed`, `Spec.Validate`/`Endpoint`, `Backend`), `pkg/instance/memory.go` (`MemoryBackend`); `pkg/dockerbackend/backend.go` (`Backend`, `ContainerClient`, `fireflyEnv`, `specHash`, Labels), `pkg/dockerbackend/client.go` (SDK-Adapter); `cmd/wayfinder-orchestrator/main.go` (`newBackend`, Backend-Env) | `pkg/instance/instance_test.go` (`TestSpecFromFeed*` Ableitung/Secret-Refs distinkt+sortiert/Dedup, `TestSpecValidate`/`…Endpoint`, `TestMemoryBackendLifecycle`/`…RejectsInvalidSpec`/`…StartHookFailure`/`…Concurrent` unter `-race`), `pkg/dockerbackend/backend_test.go` (`TestStartCreatesAndRunsContainer`/`…IsIdempotentOnSameSpec`/`…RestartsStoppedContainer`/`…ReplacesDriftedContainer`, `TestStopRemovesContainer`/`…UnknownFeedIsNoop`, `TestStatusFailedForExitedContainer`, `TestRunningFeedsListsManaged`, `TestStartRejectsInvalidSpec`, `TestFireflyEnvMapsSpec` unter `-race`), `cmd/wayfinder-orchestrator/main_test.go` (`TestLoadConfigBackendDefaultsToMemory`/`…DockerRequiresImage`/`…UnknownBackendErrors`) |
| NFR-SEC-004 | **Quell-Credential-Isolation (Orchestrierung, ADR 0012 §6):** Quell-Zugangsdaten (OpenSky-Client-Credentials, FLARM-/Radar-Zugänge) werden als **Pro-Feed-Secrets** geführt — in der Quell-Konfig steht nur eine `cred_ref` (ein Verweis/Handle), **nie** der Klartext; das Secret wird nie in ein DTO geschrieben, nie an den Browser gereicht und erst dem `InstanceBackend` beim Start gereicht (analog OpenAIP-Key-Isolation ONB-6). **Verschlüsselter Secret-Speicher (ORCH-2c 3a):** Die Werte liegen **AES-256-GCM-verschlüsselt** in `feed_secrets` (`pkg/secret.Cipher`: zufälliger Nonce je Seal, Auth-Tag → Manipulation wird beim Open erkannt; `ErrDecrypt` nie Teil-Klartext); ein DB-Leak allein liefert **keine** Klartext-Credentials. Der Schlüssel ist deployment-verwaltet (`WAYFINDER_SECRET_KEY`, base64-32-Byte), **nie** in der DB. `store.SecretRepo` ist crypto-agnostisch (speichert nur den opaken Blob; `Set`/`Get`/`Delete`/`ListRefs`, Cascade beim Feed-Löschen). `orchestrator.SecretResolver` liest den Blob und entschlüsselt ihn — nur in der getrennten Control-Plane, nie am Browser-Rand. **Write-only Admin-API (ORCH-2c 3a-API):** `GET/PUT/DELETE /api/admin/feeds/{id}/secrets[/{ref…}]` (requireAdmin); der Wert geht nur **rein** (PUT `{value}`) und wird vom `orchestrator.SecretSealer` (Schreib-Gegenstück zum Resolver) **vor** der Speicherung versiegelt — der `GET` meldet nur, **welche** Refs gesetzt sind (`{secrets:[{ref,configured}]}`), **nie** einen Wert. Der Server hält den Schlüssel (`WAYFINDER_SECRET_KEY`) nur zum Seal beim Schreiben; ohne Schlüssel sind die Routen **deaktiviert** (503), nie unverschlüsselt speichernd. Das `{ref…}`-Trailing-Wildcard erlaubt `cred_ref`s mit Slash (z. B. `secret/opensky`). **Stand:** Datenmodell `cred_ref` (ORCH-1a) ✅; `Spec.SecretRefs` nur Referenzen (ORCH-2a) ✅; verschlüsselter Speicher + Resolver (ORCH-2c 3a) ✅; write-only Admin-API + Sealer + Frontend (ORCH-2c 3a-API) ✅; Container-Injection + Firefly-Quell-Env folgen (ORCH-5). | ADR 0012 §6, ADR 0011 (ONB-6 Key-Isolation), CLAUDE.md §7 (Sicherheit) | **ADR 0012** | `pkg/secret/secret.go` (`Cipher`, `Seal`/`Open`, `KeyFromBase64`, `ErrDecrypt`), `pkg/store/migrations/00011_feed_secrets.sql`, `pkg/store/feed_secrets.go` (`SecretRepo`), `pkg/orchestrator/secret.go` (`SecretResolver`/`SecretReader`, **`SecretSealer`/`SecretWriter`**), `pkg/adminapi/adminapi_secrets.go` (`SecretService`, write-only Handler), `cmd/wayfinder/main.go` (`WAYFINDER_SECRET_KEY` → Cipher → Sealer), `frontend/.../AdminFeeds.vue` + `stores/admin.js` (`loadFeedSecrets`/`setFeedSecret`/`deleteFeedSecret`) | `pkg/secret/secret_test.go` (Round-Trip, nicht-deterministisch, falscher Schlüssel/Manipulation/Garbage → ErrDecrypt, Key-Länge, KeyFromBase64), `pkg/store/feeds_subscriptions_integration_test.go::TestIntegrationSecretRepo` (**real-PG**: Set/Get/ListRefs sortiert/Upsert/Delete/ErrNotFound/Feed-Cascade), `pkg/orchestrator/secret_test.go` (Resolver + **`…SealerStoresCiphertextAndRoundTrips`**), `pkg/adminapi/adminapi_secrets_test.go` (configured-Liste/kein-Leak/PUT-Round-Trip+Slash-Ref/Wert-Pflicht/zu-lang/Delete+404/unbekannter-Feed/503-ohne-Key/403-Non-Admin), `frontend/.../admin.test.js` (Secret-Store-Actions inkl. 503) |
| FR-ORCH-003 | **Reconciler (Orchestrierung, ORCH-3 / ADR 0012 §5):** Cloud-natives Operator-Muster — statt imperativ beim ersten Abo zu starten und beim letzten zu stoppen, hält der Reconciler **Soll = Ist**: er berechnet periodisch (und auf Anstoß) die Soll-`instance.Spec`s (Feeds mit ≥ 1 aktivem Abo, je aus ihrer Quell-Konfig abgeleitet) und treibt das `instance.Backend` darauf zu. **Idempotent & crash-fest:** jeder Soll-Spec wird ge-`Start`et (Backend ist idempotent → No-op bei gleichem Spec, Re-Apply bei geändertem, Recovery eines abgestürzten/fehlgeschlagenen); jede laufende Instanz, deren Feed nicht mehr Soll ist, wird ge-`Stop`t (Orphan-Cleanup); Ist wird **beobachtet** (`Backend.RunningFeeds`), nicht erinnert → übersteht Neustart. Ein Per-Feed-Fehler bricht den Lauf **nicht** ab (gesammelt via `errors.Join`, geloggt, nächster Tick heilt). `Run(ctx, interval)` reconciled sofort + je Tick bis ctx-Cancel. Instanz-Identität = `feed_id`. **DesiredState** ist injiziert; der **Store-Adapter** (ORCH-2c, 1. Stück) `orchestrator.StoreDesiredState` leitet das Soll aus dem Katalog ab: alle Feeds mit ≥ 1 Abo (`SubscriptionRepo.ListSubscribedFeeds`, DISTINCT) × je Quell-Konfig (`FeedRepo.GetSourceConfig`) → `instance.SpecFromFeed`; ein Lese-Fehler **bricht den Lauf ab** (kein Teil-Soll → keine fälschlich verwaisten Instanzen). Voll testbar gegen `MemoryBackend` + Fakes. **Getrennter Prozess (ORCH-2c, 2/3, 🔒 ADR 0012 §6):** Die Control-Plane läuft als **eigenes Binary `cmd/wayfinder-orchestrator`** — nur dieser Prozess erhält später das Container-Start-Privileg, der browser-zugewandte Server **nie** (Least-Privilege; ein Browser-Rand-Exploit kann keine Container starten). Verdrahtet DB + `StoreDesiredState` + Reconciler + (vorerst) `MemoryBackend`; **migriert nicht** (ein Migrator = der Hauptserver); Env `WAYFINDER_DB_URL`/`WAYFINDER_ORCHESTRATOR_INTERVAL`(15s)/`WAYFINDER_LOG_LEVEL`; Modi `--once` (ein Lauf, Exit-Codes 2/1) + Schleife mit Graceful-Shutdown. **Stand:** Reconciler-Kern ✅; Store-`DesiredState`-Adapter (1/3) ✅; getrennter Control-Plane-Prozess (2/3) ✅; Secret-Auflösung + Änderungs-Trigger + echter Docker-Adapter folgen (2c 3/3, ORCH-2b). | ADR 0012 §5/§6 (Reconciler + getrennte Least-Privilege-Control-Plane) | **ADR 0012** | `pkg/reconciler/reconciler.go` (`DesiredState`, `Reconciler`, `Reconcile`, `Run`); `pkg/instance/instance.go` (`Backend.RunningFeeds`), `pkg/instance/memory.go`; `pkg/orchestrator/desired.go` (`StoreDesiredState`, `SubscribedFeedLister`/`SourceConfigReader`), `pkg/store/subscriptions.go` (`ListSubscribedFeeds`); `cmd/wayfinder-orchestrator/main.go` (`loadConfig`, `run`, `--once`/Loop, getrenntes Binary) | `pkg/reconciler/reconciler_test.go` (`TestReconcileStartsDesired`/`…StopsOrphans`/`…IsIdempotent`/`…ReappliesChangedSpec`/`…RecoversCrashedInstance`/`…DesiredStateErrorAborts`/`…PerFeedErrorDoesNotAbort`, `TestRunReconcilesUntilCancelled` unter `-race`), `pkg/orchestrator/desired_test.go` (`TestDesiredSpecsBuildsSpecsFromSubscribedFeeds`/`…Empty`/`…ListErrorAborts`/`…SourceConfigErrorAborts`), `pkg/store/feeds_subscriptions_integration_test.go::TestIntegrationSubscriptionRepoIsolation` (**real-PG**: `ListSubscribedFeeds` DISTINCT + Drop bei letztem Unsubscribe), `cmd/wayfinder-orchestrator/main_test.go` (`TestLoadConfig*`: Defaults/DSN-Pflicht/`--once`/Interval+Fallback/LogLevel/unknown-flag) |

Das Register wird bei jedem Meilenstein ergänzt.
